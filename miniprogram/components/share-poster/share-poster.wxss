.poster-mask,
.poster-overlay {
  position: fixed;
  inset: 0;
  background: rgba(31, 41, 55, 0.32);
  z-index: 10000;
  display: flex;
  align-items: center;
  justify-content: center;
}

.poster-panel,
.poster-container {
  background: var(--bg-surface);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-popup);
}

.poster-panel {
  width: calc(100vw - 64rpx);
  max-width: 650rpx;
  max-height: calc(100vh - 64rpx);
  overflow: hidden;
}

.poster-overlay {
  position: fixed;
  inset: 0;
  z-index: 10000;
}

.poster-container {
  width: calc(100vw - 64rpx);
  max-width: 650rpx;
  max-height: calc(100vh - 64rpx);
  margin: 0;
  padding: 0;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
}

.poster-header,
.poster-actions {
  padding: var(--space-section);
}

.poster-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-lg);
  font-size: 32rpx;
  font-weight: var(--font-weight-bold);
  color: var(--text-primary);
  flex-shrink: 0;
}

.poster-header text {
  min-width: 0;
  flex: 1;
}

.poster-header .close-btn {
  flex: 0 0 56rpx;
  padding: 0;
  color: var(--text-secondary);
  box-shadow: none;
  background: transparent;
}

.poster-title {
  font-size: 32rpx;
  font-weight: var(--font-weight-bold);
  color: var(--text-primary);
}

.poster-subtitle,
.poster-desc {
  font-size: var(--font-size-caption);
  color: var(--text-secondary);
}

.poster-container {
  background: var(--bg-surface);
}

.poster-body {
  min-height: 0;
  flex: 1;
  padding: 0 var(--space-section);
  box-sizing: border-box;
}

.poster-preview {
  display: block;
  width: 100%;
  max-height: calc(100vh - 280rpx);
}

.poster-placeholder {
  min-height: 420rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: var(--space-lg);
  color: var(--text-secondary);
}

.poster-footer {
  padding: var(--space-lg) var(--space-section) calc(var(--space-section) + env(safe-area-inset-bottom));
  flex-shrink: 0;
}

.poster-footer .save-btn {
  width: 100%;
}

.poster-footer .tip {
  margin-top: var(--space-md);
  color: var(--text-secondary);
  font-size: 22rpx;
  line-height: 1.45;
  text-align: center;
}

.poster-cover {
  width: 100%;
  border-radius: var(--radius-lg);
  overflow: hidden;
}

.poster-actions {
  display: flex;
  gap: var(--space-lg);
}

.poster-btn,
.save-btn,
.close-btn {
  flex: 1;
  height: 84rpx;
  line-height: 84rpx;
  border-radius: var(--radius-full);
  font-size: var(--font-size-body);
  font-weight: var(--font-weight-medium);
  margin: 0;
}

.save-btn,
.poster-btn.primary {
  background: var(--brand-primary);
  color: var(--text-inverse);
}

.close-btn,
.poster-btn.secondary {
  background: var(--bg-surface);
  color: var(--text-brand);
  box-shadow: inset 0 0 0 1rpx var(--border-brand);
}

.save-btn::after,
.close-btn::after,
.poster-btn::after {
  border: none;
}
