<!-- Shared professional data Tab: owners read enterprise facts; staff read only their own facts. -->
<view class="enterprise-operations-page sfp-tab-page">
  <role-workbench wx:if="{{role}}" role="{{role}}" focus="operations" />
</view>
