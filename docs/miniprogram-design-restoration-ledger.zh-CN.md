# 小程序设计还原台账

当前 `packages/business/referrer-progress/referrer-progress` 设计源为 `design-references/referrer-customers-v1/referrer-customers-v1.png`。生产已还原胶囊安全的「客户」Tab：小 K 作为客户档案管家、不含个人信息的记录编号、实底「撤回」胶囊，以及 10 分钟内「恢复」。推广人 TabBar 文案为「客户」。运行视觉 QA 仍等待用户提供 `390x844` 手动截图。

本文是当前路由与设计源的唯一查询表。每个运行路由只保留一行，每行只保留一份
最新批准设计源；设计源或生产状态变化时直接替换，不追加还原历史。

共享自定义 TabBar 的当前设计源为
`design-references/tabbar-icons/tabbar-earnings-appointment-v1.png`：收益/提成使用其中成对的
钱包收入状态。企业负责人预约仍受 `enterprise.appointments` 权限保护，但改为从工作台上下文进入，不再占用固定 Tab。

共享 `components/designer-contact-sheet` 的当前设计源为
`design-references/designer-contact-sheet/designer-contact-sheet-xiao-k-bubble-v8-candidate.png`，适用于
`pages/index/index`、`packages/business/free-design-service/free-design-service` 与
`packages/business/customer-project/customer-project`。生产映射为二维码优先的原生 WXML/Less 弹窗及
`miniprogram/images/designer-contact/xiao-k-peeking.png` 及同目录三枚已记录许可的职业背书图标。小 K 右侧新增
暖白绿描边的原生文字对话气泡，尾巴指向小 K，以 `22rpx` 前句「比小红书更方便贴心的」和 `32rpx` 强调句
「家装顾问」表达服务定位，不改变二维码舞台和联系操作。组件挂载及每次打开时读取原生菜单胶囊底边，气泡顶部
始终在该边界下方保留 `16rpx`；本次用户运行截图记录了修正前的遮挡问题并作为本次修正依据。宿主页存在可见
自定义 TabBar 时，弹窗贴在 `--sfp-custom-tabbar-safe-height` 之上，避免外置关闭按钮被 Tab 栏挡住；无 TabBar
的调用页保持原底部留白。用户提供的「服务」Tab 关闭按钮被遮挡截图是本次修正依据。头衔可见时，
关系条只显示「你的专属家装设计顾问」，资历卡标题组合最终头衔与家装设计顾问姓名，经验和服务客户背书分别使用
`50rpx` 指南针与客户爱心图标。按设计源校准的元素台账为：气泡 `316rpx × 112rpx`、气泡前句 `22rpx`、
强调句 `32rpx`、关系/背书文字 `24rpx`、资历标题 `32rpx`、
徽章圆底 `96rpx` 与可见图形 `62rpx`、背书图标 `50rpx`。头衔隐藏态仍在关系条保留姓名，并只收起头衔标题。
组件、字号、分包与调用路由聚焦测试通过；更新后的 `390x844` 原生胶囊运行态核验仍待用户手动截图。

以下 V3 设计源仅作为 `pages/index/index` 企业负责人部分的历史记录：
`design-references/enterprise-owner-activity-code-entry-v3/enterprise-owner-operations-home-v3.png`。
V3 使用等宽 Hero 双入口、规则三列经营路径和两段卡间短连线；将对老板含义不清的「闭合率」替换为
来自 `schemeFacts.publishedLeadCount` 的周期统计「已发布方案 N 份」。V3 中的业务插画均通过
ImageGen 独立生成，原先映射到 `miniprogram/images/operations-dashboard/enterprise-guide.png`
（17,943 字节）和 `staff-load.png`。未再使用的 `lead-inbox.png`、`staff-onboarding.png`、
`scheme-delivery-rate.png`、`signing-rate.png` 已从运行时源码包删除；当前生产使用下文密度 V2 /
经营 KPI 切图。保留的切图均为独立透明
PNG，不从复合设计图裁切，且小于主包 `300KB` 限制。下方既有 `pages/index/index` 单行继续保留
路由/API/权限合同，本段只保留此前 V3 构图说明。认证态 `390x844` 原生胶囊运行态
核验仍待用户手动截图。最新用户高屏真机图暴露了入驻入口重复加号，以及三段阶段卡下方错误的整条进度轨。
V3 修正使用单一 `64rpx` 生图入驻切图、`26rpx` 纯绿色标题、`24rpx` 经营板内边距、
`18rpx 18rpx 24rpx` 阶段卡内边距、`44rpx` 阶段间距与两段卡间连线、
`18rpx 20rpx` 效率卡内边距及 `72rpx` 生图图标盒，并移除卡片下方的进度线和圆点。
最新高屏对照继续暴露出共享 flex 行把负责人三枚状态胶囊推得过低、Hero 层级被压小的问题。负责人专属映射
现改为 `114rpx` 顶部预留、`12rpx` 胶囊间距、绝对定位的 `190rpx` 小 K 图盒、`136rpx` 等宽白底双入口、
`64rpx` / `72rpx` 入口切图及 `30rpx` / `24rpx` 入口文案；窄屏保护仍保留 `60rpx` / `68rpx`
切图，不再退回此前偏小的图标尺寸。
下一张运行图又确认白底入驻切图对比不足、人员负荷小 K 偏小以及阶段层级偏弱。现在由内置 ImageGen 生成的
`staff-onboarding-white-card-v2.png` 透明源；该 V3 切图随后从运行时源码包删除，改由 `team-onboarding-v2.png` 承担入驻入口。人员负荷使用 `120rpx × 104rpx`，三张阶段卡
使用 `168rpx` 高度、`34rpx` 序号圆、`26rpx` 标题和 `14rpx` 下内边距；收紧无效底部留白，两行说明仍可按内容自然撑开。

当前企业负责人工作台的批准设计源为
`design-references/enterprise-owner-workbench-density-v2/comp-b-with-operations-reminder.png`。
`pages/index/index` 为**工作台**：紧凑 Hero 下方只有一个白色「获客与团队」中枢，其中依次包含 430rpx 高的左侧获客主卡、右侧两张 208rpx 的「邀请入驻」/「查看推广人」上下卡、「查看预约安排」上下文行、细窄的 B+C「经营提醒」及蓝/紫两张洞察卡。「经营提醒」仅读取工作台真实 `newLeads`、`signedCount` 行及其单位，绝不使用效果图示例数值，并通过 `switchTab` 进入**经营** Tab。异常事项保持为中枢外单独的白色分组托盘，告警图标和处理按钮独立排布。当前独立生成、未从复合效果图切片的生产插画为 `miniprogram/images/operations-dashboard/activity-code-share-v3.png`（592×464，20,073 字节）、`team-onboarding-v3.png`（480×384，21,267 字节）、`referrer-roster-v2.png`（180×201，10,011 字节）、`lead-inbox-v2.png`（240×239，10,044 字节）和 `priority-alert-v2.png`（90×88，3,347 字节）；均为小于 300KB 的透明 PNG。企业负责人 TabBar 仍为**工作台 / 经营 / 客户 / 提成 / 我的**；「客户」仍为 `pages/leads-management/leads-management`，预约不作为固定 Tab，预约路由权限和行为不变。该段仅为当前**工作台**构图；独立**经营** Tab 的当前设计源在下方对应路由行单独记录。聚焦结构、导航、字号、资源签名与包体预算核验已通过；认证态 `390x844` 原生胶囊截图仍待用户手动提供。

当前设计源元素台账不改变业务行为，仅修正运行时层级：经营提醒和两张洞察卡均位于批准稿中的同一个操作中枢；中枢标题为 `34rpx`，左侧获客主卡标题/说明/CTA 分别为 `36rpx`/`26rpx`/`28rpx`。横向活动码插画左右内缩并使用 `220rpx` 图盒（`<=360px` 时为 `184rpx`），完全停在原生文案下方。两张右侧入口使用 `32rpx`/`24rpx` 文案，入驻/推广人图盒为 `150rpx × 140rpx`、`148rpx × 160rpx`。`80rpx` 预约行展示真实预约数组数量；`72rpx` 经营提醒行使用 `32rpx` 标题分隔线，两组真实指标均匀排布。洞察卡为 `176rpx`：文案占上层，`40rpx` 圆形箭头独占下层，待处理线索图盒为 `124rpx × 116rpx`，人员负荷图盒下移并收至 `120rpx × 96rpx`。异常行保持 `104rpx` 内容固有最小高度、`38rpx` 可见告警图，`52rpx` CTA 使用 `10rpx` 小圆角。运行态视觉核验仍待用户下一张手动截图。

当前工作流文字基线已按下表设计源复核：页面/导航标题不低于 `32rpx`，分区标题不低于
`28rpx`，业务数值、操作与正文不低于 `24rpx`，辅助说明不低于 `22rpx`；仅非关键图上角标可保留
`20rpx`。已审计的服务工作流样式由 `miniprogram/test/miniprogram-typography-floor.test.js` 持续防护。

| 运行路由 | 最新设计源 | 当前视觉核验 | 已还原 |
| --- | --- | --- | :---: |
| `pages/enterprise-operations/enterprise-operations` | 用户 2026-08-28 提供的经营页效果图，并对齐 `design-references/enterprise-owner-workbench-ia-v1/enterprise-owner-workbench-and-operations-ia-v1.png` | 企业负责人**经营** Tab 按效果图恢复：浅薄荷 Hero 内收标题、四个原生周期控件、四张真实 KPI（新增线索、已完成量房、已签约、签约金额）、重新生成的完整小 K 与独立生成的增长图表板；下方为无边框三节点经营闭环和两张效率卡。「签约金额」拆成原生数值/单位并按量级紧凑显示（`5万元`、`10万元`、`100万元`，再到`亿元`/`万亿`）；Hero KPI 数字通常为 `44rpx`，仅紧凑数值达到三个及以上字符时使用 `36rpx`，后续单位恒为 `26rpx`，所以各种金额不会挤出窄 KPI 卡。真实 canvas 趋势使用既有 `GET /api/miniprogram/workbench?period=` 追加的负责人专属 `contractAmountTrend`：本周/本月/不超过 62 日的自定义周期按上海时区逐日聚合，其他周期按月聚合，另带等长的上一可比周期序列；仅两条序列都为 0 时才显示空态。路由专属透明 ImageGen 资源为 `images/operations-dashboard/{enterprise-hero-k-v2,new-leads-kpi-v2,completed-survey-kpi-v2,signed-contract-kpi-v2,contract-amount-kpi-v2,operations-growth-chart-v2}.png`；均为非截图切片的独立 RGBA PNG，且小于 300KB。更大的 Hero 插画已按原图的可用薄荷面板比例校准，避免留下视觉空白。页面仍由 `enterprise.operations` 能力控制并写入原生 `tabBar.list`；自定义周期 sheet 不变，既有 API 新增负责人可读的 `contractAmountSum` 与 `contractAmountTrend` 事实字段。聚焦源码/导航/资源测试通过；刷新后的认证态 `390x844` 原生胶囊截图待用户提供 | 是 |
| `packages/business/profile-edit/profile-edit` | 沿用 `miniprogram/DESIGN.md` 与 `design-tokens.json` 约束的既有家客来账号/资料界面；专业背书是已批准的功能态增量，不替换原构图 | 家装设计顾问/家装现场顾问资料页保留既有账号壳、有标签的头衔/年份字段、企业/锁定提示、可访问开关、后台真实人数说明和客户最终预览。从业起始年份说明改为标签/输入下方整行左对齐，七字标签单行显示并与头衔、开关行对齐；企业控制或锁定时只禁用对应控件。聚焦静态测试通过；用户已暂缓 `390x844` 原生胶囊运行态核验并将在真机提供截图 | 是 |
| `packages/business/service-needs/service-needs` | `design-references/customer-workbench-tob-needs-v3.png` | 已用原生 WXML/Less 实现二级服务需求页：胶囊安全品牌锁、多选服务记录、「暂时没有其他需求」、加载/错误恢复与固定保存操作。仅从已批准成功态的弱入口进入，不改变服务阶段进度条。分包、字号和路由合同测试通过；登录态 `390x844` 原生胶囊截图待补 | 是 |
| `packages/business/lead-claim-pool/lead-claim-pool` | `design-references/lead-claim-racing-v1/designer-lead-claim-pool-v1.jpg` | 已按用户批准的 `390x844` 胶囊安全抢单池还原：领取前脱敏事实、服务端校准倒计时、容量状态、3 秒刷新及原生抢单/提醒动作。已批准的同批状态稿约束加载、空态、关闭、过期、并发失败与成功行为，但不替代本行唯一最新设计源。页面、订阅、导航与字号聚焦测试通过；登录态原生胶囊宿主截图待补 | 是 |
| `pages/index/index` | `docs/superpowers/specs/2026-08-25-customer-service-home-three-free-design.zh-CN.md`；`design-references/customer-service-home-three-free-v1/customer-service-home-three-free-v1.png`（客户“三项免费权益”当前设计源）、`04b-designer-workbench-dashboard.jpg`、`05b-measurer-workbench-dashboard.jpg`、`design-references/enterprise-owner-workbench-density-v2/comp-b-with-operations-reminder.png`（企业负责人工作台当前设计源） | 客户「服务」首屏已按批准的“三项免费权益”设计还原：胶囊安全的单一绿色主场景以「三项免费权益」及「三个免费，装修更省心」为第一层，完整小 K 只出现一次并手持效果图、设计顾问和现场顾问三张权益牌；跨层白色服务票据保留真实户型/方案缩略图、唯一一条客户可读 `appointmentSummary`、连线式四步进度和服务端派生主动作，主动作与档案动作同时存在时并排显示。客户服务票据在本路由弱化「上门」：标题为「待预约量房 / 已预约量房 / 量房进行中」，说明为「可预约量房时间」，主动作为「预约量房」；员工工作台与预约页的上门用语不变。三张紧凑原生权益卡分别为「免费效果图 / 出到客户满意为止」「免费家装设计顾问 / 解答你的装修问题」「免费家装现场顾问 / 解答现场问题」，分别进入当前线索的交付方案册 `customer-ai-schemes`（尚未发布时进入同一方案册空态）、共享设计顾问联系和预约/改期/档案路径；保障条把「三项服务不收费」与按真实阶段派生的状态并列。未登录访问本路由时停留在批准的空态/早期三项免费权益陪伴页，可先浏览再自行到「我的」登录，不再立刻切到登录入口；未登录会话复用客户「服务/我的」TabBar。整页设计稿不切片；生产界面复用批准的小 K，并使用设计合同记录的路由专属 ImageGen 素材 `effect-room.jpg`、`design-advisor-3d.png` 和 `onsite-advisor-3d.png`，不再拿通用线性图标代替设计插画。空态/早期态不虚构媒体，多项目切换仍为 `N = length − 1`。未登录根路由仍切到 JoveKore｜家客来「我的」入口；同一线索的家装设计顾问/家装现场顾问代约入口仍保留在员工面。 角色工作台继续沿用极简透气版壳层；任务卡描边胶囊（电话联系/导航/立即量房/继续量房/新增量房/确认完成量房/改期/预约上门/查看预约）复用共享 `.sfp-icon-action` 与已打包电话/定位/尺规/日历 PNG；隔离的 `role-workbench` 同时把这些 CTA `<image>` 钉在共享的 `28rpx`，避免微信原生 320×240 默认尺寸把量房/电话/导航图标撑出卡片；家装现场顾问概览 Hero 为「链接测距仪」并展示蓝牙连接状态，点击打开既有 `ble-connector` 弹窗（正式量房仍只从任务卡进入）；家装设计顾问工作台概览下方不再展示静态「常用配方」条（快捷入口「风格配方」仍可进入设计 Tab）；企业负责人/家装设计顾问/家装现场顾问经营态已按增强稿还原可筛选五卡经营大盘（已签约只读 KPI；负责人可展示签约金额），Hero 胶囊为待派单/待量房/待交付（第三项为方案设计中且尚未对客户可见的线索，已签约仍只在周期大盘），异常区语义不变，数据来自 `GET /api/miniprogram/workbench?period=`。自定义周期 sheet（`18c`）贴在 `--sfp-custom-tabbar-safe-height` 之上，避免 Tab 页把「取消/确定」裁到自定义 TabBar 后面。员工/经营端身份栏在胶囊同一行居左放置 Logo +「家客来 · 家装设计顾问端 / 家装现场顾问端 / 经营端」，企业名紧贴「家客来」下方；两行叠在原生胶囊高度内，避免把绿色 Hero 顶下去（无企业名则不显示；过长企业名单行省略）；登录用户姓名显示在绿色 Hero 标题上方（无姓名则不显示）；仍不放扫码和提醒图标；出示活动码与次要入口仍在 Hero CTA、分区链接和 TabBar 徽标；经营端现按批准的获客优先 Hero 双入口还原：白色主卡「分享活动码」及说明「发给客户 · 扫码留资」进入既有活动码出示页，白色次卡「邀请入驻」及说明「员工 · 推荐人」进入企业双入驻码页；小程序在前后端错峰发布时会归一化旧版单一入驻码 payload，避免把招人入口误标成客户获客。客户「服务」首页同一套胶囊安全锁，保留扫码（页内领取服务码/邀请码），去掉铃铛（原先只是切到「我的」Tab）。用户提供的高屏真机截图暴露了权益卡使用通用图标、无媒体服务票据纵向堆叠的问题；两项均已改为路由专属素材及“文案/进度同排”的两列票据。后续真机截图显示三张权益卡绿圆内箭头未居中；生产现已在 `58rpx` 绿圆内使用已授权 Lucide `chevron-right` PNG（`images/customer-service-three-free/chevron-right.png`），不再用 CSS 边框箭头。用户于 2026-08-26 提供的 `1080x2400` 真机截图确认经营端 Hero 容器和「邀请入驻」样式在生产包中已经生效。批准的 V2 下半区现已写入原生工作台：负责人现有五项 KPI 重组为不对称经营路径、次级效率卡和浅薄荷异常行动托盘，聚焦结构测试已通过。用户于 2026-08-27 提供的 `1080x2400` 真机截图确认生产包已呈现完整 V2 负责人构图：Hero 双入口、三段异型经营闭环、两张效率卡和优先处理托盘；截图复核后补充了三枚原生路线业务节点、下移并弱化户型线稿，并将异常空态圆点替换为已打包 `images/leads-v4/map-pin.png`；聚焦结构测试保持通过。最新用户高屏真机图确认 V3 业务插画已经生效，同时暴露了入驻图标重复加号、阶段卡间距不足及错误的卡下整条进度轨；生产现已改用密度 V2 / 经营 KPI 切图，未再使用的 V3 切图已从运行时源码包删除。阶段卡内边距为 `18rpx 18rpx 24rpx`、`18rpx 20rpx` 效率卡内边距、`44rpx` 三卡间距与两段卡间短连线，并在旧后端仍返回「闭合率」时显示「方案同步中」。负责人 Hero 现使用 `220rpx` 绝对定位小 K 图盒与 `116rpx` 文案预留；批准稿中的获客栅格、预约行、真实经营提醒和两张洞察卡同属一个白色中枢，异常托盘保持独立。最新 `activity-code-share-v3.png` 与 `team-onboarding-v3.png` 独立切图恢复设计源的横向插画轮廓，未裁切复合效果图。最终「分享活动码 / 邀请入驻」双卡构图仍取决于生产 Workbench 返回新版负责人 payload（`activityCode` + `joinCode`），认证态 `390x844` 原生胶囊视觉核验需等待生产后端发布后复核。本调用页已还原上文唯一共享 v8 家装设计顾问联系弹窗，并在有自定义 TabBar 时把关闭按钮停在 Tab 栏之上；运行态截图按用户要求待其真机提供 | 否 |
| `pages/mine/mine` | `docs/miniprogram-role-shell-design-v1.zh-CN.md`、`design-references/auth/miniprogram-guest-login-jovekore-v2-full.png` | 已登录的客户、推广人和员工继续沿用批准的资料卡壳层及内联账号菜单；「当前身份」保留账号卡槽位及两行切换说明。未登录态继续使用胶囊安全 JoveKore｜家客来品牌锁、独立 `images/home-ip-v1/login-identity-portal-v2.jpg` 门厅场景、原生「个人用户 / 员工 / 推荐人」身份轨道、唯一「立即登录」CTA 和账号切换信任说明。用户提供的 `1080x2400` 真机图暴露了窄屏规则叠加压缩、CTA 偏窄、身份图形可见边界偏小及信任说明被 CSS 竖胶囊替代的问题；生产现让 `<=360px` 保持 `700rpx` 场景、`32rpx` 面板边距和 `40rpx` 标题，三枚带透明留白的 Lucide PNG 使用 `70rpx` 图片盒获得约 `58rpx` 可见图形，CTA 显式满内容区宽并清除原生最大宽度/左右外边距，信任说明复用 `images/mine-icons/shield-check.png`。批准整页稿仍不切片；Logo、文字、轨道、按钮和说明均为原生节点。未登录访客复用客户「服务/我的」TabBar，以便先浏览服务首页，登录仍由「我的」的「立即登录」发起；bootstrap 后恢复既有角色/能力导航。访客入口改为落在 `--sfp-custom-tabbar-safe-height` 之上，不再铺满 `100vh`；已登录账号卡在账号任一有效身份拥有已批准引导时显示原生「角色使用引导」行（推荐人、企业负责人、家装设计顾问、家装现场顾问），当前切到个人用户时仍保留该行以便回看；仅有个人用户、且没有上述工作身份时按约定不强制出现，也不放置未批准占位引导。当前身份有引导则直接打开对应 `packages/guides` 页面，否则回看已有身份的引导。其余路由、API 与权限边界不变。聚焦布局、窄屏防压缩与素材检查通过；修订后的 `390x844` 及用户高屏原生胶囊截图待手动复核 | 否 |
| `packages/business/login/login` | `design-references/all-pages-ip-v1/06-login.png` | 已批准的小 K 开门登录构图未改，仅按微信审核去掉易混淆元素：快捷登录标题改为「手机号授权登录」，官方微信 logo 替换为既有圆形手机号授权 PNG，调用 `getPhoneNumber` 前不再出现「微信授权登录」或腾讯品牌标识。绿色主按钮仍为「手机号快捷登录」。协议行加大整行点击范围并切换既有 24rpx 圆圈；仅勾选后才挂载微信 `getPhoneNumber`，避免原生按钮抢走点击或未同意即授权。未勾选时点「手机号快捷登录」提示「请先勾选同意协议」。《用户协议》《隐私政策》用 `catchtap` 进入 `legal-webview`，不切换勾选。《免责协议》仍保留解析与托管页，登录协议行暂不展示。两份链接允许协议行换行，字号仍为 `24rpx`。聚焦合同测试通过；登录态 `390x844` 原生胶囊截图待补 | 是 |
| `packages/business/account-security/account-security` | `design-references/miniprogram-airy-minimalist-v1/30-account-security.jpg` | 既有批准的账号安全页布局、字体、素材和导航均未改。通过账号密码登录且携带 `requiresPasswordChange` 的员工现只在登录后收到一次原生提醒，随后进入已签名角色落点并保留可用 TabBar；「去修改」可 `navigateTo` 本路由，「我的」仍可随时打开。小程序 API 不再因此锁定。改密成功后清除服务端标记和本地会话，要求用新密码重新登录。微信/手机号快捷登录不进入此状态。聚焦登录/token 合同测试通过；登录态 `390x844` 原生胶囊截图待补 | 是 |
| `packages/business/legal-webview/legal-webview` | 无独立设计稿；使用微信默认导航栏与原生 `web-view` | 访客可打开的文档页：`onLoad` 读取 `url` 与可选 `title`，仅加载 `https:` 到 `<web-view>` 并设置导航标题。登录页当前打开 `https://smartfloor.zlyun168.com/user-agreement.html` 与 `https://smartfloor.zlyun168.com/privacy-policy.html`；`disclaimer.html` 仍可经 `legal-docs.js` 解析，但登录行暂不露出。域名须加入小程序业务域名白名单。聚焦合同测试通过 | 是 |
| `pages/leads-management/leads-management` | `docs/miniprogram-role-shell-design-v1.zh-CN.md` | 既有批准的家装设计顾问/企业负责人客户入口保留；列表卡把小区单独放在手机号下一行（最多两行截断），避免被右侧户型缩略图挤成几个字。当前签名家装现场顾问只呈现角色工作台中的本人已确认任务和尚需正式量房的无预约任务，已完成量房不再占待办。冷启动先按本地已签名身份选择路由，避免客户/家装设计顾问先看到老板旧壳。聚焦导航测试通过；登录态 `390x844` 原生胶囊宿主截图待补 | 否 |
| `packages/business/lead-detail/lead-detail` | `design-references/lead-detail-delivery-stack-v1/lead-detail-below-survey-v1.png`（下方三卡）；正式量房卡为「下一步」右对齐 + 地址独占行 + 预约/开始量房双钮同排；Hero「补充资料」与可拨电话图标约定覆盖原状态∥补充资料并列写法 | 独立上门量房容器已移除。正式量房卡：角标左侧、「下一步」文案靠右、地址独占一行，`安排上门量房`/`重新预约`/`查看预约`（`survey_ready` 时改为「确认完成量房」）与「开始量房」/「继续量房」同时出现时同一排等宽 84rpx 胶囊（间距 16rpx），仅一个按钮时仍满宽，圆角与「标记已签约」一致。量房完成后同一卡保留房源事实（小区、面积、已存预约地址、闭合房间名）及鉴权测绘 PNG 只读预览（**查看大图**，`fetchProtectedImage` + `wx.previewImage`，不进 `surveying-editor`）。其下「房屋现场图」共用先选标签再拍摄的网格。每张员工卡右侧在 `assignmentActions` 允许时显示「分配」或「更换」（24rpx 品牌绿）；电话仍单独走 `.sfp-icon-action`。选人走 `GET /api/leads/[id]/assignable-staff?role=`（分页加载更多）并提交 `POST /api/leads/[id]/assign-staff`（可补齐或覆盖；家装现场顾问无改派操作）。其下方案卡复用同一壳：未归档未签约线索上，家装设计顾问即使尚无已发布方案、量房未完成也会看到「进入 AI 设计」（空态标题「方案设计」，说明「可用现场图开始出图」；量房完成或已有正式户型时说明「量房完成，可开始出图」）；有发布轮次时仍进只读方案册，行 meta 在定稿时显示「定稿」，进入方案册或「查看全部方案」默认带上定稿 `schemeId`。「查看全部方案」与「进入 AI 设计」同时出现时同一排等宽胶囊、间距 16rpx；仅一个按钮时仍满宽。历史/成交卡未改。Hero 小区在绿色文案区内提前换行（最大 300rpx）并加浅字影，避免白字叠到右侧白插画；阶段轨未改。Hero 与阶段轨之间增加双卡展示已派家装设计顾问/家装现场顾问姓名和电话（`GET /api/leads/[id]` 的 `assignedTo`/`measurerId` 员工摘要；未派显示待分配，有电话可点拨）。Hero「补充资料」绝对定位于卡片右上角，配已打包 `images/mine-icons/edit.png`，不再与只读状态胶囊并排；客户手机与员工电话行使用 `.sfp-icon-action` + `images/leads-v4/phone.png` 可拨打；电话裁切已抠掉白底，绿色 Hero 上不再露出不透明方块，`.hero-phone` 与文案区左对齐（`justify-content: flex-start`），电话图标放在号码后面，并保留原来的「手机：」+ 号码文案。状态胶囊保持纯文字。聚焦预览/派单/图标动作合同测试通过；登录态 `390x844` 宿主截图待补 | 是 |
| `packages/business/lead-form/lead-form` | 沿用 all-pages-ip-v3 客户资料表单壳（`packages/business/assets/leads/lead-form.png`）；微信默认导航栏（用户要求使用原生返回）；小区字段对齐预约页的地图选点 + 手输 | 使用微信默认导航栏与原生返回；小区行保留原 input-shell，右侧增加 24rpx「选择地点」`.sfp-icon-action`（`images/leads-v4/map-pin.png`），`wx.chooseLocation` 的 POI 名写入可继续手改的 `communityName`（160 字上限），坐标不写入线索。聚焦测试覆盖默认导航与地图回填；登录态 `390x844` 原生胶囊截图待补 | 是 |
| `packages/guides/enterprise-owner-guide/enterprise-owner-guide` | `design-references/role-onboarding-v1/enterprise-owner-onboarding-v1.png` | 已批准的三步企业负责人原生引导：活动码获客、伙伴入驻、经营异常优先处理。三步内容为原生非循环 `swiper`（可左右滑动或点步骤点回看，「下一步」仍前进，不自动播放）；首次进入经营端自动展示一次，可从「我的」重复查看，完成后进入既有 `staff-activity-code` 活动码页。独立 ImageGen 透明 PNG 映射为 `packages/guides/assets/enterprise-owner-v1/{activity-code,team-onboarding,operations-priority}.png`（调色板压缩，每张 ≤300KB）；标题 `32rpx`、页内标题 `40rpx`、正文 `24rpx`、保障条 `22rpx`、CTA `28rpx`。自定义顶栏改为 `border-box`，把 `navigationRight` 算进标题行宽度，避免「跳过」被原生胶囊挡住。分包/契约测试通过；登录态 `390x844` 原生胶囊截图待手动复核 | 是 |
| `packages/guides/designer-guide/designer-guide` | `design-references/role-onboarding-v1/designer-advisor-onboarding-v1.png` | 已批准的三步家装设计顾问原生引导：领取客户、量房资料衔接、方案交付与收益记录。三步内容为原生非循环 `swiper`（可左右滑动或点步骤点回看，「下一步」仍前进，不自动播放）；首次进入家装设计顾问工作台自动展示一次，可从「我的」重复查看，完成后进入既有 `staff-earnings` 收益页。独立 ImageGen 透明 PNG 映射为 `packages/guides/assets/designer-advisor-v1/{lead-claim,survey-sync,scheme-delivery}.png`（调色板压缩透明抠图，每张 ≤300KB；640×529 / 640×514 / 439×448）。此前全不透明导出把透明棋盘格画进了像素；现仍保留真实透明通道，薄荷绿舞台会透出来。；标题 `32rpx`、页内标题 `40rpx`、正文 `24rpx`、保障条 `22rpx`、CTA `28rpx`。首次引导完成后才继续检查微信资料补全提示。自定义顶栏改为 `border-box`，把 `navigationRight` 算进标题行宽度，避免「跳过」被原生胶囊挡住。分包/契约测试通过；登录态 `390x844` 原生胶囊截图待用户复核跳过按钮与胶囊间距 | 是 |
| `packages/guides/measurer-guide/measurer-guide` | `design-references/measurer-onboarding-v1/measurer-onboarding-directions-v1.png`（已确认 B「现场路径」） | 已批准的三步家装现场顾问原生引导：连接测距仪、沿墙线测量、完成可交付量房。三步内容为原生非循环 `swiper`（可左右滑动或点步骤轨回看，「下一步」仍前进，不自动播放）；首次进入现场顾问工作台自动展示一次，可从「我的」重复查看，末页 CTA「去看今日任务」回到既有工作台任务，不新增虚构入口。B 设计源三个场景现映射为重新生成的高分辨率独立 ImageGen 透明 PNG：`packages/guides/assets/measurer-v1/measurement-bench.png`（900×960，100,280 字节）、`measurement-path.png`（960×640，64,543 字节）与 `measurement-complete.png`（867×960，82,506 字节）。三图均使用索引调色板 PNG 压缩、保留透明通道、单图小于 300KB，并在既有插画尺寸下保持完整小 K 与测量设备轮廓清晰；`packages/guides` 分包合计 1,276,739 字节，低于微信 2MB 上限。标题 `32rpx`、页内标题 `40rpx`、正文 `24rpx`、保障条 `22rpx`、CTA `28rpx`。自定义顶栏改为 `border-box`，把 `navigationRight` 算进标题行宽度，避免「跳过」被原生胶囊挡住。分包/契约测试通过；登录态 `390x844` 原生胶囊截图待手动复核 | 是 |
| `packages/business/settings/settings` | `design-references/miniprogram-airy-minimalist-v1/29-settings.jpg`（内容已并入 `pages/mine/mine`） | 兼容深链路由；`onLoad`/`onShow` 直接 `switchTab` 回“我的”，不再渲染独立设置页壳层。账号菜单的视觉与交互以 Mine Tab 内联区块为准 | 是 |
| `packages/business/identity-switch/identity-switch` | `design-references/identity-switch-card-grid-v2/identity-switch-card-grid-v2.png` | 已批准的 V2 将窄横滑轨道替换为按内容自然排布的双列身份卡，因此服务端定义的全部岗位名（包括「家装设计顾问」「家装现场顾问」）都以原生文字完整可读呈现，不再省略。选中卡使用绿色描边与「当前使用」状态签；上方档案场景仅保留一个完整小 K，随本地预览身份更新。独立透明 ImageGen 岗位工具图映射为 `packages/business/assets/identity-switch/role-cards/{customer-service,designer-plan,measurer-laser,referrer-contact,enterprise-operations,sales-promotion,platform-console}.png`（RGBA、48px、单张 3.8–4.6KB）；不切片复用复合设计稿，也不在卡片内重复使用小 K。一个身份仍隐藏选择控件；两个及以上身份展示卡片。点按只做本地预览；仅非当前身份才会打开既有原生确认与签名 token 切换。加载、失败、登录失效、API 与权限行为不变。聚焦身份切换/源码检查通过；登录态 `390x844` 原生胶囊截图仍待人工核验 | 是 |
| `pages/ai-design/ai-design` | `design-references/miniprogram-airy-minimalist-v1/37-ai-design-workbench.jpg` | 家装设计顾问「设计」Tab 已按 D01 还原：品牌头（仅品牌锁，不再并列「设计记录」任务流水入口）、绿色创建方案 Hero（含点数胶囊）、不带箭头和选中态的静态户型/拍照能力说明、严格按空间分类筛选配方并复用既有空态，以及进入 `scheme-studio` 的最近设计项目卡片；每张配方会使用兼容的输入模式打开。带 `leadId` 且含 `floorPlanId`/`workflowId` 的深链直达方案工作台。家装现场顾问/企业负责人替代壳层不变。聚焦合同测试通过；登录态 `390x844` 原生胶囊截图交由负责人本地核验 | 是 |
| `packages/ai-workflow/create/ai-design-create` | `design-references/all-pages-ip-v3/14-ai-design-create-v3.png` | 已有还原 | 是 |
| `packages/ai-workflow/recipe-detail/recipe-detail` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | 当前方案说明和输入合同已实现；原生截图待补 | 是 |
| `packages/ai-workflow/recipe-project/recipe-project` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | 选择逻辑对齐后台工作台：先客户（`studio/leads`，可设计/待量房）→ 再方案对话（`studio/workflows`，点已有方案续聊，「新建」另开命名方案）→ 再「应用到哪里」（默认完整户型 / `whole_floor_plan`，闭合房间来自该方案绑定户型）。方案卡片沿用既有封面槽，优先显示最近已确认图的签名地址，否则最近成功出图，没有生成图才用方案册占位图。复用既有卡片/搜索壳，列表主键不再是 `/ai/sources` 正式量房。客户列表走 `studio/leads` 分页加载更多。原生截图待补 | 是 |
| `packages/ai-workflow/recipe-confirm/recipe-confirm` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | 确认页读取已选方案详情并续接该对话；拍照来源为拍照 / 从微信相册选择 / 从本户现场图选择；新拍须先选房间标签写入本户图库并直接复用该 assetId。创建并启动任务后 redirect 进入 `scheme-studio`（无方案的孤立任务仍进 `ai-design-result`）。原生截图待补 | 是 |
| `packages/ai-workflow/result/ai-design-result` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | 当前生成、失败、交付及真实客户发布控制已实现；原生截图待刷新 | 是 |
| `packages/ai-workflow/history/ai-design-history` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | 页面与历史 API 保留；设计 Tab 顶栏不再进入本路由。孤立任务结果页仍可「查看历史」。任务筛选与真实任务卡片保留；已同步方案任务提供「进入方案」跳转 `scheme-studio`。原生截图待补 | 是 |
| `packages/ai-workflow/scheme-studio/scheme-studio` | `design-references/miniprogram-airy-minimalist-v1/45-ai-scheme-studio.jpg`、`45b-ai-scheme-studio-templates.jpg`；出图条按用户确认的小云雀底栏交互落地到家客来 token（方案区仍用 D09） | 移动端 AI 出图台：默认收缩悬浮 dock（主题绿 `#00c365` 点数条白字 + 单行输入 + 生成按钮保持在 dock 内）；聚焦后展开控制图行、提示词，再在输入框下方显示不横滑的底栏（**户型** / **模型** / **模板** / **设置** + 生成钮）；点工具先取消输入框聚焦、等原生键盘收起再弹出底部列表，选中或点遮罩取消后都恢复聚焦；「应用到哪里」走**户型**入口与「出图设置」行（默认完整户型，闭合房间为单房间，说明「只应用到当前选择…」），续聊批次提交 `targetScope`/`roomId`；参考图槽锁定展示绑定户型控制图缩略图（选闭合房间时为该房间裁切图，与批次首张参考图一致）；额外用户参考图来自先选房间标签的拍照/相册写入本户图库，或点选本户现场图（直接用 assetId）；顶栏动作为「切换方案」；发送/更新弹窗可改方案名，并同时写入工作流标题与客户可见图集名；发送/重命名/定稿确认钮包在 flex 单元格内，避免原生 `button` 撑出弹层，底部保留 `constant()`/`env()` 安全区；已发送方案横幅旁增加「设为定稿」（复用确认弹窗；已定稿时横幅改为「客户可见定稿」，构图不变）；项目卡与空轮次卡之间保留与轮次列表一致的 24rpx 间距；方案区滚动留白随 dock 状态变化，底部渐变 scrim 与白色方案卡分离。模板 sheet 保留分类/搜索与先放大再使用。登录态 `390x844` 截图待补。Limited：无后台画布标注精修、无深色主题 | 是 |
| `packages/surveying/editor/surveying-editor` | `docs/superpowers/specs/2026-08-21-surveying-cursor-reticle-design.md`；用户确认的图一绿色准星裁切；既有顶部栏仍参考 `design-references/surveying/cursor-guide-state-reference-20260812.jpg` | 画布默认光标与底部「重置光标」「光标拖动到墙体」共用细线版图一绿色准星（`drawCursorGlyph` + `icons/cursor-reticle.png`，底部图标 108rpx ≈ 画布 56px 外臂跨度）；两种文案都放在底部操作栏容器下方。底部拖放把准星瞄准在指尖左上 24×40 CSS px（夹紧到画布）；吸附、左上角放大镜和松手都以瞄准点为准。Dock 拖放把 touchmove 限到 24ms，自由跟手帧跳过吸附并只擦准星脏区。画布拉墙使用粘滞抓取和右下偏命中，不套用 Dock 偏移。拖动放大镜只在中心叠 12px 绿色小十字，不放大图一准星（正式画布裁切使用 `omitCursor`）。「合」仍画在 Canvas 并跟随平移/缩放；CAD chip 合同不变。顶栏返回箭头保持原字形，点击区为 `88rpx` cover-view 并画在居中标题层之后，避免标题层抢走点击。聚焦渲染/控件/瞄准映射测试已覆盖字形、放大镜十字、dock 资源、返回点击区与跟手偏移；新的 `390x844` 原生胶囊截图待补 | 是 |
| `packages/business/promotion-service-code/promotion-service-code` | `design-references/enterprise-code-presenters-fullscreen-v1/staff-activity-code-fullscreen-v1.png`（用户指定按活动码出示页对齐元素尺度；仍不展示企业名） | 推荐人推广码出示页现与活动码共用内容固有高度的薄荷海报尺度：大标题/副标题、二维码框、右下叠放扫码引导小 K、说明、白色权益卡、保障条和分享按钮连续阅读。`390x844` 台账：导航 `36rpx`；标题/副标题 `64/30rpx`；二维码框/图 `478/450rpx`；小 K `260rpx`；说明 `30rpx`；权益图标/标题/说明 `92/26/22rpx`；保障文案 `24rpx`；CTA 高 `96rpx`、字 `30rpx`。复用已打包 `packages/business/assets/code-presenter-v3/{xiao-k-scan-presenter,home-outline,advisor-outline}.png` 与 V2 扫码线框；`¥0` 为原生文字圆章。匿名出示（不展示企业名）、领取页分享目标、成员关系 API、加载/错误恢复和胶囊安全返回不变；`open-type="share"` 主按钮仍为「分享给客户」。聚焦合同测试通过；更新后的登录态 `390x844` 原生胶囊宿主截图待补 | 是 |
| `packages/business/staff-activity-code/staff-activity-code` | `design-references/enterprise-code-presenters-fullscreen-v1/staff-activity-code-fullscreen-v1.png` | 活动码按批准稿还原为内容固有高度的薄荷主海报：大标题/副标题、二维码框、右下叠放的扫码引导小 K、说明、白色权益卡、保障条和分享按钮连续阅读，不再依赖撑满视口的弹性空白。`390x844` 台账：导航 `36rpx`；标题/副标题 `64/30rpx`；二维码框/图 `478/450rpx`；小 K `260rpx`；说明 `30rpx`；权益图标/标题/说明 `92/26/22rpx`；保障文案 `24rpx`；CTA 高 `96rpx`、字 `30rpx`。独立透明 ImageGen PNG 映射到 `packages/business/assets/code-presenter-v3/{xiao-k-scan-presenter,home-outline,advisor-outline}.png`；`¥0` 为原生文字圆章，扫码线框沿用 V2 资源。所有生成文件均为小于 `300KB` 的 alpha PNG。胶囊安全品牌锁/返回、企业名、分享目标及资料补全路径不变。聚焦合同/分包检查通过；更新后的登录态 `390x844` 原生胶囊截图待补 | 是 |
| `packages/business/enterprise-join-codes/enterprise-join-codes` | `design-references/enterprise-code-presenters-fullscreen-v1/enterprise-join-codes-fullscreen-v1.png` | 企业负责人双码页按批准稿拆分为两段：Tab 下方为内容固有高度的薄荷二维码卡（大标题/副标题、二维码、叠放小 K、说明），卡外白底为三步路径，再接保障条和分享/换新/停用。`390x844` 台账：导航/Tab `36/32rpx`；标题/副标题 `56/28rpx`；二维码框/图 `380/352rpx`；小 K `188rpx`；说明 `28rpx`；路径图标/标题/说明 `92/26/22rpx`；保障文案 `24rpx`；动作高 `96rpx`、字 `30rpx`。独立透明 ImageGen PNG 映射到 `packages/business/assets/code-presenter-v3/{xiao-k-scan-presenter,scan-journey,role-journey,start-journey}.png`；扫码线框沿用 V2 资源。入驻码两个 Tab 均在 info-bar 与 action-dock 之间保留 26rpx 文字链「查看已入驻推荐人」，进入负责人专属 `enterprise-referrers`；二维码卡、路径轨、生成/换新/停用不改。经营台 Hero 在获客双砖下增加整行「已入驻推荐人」，「我的」同步该快捷入口。生成/换新/停用确认、查看不换新、胶囊安全返回、入驻分享目标及 API/角色边界不变。聚焦合同/分包检查通过；更新后的登录态 `390x844` 原生胶囊截图待补 | 是 |
| `packages/business/enterprise-staff/enterprise-staff` | 复用 R03 `20-enterprise-appointments.jpg` 卡片语言 + 领取页胶囊安全返回；向导小 K 用已打包 `images/airy-v1/xiao-k-measurer-3d.png` | 企业负责人深链人员名册：家装设计顾问/家装现场顾问可派状态、角色 chips、经 `GET/PATCH /api/miniprogram/enterprise-staff` 暂停/恢复派单，空态出示入驻码；不做建账或改微信/二维码。经营台人员缺口「查看详情」与「人员负荷」进入本页。状态胶囊用 flex 垂直居中 + `22rpx`/`line-height: 1`，避免「可派单」「待补微信资料」贴顶。描边「电话联系」使用 `.sfp-icon-action` + `images/leads-v4/phone.png`（实心暂停/恢复派单主钮不加图标）。名册 GET 分页（`page`/`limit`，默认 20），页脚 `.sfp-list-footer` 加载更多；汇总用 COUNT。聚焦合同测试通过；登录态 `390x844` 原生胶囊截图待补 | 是 |
| `packages/business/enterprise-referrers/enterprise-referrers` | 复用 `enterprise-staff` / R03 `20-enterprise-appointments.jpg` 卡片语言 + 领取页胶囊安全返回；向导小 K 用已打包 `images/airy-v1/xiao-k-measurer-3d.png` | 企业负责人深链推荐人名册：芯片「全部 / 活动 / 已停用 / 已退出」、姓名/手机搜索、`wx.makePhoneCall`，经 `GET/POST /api/miniprogram/enterprise-referrers` 停用后续扫码（能力 `enterprise.operations`）。空态出示入驻码。无重新启用；历史线索和提成不变。已停用/已退出卡把打包电话图标贴在号码同一行（对齐线索详情电话行），不再单独开操作底栏；活动卡保留双钮底栏：左「电话联系」、右「停用后续扫码」。入口：经营台 Hero「已入驻推荐人」、我的「已入驻推荐人」、入驻码页两个 Tab 的文字链。名册 GET 分页，页脚 `.sfp-list-footer` 加载更多；芯片/搜索汇总用 COUNT。聚焦合同测试通过；登录态 `390x844` 原生胶囊截图待你手动机提供 | 是 |
| `packages/business/free-design-service/free-design-service` | `docs/superpowers/specs/2026-08-25-free-design-service-contact-conversion-design.zh-CN.md` | 手机号授权态继续使用内容固有高度三权益结构；成功态继续以档案 Hero、原生「服务进度 / 户型档案 / 设计方案」目录和唯一绿色「查看服务档案」为主，设计师姓名/真实 `professionalProfile` 背书及固定 `148rpx`「查看微信」为辅助。用户现批准已有服务归属态 C 版 `design-references/free-design-service-existing-redesign-v2/option-c-continuity-archive-drawer.png`：生产已用原生 WXML/Less 落地「已为你接续原有服务」、真实阶段/更新时间、三段路径、三层可点击档案签、唯一绿色「继续查看服务档案」、紧凑设计师真实背书与归属保护说明；原第二个通栏联系按钮已移除。「现场顾问/上门」阶段归入量房安排。批准稿中的档案柜小 K 映射到 `720×346`、`25733` 字节的索引色透明 `xiao-k-continuity-archive-drawer.png`，文字、进度和控件均为原生节点。已有态元素台账为 Hero `48/28rpx`、归属徽标/服务标识 `24/22rpx`、阶段标签/值/更新 `22/36/22rpx`、阶段圆/文字 `46/24rpx`、档案柜舞台 `318rpx`、目录 `24rpx`、CTA `100/32rpx`、设计师 `32/24/22/24rpx`、背书 `22/32rpx`。聚焦档案柜、透明 PNG、字号和分包检查通过；更新后的已有态高屏及 `390x844` 原生胶囊运行图待用户手动复核 | 是 |
| `packages/business/onboarding/onboarding` | 推荐人就绪态唯一设计源为用户确认的 `design-references/onboarding/referrer-enterprise-invitation-20260828-v7.png`；员工/恢复态继续使用 `design-references/miniprogram-airy-minimalist-v1/16-onboarding.jpg` 与 `17-onboarding-recovery.jpg` | 推荐人扫码就绪态按新稿还原为胶囊安全品牌锁、薄荷建筑 Hero（**欢迎加入 / 推广团队**）、三项真实权益、企业邀请卡、三步推广流程、手机号授权保障和 **立即加入推广团队** CTA；动态企业名仍为原生文本。本次根据运行截图将旧灰色 mine 图标替换为独立生成的透明 PNG：Hero/步骤二维码、进度、提成图标，企业推广人人像加号图标，以及 CTA 高对比白色盾牌，映射至 `packages/business/assets/onboarding-referrer-v7/{promotion-code,promotion-progress,promotion-commission,promotion-person-plus,promotion-cta-shield}-v7.png`（均小于 `5KB`）；Hero 小 K 和楼宇继续使用 `images/onboarding-referrer-v7/{xiao-k-promoter-hero-v7,enterprise-building}-v7.png`（`620×688`，`274,863` 字节；`840×560`，`75,311` 字节）。所有图均从设计语言独立生成的透明 PNG，未切割组合稿。`390x844` 元素台账：Hero 标题 `58/78rpx`、放大的权益图形 `34rpx`、企业卡标题 `48rpx`、邀请首句保持单行 `23rpx`、步骤图标/序号/标题/说明为 `104/38/27/22rpx`，序号与标题采用同一对齐行、授权行 `24rpx`、CTA 高 `96rpx`/文案 `34rpx`。员工就绪/17 号恢复态行为、API、角色、令牌解析与重开、授权和姓名确认流程不变；「加入后即可获取专属推广码」仅展示、不再作为跳转操作。聚焦合同与分包资源检查通过；更新后的原生胶囊运行态截图待手动核验 | 是 |
| `packages/business/enterprise-register/enterprise-register` | 无独立设计稿；复用入驻页品牌锁与 `miniprogram/DESIGN.md` + `design-tokens.json` 极简透气体（无新 IP 插画）。返回箭头对齐领取/出示页自定义顶栏（`09`/`11`） | 平台 `er_` 开户：解析 → 填写对齐 Web 的表单 → 联系人手机号支持手动输入 → 点击「授权手机号并提交」调起微信手机号授权并立即提交。手动输入仅用于填写便利，仍受 API 约束，必须与微信授权手机号一致；表单内明确提示“可手动填写、需授权验证”和“授权成功后自动提交”，号码不一致时留在表单并提示修改。企业全称、信用代码、联系人姓名未齐时主按钮仍用 `--action-disabled-bg` 薄荷底（与 `#f8faf9` 页底区分，不使用微信原生 `#f7f7f7`）但保持可点，页上列出「还需填写：…」，点击后对应输入框标红并显示「请填写…」；`getPhoneNumber` 仅在三项填齐后绑定。胶囊安全品牌锁含领取页返回箭头（有上一页 `navigateBack`，否则角色落点或「我的」），避免开户码把该页做成栈根后无法离开。之后从微信最近使用/主入口/桌面冷启动时任何已签名身份都会离开开户页；真正扫码或会话分享进入时任何已签名身份（含工作台）都留在表单。表单/成功/失败/恢复复用入驻页 identity-link / state-action 提供「去登录」，表单链为原生按钮；审核通过后的 `ACCOUNT_CONFLICT` 进入已有账号退出；授权手机号若已是工作台身份则进入已有账号态并用 `wx.showModal` 确认是否离开。聚焦合同测试已通过；登录态 `390x844` 原生胶囊截图待补 | 是 |
| `packages/business/onboarding-debug/onboarding-debug` | 仅开发版扩展入驻页已确认的 `miniprogram/DESIGN.md`、`design-tokens.json` 与 `app.less` 语言；不设单独的生产设计源 | 仅开发版可用。原生扫码器选择本地小程序码，仅接受入驻目标路径，随后进入正常的服务端校验、手机号授权和身份写入流程；非开发版锁定该路由 | 否 |
| `packages/business/referrer-workbench/referrer-workbench` | `design-references/miniprogram-airy-minimalist-v1/06b-promoter-workbench-signed-count.jpg` | 保持设计稿构图并提升可读性：顶栏不再放装饰性扫码/提醒图标，把空间留给「家客来 · 推广端」；身份栏与员工/经营端对齐：胶囊行居左 Logo +「家客来 · 推广端」，登录用户名叠在「家客来」下方并收进同一胶囊高度（40rpx 品牌标，长名单行省略，去掉原先同一行的灰色身份胶囊）；品牌标使用生产 `/images/home-ip-v1/brand-logo.png`（房屋轮廓 + JK，与客户/员工工作台同一套品牌锁），不再用合成稿里的绿色 `JK` 文字方块；导航标题 `32rpx`；业务主文字为 `24–28rpx`，说明文字为 `20–22rpx`。企业胶囊恢复为设计稿横向选择区中的单行自然宽度，不再按序号写死宽度，长企业名、后续企业和“加入企业”入口彼此独立。“加入企业”调用原生扫码，仅接受带码参数的既有入驻页路径后进入服务端校验的入驻流程。Hero 数量位在扫码/客户/待发放笔数旁增加只读「已签约」（来自进度 `stage.key=converted`），本角色不上完整经营大盘，也不展示待结收益金额。280rpx「出示推广服务码」CTA 用内层 view 包图标和文案并 `nowrap`，避免 ColorOS/一加上末字换行。本页不再放切换身份与退出当前账号（改由「我的」承担），保留「退出该企业」。推广工作台与角色导航回归测试通过；里程碑状态标签 flex 垂直居中 `22rpx`。当前 DevTools 窗口没有可验证的 automator endpoint，登录态 `390x844` 宿主截图待补 | 是 |
| `packages/guides/referrer-guide/referrer-guide` | `design-references/role-onboarding-v1/referrer-onboarding-v1.png` | 三张胶囊安全的原生引导页依次讲清服务码、脱敏进度与本人收益。三步内容为原生非循环 `swiper`（可左右滑动或点步骤点回看，「下一步」仍前进，不自动播放）。批准整页稿不切片：ImageGen 独立生成的透明业务插画映射为 `packages/guides/assets/referrer-v1/service-code.png`（720×531，86,655 字节）、`progress.png`（720×359，116,232 字节）和 `earnings.png`（720×461，123,044 字节）；标题、说明、保障文案、步骤点、跳过和 CTA 均保留原生 WXML。页面与插画全部进入独立 `packages/guides` 分包，主包只保留很小的版本化角色引导注册与「我的」触发逻辑。推荐人进入工作台后按本地已签名账号/角色/版本仅自动展示一次，可跳过，也可从「我的」重复查看；末页 CTA 打开当前成员关系对应的既有服务码页。深链要求 `referrer.promotion`。个人用户不强制展示，也不放置未批准引导占位。`390x844` 元素台账按内容固有高度实现：胶囊安全导航标题 `32rpx`，插画舞台 `690rpx`，步骤标题/正文 `40rpx`/`24rpx`，保障说明/盾牌盒 `22rpx`/`26rpx`，步骤点上距 `24rpx`，`96rpx` 主按钮使用 `28rpx` 字号并保留底部安全区。自定义顶栏改为 `border-box`，把 `navigationRight` 算进标题行宽度，避免「跳过」被原生胶囊挡住。导航、版本存储、分包体积、透明通道、字号和三步行为聚焦检查通过；原生胶囊运行态待用户手动截图核验 | 是 |
| `packages/business/referrer-progress/referrer-progress` | `design-references/referrer-customers-v1/referrer-customers-v1.png`；打包 `packages/business/assets/referrer-customers-v1/xiao-k-customer-dossier.png`（531×720，18,329 字节索引色透明 PNG） | 当前成员关系的胶囊安全「客户」Tab：薄荷绿 Hero「我的推荐客户」+ 小 K 客户档案管家、记录编号卡片、实底绿色「撤回」，以及 10 分钟窗口内的实底「恢复」。加载/重试/空态不暴露手机号、精确地址、户型 graph、内部预约原因或设计文件。推广人 TabBar 与工作台入口现为「客户」。元素台账：导航 `34rpx`、Hero 标题/说明 `36/24rpx`、小 K 盒 `176×212rpx`、记录编号 `32rpx`、阶段/操作 `24/26rpx`、「撤回」胶囊高 `64rpx`。客户列表 `onReachBottom` + `.sfp-list-footer` 分页。聚焦路由/隐私/素材检查通过；认证 `390x844` 原生胶囊截图待用户提供 | 是 |
| `packages/business/referrer-earnings/referrer-earnings` | 用户明确授权第 13 阶段按 `miniprogram/DESIGN.md`、`design-tokens.json`、`app.less` 及现有绿色工作台语言直接开发 | 当前成员关系下仅展示本人的待发放/已发放**笔数**汇总及脱敏客户标识与发放状态（不展示金额）；加载、重试与无数据状态已实现。行列表 `onReachBottom` + `.sfp-list-footer` 分页，笔数仍用 COUNT。已在微信开发者工具 `390x844` 核验收益汇总、空态 CTA 和胶囊安全区。发放状态胶囊 flex 垂直居中 | 是 |
| `packages/business/staff-earnings/staff-earnings` | 与 `referrer-earnings` 同一套第 13 阶段收益语言（`miniprogram/DESIGN.md`、`design-tokens.json`、`app.less`）；仅替换家装设计顾问/家装现场顾问文案 | 当前企业下仅展示已签名家装设计顾问或家装现场顾问本人的待发放/已发放**笔数**汇总及脱敏客户标识与发放状态（不展示金额）。发放状态胶囊 flex 垂直居中。行列表 `onReachBottom` + `.sfp-list-footer` 分页。聚焦 TabBar/能力测试已通过；认证 `390x844` 原生胶囊核验仍待补 | 否 |
| `packages/business/enterprise-commissions/enterprise-commissions` | 用户要求的付款台账重设计：`design-references/enterprise-commissions-payout-ledger-v1/enterprise-commissions-payout-ledger-v1.png`；金额调整扩展继续以该稿的岗位行与操作层级为设计源 | 企业负责人付款台账继续采用“客户完整付款批次”。推荐人、家装设计顾问、家装现场顾问三种岗位的每条待支付行，均在绿色「确认线下付款」旁新增薄荷色次级「调整金额」操作；它打开固定到视口的原生金额弹层，接受不小于 0、最多两位小数的新金额，接口失败时保留输入和错误，成功后刷新汇总但不自动付款。`PATCH /api/miniprogram/enterprise-commissions/[id]` 仅企业负责人可用，按租户复用既有待支付调整审计，不能改受益人；已支付和已作废行保持只读。记录 ID 缺失/过期及重复提交都有可见反馈。待支付 `¥0.00` 仍保留独立的快速付款记账路径：`POST /api/miniprogram/enterprise-commissions/record-zero-payment` 以「小程序线下付款补录」写入审计并在同一事务标记已支付；常规小程序 `markPaid` 拒绝零金额，包含零金额的批次不显示批量确认。两个居中金额弹层都使用 `border-box` 外壳与可收缩的 flex 操作行，窄屏下原生按钮保持在卡片内。顶部汇总、筛选、服务端分页、付款核对员小 K 素材（`100×102`、`14,456` 字节 RGBA PNG）及已批准客户批次构图不变。`390x844` 元素台账：导航标题 `36rpx`，Hero 标题/说明/主金额 `38/22/48rpx`，小 K 盒 `228×232rpx`，分区标题 `30rpx`，客户/金额 `28/32rpx`，岗位/操作 `24rpx`，说明/状态 `22rpx`，两个行操作均高 `68rpx`，金额调整/快速记账弹层标题、输入、操作为 `32/32/26rpx`。聚焦 model/page/API 合同与弹层边界检查通过；两操作行和打开金额弹层的认证 `390x844` 原生胶囊运行截图仍待用户手动提供 | 是 |
| `packages/business/customer-projects/customer-projects` | 仅兼容深链壳（产品列表已退役；主卡排序/切换由阶段陪伴服务首屏承接，见 `docs/superpowers/specs/2026-08-21-customer-service-home-stage-companion-design.md`） | 不再作为产品项目列表。进入后按当前 JWT 客户未归档项目排序，重定向到最高优先级服务档案（`customer-project`），或回落「服务」Tab；无 TabBar 列表 UI。真实深层页仍是无 TabBar 的服务档案。旧外链仍可落到本路由 | 是 |
| `packages/business/customer-project/customer-project` | `design-references/customer-project-archive-redesign-v4/customer-project-archive-balanced-color-v4.png` | V4 已用原生 WXML/Less 落地：小 K 只在压缩后的档案整理 Hero 中出现一次，四步进度改为编号路径；预约区使用暖杏色并统一采用较温和的客户文案「预约量房」，状态徽章与标题同行，说明文「选择方便的时间，家装现场顾问会提前与你确认」单独占满一行并可换行，不再被徽章挤成省略号；家装设计顾问卡为薄荷绿、家装现场顾问卡为雾蓝；户型档案、房屋现场图与交付方案合并为一册连续目录，并分别使用绿/琥珀/蓝状态语义。用户已批准将 Hero 主副标题写死为「您的家装顾问 / 现场顾问与设计方案全记录」，不再展示线索姓名。人员已分配时渲染服务端按岗位卡解析的 `professionalProfile`（可见时的 `title`，以及 `experienceLabel` / `serviceLabel`），头衔按后台录入原文展示，不再改写「设计师/测量员」；家装设计顾问卡角色为「家装设计顾问」，不加「专属」。既有家装设计顾问微信联系、人员拨号、预约/改期/查看预约、受保护户型预览、带空间标签的现场图采集（房间标签弹层打开时隐藏吸底原生按钮栏）、只读方案册及方案海报分享仍沿用当前真实数据与权限合同。用户首张高屏运行截图暴露旧资源替代和间距偏松；当前已改为许可已记录的同套圆角线性图标（日历、人物、尺子、文档、图片、交付文件、电话、微信），源 SVG 位于 `docs/icon-sources/customer-project-v4/`，生产 PNG 位于 `miniprogram/packages/business/assets/customer-project-v4/`。按设计源校准的元素台账为：导航 `36rpx`，Hero 标题/说明 `44/26rpx`，小 K 容器 `118×132rpx`，步骤圆/标签 `38/24rpx`；预约标题/说明/CTA `34/24/28rpx`，图标容器 `84rpx`、可见图形 `60rpx`；服务标题/角色/背书 `32/28/24rpx`，人员图标容器/可见图形 `72/50–52rpx`，人员卡最小高度/内边距 `296/20rpx`；档案标题/说明/状态 `30/24/24rpx`，档案行最小高度 `110rpx`，图标容器/可见图形 `72/52rpx`；底栏文字/联系图标 `28/34rpx`。聚焦源码/模板测试通过；修订后的登录态 `390x844` 原生胶囊与高屏光学比例仍待用户手动运行截图复核 | 是 |
| `packages/business/customer-ai-schemes/customer-ai-schemes` | `design-references/miniprogram-airy-minimalist-v1/44-customer-ai-scheme-timeline.jpg`（D08） | 已按 D08 落地只读客户 AI 方案册：胶囊安全自定义导航、客户摘要 Hero（房屋体块小 K `project-delivery-xiao-k.png`，不用机器人图）、方案轮次 chips（定稿套 chip 前缀「定稿」）、当前轮主预览与交付时间轴、底部返回/（客户）保存分享；无 `schemeId` 深链时默认选中定稿套，否则仍选最后一轮；客户分享打开 `scheme-share-poster`（当前轮封面 + 方案名 + 家客来品牌，无二维码），存相册后经 `wx.showShareImageMenu` 分享图片并隐藏胶囊页面转发，不再转发小程序卡片；非主人从海报「打开小程序」进入时复用本 D08 方案册，数据走 `GET /api/miniprogram/published-scheme-folios/[leadId]`，不展示保存/分享；员工态仍无分享钮；零生成零编辑。入口来自线索详情（`mode=staff`）、客户服务首页「免费效果图」与客户服务档案（`mode=customer`），数据分别复用 `GET /api/leads/[id].publishedSchemes` 与客户项目聚合。聚焦合同测试通过；登录态 `390x844` 原生胶囊宿主截图待补 | 是 |
| `packages/business/appointment-detail/appointment-detail` | `docs/superpowers/specs/2026-08-21-appointment-detail-inline-reschedule-design.md`；`design-references/miniprogram-airy-minimalist-v1/03-appointment-reschedule.jpg`；员工次要动作行对齐 `07-measurer-calendar.jpg` 卡片按钮语言；吸底栏对齐服务档案 `footer-bar` 全宽背景壳 | 可改期时在详情内嵌可用时段选择（5 日窗 + 时段）；归档/找不到/已签约/已关闭线索视为终态，不拉取可用时段。吸底为全宽半透明页底色父容器（含安全区）+ 取消\|确认约 0.9∶1.3 flex、20rpx 间距、统一 26rpx 居中；未选时段时确认按钮用 `--action-disabled-bg` 薄荷底与 `#f8faf9` 栏底区分；页面 `padding-bottom: 200rpx` 让出操作栏；次要行改地址∥导航等宽胶囊 + 16rpx 间距，按钮内用已打包编辑/定位 PNG（无 emoji）；「拍现场图」同日历卡片按钮语言，先选房间标签再写入本户图库；房间标签弹层用 `root-portal`（z-index 2000），打开时隐藏吸底原生取消|确认栏，避免客厅/主卧等标签被挡住；开始量房用 `ruler-green.png`。聚焦测试通过；登录态 `390x844` 原生胶囊截图待补（含打开中的房间标签弹层） | 是 |
| `packages/business/appointment-reschedule/appointment-reschedule` | 对齐 `03` / 详情内嵌改期的兼容跳转壳 | 不再渲染独立改期 UI；`onLoad` `redirectTo` 到 `appointment-detail`（`internal` → 员工详情；缺省/`customer` → `mode=customer`）。入口优先直达详情。聚焦壳测试通过 | 是 |
| `packages/business/appointment-booking/appointment-booking` | `design-references/referrer-network-appointment-v1/phase-5-designer-appointment-booking-v1.png` | 保持已批准的预约页结构，把原地址行拆为原生地图选地点与手输楼栋/单元/门牌；「选择地点」/「重新选择」使用 `.sfp-icon-action` + `map-pin.png`。客户本人和负责家装设计顾问提交真实可用时段、服务详细地址及可选 GCJ-02 地图点；聚焦测试通过。新的家装设计顾问/客户态 `390x844` 截图待补 | 是 |
| `packages/business/measurer-calendar/measurer-calendar` | `design-references/miniprogram-airy-minimalist-v1/07-measurer-calendar.jpg` | 已按极简透气版家装现场顾问量房日程重构：带胶囊安全区与月份切换的顶部导航、带排班统计与 3D 测量搭档小 K 的轻量卡片、7 日横向周历选择条（带任务指示点）、结构化量房预约卡（直达量房编辑器、电话/导航/开始量房快捷操作，按钮内为已打包 PNG、无 emoji）、不可用时段快捷维护卡。预约状态标签与 Hero 统计胶囊 flex 垂直居中。全部测试通过 | 是 |
| `packages/business/measurer-unavailability/measurer-unavailability` | `design-references/miniprogram-airy-minimalist-v1/33-measurer-unavailability.jpg` | 已按极简透气版不可用时段管理重构：智能防护说明卡、触控日期选择、分段式时段选择（全天/上午/下午/自定义）、预设高频请假标签与备注输入、大圆角保存主按钮与已生效卡片列表（含删除操作）。全部测试通过 | 是 |
| `packages/business/enterprise-appointments/enterprise-appointments` | `design-references/miniprogram-airy-minimalist-v1/20-enterprise-appointments.jpg` | 企业负责人预约调度是从工作台「查看预约安排」进入的无 TabBar 二级页（`navigateTo`，胶囊安全返回）。已按 R03 还原：「预约调度中心」标题与所选周期真实预约计数、本周/本月/本年芯片加自定义周期（复用经营大盘周期控件）。自定义选中区间显示在「自定义」芯片右侧（同年为 `8/1 ~ 8/31`，跨年保留年份），避免被微信胶囊截断；顶栏副标题只保留「自定义 N 单预约」。所选上海日历窗的横滑日期条、`confirmed`/`expired` 左色条卡片；未结束线索的过期或已过结束时间预约若早于今日，仍会出现在今日列表以便协调；归档或找不到关联线索按 `closed` 水合，只读「已关闭」角标，不再画出可点进可用时段的假「客户量房」卡；`converted`/`closed` 线索改为只读「已签约」/「已关闭」角标，隐藏 CTA 且不可进入预约详情。不发明调度按钮文案以外的新动作，也不展示面积/户型/家装现场顾问手机等未返回字段。卡片状态胶囊 flex 垂直居中 `22rpx` 文案。聚焦合同测试通过；登录态 `390x844` 原生胶囊宿主截图待补 | 是 |
| `packages/business/promotion-records/promotion-records` | 沿用现有企业报备页与已打包 `packages/business/assets/promotion-records/hero-scene.jpg`（不做新的 airy 地推首页）；渠道地推 TabBar「报备/我的」复用 `tab-home` / `tab-mine` | 渠道地推 bootstrap 落点；页面内嵌 custom-tab-bar。各 Tab 列表分页（`page`/`limit` 与 `search=`），页脚 `.sfp-list-footer`；已超时仍走有上限的工作台待办 API。身份/TabBar/我的回归测试通过；登录态 `390x844` 原生胶囊截图待补 | 是 |
| `packages/platform/devices/devices` | 由主包 `platform-device-workbench` 迁出的整页；沿用胶囊安全身份顶栏与 BLE 录入/列表语言，无新设计稿 | 平台管理员「设备」Tab，落在普通分包 `packages/platform`（`reLaunch`，不进原生 `tabBar.list`）。BLE 扫描/录入/`GET/POST /api/miniprogram/devices` 合同不变，仅已登记设备行分页；企业选择器仍拉全量。聚焦合同测试通过；登录态 `390x844` 原生胶囊截图待你提供 | 是 |
| `packages/platform/enterprise-review/enterprise-review` | 沿用设备工作台胶囊安全顶栏 + 预约列表卡片语言；不新画小 K、不切后台截图 | 平台管理员企业审核队列：默认待审核、状态 chips、名称/电话/信用代码搜索、列表卡、通过确认、拒绝原因半屏、可拨打电话、顶栏「开户码」、内嵌 custom TabBar。队列 GET 分页，页脚 `.sfp-list-footer`，副标题用 COUNT。聚焦合同测试通过；登录态 `390x844` 原生胶囊截图待你提供 | 是 |
| `packages/platform/enterprise-review-detail/enterprise-review-detail` | 与审核列表同一套运营列表语言，另加领取页胶囊安全返回 | 平台管理员企业详情与后续状态动作（通过 / 拒绝 / 重新提交审核 / 停用 / 启用），不展示 AI/自动化密钥。聚焦合同测试通过；登录态 `390x844` 原生胶囊截图待你提供 | 是 |
| `packages/platform/registration-code/registration-code` | 入驻码出示信息结构 + 审核运营列表语言；主包 `scan.png` / `shield-check.png` / 品牌标，不新画小 K、不引用 business 分包资源 | 只读出示当前生效 `er_` 开户码（查看不换新）。入口：审核顶栏与「我的」账号行。聚焦合同测试通过；登录态 `390x844` 原生胶囊截图待你提供 | 是 |

| `packages/guides/customer-guide/customer-guide` | `design-references/customer-onboarding-v1/customer-onboarding-four-slides-v2.png`；「服务」入口 `design-references/customer-onboarding-v1/customer-service-home-guide-entry-v1.png` | 个人用户可选四页原生引导：宽屏服务首页的小K气泡文案为「我是小K / 想了解服务？/ 点击我带你看看」，点击气泡或小K进入非循环 `swiper`。`<=400px` 时隐藏多行悬浮气泡，改为 Hero 说明文案下方、正常排版流内的单行「点击我带你看看」入口，确保标题与完整小K均不被遮挡。不在首次进入自动弹出、不在「我的」重复入口，也不记录已读状态。四页依次说明三个免费权益、表达装修需求、真实需求沟通/预约量房/方案沟通步骤与既有服务档案；跳过和末页 CTA 都回到「服务」Tab。独立 ImageGen 透明 PNG 映射为 `packages/guides/assets/customer-v1/{three-free-benefits,home-archive,service-route,service-archive}.png`，每张均 <=300KB。`390x844` 下导航/标题/正文/保障/CTA 为 `32/40/24/22/28rpx`，插画舞台 `500rpx`；「装修服务向导」标题已强制单行，而「跳过」按钮保持不可压缩，标题可完整使用胶囊左侧导航可用区域，不会再出现无故换行。自定义顶栏改为 `border-box`，把 `navigationRight` 算进标题行宽度，避免「跳过」被原生胶囊挡住。原生胶囊运行态待用户截图核验。不新增能力、API 或权限边界 | 是 |

## 角色引导更新

已批准的重复查看入口现在覆盖 `referrer`、`enterprise_admin`、`designer` 与 `measurer`；企业负责人、
家装设计顾问和家装现场顾问路由、独立 `packages/guides` 页面及素材映射已在上方单独登记。四套工作身份引导均使用原生非循环 `swiper`，可滑动或点选回看。个人用户现有独立四步向导，仅由「服务」首页小K气泡手动进入；不强制出现，也不放在「我的」页重复入口。

## 记录规则

当前路由说明——`pages/enterprise-operations/enterprise-operations` 记录已包含当前生产修正：真实 canvas 为选中的非零 `contractAmountTrend` 点显示紧凑万元标注，并为峰值预留顶部标签区；零值点不显示标注，既有双序列/空态合同保持不变。用户提供的经营页效果图仍是设计源；刷新后的认证态 `390x844` 原生胶囊核验待用户截图。

- 使用 `miniprogram/app.json` 的标准路由作为唯一键。
- 记录设计映射和一条简短当前视觉核验结论。
- 截图、指标和测试日志放在本地证据目录，不放入 canonical 台账。
- 视觉还原变化时同步更新中英文台账。

English mirror: [miniprogram-design-restoration-ledger.md](./miniprogram-design-restoration-ledger.md)
