# Admin UI Current Contract
| `/ai-presets` — platform AI presets and formal-floor-plan constraint | Existing bright Ant Design/Admin Pro settings surface: a full-width `admin-panel-card` above the preset table explains injection scope, keeps the maintained English constraint in a counted multiline editor, shows saved default/custom plus unsaved-draft state, provides load-error retry, and exposes Save plus load-default-draft actions through shared operation feedback. The preset ProTable remains below with its existing filters and edit actions | Route and `ai-presets` permission remain platform `admin`/`super_admin`. `GET/PATCH /api/platform/ai-prompt-config` reads/writes `platform_configs.ai_prompt_config`; saving affects only later formal-plan-bound generation snapshots. Existing `/api/ai/presets`, preset editing, tenant AI permissions, and unbound free creation remain unchanged | Focused AI tests, ESLint, and the production build pass; authenticated Chrome visual QA at `http://localhost:3006/ai-presets` remains pending | Reopen for a prompt-config contract/permission change or a reproducible editor/table responsive defect |

This file records the current Admin UI rules and the latest state of routes
that have an approved refactor direction. It is not a chronological migration
log. Git history contains earlier refactor attempts.

## Shared contract

- Next.js App Router pages use the established Ant Design 5 and Ant Design Pro
  system, including the shared `AdminAntdProvider` token configuration.
- Use `PageContainer`, `ProTable`, Ant Design form/feedback primitives, and
  established Admin business components where they fit the workflow. Do not
  introduce a parallel UI system for new Admin work.
- Page frames keep a 20px stack gap between sibling sections, wrap page-header
  extras instead of compressing titles, and keep table/filter/action labels on
  one line. Buttons that render as links (or sit inside `Link`) keep their
  intended text color so primary green and danger surfaces stay readable.
- Business pages preserve route, API, tenant, role, and mutation boundaries.
- Shared Admin controls belong in `admin/src/components/admin/*` or the
  established business-component area.
- Visible mutations use the shared success/failure operation feedback UI.
- Use `http://localhost:3006` for visual checks. Authenticated checks use the
  user's existing Chrome session.

## Route record format

Keep at most one current record per route. A record contains:

| Field | Required value |
| --- | --- |
| Route | Current pathname and page owner |
| Visual scope | The current surface changed or restored |
| Boundaries | Unchanged API, permission, tenant, and navigation contracts |
| Verification | Latest focused test/build or visual evidence |
| Open issue | Only unresolved current risk or next reopen trigger |

Replace the previous record when a route changes. Do not append dated progress
notes, superseded design sources, or repeated test transcripts.

## Current queue

Choose an unrecorded or explicitly queued route for a generic refactor request.
Do not reopen a route marked `Hold` unless the user names it, a reproducible
defect identifies it, its workflow contract changes, or a new design direction
is explicitly approved.

## Current route records

| Route | Visual scope | Boundaries | Verification | Open issue |
| --- | --- | --- | --- | --- |
| `/staff` — staff roster, phone login, and professional endorsements | Existing Ant Design/ProTable roster keeps the endorsement forms and now treats the 11-digit phone as the visible primary login account. It shows the generated internal username and a first-login-change status, removes username/password from normal creation, explains initial password `123456`, and adds a row-level reset action with shared confirm/operation feedback | `POST /api/staff` generates `staff_e{enterpriseId}_{phone}` and marks the initial credential for change; `POST /api/staff/[id]/reset-password` is tenant-scoped, forbids self-reset, and limits enterprise owners to ordinary staff. Endorsement, assignment-capacity, WeChat-profile, menu, delete, and customer-DTO boundaries stay unchanged | Focused credential/migration tests and ESLint pass; production build passes. Authenticated Chrome visual QA at `http://localhost:3006/staff` remains pending | Reopen for an observed dense-action/responsive defect, a credential/endorsement contract change, or a permission/API boundary change |
| `/lead-pool` — designer claim pool and owner monitor | Established Ant Design/Admin Pro surface: summary cards, server-timed open-window ProTable, masked lead facts, designer claim feedback, and owner-only manual-assignment modal. It reuses the shared page frame and operation feedback rather than introducing a separate component system | Reuses the `leads` menu permission. Designers see only their signed enterprise pool and claim action; enterprise owners and platform roles see the monitoring table and existing manual-assignment path. Pool data stays masked before claim and every tenant is server-derived | Focused ESLint and assignment-racing/referral PostgreSQL tests pass. Authenticated Chrome QA on `http://localhost:3006/lead-pool` confirmed the empty monitor state and API response; the selected sidebar item was corrected to a pale-green background with readable green label/icon | Reopen for a populated-table responsive defect, a pool DTO/privacy change, or a claim/manual-assignment workflow change |
| `/assignment-settings` — racing rules and performance | Established Ant Design/Admin Pro settings and performance dashboard: one versioned rule form, active-version summary, high/standard grouping table, capacity utilization, and actual auto-distribution counters. Percentages and capacity use bounded numeric controls with shared success/failure feedback | Reuses the operations-workbench parent permission. Only enterprise owners and platform management roles may read/write settings or performance; changes create a new version and never mutate open-window snapshots. Staff-specific capacity override remains on `/staff` | Focused ESLint and deterministic 70/30/capacity tests pass. Authenticated Chrome QA on `http://localhost:3006/assignment-settings` confirmed the rule form, performance table, and save mutation creating version 2 | Reopen for a rule-contract change, a dense-table layout defect, or a new performance explanation requirement |
| `/lead-commissions` — merchant three-role commission workbench | Approved Phase-7 source plus lead-parent expandable ledger (one row per lead with shared customer/staff/appointment, payable total, and status summary; nested role/rule/amount/status/adjust table, default expanded) and Adjust modal on payable rows: amount and/or beneficiary change required, beneficiary and reason optional. Nested status tags (`待支付` / `已支付` / `已作废` / `已调整`) stay content-sized (`Flex align="flex-start"`) instead of stretching across the status column. Rule cards and ledger filters (including lead source) remain as before | Route/API permission `lead-commissions` stays `super_admin`/`admin`/`enterprise_admin`; `PATCH /api/lead-commissions/[id]` accepts optional reason/beneficiary while still requiring amount and/or beneficiary change; `GET /api/lead-commissions/beneficiaries?role=` unchanged. Ledger referrer cells still use membership profiles | Nested status tags no longer stretch to column width; parent checkbox selects all payable children; nested checkboxes fine-tune; pagination is by lead; authenticated Chrome visual QA optional | Reopen only for a changed approved source, an observed populated-ledger layout defect, or a workflow/permission-contract change |
| `/referrer-network-operations` — referral-network operations and acceptance workbench | Established Ant Design/Admin Pro direction: readiness counts, an actionable real-state acceptance checklist whose rows expose direct operating links (including **企业安全密码**, which opens the shared avatar-menu settings modal instead of an inline card), a five-row join-code audit preview with Chinese action/result labels, and a **current-tenant test-data danger card** with two actions (preview counts, retained-operator note for reset, full enterprise-name confirm, “清空并重跑入驻” plus “删除整家企业（含企业壳）”). Dual-code CRUD, the referrer roster, assignment-eligibility tables, and the full audit are no longer on this page. Promotion-code readiness displays the persisted active-code count separately from active memberships and links to `/referrers`. The Mini Program code-provider item reports availability only and links to selected-tenant delivery diagnostics; it does not present platform credentials as enterprise-configurable | Route and `referrer-network-operations` permission remain limited to `super_admin`, `admin`, and `enterprise_admin` in the selected tenant. Sibling pages `/join-codes`, `/referrers`, and `/appointment-settings` reuse that permission. `GET/PUT /api/enterprise/sensitive-password` is `enterprise_admin` only and writes `enterprises.sensitive_operation_password_hash` (bcrypt; plaintext is never returned); the settings UI lives in the sidebar avatar menu via `AccountSettingsProvider`, not on this page. Reset APIs `GET/POST /api/enterprise/enterprise-reset*` and purge `POST /api/enterprise/enterprise-purge` (preview via `?mode=purge`) reuse the same permission, require exact enterprise-name confirmation, and are blocked when `NODE_ENV=production` (Docker Compose / `next start` included) unless `ALLOW_TENANT_ENTERPRISE_RESET=true` is in the runtime env. The danger-card empty state surfaces that preview API error. Reset keeps the enterprise shell plus the acting operator staff row; purge deletes all staff, status events, and the enterprise row (nulls `users.enterprise_id` only). After purge, merchant operators log out to `/login`; platform admins go to `/enterprises`. The checklist treats appointment defaults as pending until an administrator saves them; it never creates test records or bypasses customer authorization. Audit `eventType`/`result` display reuses shared `code-audit-labels` | Focused PostgreSQL tenant-reset/purge, attribution, Chinese audit-label, auth-password, and enterprise sensitive-password contract tests pass. Sensitive-password settings GET runs only when the modal opens and does not refresh readiness on load. Danger-card empty state now surfaces the preview API error, including the production env gate. Authenticated Admin visual QA of the split navigation remains to be added | Reopen only for an observed checklist/reset/purge/sensitive-password/audit-label defect or a workflow/permission-contract change |
| `/join-codes` — enterprise staff and referrer onboarding codes | Established Ant Design/Admin Pro direction: two code cards with rotate/disable plus an inline current-QR preview/download that does not rotate or auto-hide, and the full join-code audit table with Chinese action/result labels (shared `code-audit-labels`). Tokens never appear in the Admin page or list DTO. QR images remain private no-store Blobs for the current page session | Reuses `GET /api/enterprise/join-codes` (codes + events) and existing rotate/disable/image endpoints under `referrer-network-operations`. Image reveal does not rotate. Does not create referrer or staff records | Focused attribution and Chinese audit-label contract tests pass. Authenticated Chrome QA pending | Reopen only for a dual-code contract change or a reproducible QR/audit defect |
| `/referrers` — enterprise referrer roster and referral network | Established Ant Design/Admin Pro direction: enterprise owners and selected-tenant platform admins receive a `推广网络` / `全部推广人` segmented switch. designer/measurer sessions see a read-only `我的推广人` roster. The network is a collapsible employee tree with direct invitees, branch/active totals, zero-referrer employees, departed-inviter snapshots, and unassigned history; the flat ProTable preserves display name, phone, join time, promotion-code presence, membership status, name/phone search, and disable-scan. No Admin create form | The flat roster reuses `GET /api/enterprise/referrer-memberships`; `view=network` is available to selected-tenant `super_admin`, `admin`, and `enterprise_admin`; `designer`/`measurer` use the `referrers` permission with API scope limited to the current staff member. Historical leads and commissions are unchanged by disable | Focused roster contract tests cover referral-network routing, staff read-only roster, and permission markers. Authenticated Chrome QA pending | Reopen only for a membership-contract change or a reproducible network/roster defect |
| `/appointment-settings` — enterprise appointment policy | Established Ant Design/Admin Pro settings-sheet direction: confirmation banner, timezone, seven-day multi-window availability rows, and numeric booking boundaries with one primary save action | Reuses `GET/PUT /api/appointment-settings` and the `referrer-network-operations` permission for selected-tenant `super_admin`, `admin`, and `enterprise_admin`; the merchant sidebar now places this route under the referral-network category. Saving confirms the default policy without changing appointment-role APIs | Focused lint and `npm run build` pass. Authenticated Chrome on `http://localhost:3006` confirmed the confirmation state, loaded seven-day schedule, add-window interaction, booking boundaries, and enabled save action | Reopen only for a policy-contract change or a reproducible form defect |
| `/` — designer/measurer employee workbench and enterprise_admin business overview | Existing Ant Design/Admin Pro home: inventory totals (owner) retained; inserts a read-only period ops dashboard (`本周`/`本月` default/`本年` + custom RangePicker) with five KPI cards (new leads, completed surveys, scheme delivery rate, signed count, signing rate = same-window signed ÷ new leads). Staff see personal attribution without contract amount; enterprise admins see store-wide cards with optional signed-amount detail. Design source: `design-references/admin-merchant-dashboard-v1/` (`01`–`04`). The former home “协作待办” block (enterprise promotion records, legacy assignment/overdue todos, and old pending-settlement commissions) is removed from `/` because it belongs to the legacy enterprise-reporting workflow, not the referrer-network lead funnel. Measurers keep the Mini Program BLE boundary notice | Home derives role from the Admin Cookie session. Shared `loadOpsDashboard` powers `GET /api/workbench/ops-dashboard` (Admin Cookie) and `GET /api/miniprogram/workbench?period=` (Mini Program). `GET /api/workbench/staff` remains personal queues only. Legacy `/api/workbench/summary` and promotion-record todos stay available for `/promotion-records` but are no longer loaded on merchant home. `/leads`, `/measurements`, `/floorplans`, AI routes, and formal-survey boundaries are unchanged | Shared period helpers and signing-rate unit tests pass; Admin panel wired to MerchantDashboard/StaffWorkbench; legacy collab strip removed from merchant home. Role-authenticated visual QA against `01`–`04` pending | Reopen for a KPI-definition, period-boundary, attribution-scope, or formal-survey-entry contract change |
| `/leads` and `/leads/[id]` — lead operations and appointment handoff | Existing Ant Design ProTable conversion cards and the detail drawer hide scheme, appointment, profile, follow-up, publication, and retry CTAs when `status=closed` (including `terminationType=referrer_withdrawn`); 详情/归档 remain, and enterprise owners still see **重新激活**. Assignment-audit `eventType`/`errorCode` and claim-window `resolutionReason` use Chinese labels. Designer/measurer **分配**/**更换** stay driven by DTO `assignmentActions` (already false on closed). Export toolbar, muted 16px canvas rhythm, 20px header/footer inset, and `/leads/[id]` drawer handoff stay as previously restored | `PUT /api/leads/[id]` still rejects `assignedTo`. Tenant scope, archive/closed forbid, export/sensitive-password, and designer own-lead restrictions stay. Staff overwrite, role matrix, `assignmentActions`, lead-scoped roster, and confirmed-appointment rewrite (`staff_reassigned`, measurer slot 409 excluding this appointment) remain the workflow contract | Focused `lead-lifecycle`, `lead-assignment-feedback`, and `lead-assign-staff` contract tests; authenticated Chrome visual QA of a withdrawn lead remains pending | Reopen only for an observed populated-card layout defect, responsive clipping, export/sensitive-password contract change, or a further appointment/staff-assignment/closed-lead workflow-contract change |
| `/ai-studio/scenarios` — merchant AI workbench | Fullscreen studio window (no Admin sidebar): left lead search and named conversations from the current workbench list, right creation-studio composer (apply-to scope: default whole floor plan or a closed room, hidden when no formal plan is bound, helper copy 只应用到当前选择…, model/count/aspect/resolution, prompt templates, a locked control thumbnail in the reference slot that switches to the room crop when a closed room is selected, extra user references (a selected prompt-template cover is cloned into this stack), round gallery, send-to-customer). The apply-to Select uses a short trigger label so the generate action stays inside the composer frame. Conversation header shows the bound survey-canvas snapshot PNG for designer comparison (enlarge, generation side-by-side, formal viewer link), matching `/floorplans/[id]` walls, openings, dimensions, and room labels. The effect-only preview keeps a bounded viewport and reuses the creation studio image shortcuts for zoom, reset, clockwise rotation, fullscreen, and download. Night theme matches the creation studio; its create/send dialogs apply the existing dark secondary-control token to Cancel so its label remains legible. Day theme uses Admin `#16a34a` / `#f6f8f6`. Theme is remembered. Shared workbench/creation controls (`workbench-workspace`, `creation-workspace`, template library, image editor) use Ant Design `Button` / `Modal` / `Input` / `Select` / `Dropdown` with theme classNames; `notify` remains shared operation feedback. “Continue as reference” adds the server-resolved generation asset directly rather than browser-fetching its storage URL, so CORS and object-storage redirects no longer leave the tile action inert. The always-dark template library and image editor dialogs wrap themselves in `studioDarkAntdTheme`, so their Ant Design controls stay legible when opened from the day theme. Fullscreen studio paths are wrapped by `AdminAntdProvider`. The composer prompt textarea fills the upper dialog row; disabled “发送给客户” uses a muted gray surface and gray label (light theme uses a pale green surface) instead of purple-on-white low contrast; the dedicated header “返回管理后台” icon is removed while the brand logo still returns home; the bootstrap-error state keeps the text “返回管理后台” link. Published generation tiles use a solid green “已发送” badge with white text | Route remains `/ai-studio/scenarios` under tenant `ai-scenarios` and opens in a new tab. Surveyed leads still bind an eligible completed formal v4 floor plan; unsurveyed leads create a lead-only `rough_sketch` conversation (apply-to hidden, batches omit `targetScope`/`roomId`) and can still send to the customer. Workbench batches use `POST /api/ai/creation/tasks/[id]/batches` with `workflowId` plus optional `targetScope`/`roomId` when a formal plan is bound (default whole floor plan), inject the matching control PNG (whole-plan snapshot or single-room crop) as the first uploaded reference, persist `roomData`/scope on the batch snapshot, and bind generations to that conversation. Workflow detail exposes closed rooms for the picker. `GET /api/ai/workflows/[id]/floor-plan-preview` streams the whole-plan snapshot, or `?roomId=` for the matching room crop, for the designer only. Scheme albums still use `POST /api/leads/[id]/ai-scheme-publications`. Mini Program single-image publish APIs stay; `POST /api/ai/creation/assets/from-generation` remains enterprise-scoped and returns only a succeeded tenant result | Composer shows the locked control thumbnail for the current apply-to scope when a formal plan is bound, and hides apply-to for photo conversations; focused `workbench-studio` and `photo-scheme-contract` tests cover the room preview URL and unsurveyed New conversation; PostgreSQL preview test covers `roomId` crop vs invalid room; authenticated visual QA of both themes remains to be run | Reopen only for a workbench workflow/permission change or a reproducible layout defect |
| `/ai-studio/create` — free-creation studio | Keep the existing dark night-studio composition (`#7047ff` accents, dark surfaces). Shared AI creation components replace shadcn `Button` / `Dialog` / `Input` / `Select` / `Textarea` / `DropdownMenu` with Ant Design 5 `Button` / `Modal` / `Input` / `Input.TextArea` / `Select` / `Dropdown`, restyled via className/`popupClassName` and a studio `ConfigProvider` (`studioDarkAntdTheme`). “Continue as reference” now immediately adds the tenant-scoped generation asset through the server instead of browser-fetching its storage URL, so redirects and CORS do not leave the action inert. Shared `notify` lives at `@/components/admin/operation-feedback` | Route, creation APIs, tenant `ai-creation` permission, and business flows are unchanged; the shared generation-reference endpoint remains enterprise-scoped | Focused ESLint on the shared AI creation files; authenticated dark-theme visual QA at `http://localhost:3006/ai-studio/create` remains to be run | Reopen only for a creation-studio workflow/permission change or a reproducible dark-control layout defect |
| `/ai-studio/floor-plan`, `/ai-studio/furnishing`, `/ai-studio/soft-furnishing`, `/ai-studio/floor-plan/[id]`, `/ai-studio/designer` — merchant AI tool pages | Keep existing tool-page compositions (quota bar, plan/style selectors, generate actions, history). Replace shadcn `Button` / `Badge` / `Select` / `Dialog` / `Sheet` with Ant Design `Button` / `Tag` / `Select` / `Modal` / `Drawer` in the pages and shared helpers (`AiQuotaBar`, `RechargeDialog`, `ai-tool-frame`, `ChatInterface`, `workflow-runner`, `ImageCropperDialog`) | Routes, AI generate/history/quota APIs, and tenant AI permissions are unchanged; only the visual control library moved to Ant Design | Focused ESLint on the tool pages and shared AI studio helpers; authenticated visual QA remains to be added | Reopen only for a tool-workflow/permission change or a reproducible control layout defect |

| `/enterprise-registration-codes` — platform Mini Program open-account code | Established Ant Design/Admin Pro direction: one active-code card with rotate/disable plus an inline composed open-account poster preview/download (`variant=poster`, no rotate or auto-hide), a secondary bare-code download (`variant=raw`), same-page QR placement controls, optional Web `/register` copy link, and a recent audit table with Chinese action/result labels (shared `code-audit-labels`). Tokens never appear in the Admin page or list DTO; images remain private no-store Blobs for the current page session | Platform-only `super_admin`/`admin`; reuses `enterprises` menu permission and `GET/POST /api/admin/enterprise-registration-codes` (+ `rotate` / `disable` / `image`, `requireEnterprise: false`), with template config via `GET/PATCH /api/platform/enterprise-registration-code-template-config`. Merchant `ej_` join-codes stay on `/join-codes` | Focused repository, self-service registration, poster composition, Admin QR-view, and Chinese audit-label contract tests pass. Authenticated Chrome QA pending | Reopen only for an open-account contract change or a reproducible QR/audit defect |
| `/mini-program-code-settings` — global Mini Program code environment | Existing Ant Design 5/Admin Pro settings-sheet direction: one clearly labelled segmented choice for 开发版 / 体验版 / 正式版, the active-state tag, a single save action, and an explicit warning that historical images are unchanged | The route and `GET/PATCH /api/platform/mini-program-code-config` are restricted to platform `super_admin`/`admin`; it changes only the environment for new enterprise onboarding (`ej_`), platform enterprise registration (`er_`), referrer-promotion, and staff-activity code images | Focused lint and code-generation contract tests cover the image endpoints including open-account; authenticated visual QA remains pending | Reopen only for an observed settings-page defect or a code-environment/API/permission contract change |
| `/floorplans/[id]` — formal floor-plan 2D viewer | Keep the existing detail chrome (back, title, wall/space/opening/node counts, CAD export). Replace the independent SVG reconstruction with the Mini Program `surveyCanvasRenderer` canvas: session-stripped committed walls, openings, dimensions, and room labels; drag pan, wheel zoom, double-click fit. Completed saves export that canvas as a stored PNG snapshot for AI/workbench reuse. Shared `FloorPlanViewer` chrome controls (`Button` / `Modal` / `Select` for AI style preview and CAD export) use Ant Design instead of `@/components/ui` | Route, tenant/assigned-staff permission, `layoutData` contract, DXF export, and Mini Program editor/BLE entry are unchanged. Viewport is session-only and is not written back to the graph. Snapshot metadata lives on `floor_plans.preview_asset_id`, not in `layoutData` | Focused `npm run test:survey-canvas` covers fit/pan/zoom, renderer revision parity, PNG snapshot export, and no graph mutation. Authenticated visual QA at `http://localhost:3006/floorplans/[id]` remains to be added | Reopen only for an observed canvas mismatch with the Mini Program, a later approved Admin edit contract, or a route/API/permission change |
| `/login` and `/change-password` — public sign-in and forced first change | Preserve the brand-first Ant Design login composition. The identifier label is now「手机号 / 登录账号」for staff and administrators, and the inert recovery action is replaced by guidance to contact the enterprise owner. Flagged sessions enter a matching dedicated password-change shell | `POST /api/auth/login` shares username/phone credential matching and returns `requiresPasswordChange` plus `nextPath`. The restricted Cookie may access only auth-me/password/logout and `/change-password`; successful `PUT /api/auth/password` clears the flag, refreshes the Cookie, and enters `/`. The public login shell still opts out of account settings | Focused credential/session tests and scoped ESLint pass; production build passes. Playwright at `http://localhost:3006` confirms the new public copy, unauthenticated `/change-password` redirect, flagged-session form, `/staff` redirect, and `GET /api/staff` 403 `password_change_required` | Reopen only for an observed credential/change-form defect or a further auth/session-contract change |
| `/floorplans/kujiale` — KuJiale open floor-plan search | Keep the existing search-page composition (back link, city/community filters, result cards). Replace shadcn `Cascader` / `Input` / `Select` / `Button` / `Badge` / `Pagination` with Ant Design `Cascader` / `Input` / `Select` / `Button` / `Tag` / `Pagination`. Shared `notify` remains for search feedback | Route, KuJiale city/search APIs, sandbox-openapi boundary, and read-only (no local import) behaviour are unchanged; still linked from `/floorplans` | Focused ESLint on the page passes. Authenticated Chrome QA at `http://localhost:3006/floorplans/kujiale` remains to be added | Reopen only for a KuJiale API/search-contract change or a reproducible filter/result layout defect |
| `/workflow-logs` — notification delivery ledger | Established Ant Design/Admin Pro ProTable: status with channel (`station` / `miniprogram_sub`), type, recipient display name + username + phone + role, Chinese send reason, delivery outcome note, enterprise/report context, and send time. Manual scan button runs appointment expiry only; legacy promotion nudge types are labelled as retired historical rows. Platform template card adds Switch「启用微信订阅消息下发」(`subscriptionMessagesEnabled`, default off) above the eight template IDs | Route and `GET /api/workflow-notification-logs` remain `super_admin`/`admin`/`enterprise_admin`; list DTO includes recipient `username`/`phone`; template config card and send switch stay platform-only via `GET/PATCH /api/platform/notification-config`; `sendSubscriptionMessage` skips WeChat when the switch is off while in-app notifications continue; promotion create/update and reminder cron no longer dispatch legacy workflow notifications | Focused wiring tests assert retired promotion dispatch, appointment-only cron, and the subscription send gate; authenticated Chrome QA pending | Reopen only for a notification-contract change or a reproducible ledger layout defect |

## Handoff

`/login`, `/floorplans/kujiale`, the AI Studio cluster (`create` / `scenarios` / tool pages plus shared creation/workbench/ChatInterface helpers), `FloorPlanViewer`, and the Admin `Sidebar` shell now use Ant Design 5 controls. Dark studio shells keep their night theme via `ConfigProvider` + `studio-antd-theme`. Root layout mounts client `AntdReact19Patch` (`@ant-design/v5-patch-for-react-19`) so Ant Design 5 wave effects and static methods work on React 19. Root `AntdRegistry` enables css-in-js `layer` with `@layer theme, base, antd, components, utilities` so Tailwind utilities override Ant Design's global `a { color: colorLink }` (inactive sidebar items stay muted gray; only the active route uses primary green). Unlayered `a.ant-btn*` / `a .ant-btn*` color rules restore intended button contrast when those links still win. Shared `notify`, `ConfirmDialogProvider`/`useConfirmDialog` (antd `App.useApp().modal.confirm` under `AdminAntdProvider`'s `ConfigProvider` + `App`), `AccountSettingsProvider`/`useAccountSettings` (sidebar avatar account menu with login/sensitive-password modals), and `ImageUploadField` live under `admin/src/components/admin/*`. The former `@/components/ui` parallel kit and unused dead components (`UserDashboard`, `DepartmentTree`, `EnterprisePageHeader`, `enterprise-utils`) were removed. No route/API/permission or admin module-inventory behaviour change.

When a refactor changes visible behavior, update the route's single current
record and both language files. When it changes only implementation without a
route/API/permission or visual impact, state that in the handoff instead of
adding a new ledger entry.

Current AI workbench composer behavior: `/ai-studio/scenarios` exposes an explicit
`整屋户型` / `单间现场` render-mode selector. Whole-plan mode attaches the formal
floor-plan control reference and keeps the scope picker; single-room-photo mode
does not upload the control image and treats uploaded site photos as the camera
authority. In single-room-photo mode, `客户现场图` opens a multi-select gallery
from the current lead's persisted `lead_site_photos`; temporary upload remains
available. The batch submits explicit `styleReferenceAssetId` and
`sitePhotoAssetIds`; a template cover never satisfies the required site-photo
role, and both client and server block single-room-photo generation until at
least one gallery-selected or computer-uploaded site photo exists. Reference
thumbnails are labeled by role (`户型结构`, `风格图`, `现场图`).

Chinese mirror: [admin-ui-refactor.zh-CN.md](./admin-ui-refactor.zh-CN.md)
