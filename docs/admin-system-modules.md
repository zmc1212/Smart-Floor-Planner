# Admin System: Current Module Inventory

This is the current Admin runtime inventory. It records stable entry points,
contracts, permissions, and limitations only. Implementation history belongs in
Git commits; do not append dated change logs here.

## Shared architecture

- Next.js 16 App Router, React 19, Tailwind CSS 4, Ant Design 5, and Ant Design Pro.
- PostgreSQL 17 with `drizzle-orm`/`pg` is the deployed runtime. Tenant-aware
  reads and writes use repositories, transactions, and RLS.
- Admin sessions use cookie/JWT authentication. Platform and enterprise roles
  are enforced by route guards and menu permissions.
- External provider and object-storage I/O runs outside short database
  transactions. API handlers serialize `bigint` values through DTOs.
- Formal floor plans are version-4 surveying graphs. Viewers, DXF, 3D, and AI
  consume derived read models and never write legacy layout fields.

## Status legend

`Implemented` means a real page/API/data path exists. `Limited` means a defined
role, provider, source shape, or operational condition restricts it.
`Placeholder` means UI or a mock path exists without the promised persistence or
integration.

## Module inventory

Service-address completion is part of the appointment service fact. `measurement_appointments` stores the manual service address plus optional `location_name`, bounded GCJ-02 latitude/longitude, and coordinate system. `POST /api/appointments` and `POST /api/appointments/[id]/address` accept that optional map location; address updates remain versioned and write previous/new location snapshots to the `address_updated` audit event. Mini Program callers are resolved as staff first (`staff._id` vs assigned designer/measurer); Admin cookie JWT remains the fallback. The Admin lead drawer and Mini Program appointment detail use the same entry. After Admin create/update of a service address, the assigned designer, assigned measurer, or enterprise owner may confirm syncing that address into an empty lead `communityName` through `PUT /api/leads/[id]` (no overwrite when community is already set). `POST /api/appointments/[id]/complete` additionally requires a linked completed formal v4 survey floor plan with at least one closed space and returns `appointment_survey_required` (409) otherwise.

| Module | Current entry points | API/data boundary | Permission/status | Current limitation |
| --- | --- | --- | --- | --- |
| Lead conversion card presentation | `/leads` list via `tableViewRender` | UI-only presentation of the existing lead DTO: customer header (stage, contact, Chinese source, area/style, scheme CTA), derived workflow `Steps` plus next action, operational appointment with eligible reschedule action, and a four-column collaborator/assignment group. The list has a muted 16px canvas gutter and 16px card rhythm; card headers/footers have a 20px inset and visible 12px identity/action gaps; pagination sits in its own bordered footer. Footer actions still call the same archive, restore, purge, and assignment-retry handlers | Existing tenant, role, and action boundaries; Implemented | Authenticated Chrome visual QA on 2026-08-21 confirmed two populated cards, 16px card/pagination spacing, 12px action spacing, and a 20px archive inset at `http://localhost:3006/leads`; no API, model, or permission behavior changed |
| Authentication and sessions | `/login`, `/register`; auth middleware | `/api/auth/*`; Mini Program JWTs use base user `sub`, selected `customer/staff/referrer` context, and `contextVersion`. Platform-only `admin`/`super_admin` staff records are not Mini Program workbench roles: Mini Program login/refresh selects the same base user's active referrer context, or customer context if no referrer membership exists | Public entry plus authenticated routes; platform permissions remain backend-only; Implemented | WeChat provider configuration remains environment-dependent; legacy identity columns coexist until the old acquisition flow is retired |
| Navigation, roles, access | Shared sidebar, `/roles`, and route guards (`proxy.ts`) | `/api/roles`, `/api/auth/me`, role/menu repositories. `/roles` checkbox labels come from `ALL_MENUS` and must match Sidebar names (e.g. `dashboard` = 业务概览, `leads` = 线索转化). Enterprise home `/` under `dashboard` loads workbench summary/todos plus `GET /api/workbench/ops-dashboard` for enterprise_admin / designer / measurer period KPIs; lead/floor-plan/staff aggregates are requested only when those menu keys are present. `GET /api/auth/me` refreshes the auth cookie when role-menu permissions change so API guards match without re-login | `super_admin`, `admin`, enterprise roles; Implemented | Effective permissions are tenant and role scoped; child routes such as join-codes/referrers reuse parent menu keys, and `/enterprise-registration-codes` reuses `enterprises` |
| Platform and enterprises | `/dashboard`, `/enterprises`, `/enterprise-registration-codes` | Enterprise, branding, activation, and platform repositories. Platform-global Mini Program open-account codes use `platform_enterprise_registration_codes` / `platform_enterprise_registration_code_events` (HMAC `er_` tokens, SHA-256 hash only in DB, one active code) via `EnterpriseRegistrationCodeRepository` (`rotate` / `disable` / `revealActive` / `resolve` / `inspect` / `listEvents`). Admin APIs: `GET /api/admin/enterprise-registration-codes`, `POST .../rotate` (optional `expiresAt`), `POST .../disable`, `POST .../image` (private no-store WeChat code for `packages/business/enterprise-register/enterprise-register`). Merchant `ej_` join-codes remain tenant-scoped on `/join-codes` and must not be mixed with open-account | Platform roles; `/enterprise-registration-codes` and its APIs reuse the `enterprises` permission; Implemented | Web `/register` still works without an `er_` token; approval SMS and in-app progress lookup remain out of scope |
| Staff, accounts, and appointment operations | `/staff`, `/departments`, `/users`, `/referrer-network-operations`, `/join-codes`, `/referrers`, `/appointment-settings`, `/` (enterprise_admin business overview + designer/measurer home workbench) | Staff, department, shared notification, admin-user, and appointment repositories. The merchant sidebar groups `/referrer-network-operations`, `/join-codes`, `/referrers`, and `/appointment-settings` under the referral-network category, all reusing the `referrer-network-operations` permission. `/referrer-network-operations` is a readiness hub of counts, an actionable checklist, and a five-row join-code audit preview; dual-code rotate/disable/QR and the full audit live on `/join-codes`. `/referrers` lists membership display name, phone, join time, promotion-code presence, and disable-scan, with name/phone search; Admin never creates referrers, and the display name is collected on Mini Program referrer join (`POST /api/miniprogram/onboarding/referrer` requires `displayName` and does not fall back to `推荐人`). The appointment-settings page reads and updates timezone, weekly availability, default duration, slot step, booking horizon, and customer reschedule cutoff through `GET/PUT /api/appointment-settings`; an auto-created default row is explicitly distinguished from an administrator-confirmed policy. The operations workbench reads join-code audit, active referrer memberships and persisted active promotion-code count, active staff-activity-code count, assignment eligibility, confirmed appointment settings, commission rules, and WeChat-code configuration through `GET /api/enterprise/referrer-network-readiness`; the promotion-code readiness row no longer infers code availability from memberships alone, and each acceptance item links to its operating surface. `GET /api/enterprise/join-codes` returns token-free codes plus audit events; `GET /api/enterprise/referrer-memberships` lists tenant memberships with optional query/status; disable remains `POST /api/enterprise/referrer-memberships/[id]/disable`. The Admin home `/` now renders: enterprise_admin `MerchantDashboard` with inventory totals, a read-only period ops dashboard, and recent lead flow (the legacy enterprise-reporting “协作待办” strip is removed from home); designer/measurer `StaffWorkbench` with personal queues plus the same five-card dashboard under personal attribution. `GET /api/workbench/ops-dashboard` (`period=week|month|year|custom`, custom also needs Shanghai-calendar `from`/`to`) shares `loadOpsDashboard` with Mini Program workbench: new leads, completed surveys, scheme delivery rate, signed count, signing rate = same-window signed ÷ new leads (`status=converted` + `convertedAt`); owners may see signed-amount detail, staff do not. `GET /api/workbench/staff` uses a tenant transaction and returns only the current staff member's leads, appointments, unscheduled survey tasks, and survey handoff data. Formal BLE surveying remains the Mini Program editor's sole production entry. The audited image endpoint generates a private, no-store onboarding image and preserves WeChat's PNG/JPEG media type. Both enterprise onboarding and referrer promotion images use `getwxacodeunlimit` with `env_version: develop`, including when the server process runs in production mode | `referrer-network-operations`, `/join-codes`, `/referrers`, `/appointment-settings`, and their APIs reuse the `referrer-network-operations` permission. The employee home and `GET /api/workbench/staff` are limited to active `designer`/`measurer` sessions in the selected tenant; enterprise owners retain the enterprise workbench; Implemented/Limited | Employee workbenches provide task aggregation and existing Admin links only: they do not create test business records, bypass Mini Program phone authorization, or provide a desktop BLE/H5 editor. External WeChat credentials are diagnosed but not configured by this page. Authenticated role-specific Admin visual QA remains to be added |
| Promotion and collaboration | `/promotions`, enterprise collaboration pages | Promotion, referral, and shared notification repositories | Enterprise and staff role boundaries; Implemented | WeCom delivery is optional and external |
| Packages, orders, commissions | `/packages`, `/orders`, `/commissions`, `/lead-commissions` | Package, order, existing commission, and `LeadCommissionRepository` (conversion snapshots also persist immutable `original_payable_amount` / `original_beneficiary_user_id` plus nullable last-adjust audit columns); `GET/PUT /api/commission-rules`, filtered `GET /api/lead-commissions` (including original/adjust audit fields and `source=referrer_network|staff_activity`), `GET /api/lead-commissions/beneficiaries?role=`, `PATCH /api/lead-commissions/[id]` (payable-only amount and/or beneficiary change with optional reason; referrer targets must be active enterprise referrer members, designer/measurer targets must be active role-matched staff with bound `userId`), `POST /api/lead-commissions/mark-paid`. The `/lead-commissions` workbench uses Admin Pro `PageContainer`/`ProTable` plus Ant Design rule cards (`Segmented`, `InputNumber` with a nowrap `元/单` addon, `Switch`) to maintain all three rules, filter by lead source, show customer, referrer membership (empty for staff-activity leads), enterprise, designer, measurer, and current confirmed appointment per report record, and show a lead-parent expandable ledger (shared lead context on the parent row; nested three-role commission rows) and expose an Adjust modal on payable rows with optional beneficiary and reason. Referrer identity on the ledger uses `referrerMembershipId -> referrerEnterpriseMemberships -> referrerProfiles` display name/phone, the same tenantless-user path as `/leads`, because Mini Program `users` rows with null `enterprise_id` are hidden by tenant RLS | `lead-commissions` is available to `super_admin`, `admin`, and `enterprise_admin`; system administrators always expose the protected entry even when an older stored menu snapshot lacks the new key; rule changes, payable adjustments, and offline batch paid marking use RLS and tenant transactions; Implemented/Limited | The old acquisition-commission route is retired. Payment settlement remains outside this system. Paid/voided rows cannot be adjusted; no manual ledger create, paid reversal, or single-row void outside conversion revert |
| Leads and conversion | `/leads`, `/leads/[id]` | Lead lifecycle, floor-plan, `ReferralLeadRepository`, `CustomerProjectRepository`, and `LeadCommissionRepository`; list/detail DTOs include the assigned measurer; GET /api/leads/[id] publishedSchemes images include stageKey/publishedAt for the Mini Program read-only scheme folio, derived `serviceStage`/`nextAction`/`nextActionKind`/`canRebook` (customer `book` after measurer assignment; designer booking on the same lead remains allowed), the current operational appointment (an active future/in-progress `confirmed` row outranks expired or past-end rows after rebooking), and the referrer resolved from `referrerMembershipId -> referrerEnterpriseMemberships -> referrerProfiles` (using the profile display name/phone without requiring a tenantless base-user row). The Admin lead DTO also returns the assigned designer and measurer phone from their existing staff profile for the card's read-only assignment display. Unarchived lead detail exposes **补充资料** under **客户资料**, letting enterprise admins, the assigned designer, and the assigned measurer update name, phone, community, area, and style through `PUT /api/leads/[id]` without changing designer assignment or commission beneficiaries. `/api/appointments` and `/api/appointments/availability` accept Admin Cookie sessions for authorized designers/enterprise admins to create, read available slots, and internally reschedule lead appointments. `POST /api/appointments/[id]/cancel` and `/complete` accept the same Admin Cookie role boundaries as the Mini Program; the `/leads` detail drawer exposes cancel, complete, rebook, and address/reschedule actions with the same expired-row completion rule as Slice 2. `GET/POST /api/leads/[id]/ai-publications` and `DELETE /api/leads/[id]/ai-publications/[generationId]` still accept Admin Cookie sessions for Mini Program-compatible single-image publish/withdraw. Assigned designers and enterprise owners send a named multi-image scheme from `/ai-studio/scenarios` through `POST /api/leads/[id]/ai-scheme-publications` with merge publishing semantics within the same `workflowId` (incremental selections update/insert active publications without withdrawing already-confirmed images); withdraw the whole scheme set with `DELETE /api/leads/[id]/ai-scheme-publications/[workflowId]` or withdraw a single generation publication with `DELETE /api/leads/[id]/ai-scheme-publications/[workflowId]/generations/[generationId]`; the detail drawer lists those scheme albums and deep-links to the AI workbench. Admin appointment audit writes resolve `admin_users.user_id` to `users.id`; `/leads/[id]` opens the shared lead detail drawer through the query handoff | Customer authorization or tenant/assigned-staff checks; an assigned designer may manage succeeded generations and create, reschedule, or cancel appointments only for their own leads, while an assigned measurer may complete assigned appointments and an enterprise administrator may manage the tenant; only an enterprise administrator can revert signing, and paid commissions block reversion; Implemented/Limited | The customer project renders the protected aggregate's real service facts, completed formal-plan summary, and explicitly published designs; publication images remain owner-only and are fetched into app-local temporary files. The Admin ProTable keeps filtering/pagination and renders each lead as a responsive ProCard with customer, assignment, referrer, appointment, and action groups; the card shows each resolved referrer, assigned designer, and assigned measurer as name followed by phone when available, and uses the same server-backed date/slot selection contract as the Mini Program, with audit and notification on changes; Admin staff and user audit identifiers are handled separately |
| Formal floor plans | `/floorplans`, `/floorplans/[id]`, `/floorplans/kujiale`; `GET /api/floorplans/[id]/export/dxf` | `FloorPlanRepository` and read-only surveying adapters. `/floorplans/[id]` draws the active floor with the Mini Program `surveyCanvasRenderer` mirrored under `admin/src/lib/survey-runtime/` (session-stripped, pan/zoom only, no graph writes). Completed formal v4 plans persist a PNG snapshot of that same canvas into `media_assets` (`ownerType: floor_plan_preview`) on create/update, pointed to by `floor_plans.preview_asset_id` / `preview_render_revision` and never written into `layoutData`. AI workbench control images and `GET /api/ai/workflows/[id]/floor-plan-preview` reuse that snapshot; `GET /api/floorplans/[id]/preview` streams it. The formal viewer downloads a layered millimetre AutoCAD 2007+ DXF through `@tarikjabiri/dxf@2.8.9` (MIT) with Chinese CAD layers (`墙`, `门`, `窗`, `尺寸标注`, `空间名称`, `指北针`), millimetre DIMSTYLE (`标注线` / `标注线-内墙`), `_ARCHTICK`, Heiti, and `ACAD_ISO03W100` door-swing dashes; walls are the opening-gapped `surveyWallSolidPlan` union written as inner/outer `LINE` faces plus jambs rather than per-wall thickness rectangles; hinged doors are a `DOOR` block inserted on the opening face with a green open 90° thick leaf, gray dashed arc, and 50mm jamb rectangles, sliding doors stay double rails, and windows are four inset in-opening lines away from the wall faces; dimensions reuse the shared closed-space plan as rotated `AcDbRotatedDimension` entities (`标注线-内墙` for inner segments including wall-thickness ticks, `标注线` for overall outer lengths, integer millimetres in magenta with DIMCLRT 193, wider lane spacing, planned `dimensionStart`/`dimensionEnd` placement, `DIMTAD` 2 / `DIMGAP` 10, axis angle 0 or 90, recessed L-notch spans stay on their local face); shared walls, diagonal walls, openings, dimensions, closed-space four-line MTEXT on layer `空间名称` (ACI 7 white text) (`\\P` breaks: name, inner-face area m², ceiling height m, inner-face perimeter m), a fixed cyan landscape sheet (30640×21660) with double rounded frame, Y-flipped to match the viewer, project title and DXF filename from lead name+community+area+export time, stacked Chinese/English/value title-panel text (no mid-cell vertical) and a filled yellow north arrow, floor content scaled to fill ~90% of the left draw zone with a sheet-based ~1:85 title scale, and side-by-side floors are mapped from the authoritative v4 graph without a layout copy. The endpoint requires `completed`, formal v4 data, and at least one closed space. Both Admin and Mini Program DXF routes pass `plan.name`, the linked lead's enterprise name (else the plan tenant), the lead's assigned designer, and the plan `completedAt` date into the title block | Existing tenant, assigned-designer, or assigned-measurer permission; Implemented | The 2D canvas is read-only and is not the Mini Program editor or BLE entry. No CAD file is stored and no customer phone or address is exported. The title block uses the drawing name 原始户型平面图, a computed plot scale, the completed-plan date, the linked-lead enterprise as 公司 at the top of a full-height MTEXT right panel inside a double rounded cyan frame, and the assigned designer as 设计师. KuJiale provider capability is Limited |
| Measurements and BLE devices | `/measurements`, `/devices` | Measurement, device, binding, and audit repositories | Platform/enterprise assignment boundaries; Implemented | Device protocol support is limited to the documented meter |
| AI studio and generation | `/ai-studio/scenarios` (AI workbench), `/ai-studio/create` (free-creation studio), provider/preset/credit pages | PostgreSQL AI repositories and provider adapters. The merchant workbench at `/ai-studio/scenarios` opens in a new fullscreen window like the creation studio. Designers pick a lead and named conversation on the left, then generate on the right with the creation-studio composer (model, count, aspect, resolution, prompt templates (cover click enlarges preview; name/使用 applies), reference images, and prompt assist). Conversations require an eligible completed formal v4 floor plan; each workbench batch prepends that survey-canvas snapshot PNG (the same `surveyCanvasRenderer` drawing as `/floorplans/[id]`, with walls, openings, dimensions, and room labels) and binds the resulting `free_create` generations to the workflow. The conversation header shows the same stored snapshot for designer comparison (enlarge, generation side-by-side, and a link to `/floorplans/[id]`); `GET /api/ai/workflows/[id]/floor-plan-preview` streams it and it is never published to the customer. Theme preference (night = creation-studio dark, day = Admin green/white) is remembered in `localStorage`. `POST /api/leads/[id]/ai-scheme-publications` publishes by merge publishing: selected generations within the same `workflowId` are added/updated into the existing active customer-visible publications, so customers see incremental updates within the same scheme; unselected already-confirmed generations remain visible until explicitly withdrawn or their generation/workflow is deleted. Designers can soft-delete a generation or archive a workflow via `DELETE /api/ai/workflows/[id]/generations/[generationId]` and `DELETE /api/ai/workflows/[id]`, which withdraw customer-visible publications accordingly. Mini Program designer single-image publish remains on `/api/leads/[id]/ai-publications`. Designer and enterprise-administrator mobile scheme reads and mutations use `/api/miniprogram/ai/studio/*` (bootstrap, leads, workflows, creation tasks/batches/retry, assets, prompt templates/assist, and signed generation/floor-plan preview URLs); the Mini Program `scheme-studio` route renders merged rounds with published badges, the Creation-aligned composer (model/count/aspect/resolution, references, templates, prompt assist, 4s polling, retry), merge-publish send modal via `POST /api/leads/[id]/ai-scheme-publications`, plus rename/delete workflow, delete generation, and single-image withdraw. Mobile Limited: no Admin canvas annotation editor (tile continue-as-reference only), no dark theme toggle, and nine-stage `proposal_pack`/`lighting` flows stay on existing admin_handoff paths. The nine-stage `run-stage` APIs stay for compatibility and are no longer the Admin workbench UI. Request-driven PostgreSQL reconciliation shares one in-process run per tenant and reuses its completed result for one second; database poll leases remain the final cross-instance guard, preventing concurrent Admin/Mini Program refreshes from repeatedly scanning a tenant or polling a provider task twice. | Platform plus tenant `ai-scenarios` permissions; Implemented/Limited | Provider availability and image storage are external. Free-creation studio stays lead-agnostic until attach-workflow. |
| Media storage | `/media-storage`; `npm run db:backup`, `npm run db:restore-drill`, `npm run db:cleanup:dry-run`, and `npm run db:cleanup:execute` | `media_assets`, provider configuration, storage adapters; backup emits a custom PostgreSQL dump and duration, the restore drill uses only `smart_floor_planner_restore_drill` and verifies the current app schema before removing that drill database, the cleanup dry-run sets its read-only session to the existing platform scope before it fingerprints the target and emits a Qiniu candidate manifest, and the execute command requires an exact fingerprint, approved manifest SHA-256, explicit local-production switch, and operator identity before its one-transaction database cleanup and audit output | Platform admin; Implemented/Limited | Qiniu object deletion remains a separately approved asynchronous operation after a nonempty human-approved manifest; the execute command does not call Qiniu |
| Mini Program support APIs | Admin diagnostics and shared API handlers; production anonymous claim routes are in the Mini Program | Identity/context and dual-code/referrer APIs; GET /api/miniprogram/workbench accepts period (+ custom from/to) and returns period-scoped five-card dashboard with signedCount/signingRate (owner store-wide with optional contract amount; designer/measurer personal attribution). `GET /api/miniprogram/bootstrap` validates the signed JWT `contextVersion` and active context, returning the current role, valid role groups, enterprise/membership context, landing path, capability allowlist, and a server-owned badge summary of role-scoped todo counts (`status`/`message`/`counts`; query failure is `unavailable` plus `暂时无法读取` and does not fail identity bootstrap or fill zeros); invalid contexts return `identity_context_invalid`. `/api/miniprogram/codes/resolve` returns an onboarding code's type and target enterprise display name before phone authorization, `{ kind: 'enterprise_registration', displayName, valid }` for platform `er_` open-account codes, a ten-minute pending source for referral codes, or a staff-activity pending source (`kind: staff_activity`) that may include the enterprise name. `POST /api/miniprogram/enterprise-registration` submits a pending self-service enterprise when the Bearer JWT phone matches `contactPerson.phone` and the `er_` code is active. A signed customer with an open attribution lock on an unarchived, unclosed lead receives `existingAttribution` and the existing lead instead of a new pending source or new-claim designer payload. Archiving or closing a lead releases that lock so the customer can claim again. `GET /api/miniprogram/staff-activity-code` and `/image` issue a designer/measurer activity code. `/api/miniprogram/referrer-memberships/[id]/promotion-code/image` returns a protected PNG/JPEG generated by WeChat, and `/api/miniprogram/referrals/authorize-and-create-lead` atomically links the customer, locks attribution, creates the lead, and assigns staff; phone/WeChat identity conflicts return 409 `staff_phone_linked_to_other_user` / `wechat_identity_conflict` with Chinese copy (same codes on `/api/auth/miniprogram`). Activity claims lock the presenter as `measurerId`. If the presenter is paused at claim time the measurer slot stays empty; retry assignment fills that presenter after they resume and does not pick a replacement from the pool.); phase 5 provides appointment APIs; phase 6 provides customer-project aggregation, owner-only published-design images, and designer/enterprise-admin publication or withdrawal; phase 13 adds the owner-only `GET /api/miniprogram/customer-projects` index plus signed-membership `GET /api/miniprogram/referrer-progress` and `GET /api/miniprogram/referrer-earnings`; phase 14 adds `GET /api/miniprogram/workbench` for the signed designer, measurer, or enterprise owner. Removed unused POST /api/location/reverse (legacy Mini Program city locator)| Promotion resolution and code-image delivery are anonymous to the customer but membership-scoped for the referrer. Customer project endpoints validate `customer_user_id` and never trust client enterprise context; referrer progress and earnings validate both the JWT user and active membership before querying; earnings rows follow the current commission `beneficiaryUserId` and `payableAmount`. The workbench derives staff role, enterprise, and staff scope from the signed context: designers receive assigned leads, expired unrebooked work, and own unscheduled survey tasks when they are `measurerId`; measurers receive confirmed appointments (expired, completed, converted, and closed leads leave the workbench queue), expired follow-ups, and unscheduled survey tasks that still need a formal survey (draft plans keep continue/add survey; a completed v4 survey leaves the pending queue and the `待量房任务` count); those remaining items expose `floorPlanId`, `canContinueSurvey`, and `canStartNewSurvey`, and hide `canBookAppointment` once a plan exists; enterprise owners receive R01 operations payload with hero summary (`待派单`/`待量房`/`已交付`), `quickNav`, monthly `dashboard` metrics, staff-activity code entry, exception `primaryItems` (pending assignment, expired unrebooked, staffing gaps), and confirmed/expired appointment lists whose workbench items keep terminal serviceStage/statusBadge for converted or closed leads (no rebook flags). The customer-project index returns the shared derived `serviceStage` plus a single `nextActionKind` (`reschedule` / `rebook` / `view_project` / `wait_designer`). Designers can maintain their WeChat ID and personal QR through Mini JWT `GET/PATCH /api/miniprogram/staff/wechat-profile` and `POST /api/miniprogram/staff/wechat-qr`; measurers are not required to upload a QR. Floor-plan create/update authorizes `assignedTo` or `measurerId`. Appointment APIs are isolated by customer ownership, responsible designer, assigned measurer, or enterprise owner and use tenant transactions; `/api/miniprogram/notification-template` supplies authenticated identities with the configuration needed for customer subscription consent; Implemented/Limited | The customer project uses the completed-plan summary plus protected published-design images grouped as named schemes (`publishedSchemes`); Mini Program single-image publications without a workflow appear under 其他效果图. The customer project is not an editable graph or measurement editor. Referrer aggregates expose only a masked customer label, service facts, and the referrer's own commission status. Workbench summaries are read-only and use the existing authoritative lead, appointment, and formal-survey entry contracts. Appointment create, reschedule, and cancel attempt staff and authorized-customer delivery after commit; WeChat code generation, authorization, and notifications depend on external configuration |
| Notifications, automation, diagnostics | Notification settings, reminder runtime, diagnostics | Notification templates, scheduler, operational records | Platform/enterprise roles; Implemented/Limited | Subscription delivery can be rejected by WeChat |

## Global Mini Program code environment

`/mini-program-code-settings` provides one persisted platform setting for every
new enterprise onboarding (`ej_`), platform enterprise registration (`er_`),
referrer-promotion, and staff-activity Mini Program code. `GET/PATCH
/api/platform/mini-program-code-config` reads or saves `develop`, `trial`, or
`release` in `platform_configs.mini_program_code_config`.
Only `super_admin` and `admin` may use the page or API. The setting applies
immediately to subsequently generated code images; it does not alter images
already downloaded or distributed. It supersedes the previous fixed-development
environment behavior.

## Formal surveying boundary

The authoritative contract is
[`surveying-module/formal-surveying.md`](./surveying-module/formal-surveying.md).
`FloorPlan.layoutData` contains only `version: 4`,
`measurementMode: 'surveying'`, and `surveyGraph`. Measurements are immutable
audits; dimensions and room summaries are derived read models. `/floorplans/[id]`
renders the 2D plan with the Mini Program `surveyCanvasRenderer` mirrored into
`admin/src/lib/survey-runtime/` and does not write the graph. Completed formal
v4 saves persist a PNG snapshot of that canvas on `floor_plans.preview_asset_id`
and never into `layoutData`. DXF still uses
`admin/src/lib/surveyWallSolidPlan.js`, which uses the same local convex/concave
join rules as the Mini Program planner.
DXF generation is likewise read-only: `admin/src/lib/dxf.ts` adapts the graph
to the MIT-licensed writer, emits unioned inner/outer wall `LINE` faces after
opening gaps, inserts hinged-door blocks with an open 90° thick leaf, gray dashed
arc, and jamb rectangles, writes sliding-door double rails and four inset window
lines away from the wall faces, labels closed rooms with four-line MTEXT, wraps the drawing in a
full-height model-space title panel and yellow north arrow filled from the
linked customer lead (enterprise name and assigned designer), and rejects drafts, invalid v4 data, and plans
without a closed space before emitting a download.

## Commission boundary

The measurer-designer acquisition contract is retired and retained only as a
record in [`measurer-designer-acquisition.md`](./measurer-designer-acquisition.md).
Referral-network signing commissions are governed by this inventory and the
development plan. Conversion snapshots three payable rows for `referrer_network`
and two rows for `staff_activity` (designer + measurer; the same beneficiary may
own both). Payable rows may later be adjusted for amount and/or beneficiary with
an audited reason; paid and voided rows cannot. The separate Admin workbench is
implemented from its approved Phase-7 design source and can filter by lead source.

## Maintenance

When a route, API, model, permission, status, or limitation changes, update the
affected row and its Chinese mirror. Keep one current description per module;
do not record the sequence of edits or paste test transcripts into this file.

Chinese mirror: [admin-system-modules.zh-CN.md](./admin-system-modules.zh-CN.md)
