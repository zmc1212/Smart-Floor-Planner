# Admin System: Current Module Inventory

This is the current Admin runtime inventory. It records stable entry points,
contracts, permissions, and limitations only. Implementation history belongs in
Git commits; do not append dated change logs here.

## Shared architecture

- Next.js 16 App Router, React 19, Tailwind CSS 4, Ant Design 5, and Ant Design Pro.
- PostgreSQL 17 with `drizzle-orm`/`pg` is the deployed runtime. Tenant-aware
  reads and writes use repositories, transactions, and RLS.
- Admin sessions use cookie/JWT authentication. Platform and enterprise roles
  are enforced by route guards and menu permissions.
- External provider and object-storage I/O runs outside short database
  transactions. API handlers serialize `bigint` values through DTOs.
- Formal floor plans are version-4 surveying graphs. Viewers, DXF, 3D, and AI
  consume derived read models and never write legacy layout fields.

## Status legend

`Implemented` means a real page/API/data path exists. `Limited` means a defined
role, provider, source shape, or operational condition restricts it.
`Placeholder` means UI or a mock path exists without the promised persistence or
integration.

## Module inventory

Service-address completion is part of the appointment service fact. `POST /api/appointments/[id]/address` is called from appointment detail; the assigned designer or measurer may add or correct an existing appointment address. The server uses the appointment version and a `measurement_appointment_events` `address_updated` audit record for concurrent edits. `POST /api/appointments/[id]/complete` additionally requires a linked completed formal v4 survey floor plan with at least one closed space and returns `appointment_survey_required` (409) otherwise. The Admin lead drawer and Mini Program appointment detail use the same entry.

| Module | Current entry points | API/data boundary | Permission/status | Current limitation |
| --- | --- | --- | --- | --- |
| Authentication and sessions | `/login`, `/register`; auth middleware | `/api/auth/*`; Mini Program JWTs use base user `sub`, selected `customer/staff/referrer` context, and `contextVersion` | Public entry plus authenticated routes; Implemented | WeChat provider configuration remains environment-dependent; legacy identity columns coexist until the old acquisition flow is retired |
| Navigation, roles, access | Shared sidebar and route guards | `/api/permissions`, role/menu repositories | `super_admin`, `admin`, enterprise roles; Implemented | Effective permissions are tenant and role scoped |
| Platform and enterprises | `/dashboard`, `/enterprises` | Enterprise, branding, activation, and platform repositories | Platform roles; Implemented | Enterprise context is required for tenant mutations |
| Staff, accounts, and appointment operations | `/staff`, `/departments`, `/users`, `/referrer-network-operations`, `/appointment-settings`, `/` (designer/measurer home workbench) | Staff, department, shared notification, admin-user, and appointment repositories. The merchant sidebar now links directly to `/appointment-settings` under team assets, reusing the `referrer-network-operations` permission. The appointment-settings page reads and updates timezone, weekly availability, default duration, slot step, booking horizon, and customer reschedule cutoff through `GET/PUT /api/appointment-settings`; an auto-created default row is explicitly distinguished from an administrator-confirmed policy. The operations workbench reads join-code audit, active referrer memberships and persisted active promotion-code count, active staff-activity-code count, assignment eligibility, confirmed appointment settings, commission rules, and WeChat-code configuration through `GET /api/enterprise/referrer-network-readiness`; the promotion-code readiness row no longer infers code availability from memberships alone, and each acceptance item links to its operating surface. The Admin home now renders role-specific designer/measurer workbenches from the authenticated Cookie role; `GET /api/workbench/staff` uses a tenant transaction and returns only the current staff member's leads, appointments, unscheduled survey tasks, and survey handoff data. Formal BLE surveying remains the Mini Program editor's sole production entry. `GET /api/enterprise/join-codes` remains token-free; the audited image endpoint generates a private, no-store onboarding image and preserves WeChat's PNG/JPEG media type. Both enterprise onboarding and referrer promotion images use `getwxacodeunlimit` with `env_version: develop`, including when the server process runs in production mode | `referrer-network-operations`, `/appointment-settings`, and their APIs reuse the `referrer-network-operations` permission. The employee home and `GET /api/workbench/staff` are limited to active `designer`/`measurer` sessions in the selected tenant; enterprise owners retain the enterprise workbench; Implemented/Limited | Employee workbenches provide task aggregation and existing Admin links only: they do not create test business records, bypass Mini Program phone authorization, or provide a desktop BLE/H5 editor. External WeChat credentials are diagnosed but not configured by this page. Authenticated role-specific Admin visual QA remains to be added |
| Promotion and collaboration | `/promotions`, enterprise collaboration pages | Promotion, referral, and shared notification repositories | Enterprise and staff role boundaries; Implemented | WeCom delivery is optional and external |
| Packages, orders, commissions | `/packages`, `/orders`, `/commissions`, `/lead-commissions` | Package, order, existing commission, and `LeadCommissionRepository`; `GET/PUT /api/commission-rules`, filtered `GET /api/lead-commissions` (including `source=referrer_network|staff_activity`), `POST /api/lead-commissions/mark-paid`. The workbench maintains all three rules, can filter by lead source, and shows customer, referrer membership (empty for staff-activity leads), enterprise, designer, measurer, and current confirmed appointment per report record | `lead-commissions` is available to `super_admin`, `admin`, and `enterprise_admin`; system administrators always expose the protected entry even when an older stored menu snapshot lacks the new key; rule changes and offline batch paid marking use RLS and tenant transactions; Implemented/Limited | The old acquisition-commission route is retired. Payment settlement remains outside this system |
| Leads and conversion | `/leads`, `/leads/[id]` | Lead lifecycle, floor-plan, `ReferralLeadRepository`, `CustomerProjectRepository`, and `LeadCommissionRepository`; list/detail DTOs include the assigned measurer, derived `serviceStage`/`nextAction`/`canRebook`, the latest appointment fact, and the referrer resolved from `referrerMembershipId -> referrerEnterpriseMemberships -> referrerProfiles` (using the profile display name/phone without requiring a tenantless base-user row). `/api/appointments` and `/api/appointments/availability` accept Admin Cookie sessions for authorized designers/enterprise admins to create, read available slots, and internally reschedule lead appointments. `POST /api/appointments/[id]/cancel` and `/complete` accept the same Admin Cookie role boundaries as the Mini Program; the `/leads` detail drawer exposes cancel, complete, rebook, and address/reschedule actions with the same expired-row completion rule as Slice 2. `GET/POST /api/leads/[id]/ai-publications` and `DELETE /api/leads/[id]/ai-publications/[generationId]` accept Admin Cookie sessions; the detail drawer lists publishable generations and supports publish/withdraw for assigned designers and enterprise owners. Admin appointment audit writes resolve `admin_users.user_id` to `users.id`; `/leads/[id]` opens the shared lead detail drawer through the query handoff | Customer authorization or tenant/assigned-staff checks; an assigned designer may manage succeeded generations and create, reschedule, or cancel appointments only for their own leads, while an assigned measurer may complete assigned appointments and an enterprise administrator may manage the tenant; only an enterprise administrator can revert signing, and paid commissions block reversion; Implemented/Limited | The customer project renders the protected aggregate's real service facts, completed formal-plan summary, and explicitly published designs; publication images remain owner-only and are fetched into app-local temporary files. The Admin ProTable keeps filtering/pagination and renders each lead as a responsive ProCard with customer, assignment, referrer, appointment, and action groups; the card uses the same server-backed date/slot selection contract as the Mini Program, with audit and notification on changes; Admin staff and user audit identifiers are handled separately |
| Formal floor plans | `/floorplans`, `/floorplans/[id]`, `/floorplans/kujiale` | `FloorPlanRepository`, surveying adapters, DXF exporter. Create/update authorizes the tenant owner, `assignedTo`, or `measurerId` so a staff-activity presenter can save a formal v4 survey without a designer-only check | Tenant, assigned-designer, or assigned-measurer permission; Implemented | KuJiale provider capability is Limited |
| Measurements and BLE devices | `/measurements`, `/devices` | Measurement, device, binding, and audit repositories | Platform/enterprise assignment boundaries; Implemented | Device protocol support is limited to the documented meter |
| AI studio and generation | AI workflow, asset, provider, pricing, and credit pages | PostgreSQL AI repositories and provider adapters | Platform plus tenant AI permissions; Implemented/Limited | Provider availability and image storage are external |
| Media storage | `/media-storage`; `npm run db:backup`, `npm run db:restore-drill`, `npm run db:cleanup:dry-run`, and `npm run db:cleanup:execute` | `media_assets`, provider configuration, storage adapters; backup emits a custom PostgreSQL dump and duration, the restore drill uses only `smart_floor_planner_restore_drill` and verifies the current app schema before removing that drill database, the cleanup dry-run sets its read-only session to the existing platform scope before it fingerprints the target and emits a Qiniu candidate manifest, and the execute command requires an exact fingerprint, approved manifest SHA-256, explicit local-production switch, and operator identity before its one-transaction database cleanup and audit output | Platform admin; Implemented/Limited | Qiniu object deletion remains a separately approved asynchronous operation after a nonempty human-approved manifest; the execute command does not call Qiniu |
| Mini Program support APIs | Admin diagnostics and shared API handlers; production anonymous claim routes are in the Mini Program | Identity/context and dual-code/referrer APIs; `GET /api/miniprogram/bootstrap` validates the signed JWT `contextVersion` and active context, returning the current role, valid role groups, enterprise/membership context, landing path, capability allowlist, and a server-owned badge summary of role-scoped todo counts (`status`/`message`/`counts`; query failure is `unavailable` plus `暂时无法读取` and does not fail identity bootstrap or fill zeros); invalid contexts return `identity_context_invalid`. `/api/miniprogram/codes/resolve` returns an onboarding code's type and target enterprise display name before phone authorization, or issues a ten-minute pending source for referral codes, or a staff-activity pending source (`kind: staff_activity`) that may include the enterprise name. A signed customer with an open attribution lock on an unarchived, unclosed lead receives `existingAttribution` and the existing lead instead of a new pending source or new-claim designer payload. Archiving or closing a lead releases that lock so the customer can claim again. `GET /api/miniprogram/staff-activity-code` and `/image` issue a designer/measurer activity code. `/api/miniprogram/referrer-memberships/[id]/promotion-code/image` returns a protected PNG/JPEG generated by WeChat, and `/api/miniprogram/referrals/authorize-and-create-lead` atomically links the customer, locks attribution, creates the lead, and assigns staff (activity claims lock the presenter as `measurerId`); phase 5 provides appointment APIs; phase 6 provides customer-project aggregation, owner-only published-design images, and designer/enterprise-admin publication or withdrawal; phase 13 adds the owner-only `GET /api/miniprogram/customer-projects` index plus signed-membership `GET /api/miniprogram/referrer-progress` and `GET /api/miniprogram/referrer-earnings`; phase 14 adds `GET /api/miniprogram/workbench` for the signed designer, measurer, or enterprise owner | Promotion resolution and code-image delivery are anonymous to the customer but membership-scoped for the referrer. Customer project endpoints validate `customer_user_id` and never trust client enterprise context; referrer progress and earnings validate both the JWT user and active membership before querying. The workbench derives staff role, enterprise, and staff scope from the signed context: designers receive assigned leads, expired unrebooked work, and own unscheduled survey tasks when they are `measurerId`; measurers receive confirmed appointments (expired rows leave the confirmed list), expired follow-ups, and unscheduled survey tasks; enterprise owners receive only pending-assignment failures, expired unrebooked work, staffing gaps, and confirmed/expired appointment lists. The customer-project index returns the shared derived `serviceStage` plus a single `nextActionKind` (`reschedule` / `rebook` / `view_project` / `wait_designer`). Designers can maintain their WeChat ID and personal QR through Mini JWT `GET/PATCH /api/miniprogram/staff/wechat-profile` and `POST /api/miniprogram/staff/wechat-qr`; measurers are not required to upload a QR. Floor-plan create/update authorizes `assignedTo` or `measurerId`. Appointment APIs are isolated by customer ownership, responsible designer, assigned measurer, or enterprise owner and use tenant transactions; `/api/miniprogram/notification-template` supplies authenticated identities with the configuration needed for customer subscription consent; Implemented/Limited | The customer project uses the completed-plan summary plus protected published-design images, not an editable graph or measurement editor. Referrer aggregates expose only a masked customer label, service facts, and the referrer's own commission status. Workbench summaries are read-only and use the existing authoritative lead, appointment, and formal-survey entry contracts. Appointment create, reschedule, and cancel attempt staff and authorized-customer delivery after commit; WeChat code generation, authorization, and notifications depend on external configuration |
| Notifications, automation, diagnostics | Notification settings, reminder runtime, diagnostics | Notification templates, scheduler, operational records | Platform/enterprise roles; Implemented/Limited | Subscription delivery can be rejected by WeChat |

## Formal surveying boundary

The authoritative contract is
[`surveying-module/formal-surveying.md`](./surveying-module/formal-surveying.md).
`FloorPlan.layoutData` contains only `version: 4`,
`measurementMode: 'surveying'`, and `surveyGraph`. Measurements are immutable
audits; dimensions and room summaries are derived read models. Floor-plan
viewers render wall solids with `admin/src/lib/surveyWallSolidPlan.js`, which
uses the same local convex/concave join rules as the Mini Program planner.

## Commission boundary

The measurer-designer acquisition contract is retired and retained only as a
record in [`measurer-designer-acquisition.md`](./measurer-designer-acquisition.md).
Referral-network signing commissions are governed by this inventory and the
development plan. Conversion snapshots three payable rows for `referrer_network`
and two rows for `staff_activity` (designer + measurer; the same beneficiary may
own both). The separate Admin workbench is implemented from its approved
Phase-7 design source and can filter by lead source.

## Maintenance

When a route, API, model, permission, status, or limitation changes, update the
affected row and its Chinese mirror. Keep one current description per module;
do not record the sequence of edits or paste test transcripts into this file.

Chinese mirror: [admin-system-modules.zh-CN.md](./admin-system-modules.zh-CN.md)
