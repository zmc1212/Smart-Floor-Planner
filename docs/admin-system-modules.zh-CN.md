# 后台系统当前功能清单

本文只记录后台当前运行入口、合同、权限和限制。实现过程由 Git 提交保留，
不在清单中追加日期流水。

## 共用架构

- Next.js 16 App Router、React 19、Tailwind CSS 4、Ant Design 5 和 Ant Design Pro。
  根布局挂载客户端 `AntdReact19Patch`，引入 `@ant-design/v5-patch-for-react-19`，
  使 Ant Design 5 的波纹效果与静态方法可在 React 19 下工作。
- 部署运行时使用 PostgreSQL 17、`drizzle-orm`/`pg`；租户读写通过 Repository、
  事务和 RLS 执行。
- 后台会话使用 cookie/JWT；平台和企业角色由路由守卫与菜单权限共同约束。
- 外部供应商和对象存储 I/O 在短数据库事务之外执行；API handler 通过 DTO 显式
  序列化 `bigint`。
- 正式户型为 v4 量房 wall graph。查看器、DXF、3D 和 AI 只消费读模型，不写旧布局字段。

## 状态定义

`Implemented` 表示存在真实页面/API/数据链路；`Limited` 表示受角色、供应商、
数据形状或运行条件限制；`Placeholder` 表示只有 UI 或 mock，尚无承诺的持久化/集成。

## 模块清单

专业背书已作为租户隔离的员工能力落地。企业负责人和平台管理员在 `/staff` 通过 `GET/PUT /api/professional-profile-settings` 配置设计师/测量员默认头衔、默认经验年限、不得低于 100 的服务门槛、企业统一内容覆盖，以及 `follow_staff` / `force_show` / `force_hide` 头衔显示策略；再通过 `GET/PATCH /api/staff/[id]/professional-profile` 管理单员工专属头衔、从业年份、显示偏好、资料锁定和真实人数展示开关。设计师与测量员通过 `GET/PATCH /api/miniprogram/staff/professional-profile` 自助维护未锁定字段。客户 DTO 只返回最终计算后的 `professionalProfile`，头衔按后台录入原文展示，不再改写「设计师/测量员」；服务档案按设计卡/测量卡岗位分别解析，同一人兼任两岗时各用该岗企业默认头衔。真实人数仅内部/后台可见。设计师按发布时归属且曾有客户可见方案的去重线索计数，测量员按本人完成的正式 v4 户型去重线索计数；真实人数不高于企业门槛时绝不下发客户侧，只有超过门槛后才允许开启准确数字。新增字段不参与派单资格判断。

预约服务地址补录属于预约服务事实：`measurement_appointments` 在手输服务详细地址外，可选保存 `location_name`、范围校验后的 GCJ-02 纬经度和坐标系。预约与线索 DTO 会把 PostgreSQL `tstzrange` 文本（`2026-08-23 01:00:00+00`）改写成 ISO-8601（`2026-08-23T01:00:00.000Z`），以便小程序 `Date` 解析后按上海日历日归入量房日程。`GET /api/appointments/availability` 会去掉已经开始的档；若该线索已有确认预约，则排除这一条自身占用，因此当天改期可以选与当前上门重叠的邻近档，其他线索仍会看到该测量员已被占用。`POST /api/appointments` 与 `POST /api/appointments/[id]/address` 接收可选地图位置；地址更新继续受预约版本保护，并把修改前后的位置快照写入 `measurement_appointment_events` 的 `address_updated` 审计。小程序请求先按员工 `staff._id` 与已派设计师/测量员比对，Admin Cookie JWT 作为回退。后台线索详情与小程序预约详情共用该入口。创建或更新服务地址时，若线索 `communityName` 为空，同一事务会优先用地图 `locationName`、否则用手输地址回填（截断至 160 字；已有小区不覆盖）。小程序预约详情仍保留员工对历史空小区的显式同步。`POST /api/appointments/[id]/complete` 还要求线索关联已完成的正式 v4 量房户型且至少存在一个闭合空间，否则返回 `appointment_survey_required`（409）。编辑器「完成」只提交户型；线索 `designing`、设计师量房完成通知与释放时段只在预约详情确认完成量房之后发生。

| 模块 | 当前入口 | API/数据边界 | 权限/状态 | 当前限制 |
| --- | --- | --- | --- | --- |
| 抢单与赛马派单 | `/lead-pool`、`/assignment-settings`、`/leads` 详情抽屉、`/staff` 个人容量覆盖 | `GET /api/lead-claim-pool`、`POST /api/leads/[id]/claim`、`GET/PUT /api/assignment-settings`、`GET /api/assignment-performance`、`POST /api/leads/[id]/close-lost`、`POST /api/leads/[id]/reopen` 及 `GET/POST /api/internal/lead-claim-windows/run`。企业规则按不可变版本保存抢单时长、70/30 目标、绩效门槛/周期/样本和容量；窗口按服务端截止时间、行锁、哈希幂等键、脱敏池 DTO 与截止兜底运行。签单/正常未签单结案快照当时负责设计师；无效、重复、误录不计绩效。自动派单使用持久化确定性补差与组内稳定负荷排序。每次成功归属设计师还会按线索+设计师写入一次幂等短信提醒，测量员不会进入短信分支。`/lead-pool` 仅在标签页可见且 2 分钟内有指针/键盘/滚动操作时每 3 秒轮询 `GET /api/lead-claim-pool`、每 1 秒刷新倒计时；切回标签页或空闲后下一次操作会立即补刷。服务端抢单窗口与 worker 扫描不受影响。详见[运行合同](./lead-claim-racing.zh-CN.md)和[worker 运维说明](./lead-claim-worker-operations.zh-CN.md) | 设计师只能读取/领取当前签名企业的池，并可正常结案本人线索；企业负责人可监控、指派、配置、选择无效分类和重新激活；平台角色可操作已选企业。新增业务表全部启用并强制 RLS；Implemented/Limited | 可选 `lead_claim_available` 微信模板仍须运营配置真实模板 ID/字段并由设计师明确授权；短信为平台全局配置，须有已审核的阿里云/腾讯云签名模板和有效设计师手机号；短信失败不阻断线索事务 |
| 线索转化卡片呈现 | `/leads` 列表的 `tableViewRender` | 仅调整既有线索 DTO 的可视化：客户头部展示阶段、联系方式、中文来源、面积/风格与方案主操作；派生工作流 `Steps` 配合下一步行动；上门预约与符合条件的卡内改期；推广人/设计师/测量员/派单状态四列协作信息。设计师/测量员字段按服务端 `assignmentActions` 提供「分配/更换」，弹窗 `Select` 数据来自 `GET /api/leads/[id]/assignable-staff`，提交 `POST /api/leads/[id]/assign-staff`，成功和 409 走共享 `notify`。待派单展示具体 `assignmentErrorCode` 文案（如「暂无可用设计师」）与补齐指引；设计师缺微信号/二维码时在重试旁提供「去员工管理」；重试仍失败时用警告反馈同一原因，不再用泛化成功提示。列表增加浅色 16px 内层与 16px 卡片节奏；卡片头部/底部明确保留 20px 内边距，身份/操作组间保留可见 12px 间距；分页器独立为有边界的收尾栏。底部仍调用原有归档、恢复、永久删除和重试派单处理器。`status=closed`（含 `terminationType=referrer_withdrawn`）的卡片隐藏方案主操作、卡内改期和重试派单 | 原有租户与归档/关闭边界；Implemented | 聚焦 `lead-assignment-actions`、`lead-assign-staff`、`referral-lead` 测试覆盖改派写入、角色矩阵、预约改写与后台 notify；自动池重试路径不变 |
| 登录与会话 | `/login`、`/register` | `/api/auth/*`；`PUT /api/auth/password` 允许任何已登录 Admin 用户修改自己的登录密码（校验当前密码、6–32 位新密码与确认、bcrypt 写入 `admin_users.password_hash`；不强制重登）。`admin_users` 在租户内对 `(enterprise_id, user_id)` / `(enterprise_id, phone)` 唯一，`username` 仍全局唯一；身份查找（`findByUsernameOrPhone` / `findByOpenidOrPhone` / `existsWithPhone` / 小程序 `findActiveStaffByPhone`）在命中 0/1 行时与原先一致，≥2 行时禁止静默 `limit(1)`，须按 `enterpriseId`/`staffId` 过滤或返回列表。密码登录成功后用 `window.location.assign('/')` 整页进入后台（与退出同一模式），避免登录前未认证的 `GET /api/auth/me` 被 SWR 缓存复用。`/login` 使用 `AdminAntdProvider includeAccountSettings={false}`，不挂载 `AccountSettingsProvider`/`useCurrentUser`。`GET /api/auth/logout` 会清理 Cookie 并重定向到 `/login`；当 `/` 发现签名仍有效的 JWT 已无法解析为数据库中的有效会话时，会先经过该端点，避免 `/` 与 `/login` 形成 307 循环。已登录的 `useCurrentUser` 在 `/api/auth/me` 非 OK 时抛错，且不重试 401。小程序 JWT 使用基础用户 `sub`、当前 `customer/staff/referrer` 上下文和 `contextVersion`。`POST /api/auth/miniprogram` 的 `wechat_phone` 接受当前微信 `phoneCode`，或旧客户端的 `encryptedData`/`iv`（用该 `loginCode` 换到的 `session_key` 解密）。`getTenantContext` 会拒绝 `aud=miniprogram` 的 Bearer，避免把小程序员工 JWT 当成 Admin 会话（`payload.id` 是 `users.id`，不是 `admin_users.id`）。平台 `admin`/`super_admin` 员工记录不属于小程序工作台角色；小程序登录/刷新会选择同一基础账号的有效推荐人上下文，无推荐成员关系时使用客户上下文 | 公开入口与登录后路由；平台权限仅限后台；Implemented | 微信供应商配置依赖环境；旧身份字段在旧获客流程下线前并存 |
| 导航、角色与权限 | 共用侧栏（底部头像账户菜单）、`/roles`、路由守卫（`proxy.ts`） | `/api/roles`、`/api/auth/me`、`/api/auth/password`、角色/菜单 Repository。侧栏头像悬浮或点击打开 Ant Design 下拉菜单（收起侧栏时向右弹出）：资料头部、所有角色可用的**修改登录密码**、仅 `enterprise_admin`、`admin` 与 `super_admin` 的**修改安全密码**，以及**退出系统**（取代原先独立退出按钮）。资料角色文案来自共享 `getAdminRoleLabel` / `ROLE_LABELS`（`admin`=平台管理员、`super_admin`=超级管理员；未知/空仍为职员）。已登录壳下的共享 `AccountSettingsProvider` 向 `/referrer-network-operations`、线索导出弹窗、`/enterprises` 删除弹窗等页面暴露 `openLoginPassword()` / `openSensitivePassword()`。平台 `admin`/`super_admin` 走 `GET/PUT /api/admin/sensitive-password`（写入 `admin_users.sensitive_operation_password_hash`，bcrypt，明文不回读，不挂菜单权限；首次设置须确认新密码，修改须校验当前安全密码）；`enterprise_admin` 仍走 `GET/PUT /api/enterprise/sensitive-password`。公开 `/login` 关闭该 Provider，不请求 `/api/auth/me`。`/roles` 勾选名称来自 `ALL_MENUS`，须与侧栏一致（如 `dashboard`=业务概览、`leads`=线索转化）。权限页按 `MENU_PERMISSION_GROUPS` 分组勾选（对齐侧栏：平台管理中心、B2B 运营转化、运营工作台、推荐网络、户型图库、AI 辅助设计、团队资产管理），并提供「恢复该角色默认」（读 `DEFAULT_PERMISSIONS` / `getDefaultMenuKeys`），便于在代码默认变更后校准 `system_roles.menuKeys`（`ensureDefaults` 仅插入不覆盖）。双轨代码默认：`enterprise_admin` 含推荐网络运营、员工、三方提成、AI 与设备；`designer` 为线索/户型/量房/AI/灵感（不含 `promotion-records`）；`measurer` 为线索/量房/户型；`viewer` 使用现行 key（`dashboard`/`floorplans`/`inspirations`/`ai-scenarios`），不再含已退役 AI 菜单键。企业首页 `/` 在 `dashboard` 下读取工作台摘要/待办，并对企业负责人/设计师/测量员请求 `GET /api/workbench/ops-dashboard` 周期经营大盘；线索/户型/员工汇总仅在具备对应菜单权限时请求。`GET /api/auth/me` 在角色菜单变更后刷新 auth cookie，API 守卫无需重新登录即可对齐 | `super_admin`、`admin`、企业角色；Implemented | 有效权限按租户与角色的 `system_roles.menuKeys` 生效（账号级 `menu_permissions` 运行时不参与）；入驻码/推荐人等子路由复用父级菜单键，`/enterprise-registration-codes` 复用 `enterprises`；改代码默认后需在 `/roles` 恢复默认并保存才更新已有库行；AI 创作台/工作台全屏壳仍无侧栏账户菜单 |
| 平台与企业 | `/dashboard`、`/enterprises`、`/enterprises/[id]`、`/enterprise-registration-codes` | 企业、品牌、激活及平台 Repository。企业生命周期状态为 `pending_approval` / `active` / `disabled` / `rejected`，经 `POST /api/admin/enterprises/[id]/status` 执行 FSM（`approve` / `reject` / `disable` / `enable` / `resubmit_review`；拒绝/停用必填 4–200 字原因）。该路由走共享 `applyEnterpriseStatusChange`，供小程序平台审核复用同一路径（`GET/POST /api/miniprogram/platform/enterprises*`）：FSM `applyStatusAction`；进入 `active` 时 `ensureEnterpriseAdminForActiveEnterprise`（并把联系人手机号已有小程序用户 `ensureStaffUser` 绑到负责人）；`approve`/`reject` 提交成功后尽力发 `enterprise_join_result`。最新留痕字段在 `enterprises`（`statusReason` / `statusChangedAt` / `statusChangedByAdminId`），追加事件表 `enterprise_status_events`。`PATCH /api/admin/enterprises/[id]` 不再接受 `status`。`DELETE /api/admin/enterprises/[id]` 为平台级联删除（`super_admin`/`admin`，`requireEnterprise: false`）：body `confirmEnterpriseName`（须与目标企业全名完全一致）加 `securityPassword`，校验当前登录人 `admin_users.sensitive_operation_password_hash`（未设置 400 `sensitive_password_not_configured`，错误密码 403），再走现有 `previewPurge` + `purge`。批量 `DELETE /api/admin/enterprises` 接受 `{ ids, confirmText: "确认删除", securityPassword }`，去重后 1–20 家，每家独立事务顺序 purge，部分失败返回已删/失败清单且不回滚已成功的。两者均沿用 `isTenantEnterpriseResetAllowed()`（生产默认拒绝，除非 `ALLOW_TENANT_ENTERPRISE_RESET=true`）。不删对象存储、不影响其他企业；全局 `users.enterprise_id` 仅置空。商户侧 `POST /api/enterprise/enterprise-purge` 仍用企业全名确认，本次不改。停用仍是生命周期动作，删除不要求先停用。`/enterprises` 列表更多菜单与 `/enterprises/[id]` extra 提供「删除企业」；列表支持多选与工具栏「批量删除」。单删须输入企业全名加个人安全密码，批删须输入「确认删除」加安全密码（1–20 家）。未设置时打开 `openSensitivePassword`。成功/失败走共享 `notify`；详情删除成功后回 `/enterprises`。平台 `admin`/`super_admin` 可写可空的 `enterprises.referrerAdditionalEnterpriseLimit`（0–99 家其他企业；`null` 关闭；`enterprise_admin` 传该字段返回 403）。全局推荐人企业数上限 `referrerMembershipLimit`（默认 3，最小 1）存在 `platform_configs.promotion_config`，由 `PATCH /api/platform/promotion-config` 先读再合并保存，并在 `/enterprises` 列表「推广人可加入企业数」卡片编辑（不放进 `/promotion-records`）。企业保护 M 在 `/enterprises/[id]` 概览「推广人企业保护」卡片，仅平台 `admin`/`super_admin` 可见可改。推荐人入驻只统计 `active` 成员，取全局 N 与各企业保护 M 的更严值，分别返回 `membership_limit_reached` / `referrer_protection_limit`，收紧上限不会退出存量成员。非 `active` 企业会阻断商户 Admin 登录/会话（`/api/auth/login`、`/api/auth/me`、`withTenantRoute`）以及小程序员工/推荐人上下文。平台级小程序开户码使用 `platform_enterprise_registration_codes` / `platform_enterprise_registration_code_events`（HMAC `er_` 令牌、库内仅存 SHA-256、全局仅 1 个 active）与 `EnterpriseRegistrationCodeRepository`（`rotate` / `disable` / `revealActive` / `resolve` / `inspect` / `listEvents`）。Admin API：`GET /api/admin/enterprise-registration-codes`、`POST .../rotate`（可选 `expiresAt`）、`POST .../disable`、`POST .../image`（只还原当前生效 `er_` 令牌并返回私有 no-store 微信码，落地 `packages/business/enterprise-register/enterprise-register`，不换新）。小程序平台管理员经同一 helper 走 `GET /api/miniprogram/platform/enterprise-registration-code` 与 `/image`。后台页在卡片内直接展示当前有效二维码供查看/下载；换新/创建仍是独立动作。开户码近期审计与商户双码审计共用 `code-audit-labels` 中文映射。商户侧 `ej_` 入驻码仍在 `/join-codes`，勿与开户码混用 | 状态变更、开户码、级联删除与推广人加入上限仅平台角色；`/enterprise-registration-codes` 及其 API 复用 `enterprises` 权限；Implemented | Web `/register` 仍可不带 `er_`；新开通的企业负责人初始密码为 `123456`（`buildEnterpriseAdminUsername`：首店仍用联系人手机号；第二家起用 `phone_e{enterpriseId}` 保持 username 全局唯一，登录仍按手机号匹配）；审核短信与小程序内进度查询不在范围；申请人侧重提未实现（由平台在同一条记录上改资料再审）；生产环境级联删除默认拒绝，除非 `ALLOW_TENANT_ENTERPRISE_RESET=true` |
| 员工、账号与预约运营 | `/staff`、`/departments`、`/users`、`/referrer-network-operations`、`/join-codes`、`/referrers`、`/appointment-settings`、`/`（企业负责人业务概览 + 设计师/测量员首页工作台） | 员工、部门、共享通知、管理员与预约 Repository。`DELETE /api/staff/[id]` 会先删除该员工的 `staff_activity_codes` 再删账号；若仍有量房预约、AI 操作记录或旧提成记录则 409 中文拒绝，删除当前登录账号则 400。商户侧栏「推荐网络」分组现包含 `/referrer-network-operations`、`/join-codes`、`/referrers` 与 `/appointment-settings`，全部复用 `referrer-network-operations` 权限。`/referrer-network-operations` 为就绪枢纽：数字摘要、可执行验收清单和最近 5 条双码审计预览；双码换新/停用、卡片内直接查看/下载当前有效二维码（不换新、不限时隐藏）与完整审计在 `/join-codes`。双码与平台开户码审计表通过共享 `code-audit-labels` 将 `eventType`/`result` 英文键映射为中文（库内仍存英文键）。同页在验收清单中跟踪**企业安全密码**就绪状态（仅企业负责人）：`GET/PUT /api/enterprise/sensitive-password` 读写 `enterprises.sensitive_operation_password_hash`（bcrypt，明文不回读）。设置与修改在侧栏头像账户菜单的共享 `SensitivePasswordSettingsModal` 中完成（验收清单与线索导出未设置时也会打开同一弹窗）；首次设置与修改均须确认新密码，修改须校验当前安全密码。设置弹窗 GET 仅在打开时请求，读取后不回调刷新就绪接口；PUT 成功后通过显式 `onSaved` 刷新验收清单。运营页只在进入、手动刷新和写操作后请求，不后台轮询。同页另提供**当前企业内测两档危险操作**：（1）清空并重跑入驻 — `GET /api/enterprise/enterprise-reset/preview` 与 `POST /api/enterprise/enterprise-reset`（body `confirmEnterpriseName`）清空本企业几乎全部业务/员工/入驻码/规则数据，仅保留 `enterprises` 行与当前操作者 `admin_users`；（2）删除整家企业 — `GET /api/enterprise/enterprise-reset/preview?mode=purge` 与 `POST /api/enterprise/enterprise-purge`（body `confirmEnterpriseName`）同样全清且不保留任何员工，再删 `enterprise_status_events` 与 `enterprises` 行（仅将指向本企的 `users.enterprise_id` 置空，不物理删全局 `users` / `wechat_identities`）。`NODE_ENV=production`（含 Docker Compose / `next start`）默认拒绝；内测 Docker 也必须在 `.env.production` 设置 `ALLOW_TENANT_ENTERPRISE_RESET=true` 并重建 admin 容器才允许。危险操作卡片在预览失败时展示接口错误，而不再用笼统占位文案。不删对象存储文件、不影响其他企业。整企删除成功后，商户负责人清 `auth_token` 并跳转 `/login`，平台管理员跳转 `/enterprises`。`/referrers` 列出推荐人姓名、手机号、加入时间、活动推广码和停用后续扫码，支持姓名/手机号搜索；小程序 Mini JWT `GET /api/miniprogram/enterprise-referrers` 与 `POST .../[id]/disable` 经同一 `ReferrerNetworkRepository` 查看并停用该名册（停用幂等、无重新启用）。后台不手工新建推荐人，姓名在小程序推荐人入驻时采集（`POST /api/miniprogram/onboarding/referrer` 必填 `displayName`，不再回退为「推荐人」）。预约设置页通过 `GET/PUT /api/appointment-settings` 读取和修改时区、每周开放时间、默认时长、时段步长、最远预约天数及客户改期截止；系统自动建立的默认记录会与管理员已确认策略明确区分。运营工作台经 `GET /api/enterprise/referrer-network-readiness` 读取双码审计、活动推荐人成员关系与已持久化的活动推广码数量、活动员工码数量、派单资格、已确认预约设置、提成规则及微信服务码配置；推广服务码就绪项不再仅根据成员关系推断码可用，并为每项验收项提供真实操作入口。`GET /api/enterprise/join-codes` 返回不含令牌的双码与审计事件；`GET /api/enterprise/referrer-memberships` 按租户列出成员并可按关键词/状态筛选；停用仍为 `POST /api/enterprise/referrer-memberships/[id]/disable`。后台首页按已认证 Cookie 角色渲染：企业负责人 `MerchantDashboard` 展示库存三卡、只读经营大盘与最近线索流转（已移除旧企业报备「协作待办」条）；设计师/测量员 `StaffWorkbench` 在待办队列上方插入「我的」经营大盘。`GET /api/workbench/ops-dashboard`（`period=week|month|year|custom`，自定义另传上海日历 `from`/`to`）与小程序共用 `loadOpsDashboard`：五卡 KPI（新增线索、已完成量房、方案交付率、已签约、签单率=同窗已签约÷新增线索；签约事实 `status=converted` + `convertedAt`）；负责人全店可含签约金额 detail，员工个人归因不含金额。`GET /api/workbench/staff` 在租户事务中只返回本人线索、预约、无预约待量房任务和量房交接；测量员正式 BLE 量房仍只从小程序正式编辑器完成。写入审计的图片接口生成私有、禁止缓存的入驻图片并保留微信返回的 PNG/JPEG 类型。企业入驻码和推荐推广码均使用 `getwxacodeunlimit` 与 `env_version: develop`，即使服务端以生产模式运行也保持开发版 | `referrer-network-operations`、`/join-codes`、`/referrers`、`/appointment-settings` 及对应 API 复用 `referrer-network-operations` 权限，员工首页与 `GET /api/workbench/staff` 限当前租户内活动的 `designer`/`measurer` 会话；企业负责人继续使用企业经营工作台；Implemented/Limited | 员工工作台只提供任务聚合和既有后台入口，不创建测试业务数据、不绕过小程序手机号授权，也不提供后台 BLE/H5 编辑器。外部微信凭据只诊断、不在本页配置。认证后台视觉核验待本次角色登录态补充。仍有量房预约、AI 操作记录或旧提成时不能硬删除员工 |
| 企业负责人推广网络 | `/referrers` | 企业负责人专属「推广网络」调用 `GET /api/enterprise/referrer-memberships?view=network`，复用现有 `ReferrerNetworkRepository` 员工分支：包含每位可邀请员工、其直接邀请的推广人、零推广人分支、离职邀请人员快照及未归属的历史成员；「全部推广人」保留原有可搜索名单与停用后续扫码操作 | 复用 `referrer-network-operations`，且 `view=network` 额外要求 `enterprise_admin`；已选企业的平台角色仍只能看平铺名单；Implemented | 认证后台视觉核验待补 |
| 报备与协作 | `/promotions`、企业协作页 | 报备、推荐和共享通知 Repository | 企业和员工边界；Implemented | 企业微信投递为可选外部能力 |
| 套餐、订单与提成 | `/packages`、`/orders`、`/commissions`、`/lead-commissions` | 套餐、订单、既有提成及 `LeadCommissionRepository`（签单快照同时写入不可变的 `original_payable_amount` / `original_beneficiary_user_id`，以及可空的最近一次调整审计列）。撤销签约会作废未支付行，并把完整行快照写入 `conversion_reverted` 生命周期事件元数据；同一线索再次签约时按新合同金额、当前规则和当前受益人刷新这些行，清除上一轮作废/调整运行态并恢复为 `payable`；`GET/PUT /api/commission-rules`、可筛选的 `GET /api/lead-commissions`（含原始/调整审计字段与 `source=referrer_network|staff_activity|manual_entry`）、`GET /api/lead-commissions/beneficiaries?role=`、`PATCH /api/lead-commissions/[id]`（仅 `payable` 可改应付金额和/或受益人，原因选填；推荐人须为本企业活动推荐人成员，设计师/测量员须为本企业对应角色且已绑定 `userId` 的员工）、`POST /api/lead-commissions/mark-paid`。小程序负责人经 `POST /api/miniprogram/enterprise-commissions/mark-paid` 走同一仓储确认线下打款。`/lead-commissions` 工作台使用 Admin Pro `PageContainer`/`ProTable` 与 Ant Design 规则卡（`Segmented`、带不换行「元/单」后缀的 `InputNumber`、`Switch`）维护三条规则，可按线索来源筛选，报表按线索主行展开：主行展示客户、推荐人关系（活动线索为空）、企业、设计师、测量员和当前有效预约及提成合计/状态摘要，子表分行展示三角色规则/金额/状态，并在待支付行提供「调整」弹窗（受益人与原因选填）。台账推荐人与 `/leads` 相同，走 `referrerMembershipId -> referrerEnterpriseMemberships -> referrerProfiles` 的展示名/电话，不依赖无租户 `users` 行；小程序基础用户常空 `enterprise_id`，租户 RLS 读不到 `users` | `lead-commissions` 仅 `super_admin`、`admin`、`enterprise_admin` 可用；系统管理员即使使用旧的已存菜单快照也始终展示该受保护入口；规则修改、待支付调整和线下批量标记已支付均使用 RLS 与租户事务；Implemented/Limited | 旧获客提成路由已下线。支付不在本系统内完成。已付/已作废不可直接调整；不支持手工新建台账、已付冲正，单条作废仍走撤销签单；重新签约刷新只能通过线索签约生命周期触发 |
| 线索与转化 | `/leads`、`/leads/[id]` | 线索生命周期、户型、`ReferralLeadRepository`、`CustomerProjectRepository` 与 `LeadCommissionRepository`；列表/详情 DTO 包含已分配测量员；`GET /api/leads` 列表范围设计师按 `assignedTo`、测量员按 `measurerId`（小程序「客户」Tab 经 `resolveStaffLeadListOptions` 使用同一测量员范围；小程序员工列表另支持可选 `referrerMembershipId`，校验当前企业 membership 后与员工可见范围 AND）；GET /api/leads/[id] 的 publishedSchemes 按首次对客户可见时间（`firstPublishedAt`）排序，图片含 stageKey/publishedAt，供小程序只读方案册时间轴使用、派生 `serviceStage`（`survey_ready` = 已完成正式 v4 户型但当前有效预约尚未 `completed`；`survey_completed` 仅在 `POST /api/appointments/[id]/complete` 成功后）/`nextAction`/`nextActionKind`/`canRebook`（测量员匹配后客户下一步为 `book`，按钮「预约量房」、说明「可预约量房时间」，设计师仍可代约；`design_published` / `lead.status` 为 `designing` 不关闭补测，仅 `converted`/`closed`、已有合格正式户型、待派或已有有效确认预约才关闭。首次发给客户——不是草稿出图——把 `new`/`measuring` 升为 `designing`。创建预约接受尚未完成量房的 `designing` 线索）、当前有效预约（重约后未结束的 `confirmed` 档优先于过期或已过结束时间的旧行），以及由 `referrerMembershipId -> referrerEnterpriseMemberships -> referrerProfiles` 解析出的推广人（使用资料展示名/电话，不依赖无租户基础用户行）。后台线索 DTO 还会从既有员工资料返回绑定设计师和测量员手机号，并附带 `assignmentActions: { canAssignDesigner, canAssignMeasurer }`。`status=closed`（含 `terminationType=referrer_withdrawn`）的后台列表/详情保留详情与归档、企业负责人「重新激活」，但隐藏开始方案、预约、补充资料、跟进、方案发布和重试派单；派单审计事件键以中文展示。`POST /api/leads/[id]/assign-staff` 同时接受小程序 JWT 或后台 Cookie；企业负责人与平台 `admin`/`super_admin` 可补齐或更换设计师和测量员，本线索已绑定设计师只能改测量员，测量员 403。派单响应保留与 GET `/api/leads/[id]` 相同的 `assignedTo`/`measurerId` 员工摘要。覆盖写入 `leadAssignmentEvents`（`assignment_manual` / `assignment_manual_reassign`）；若存在已确认且尚未结束的预约则同步改写预约人员（`staff_reassigned`；测量员时段冲突 409，可用性检查排除本条预约）。`GET /api/leads/[id]/assignable-staff?role=` 返回可派花名册并去掉当前已绑定的人。`/leads` 卡片提供「分配/更换」；「重试派单」仍只走自动池。已签约线索仍可改运营归属，不改写提成快照。企业负责人/平台管理员 `POST /api/leads` 创建 `manual_entry` 线索，走与推荐网络相同的设计师/测量员池子自动派单；同一企业未关闭线索若手机号相同则复用并绑定对应微信客户。未归档线索详情抽屉的「客户资料」区提供「补充资料」，企业负责人、负责设计师与已派测量员可经 `PUT /api/leads/[id]` 更新称呼、手机、小区、面积和风格（仍拒绝改 `assignedTo`；改派走 `POST /api/leads/[id]/assign-staff`，不改写提成快照）。`/api/appointments` 与 `/api/appointments/availability` 支持授权的后台 Cookie 设计师/企业负责人创建、读取可用时段和后台改期线索预约；`POST /api/appointments` 带 `source=on_site` 可幂等补造一条开始时间为现在的上门记录（跳过必须未来/必须落在可约格子，仍做测量员时段冲突检查，不向客户发「您已预约上门」）。户型提交不再把线索写成 `designing`、也不通知设计师；`POST /api/appointments/[id]/complete` 在正式闭合户型闸门通过后才写入 `designing` 并 `notifyDesignerOfSurveyCompleted`。过期任务跳过已有正式完成户型的 `confirmed` 预约，改派测量员时重叠 `confirmed` 仍 409。`POST /api/appointments/[id]/cancel`、`/complete` 与 `/internal-reschedule` 先按小程序员工 `staff._id` 与预约 `designerId`/`measurerId` 授权，再回退 Admin Cookie JWT（与 `/address` 一致）；线索详情抽屉提供取消、完成、重新预约及改期/补地址操作，过期行完成规则与 Slice 2 一致。`GET/POST /api/leads/[id]/ai-publications` 与 `DELETE /api/leads/[id]/ai-publications/[generationId]` 仍接受后台 Cookie，供小程序单图发布/撤回兼容。负责设计师和企业负责人在 `/ai-studio/scenarios` 通过 `POST /api/leads/[id]/ai-scheme-publications` 以“合并发布”方式发送命名多图方案：同一 `workflowId` 内的本次勾选会增量更新/插入活跃发布记录，未勾选但已确认的图片不会因重复发送而隐式撤回，再次勾选已发布图片不改写 `publishedAt`；整套撤回仍走 `DELETE /api/leads/[id]/ai-scheme-publications/[workflowId]`（撤回当前定稿套会清空 `finalizedWorkflowId` 并回落最近活动），单张撤回走 `DELETE /api/leads/[id]/ai-scheme-publications/[workflowId]/generations/[generationId]`；负责设计师/企业负责人经 `POST /api/leads/[id]/ai-scheme-publications/[workflowId]/finalize` 将已发送方案设为定稿（线索级 `finalizedWorkflowId` 指针，改指覆盖，不锁创作）；`publishedSchemes[].finalized` 与 `featuredScheme` 优先定稿套，未定稿时仍取最近活动；线索详情抽屉展示这些方案套、定稿标记与「设为定稿」，并深链到 AI 工作台。后台预约审计通过 `admin_users.user_id` 映射到 `users.id`；`/leads/[id]` 通过查询参数打开共享线索详情抽屉。企业负责人可在 `/leads` 工具栏「导出客资」经 `POST /api/leads/export`（body `securityPassword`）下载本企业全部线索（在用+已归档，上限 10000 行）的 UTF-8 BOM CSV，含手机号等敏感列；须先设置企业安全密码，设计师/测量员/平台管理员无导出入口 | 客户授权或租户/责任人校验；负责设计师仅管理自己线索的已成功 generation，并只能为本人线索创建、改期或取消预约；已派测量员可完成本人预约；企业负责人可管理本企业；客资 CSV 导出仅 `enterprise_admin` 且须企业安全密码；撤销签单仅企业管理员且已支付提成会阻止撤销；Implemented/Limited | 客户项目已展示受保护聚合中的真实服务事实、完成正式户型摘要及主动发布方案；后台 ProTable 保留筛选/分页，并将每条线索渲染为响应式 ProCard，分组展示客户、派单、推广人、预约和操作；卡片会在已解析的推广人、绑定设计师和测量员姓名后显示已有手机号；卡片与小程序共用服务端日期/时段选择合同，改期写入审计并通知相关人员；后台员工主键与用户审计主键已分离处理。后台 /leads 列表只在进入页面、筛选/分页和写操作后请求，不后台轮询；导出不走列表 API |
| 正式户型 | `/floorplans`、`/floorplans/[id]`、`/floorplans/kujiale`；`GET /api/floorplans/[id]/export/dxf` | `FloorPlanRepository` 与只读量房适配器。`GET /api/floorplans` 在租户 RLS 隐藏 `users` 行（客户账号 `enterprise_id` 为空）时把 `creator` 回退成 `creatorId` 字符串，不再因连接结果的空 id 500；微信 openid 与 users 对象分开选择，不嵌进 creator 映射。`/floorplans/[id]` 用小程序 `surveyCanvasRenderer` 的后台镜像（从 `miniprogram/packages/surveying/utils` 同步到 `admin/src/lib/survey-runtime/`）绘制当前楼层，去掉编辑 session，只支持平移/缩放，不写回 graph（镜像投影接受可选仅视图 `rotationRad`，默认 0，后台查看器不传入）。已完成的正式 v4 户型在创建/更新后把同一套 canvas 导出为 PNG 快照，存入 `media_assets`（`ownerType: floor_plan_preview`），由 `floor_plans.preview_asset_id` / `preview_render_revision` 指向，不写入 `layoutData`。AI 工作台整屋控制图和 `GET /api/ai/workflows/[id]/floor-plan-preview` 复用该快照；同一预览路由加 `?roomId=` 输出单房间 Mini SVG 裁切图，与批次作为首张参考图上传的控制图一致。`GET /api/floorplans/[id]/preview` 输出整屋快照。小程序 JWT 读取 `GET /api/floorplans/[id]` 与 `GET /api/floorplans/[id]/preview` 还允许关联线索的已派设计师或已派测量员，不只看 `floor_plans.staff_id`，且线索与员工须属于同一企业（写入对企业负责人按当前租户授权，对员工仍按线索责任人授权）。正式查看器通过 `@tarikjabiri/dxf@2.8.9`（MIT）下载分图层、毫米单位、AutoCAD 2007+ DXF，图层为国内 CAD 习惯（`墙`、`门`、`窗`、`尺寸标注`、`空间名称`、`指北针`），并带毫米 DIMSTYLE（`标注线` / `标注线-内墙`）、`_ARCHTICK`、黑体和门扇弧虚线 `ACAD_ISO03W100`；墙体按开口切开后交给 `surveyWallSolidPlan` 并集，导出内外皮 `LINE`（含门垛），不再按墙段输出厚度矩形；平开门以 `DOOR` 块 `INSERT` 到洞口面，门扇为绿色打开 90° 的厚矩形加灰色虚线弧和 50mm 门垛；推拉门仍为洞口内双轨，窗为离开墙皮的四条洞口内玻璃线；尺寸复用同一套闭合尺寸计划，写成旋转 `AcDbRotatedDimension`（内圈分段含墙厚刻度用 `标注线-内墙`，外圈总长用 `标注线`，文字为整毫米且洋红 DIMCLRT 193，加大标注间距，按 `dimensionStart`/`dimensionEnd` 落标注线，`DIMTAD` 2 / `DIMGAP` 10，角度 0 或 90，L 形凹口就近标注）；闭合房间四行 MTEXT 在 `空间名称` 图层（ACI 7 白字）；固定横版图框 30640×21660，户型按左侧绘图区约 90% 等比放大或缩小居中，标题栏比例约 1:85，整图套青色双层圆角外框、Y 轴取反对齐画布、项目名称与 DXF 文件名取客户姓名+小区+面积+导出时间、右侧标题栏（中文/英文/内容竖排、无格内竖线）和黄色填充指北针，并带图纸名、计算比例与完成日期。共享墙、斜墙、门窗、尺寸、闭合空间标注及多楼层横向分区均从权威 v4 graph 映射，不复制布局。端点要求 `completed`、正式 v4 数据和至少一个闭合空间。后台与小程序 DXF 端点把户型名、关联线索的企业名（否则用户型租户）、线索负责设计师和户型完成日期写入图框 | 原有租户企业负责人、负责设计师或已派测量员权限；Implemented | 2D 画布只读，不是小程序编辑器或 BLE 入口。不存储 CAD 文件，不导出客户电话地址；图框含双层圆角外框、通高右侧 MTEXT 标题栏、填充指北针、图纸名「原始户型平面图」、计算比例、完成日期，公司取关联线索企业并置于栏首，设计师取线索负责设计师；酷家乐供应商能力为 Limited |
| 测量与 BLE 设备 | `/measurements`、`/devices` | 测量与设备 Repository。设备身份为 BLE `deviceId`/MAC，写入 `devices.code`。可选且唯一的 `devices.serialNumber`（SN 码）在后台新增/编辑和小程序录入时写入（`POST /api/devices`、`PATCH /api/devices/[id]`，以及 `POST /api/miniprogram/devices` 的单台共用字段或 `devices[].serialNumber`）。平台管理员（`super_admin`/`admin`）在 `/devices`（平台侧栏「设备管理」）增删改、多选删除（`DELETE /api/devices`，body `{ ids }`，最多 100 台）并分配企业；列表可用独立「SN 码」搜索做字母数字部分匹配（可省略横线、空格），关键词仍覆盖编码/SN/备注；企业角色仅只读本企业「设备列表」。连接授权不再要求使用人绑定。小程序 `POST /api/devices/verify-binding` 仅当设备已 `assigned` 且 `enterpriseId` 与员工企业一致时放行；优先使用 JWT 员工 id，openid 回退限定在匹配设备所属企业，避免同一手机号多店串授权（MAC/编码比对会去掉分隔符，并可用压缩广播 hex 匹配，以便 iOS UUID `deviceId` 仍能授权）。平台小程序录入走 `GET/POST /api/miniprogram/devices`（已登记列表默认可看全部企业，也可按企业过滤） | 平台可写、企业只读；Implemented | 仅支持协议文档定义的测距仪；身份以微信 BLE `deviceId` 为准（已核验安卓机为 MAC；iOS 可通过广播 MAC hex 授权）；SN 码仅作库存登记，不参与蓝牙授权 |
| AI 工作室与生成 | `/ai-studio/scenarios`（AI 工作台）、`/ai-studio/create`（创作台）、供应商/预设/点数页面 | PostgreSQL AI Repository 与供应商适配器。商户工作台 `/ai-studio/scenarios` 与创作台一样新开全屏窗口。左侧选线索和命名对话（允许仅绑线索、无 `sourceFloorPlanId` 的 `rough_sketch` 方案；小程序 `recipe-project` 现已用同一套客户 → 方案选择：`studio/leads` 与 `studio/workflows`；Mini `GET .../studio/workflows` 会为方案卡片签发 `coverUrl`，优先已确认图否则最近成功出图），右侧用创作台出图条（应用到哪里：默认完整户型或闭合单房间、模型、张数、比例、分辨率、提示词模板（点封面放大预览，点名称/「使用」会回填提示词、推荐模型，并经 `POST /api/ai/creation/prompt-templates/[id]/reference` 把封面克隆为创作参考图（不走预览接口的 302/CDN；本地库对象读不到时回退导入的 `sourceUrl`），替换上一张模板封面）、参考图和提示词优化）。两处工作室的生成图「基于此图继续/引用为参考图」均改走 `POST /api/ai/creation/assets/from-generation`：同租户的已成功结果优先直接复用已持久化资产；仅有供应商直连图时由服务端导入，从而避开浏览器 CORS 与对象存储重定向失败。工作台与创作台模型选项来自同一套平台 GRS 生图目录（`bootstrap.models`）：须目录启用且至少一档 `/ai-credit-prices` 积分价启用才列出；开积分价会自动启用对应目录模型，在「生图模型」启用模型且尚无启用价时会打开其默认分辨率价（沿用该档已有点数，否则用 `image.free_create` 场景价），`ensurePostgresGrsImageModelCatalog` / 列表会把已开价但仍关闭的档案 reconcile 为 `enabled`。`GET`/`PATCH /api/admin/ai-credit-prices` 与 `/api/admin/ai-image-model-prices` 只列出并保存已知平台动作键和 GRS 目录模型价；非目录残留行会被忽略，避免启用其他模型时被「价格配置无效」整单拒绝。动作价列表把 `_id` 序列化为字符串、`credits` 序列化为数字，并省略 Postgres `updatedBy` bigint，避免保存后再 GET 因 JSON.stringify 失败。`/ai-credit-prices` 把接口行深拷贝成独立快照，只勾选模型分辨率开关也会进入未保存状态；任一表有改动即可点保存。会先保存自由创作模型分辨率价，并把空值/越界点数钳制到 1–100000，避免场景动作价失败时 Nano Banana 2 等目录模型勾选落不了库、工作台仍只出现 GPT Image 2。`free_create` 提交目录快照 `remoteModel`（GRS 只需具备对应 image 能力，不再要求 `modelMappings` 等于所选模型、也不再用映射覆盖）；场景/配方等未选型类型仍用供应商默认映射。预选优先匹配 `provider.defaultRemoteModel`（当前优先级最高的生图供应商 `image.generate.standard` 映射），匹配不到再退回目录 `isDefault`，再按 weight；供应商映射只表示默认展示，不是可选范围。已量房线索开对话仍绑合格正式 v4 户型；未量房线索可新建仅绑线索的 `rough_sketch` 对话（隐藏「应用到哪里」，批次不传 `targetScope`/`roomId`），仍可发给客户。绑定正式户型时，出图条提供「应用到哪里」作用域（默认完整户型 / `whole_floor_plan`，或绑定户型中的闭合单房间），语义对齐小程序 `recipe-project` 与 `scheme-studio` 的 `targetScope`/`roomId`；说明文案与小程序一致：「只应用到当前选择，不自动为其他房间生成、不额外扣点」。每一轮经 `POST /api/ai/creation/tasks/[id]/batches` 可选提交 `targetScope`/`roomId`（省略或 `whole_floor_plan` 即沿用整屋控制图）。`preparePostgresCreationBatch` 复用 `resolveMiniAiFloorPlanTarget`，写入 generation `input.roomData`，把量房房间摘要拼进供应商提示词，并在批次 `parameterSnapshot` 持久化 `targetScope`/`targetLabel`/`roomId`/`controlKind`，便于轮次回看「整屋方案」或某房间。控制图：整屋仍用已存储的量房 canvas 快照（与 `/floorplans/[id]` 同一套 `surveyCanvasRenderer`）；单房间优先按 `roomId` 裁切 Mini SVG，裁切失败则回退整屋快照（`roomData.controlKind` 为 `room_crop` 或 `survey_snapshot_fallback`）。出图条参考图槽会展示同一张锁定控制图缩略图（选闭合房间时为该房间裁切图），生成时仍把它作为首张参考图上传；额外用户参考图可选。`GET /api/ai/workflows/[id]` 在 `sourceFloorPlan.rooms` 返回闭合房间列表供选择。对话标题旁展示整屋快照供设计师对照（可放大、与效果图左右对照，并链到 `/floorplans/[id]`）；`GET /api/ai/workflows/[id]/floor-plan-preview` 输出整屋快照，加 `?roomId=` 则输出对应房间裁切图，不发给客户。夜间主题沿用创作台深色，日间用后台绿/白色系，选择记在 `localStorage`。`POST /api/leads/[id]/ai-scheme-publications` 以“合并发布”方式把该对话勾选图集同步到客户可见方案：同一 `workflowId` 内的选中轮次会增量更新/插入活跃发布记录，未勾选但已确认的图片保持可见，除非被单张撤回或删轮次/删方案显式下架。设计师可通过 `DELETE /api/ai/workflows/[id]/generations/[generationId]` 删除轮次、通过 `DELETE /api/ai/workflows/[id]` 存档方案（会同步撤回客户可见发布）。已发送方案可在工作台与小程序 `scheme-studio` 经 `POST /api/leads/[id]/ai-scheme-publications/[workflowId]/finalize` 设为定稿；`publishedScheme.finalized` 与顶栏徽章区分「已定稿」。小程序设计师单图发布仍走 `/api/leads/[id]/ai-publications`。设计师/企业负责人移动端方案读取与出图走 `/api/miniprogram/ai/studio/*`（bootstrap、leads、workflows、创作任务/批次/重试、assets、提示词模板/优化，以及签名 generation/户型预览 URL）；小程序 `scheme-studio` 已渲染合并轮次与已确认角标、对齐创作台的 Composer（模型/张数/比例/分辨率、参考图、模板、提示词优化、4 秒轮询与重试）、经 `POST /api/leads/[id]/ai-scheme-publications` 的合并发送弹窗，以及重命名/删方案、删轮次与单张撤回。移动端 Limited：无后台画布标注精修（仅「作参考图继续」）、无深色主题切换，九阶段 `proposal_pack`/`lighting` 仍走既有 admin_handoff。九阶段 `run-stage` API 保留兼容，不再作为后台工作台界面。所有请求触发的 PostgreSQL 对账按租户共享一次进程内运行，并复用一秒内的完成结果；数据库轮询租约仍是多实例最终保护，后台/小程序并发刷新不会反复扫描同一租户或重复轮询供应商任务。后台客户端出图状态轮询（创作台 4.5 秒、工作台 4 秒、户型状态页 3 秒）共用同一套可见性/空闲闸门，5 分钟无操作即停；切回标签页或空闲后下一次操作会立即补刷，不继续打隐藏标签页的接口 | 平台及租户 `ai-scenarios` 权限；Implemented/Limited | 供应商可用性和图片存储依赖外部服务。创作台在挂到 workflow 前仍不绑定线索。单房间控制图走 Mini SVG 裁切而非量房 canvas 裁切；裁切失败时仍发送整屋快照，但提示词/`roomData` 保持房间作用域。应用到哪里仅方案对话工作台可用；创作台在挂到 workflow 前不能传 `targetScope`/`roomId`。小程序 `scheme-studio` 选用模板仍只回填提示词/模型，不会克隆封面为参考图。 |
| 媒体存储 | `/media-storage`；`npm run db:backup`、`npm run db:restore-drill`、`npm run db:cleanup:dry-run` 和 `npm run db:cleanup:execute` | `media_assets`（含 `ownerType: lead_site_photo` 与 `lead_site_photos` 元数据；图库软删不标记媒体资产删除）、供应商配置、存储适配器；平台开关 `directQiniuDisplayUrls`（默认开：小程序效果图走对齐 deadline 的七牛私有阅读 URL；关：对齐过期的鉴权 API 代理；字节 `/image` 接口始终鉴权）；备份输出 PostgreSQL 自定义 dump 与耗时，恢复演练只使用 `smart_floor_planner_restore_drill` 并在移除该演练库前校验当前 app schema；清理 dry-run 会在只读事务中启用现有的平台读取范围，再校验目标指纹并输出七牛候选清单；执行命令要求精确指纹、已审核清单 SHA-256、显式本地正式库开关及操作者身份，才会以单事务清理数据库并输出审计 | 平台管理员；Implemented/Limited | 非空七牛清单仍须在人工审核后异步删除；执行命令不调用七牛。Linux 独立运行镜像须拷贝 `qiniu` 及 `os-name`/`osx-release`/`win-release`；Alpine 上 Next tracing 会漏掉这些 urllib 辅助包，共享服务端 chunk 随后无法加载 |
| 小程序支撑 API | 诊断页及共用 API handler；匿名领取生产路由已接入小程序 | 身份/上下文、双码/推荐成员 API；平台管理员审核 `GET/POST /api/miniprogram/platform/enterprises*`（仅 Mini JWT `admin`/`super_admin`，共享 `applyEnterpriseStatusChange`，列表支持 `q=` 与 `page`/`limit`，bootstrap `platform.review`，`counts.review` 为 `pending_approval` 数量）以及只读 `GET /api/miniprogram/platform/enterprise-registration-code` 与 `/image`（与后台 `POST /api/admin/enterprise-registration-codes/image` 共用 `revealActive` + 微信码，不换新/不停用）；本户现场图 `GET/POST /api/miniprogram/leads/[id]/site-photos` 与 `PATCH/DELETE .../site-photos/[photoId]`（客户、负责设计师/测量员或企业负责人；必须 `spaceTag`；每户 30 张；无后台图库页）； GET /api/miniprogram/workbench 支持 period（自定义另传 from/to；`schedule=1` 按该窗过滤负责人 `appointments`，上限 500 条 / 366 天），返回周期五卡 dashboard 与 signedCount/signingRate（负责人全店可含签约金额；设计师/测量员个人归因）。 负责人工作台同时返回 activityCode（「分享活动码」，detail「发给客户 · 扫码留资」→ 员工活动码出示页）和 joinCode（「邀请入驻」，detail「员工 · 推荐人」）。负责人 `appointments` 会把已归档/找不到的关联线索按 `closed` 水合（`includeArchived`），避免预约 Tab 把假「客户量房」点进可用时段。GET /api/miniprogram/staff-activity-code 现可为家装设计顾问、家装现场顾问和企业负责人签发活动码；负责人码按门店获客记 promoter 并进入抢单/赛马。负责人 Hero「待交付」统计方案设计中且尚无客户可见方案的未归档线索；发布方案或标记已签约后离开该数。经营大盘「已签约」仍按周期 convertedAt 计数。设计师工作台在本人缺少微信号或个人二维码时，于 `primaryItems` 置顶 `action: profile` 待办；小程序端每次进入设计师工作台时读取 `GET /miniprogram/staff/wechat-profile`，缺微信号或缺二维码都会弹「请先完善微信资料」引导去 `profile-edit`。`POST /api/miniprogram/staff/wechat-qr` 与后台 `POST /api/staff/wechat-qr` 共用 `prepareStaffWechatQrUpload`（小程序上传 MIME 为空/`octet-stream` 时仍按 PNG/JPEG 魔数识别并落库 PNG；不再解码或白名单校验二维码内容，因微信「二维码名片」截图常被 jsQR 误拒）；`GET/PATCH /api/miniprogram/staff/wechat-profile` 返回 `assignmentEligible`/`missing`。`GET /api/miniprogram/bootstrap` 以签名 JWT 实时校验 `contextVersion` 与活动上下文，返回当前角色、有效角色组、企业/成员关系、落点、能力白名单和按角色范围统计的服务端徽标摘要（`status`/`message`/`counts`；查询失败为 `unavailable` 与「暂时无法读取」，不阻断身份 bootstrap、不以 0 占位）。支持的员工角色含 `designer`、`measurer`、`salesperson`（渠道地推；落点 `/packages/business/promotion-records/promotion-records`；能力 `promotion.records` / `promotion.commissions` / `referrer.network` / `account`；平台渠道账号 `enterpriseId` 可为空；徽标可为空 counts）、`enterprise_admin` 与 `platform_admin`（落点 `/packages/platform/devices/devices`；能力 `platform.review` / `platform.devices` / `account`；`counts.review` 为 `pending_approval` 队列）。`GET /api/miniprogram/platform/enterprises`（默认 `pending_approval`；`q=` 按名称/信用代码/联系人电话搜索；精简 DTO 不含 AI/自动化密钥）、`GET .../enterprises/[id]`（另含状态事件）和 `POST .../enterprises/[id]/status`（`{ action, reason? }`，`actorAdminId` 用 Mini `staff._id`）要求 Mini JWT `admin`/`super_admin`，并走与 Web 相同的 `applyEnterpriseStatusChange`。`GET /api/miniprogram/platform/enterprise-registration-code` 返回当前生效码元数据（无 token），`GET .../image` 返回私有 no-store 码图。无效上下文返回 `identity_context_invalid`。`GET /api/miniprogram/staff-earnings` 返回已签名设计师或测量员在当前企业的本人线索提成笔数与状态（不含金额）（`beneficiaryUserId` + 岗位角色），拒绝客户、推荐人和企业负责人。 `GET /api/miniprogram/enterprise-commissions` 仅授权已签名企业负责人，返回本企业未归档线索提成台账及待支付/已支付/已作废汇总；`POST /api/miniprogram/enterprise-commissions/mark-paid` 对待支付行执行与后台相同的线下打款确认。`/api/miniprogram/codes/resolve` 会在手机号授权前返回入驻码类型和目标企业展示名称，或为平台 `er_` 开户码返回 `{ kind: 'enterprise_registration', displayName, valid }`，或为推广码签发 10 分钟待确认来源，或为员工活动码签发 `kind: staff_activity` 待确认来源（payload 可含企业名称，客户手机号授权页不渲染）。`POST /api/miniprogram/enterprise-registration` 在 Bearer JWT 手机号与 `contactPerson.phone` 一致且 `er_` 码有效时创建待审自助企业。已签名客户若已有未关闭且未归档线索的归属锁，resolve 返回 `existingAttribution` 与已有线索，不再签发待确认来源；授权也不再返回新领取设计师资料。归档或关闭线索会释放该锁，客户可再次领取。`GET /api/miniprogram/staff-activity-code` 与 `/image` 为设计师/测量员签发活动码。`GET /api/miniprogram/enterprise-referrers` 由 `referrer.network` 守卫：已签名家装设计顾问、家装现场顾问和渠道地推被服务端强制为 `scope=own`，只返回本人邀请的成员；`enterprise_admin` 得到 `scope=enterprise`，可读取按 `query`、`status`、`page`/`limit` 分页的扁平全量名册，或用 `view=network` 查看全部业务员工分支（包含 0 名推广人的员工）、企业汇总和「历史未归属」分支。`POST /api/miniprogram/enterprise-referrers/[id]/disable` 仍仅限企业负责人经同一 `ReferrerNetworkRepository` 调用（停用幂等、无重新启用、无改归属接口、不改历史线索和提成）。`GET /api/miniprogram/enterprise-join-codes` 与 `/[type]/image` 复用 `referrer.network` 边界。企业负责人可出示/管理企业员工码及本人的推荐人码；家装设计顾问、家装现场顾问和渠道地推只能看到本人的推荐人码。推荐人码生成/换新/停用由服务端强制限定当前员工范围，员工码写操作仍仅限负责人；图片私有 no-store、查看不换新，小程序 GET 只返回当前范围的活动 HMAC `token` 供分享进入入驻页，后台 `/join-codes` 列表仍不返回令牌。推荐人入驻后成员仍归企业所有，同时快照首个成功邀请员工；同企业后续扫码保持幂等且不改归属。历史成员只在企业恰好一名 `enterprise_admin` 时迁移给该负责人，否则保留未归属。`POST /api/miniprogram/onboarding/staff` 扫员工入驻码自动创建设计师/测量员账号，初始密码为 `123456`（与新开户企业负责人同一常量；可用授权手机号密码登录小程序或后台）。`/api/miniprogram/referrer-memberships/[id]/promotion-code/image` 返回由微信生成的受保护 PNG/JPEG。`DELETE /api/miniprogram/referrer-memberships/[id]` 在仍有其他生效推荐人企业时保留推广身份并重签 token，否则回退员工、再回退客户。`/api/miniprogram/referrals/authorize-and-create-lead`（`loginCode` 加 `phoneCode`，或旧客户端 `encryptedData`/`iv` 并用该 `loginCode` 的 `session_key` 解密）原子关联客户、锁归属、建线索和派单；手机号/微信身份冲突返回 409 `staff_phone_linked_to_other_user` / `wechat_identity_conflict` 与中文提示（`/api/auth/miniprogram` 同码）。活动线索锁定出示人为 `measurerId`；领取时若出示人暂停派单则先不写入测量员，重试派单会在出示人恢复后补派，不从池中另换测量员。）；第 5 阶段提供预约 API；第 6 阶段提供客户项目聚合、仅客户本人读取的已发布方案图片，以及设计师/企业负责人发布或撤回方案 API；`GET /api/miniprogram/published-scheme-folios/[leadId]` 允许任意已签名小程序身份读取海报分享落地所需的已发布方案（平台事务，不含主人 PII）；第 13 阶段新增仅客户本人的 `GET /api/miniprogram/customer-projects` 索引，以及按签名成员关系读取的 `GET /api/miniprogram/referrer-progress`、`GET /api/miniprogram/referrer-earnings`；第 14 阶段新增面向当前签名设计师、测量员或企业负责人的 `GET /api/miniprogram/workbench`（负责人异常卡含人员缺口，「查看详情」进入小程序人员名册；测量员待上门队列在正式户型提交后仍保留 `survey_ready`「待确认完成」，预约 `completed` 后才出列）。`GET /api/miniprogram/enterprise-staff` 与 `PATCH /api/miniprogram/enterprise-staff/[id]/assignment` 仅企业负责人可读设计师/测量员可派状态并暂停/恢复派单（恢复后复用待派重试）；不做建账、改微信/二维码或停用。正式户型创建/更新在当前租户内授权已签名企业负责人、`assignedTo` 或 `measurerId`。已移除未使用的 POST /api/location/reverse（旧版小程序城市定位） | 推广解析与开户/服务码图片边界如上；Implemented/Limited | 客户项目、推荐人聚合与工作台摘要保持只读/所有权边界；微信码生成、授权与通知依赖外部配置。后台微信手机号、小程序码与订阅消息通过 `getStableAccessToken`（`POST /cgi-bin/stable_token`）取 `access_token`；`getuserphonenumber` 遇到 `40001`/`40014`/`42001` 会清缓存并重试一次，归档线索释放归属后再扫码授权不会被旧版 `cgi-bin/token` 挤掉的过期凭据阻断 |
| 通知、自动化与诊断 | 通知设置（`/workflow-logs`）、短信设置（`/sms-settings`）、提醒运行时、诊断 | 八个小程序订阅模板（`workflow_todo` / `lead_assignment` / `new_lead` / `measurement_appointment` / `design_published` / `enterprise_join_result` / `signing_commission` / `lead_converted`）、`/workflow-logs` 平台开关 `subscriptionMessagesEnabled`（缺省关闭）、平台全局短信配置（`GET/PATCH /api/platform/sms-config`、`POST /api/platform/sms-config/test`，阿里云/腾讯云二选一）、`sms_delivery_logs` 及送达/重试接口（`GET /api/platform/sms-delivery-logs`、`POST /api/platform/sms-delivery-logs/[id]/retry`）。固定短信文案为「您好，您有一位客户已分配给您，请打开微信添加客户好友，并及时预约上门量房。」且只发给设计师；微信订阅下发由 `sendSubscriptionMessage` 门控，站内通知、工作台徽标和既有历史记录保持不变 | 短信配置与台账仅平台角色；其他通知沿用平台/企业角色；Implemented/Limited | 短信凭据加密且不回传，须先在供应商控制台审核签名/模板并登记有效设计师手机号；不自动切换供应商，短信失败不回滚业务事务；后续同一线索+设计师的派单通知会在上次失败或跳过时重发，已发送或发送中的记录仍去重 |
| 员工手机号凭据与首次改密 | `/login`、`/change-password`、`/staff` | `admin_users.must_change_password`；共用 username/手机号密码认证；`POST /api/staff` 强制 11 位手机号，兼容凭据缺省时生成 `staff_e{enterpriseId}_{phone}` 与初始密码 `123456`；`POST /api/staff/[id]/reset-password` 恢复初始密码。迁移 `0044_staff_initial_password.sql` 与 `0045_backfill_staff_initial_password_rls.sql` 跨租户重置全部已绑定 `user_id` 的 `wx_` 设计师/测量员并标记待改密，兼容强制行级安全的数据库。重复手机号先按密码筛选，仍有多个才返回明确冲突。Web 待改密会话仅可访问 `/api/auth/me`、`/api/auth/password`、退出与 `/change-password`；改密后清除标记并刷新 Cookie。小程序密码登录仅一次性提醒，不锁定小程序 API | 企业负责人仅可重置本企业普通员工；平台角色按已选租户操作；花名册入口禁止重置自己；Implemented | username 仅作兼容与冲突备用凭据。不主动踢出旧会话，`/api/auth/me` 刷新时获取最新标记。无短信找回，登录页引导联系企业负责人 |

旧后台 `/join-codes` 页面及其就绪检查、换新、停用、图片 API 明确使用企业级未分配推荐人范围（`inviterStaffId = null`），不会随机选取员工个人码；员工个人码统一由小程序接口管理。

抢单通知补充：八个已确认小程序模板仍为必填，`lead_claim_available` 是第九个可选语义槽。抢单窗口创建始终写站内通知；微信发送还须真实模板已配置、全局开关开启且设计师明确授权。该例外以[通知模板基线](./miniprogram-subscription-notification-template-baseline.zh-CN.md)为准。

平台 `/devices` 在既有 ProTable 搜索栏提供企业下拉筛选，可按企业（含既有的「未分配企业」）
过滤当前平台可见设备列表；接口、平台写入权限和企业只读边界均不变。

## 全局小程序码环境

`/mini-program-code-settings` 为后续新生成的企业入驻码（`ej_`）、平台企业开户码（`er_`）、推荐推广码和员工活动码提供统一平台设置。`GET/PATCH /api/platform/mini-program-code-config` 读写 `platform_configs.mini_program_code_config` 中的 `develop`、`trial` 或 `release`。仅 `super_admin` 与 `admin` 可使用该页或 API。设置立即作用于之后生成的码图片，不改写已下载或已分发的历史图片。它取代此前固定开发版环境的行为。

## 正式量房边界

权威合同见 [`surveying-module/formal-surveying.md`](./surveying-module/formal-surveying.md)。`FloorPlan.layoutData` 只含 `version: 4`、`measurementMode: 'surveying'` 与 `surveyGraph`。测量为不可变审计；尺寸与房间摘要为派生读模型。`/floorplans/[id]` 用小程序 `surveyCanvasRenderer` 的后台镜像（从 `miniprogram/packages/surveying/utils` 同步到 `admin/src/lib/survey-runtime/`）绘制，不写回 graph；镜像投影接受可选仅视图 `rotationRad`（默认 0），后台查看器不传入。已完成正式 v4 保存会把该画布 PNG 快照存到 `floor_plans.preview_asset_id`，不写入 `layoutData`。闭合房间内部的多段 L 形分隔会在镜像读模型中继续沿用源房间墙面，不再把复用外墙强制到外皮，因此后台房间摘要/预览不会把外墙和新增分隔墙实体计入净面积；中心线节点/墙以及向外新建相邻房效果不变。DXF 仍使用与小程序规划器相同局部凸/凹规则的 `admin/src/lib/surveyWallSolidPlan.js`。DXF 生成同样只读：`admin/src/lib/dxf.ts` 适配 MIT 许可写入器，开口切开后导出并集内外皮墙 `LINE`，插入打开 90° 厚门扇、灰色虚线弧和门垛的平开门块，推拉门为双轨、窗为离开墙皮的四线，闭合房间写四行 MTEXT，并用关联客户线索填充通高标题栏与黄色指北针（企业名与负责设计师）；草稿、无效 v4 或无闭合空间的户型在下载前被拒绝。

后台镜像量房内核保留普通闭合的 `350 mm` 直接吸附阈值；符合条件的长正交墙链可通过带审计的墙长平差闭合更大的累计残差。每面参与墙的修正量限制为实测长度的 `2%`，并约束在 `25–150 mm`，总残差硬上限为 `1,000 mm`；超出任一预算的墙链会被拒绝，不会继续平差或补微型桥接墙。投影预览若解析到当前活动墙链起点，保持起点闭合语义；重置光标后的拼接仍保持合并闭合。零旋转拖动辅助线使用精确视口边界。该路径用于保持小程序与后台运行时一致，当前后台路由不调用编辑写路径；路由、API、权限与持久化 graph 数据不变。

`POST /api/floorplans` 与 `PUT /api/floorplans/[id]` 保留正式 v4 外壳的 400 闸门。草稿执行 `quick` 校验；完成态执行增强后的 `full` 校验并要求至少一个闭合 Space，校验先于数据库写入和预览生成。无效正式图返回 422，携带首个错误码/消息及 `validation.mode/errors/stats`；API 不修复或改写客户端 graph。增强后的完整闸门拒绝真交叉、未打断 T 接、不同节点 ID 占用同一几何端点与共线正长度重叠；同时要求按墙体模式保存有效 `lengthMm` / `angleDeg`，三个测量内缩/延伸字段是非负整数且不得将有效实测长度压到零，`rawMeasuredLengthMm` / `closureAdjustmentMm` 以完整整数对出现且之和等于保存长度。没有原始读数的零读数 `closure-merge` / `closure-bridge` 拓扑连接段仍合法，共享节点、已打断 T/十字与多房共享墙也保持合法。

`POST /api/measurements` 接受正式顶层 `auditId`，并兼容 `metadata.auditId`；正式量房审计要求非空且不超过 200 字符。nullable `measurements.audit_id` 使用 `(floor_plan_id, audit_id)` 部分唯一索引：首次创建返回 201 / `deduplicated: false`，重复提交返回同一记录和 200 / `deduplicated: true`。既有 null 行不回填、不合并。发布时先执行 nullable 迁移并上线 Admin API，再发布小程序；应用回滚保留列和索引，历史清理另立经过 dry-run 与审批的批次。小程序现会在上传前持久化每条已接受的审计，获得正式户型 ID 时再按户型绑定并重试，队列不会被静默截断；该调整不改 Admin 测量 API、数据模型、权限或租户边界。

## 提成边界

量房师-设计师获客合同已下线，仅作为 [`measurer-designer-acquisition.md`](./measurer-designer-acquisition.md) 留档。推荐网络签单提成以本清单与开发计划为准。转化快照对 `referrer_network` 生成三条应付行，对 `staff_activity` 与 `manual_entry` 生成两条（设计师 + 测量员；同一受益人可同时持有两行）。后台 `POST /api/lead-commissions/mark-paid` 与小程序镜像统一接收一至多条 `{ commissionId, paidAmount }`；同一租户事务锁定全部待支付行，最终金额发生变化时写入既有调整审计，并记录付款人、付款时间后标记已支付。最终金额必须大于 0 且最多两位小数；已付与已作废不可直接调整。撤销签约把完整作废行快照写入生命周期事件元数据；重新签约会按新合同、当前规则和当前受益人刷新并恢复待支付。后台与小程序的待支付行操作均不区分角色，包含推荐人行；当前界面只保留「确认打款」，弹窗预填当前金额并允许修正，一次提交完成金额审计与付款。后台批量确认会逐条显示可编辑金额。旧 `PATCH /api/lead-commissions/[id]`、小程序金额 PATCH 与零金额快捷接口仅作为兼容 API 保留，不再提供当前界面入口。独立后台工作台已按其批准的第 7 阶段设计源实现，并可按线索来源筛选。

## 维护

当路由、API、模型、权限、状态或限制变化时，更新受影响行及其英文镜像。每个模块只保留一条当前描述；不要在本文件记录修改序列或粘贴测试全文。

English mirror: [admin-system-modules.md](./admin-system-modules.md)
### 推广人撤销线索生命周期（已实现）

线索生命周期新增 `referrer_withdrawn`、`referrer_withdrawal_reverted` 审计动作、`referrerRecordCode` 以及小程序用户/推广企业成员关系的独立操作人字段。专用小程序接口在事务内关闭或撤回满足“实质服务前”条件的推广线索，释放客户归属锁和抢单窗口并生成去重的员工提醒；撤销线索保留为只读历史，不计入推广人有效统计。后台 `/leads` 列表和详情对这些已关闭行隐藏开始方案、预约、补充资料、跟进、方案发布和重试派单，企业负责人仍可「重新激活」。派单审计 `eventType`/`errorCode` 与抢单窗口 `resolutionReason` 以中文标签展示。
