# 后台系统当前功能清单

本文只记录后台当前运行入口、合同、权限和限制。实现过程由 Git 提交保留，
不在清单中追加日期流水。

## 共用架构

- Next.js 16 App Router、React 19、Tailwind CSS 4、Ant Design 5 和 Ant Design Pro。
- 部署运行时使用 PostgreSQL 17、`drizzle-orm`/`pg`；租户读写通过 Repository、
  事务和 RLS 执行。
- 后台会话使用 cookie/JWT；平台和企业角色由路由守卫与菜单权限共同约束。
- 外部供应商和对象存储 I/O 在短数据库事务之外执行；API handler 通过 DTO 显式
  序列化 `bigint`。
- 正式户型为 v4 量房 wall graph。查看器、DXF、3D 和 AI 只消费读模型，不写旧布局字段。

## 状态定义

`Implemented` 表示存在真实页面/API/数据链路；`Limited` 表示受角色、供应商、
数据形状或运行条件限制；`Placeholder` 表示只有 UI 或 mock，尚无承诺的持久化/集成。

## 模块清单

预约服务地址补录属于预约服务事实：`measurement_appointments` 在手输服务详细地址外，可选保存 `location_name`、范围校验后的 GCJ-02 纬经度和坐标系。`POST /api/appointments` 与 `POST /api/appointments/[id]/address` 接收可选地图位置；地址更新继续受预约版本保护，并把修改前后的位置快照写入 `measurement_appointment_events` 的 `address_updated` 审计。小程序请求先按员工 `staff._id` 与已派设计师/测量员比对，Admin Cookie JWT 作为回退。后台线索详情与小程序预约详情共用该入口。后台创建或更新服务地址后，负责设计师、已派测量员或企业负责人可确认把该地址同步到线索空的 `communityName`（`PUT /api/leads/[id]`；已有小区不覆盖）。`POST /api/appointments/[id]/complete` 还要求线索关联已完成的正式 v4 量房户型且至少存在一个闭合空间，否则返回 `appointment_survey_required`（409）。

| 模块 | 当前入口 | API/数据边界 | 权限/状态 | 当前限制 |
| --- | --- | --- | --- | --- |
| 线索转化卡片呈现 | `/leads` 列表的 `tableViewRender` | 仅调整既有线索 DTO 的可视化：客户头部展示阶段、联系方式、中文来源、面积/风格与方案主操作；派生工作流 `Steps` 配合下一步行动；上门预约与符合条件的卡内改期；推广人/设计师/测量员/派单状态四列协作信息。列表增加浅色 16px 内层与 16px 卡片节奏；卡片头部/底部明确保留 20px 内边距，身份/操作组间保留可见 12px 间距；分页器独立为有边界的收尾栏。底部仍调用原有归档、恢复、永久删除和重试派单处理器 | 原有租户、角色和操作边界；Implemented | 2026-08-21 已在 `http://localhost:3006/leads` 的认证 Chrome 中确认两张有数据卡片、卡片/分页 16px 间距、操作 12px 间距及归档 20px 右侧留白；不改变 API、模型或权限行为 |
| 登录与会话 | `/login`、`/register` | `/api/auth/*`；小程序 JWT 使用基础用户 `sub`、当前 `customer/staff/referrer` 上下文和 `contextVersion`。平台 `admin`/`super_admin` 员工记录不属于小程序工作台角色；小程序登录/刷新会选择同一基础账号的有效推荐人上下文，无推荐成员关系时使用客户上下文 | 公开入口与登录后路由；平台权限仅限后台；Implemented | 微信供应商配置依赖环境；旧身份字段在旧获客流程下线前并存 |
| 导航、角色与权限 | 共用侧栏、`/roles`、路由守卫（`proxy.ts`） | `/api/roles`、`/api/auth/me`、角色/菜单 Repository。`/roles` 勾选名称来自 `ALL_MENUS`，须与侧栏一致（如 `dashboard`=业务概览、`leads`=线索转化）。企业首页 `/` 在 `dashboard` 下读取工作台摘要/待办，并对企业负责人/设计师/测量员请求 `GET /api/workbench/ops-dashboard` 周期经营大盘；线索/户型/员工汇总仅在具备对应菜单权限时请求。`GET /api/auth/me` 在角色菜单变更后刷新 auth cookie，API 守卫无需重新登录即可对齐 | `super_admin`、`admin`、企业角色；Implemented | 权限按租户和角色生效；入驻码/推荐人等子路由复用父级菜单键，`/enterprise-registration-codes` 复用 `enterprises` |
| 平台与企业 | `/dashboard`、`/enterprises`、`/enterprises/[id]`、`/enterprise-registration-codes` | 企业、品牌、激活及平台 Repository。企业生命周期状态为 `pending_approval` / `active` / `disabled` / `rejected`，经 `POST /api/admin/enterprises/[id]/status` 执行 FSM（`approve` / `reject` / `disable` / `enable` / `resubmit_review`；拒绝/停用必填 4–200 字原因）。最新留痕字段在 `enterprises`（`statusReason` / `statusChangedAt` / `statusChangedByAdminId`），追加事件表 `enterprise_status_events`。`PATCH /api/admin/enterprises/[id]` 不再接受 `status`。非 `active` 企业会阻断商户 Admin 登录/会话（`/api/auth/login`、`/api/auth/me`、`withTenantRoute`）以及小程序员工/推荐人上下文。平台级小程序开户码使用 `platform_enterprise_registration_codes` / `platform_enterprise_registration_code_events`（HMAC `er_` 令牌、库内仅存 SHA-256、全局仅 1 个 active）与 `EnterpriseRegistrationCodeRepository`（`rotate` / `disable` / `revealActive` / `resolve` / `inspect` / `listEvents`）。Admin API：`GET /api/admin/enterprise-registration-codes`、`POST .../rotate`（可选 `expiresAt`）、`POST .../disable`、`POST .../image`（只还原当前生效 `er_` 令牌并返回私有 no-store 微信码，落地 `packages/business/enterprise-register/enterprise-register`，不换新）。后台页在卡片内直接展示当前有效二维码供查看/下载；换新/创建仍是独立动作。商户侧 `ej_` 入驻码仍在 `/join-codes`，勿与开户码混用 | 状态变更与开户码仅平台角色；`/enterprise-registration-codes` 及其 API 复用 `enterprises` 权限；Implemented | Web `/register` 仍可不带 `er_`；新开通的企业负责人初始密码为 `123456`（用户名为联系人手机号）；审核短信与小程序内进度查询不在范围；申请人侧重提未实现（由平台在同一条记录上改资料再审） |
| 员工、账号与预约运营 | `/staff`、`/departments`、`/users`、`/referrer-network-operations`、`/join-codes`、`/referrers`、`/appointment-settings`、`/`（企业负责人业务概览 + 设计师/测量员首页工作台） | 员工、部门、共享通知、管理员与预约 Repository。商户侧栏「推荐网络」分组现包含 `/referrer-network-operations`、`/join-codes`、`/referrers` 与 `/appointment-settings`，全部复用 `referrer-network-operations` 权限。`/referrer-network-operations` 为就绪枢纽：数字摘要、可执行验收清单和最近 5 条双码审计预览；双码换新/停用、卡片内直接查看/下载当前有效二维码（不换新、不限时隐藏）与完整审计在 `/join-codes`。`/referrers` 列出推荐人姓名、手机号、加入时间、活动推广码和停用后续扫码，支持姓名/手机号搜索；后台不手工新建推荐人，姓名在小程序推荐人入驻时采集（`POST /api/miniprogram/onboarding/referrer` 必填 `displayName`，不再回退为「推荐人」）。预约设置页通过 `GET/PUT /api/appointment-settings` 读取和修改时区、每周开放时间、默认时长、时段步长、最远预约天数及客户改期截止；系统自动建立的默认记录会与管理员已确认策略明确区分。运营工作台经 `GET /api/enterprise/referrer-network-readiness` 读取双码审计、活动推荐人成员关系与已持久化的活动推广码数量、活动员工码数量、派单资格、已确认预约设置、提成规则及微信服务码配置；推广服务码就绪项不再仅根据成员关系推断码可用，并为每项验收项提供真实操作入口。`GET /api/enterprise/join-codes` 返回不含令牌的双码与审计事件；`GET /api/enterprise/referrer-memberships` 按租户列出成员并可按关键词/状态筛选；停用仍为 `POST /api/enterprise/referrer-memberships/[id]/disable`。后台首页按已认证 Cookie 角色渲染：企业负责人 `MerchantDashboard` 展示库存三卡、只读经营大盘与最近线索流转（已移除旧企业报备「协作待办」条）；设计师/测量员 `StaffWorkbench` 在待办队列上方插入「我的」经营大盘。`GET /api/workbench/ops-dashboard`（`period=week|month|year|custom`，自定义另传上海日历 `from`/`to`）与小程序共用 `loadOpsDashboard`：五卡 KPI（新增线索、已完成量房、方案交付率、已签约、签单率=同窗已签约÷新增线索；签约事实 `status=converted` + `convertedAt`）；负责人全店可含签约金额 detail，员工个人归因不含金额。`GET /api/workbench/staff` 在租户事务中只返回本人线索、预约、无预约待量房任务和量房交接；测量员正式 BLE 量房仍只从小程序正式编辑器完成。写入审计的图片接口生成私有、禁止缓存的入驻图片并保留微信返回的 PNG/JPEG 类型。企业入驻码和推荐推广码均使用 `getwxacodeunlimit` 与 `env_version: develop`，即使服务端以生产模式运行也保持开发版 | `referrer-network-operations`、`/join-codes`、`/referrers`、`/appointment-settings` 及对应 API 复用 `referrer-network-operations` 权限，员工首页与 `GET /api/workbench/staff` 限当前租户内活动的 `designer`/`measurer` 会话；企业负责人继续使用企业经营工作台；Implemented/Limited | 员工工作台只提供任务聚合和既有后台入口，不创建测试业务数据、不绕过小程序手机号授权，也不提供后台 BLE/H5 编辑器。外部微信凭据只诊断、不在本页配置。认证后台视觉核验待本次角色登录态补充 |
| 报备与协作 | `/promotions`、企业协作页 | 报备、推荐和共享通知 Repository | 企业和员工边界；Implemented | 企业微信投递为可选外部能力 |
| 套餐、订单与提成 | `/packages`、`/orders`、`/commissions`、`/lead-commissions` | 套餐、订单、既有提成及 `LeadCommissionRepository`（签单快照同时写入不可变的 `original_payable_amount` / `original_beneficiary_user_id`，以及可空的最近一次调整审计列）；`GET/PUT /api/commission-rules`、可筛选的 `GET /api/lead-commissions`（含原始/调整审计字段与 `source=referrer_network|staff_activity|manual_entry`）、`GET /api/lead-commissions/beneficiaries?role=`、`PATCH /api/lead-commissions/[id]`（仅 `payable` 可改应付金额和/或受益人，原因选填；推荐人须为本企业活动推荐人成员，设计师/测量员须为本企业对应角色且已绑定 `userId` 的员工）、`POST /api/lead-commissions/mark-paid`。小程序负责人经 `POST /api/miniprogram/enterprise-commissions/mark-paid` 走同一仓储确认线下打款。`/lead-commissions` 工作台使用 Admin Pro `PageContainer`/`ProTable` 与 Ant Design 规则卡（`Segmented`、带不换行「元/单」后缀的 `InputNumber`、`Switch`）维护三条规则，可按线索来源筛选，报表按线索主行展开：主行展示客户、推荐人关系（活动线索为空）、企业、设计师、测量员和当前有效预约及提成合计/状态摘要，子表分行展示三角色规则/金额/状态，并在待支付行提供「调整」弹窗（受益人与原因选填）。台账推荐人与 `/leads` 相同，走 `referrerMembershipId -> referrerEnterpriseMemberships -> referrerProfiles` 的展示名/电话，不依赖无租户 `users` 行；小程序基础用户常空 `enterprise_id`，租户 RLS 读不到 `users` | `lead-commissions` 仅 `super_admin`、`admin`、`enterprise_admin` 可用；系统管理员即使使用旧的已存菜单快照也始终展示该受保护入口；规则修改、待支付调整和线下批量标记已支付均使用 RLS 与租户事务；Implemented/Limited | 旧获客提成路由已下线。支付不在本系统内完成。已付/已作废不可调整；不支持手工新建台账、已付冲正，单条作废仍走撤销签单 |
| 线索与转化 | `/leads`、`/leads/[id]` | 线索生命周期、户型、`ReferralLeadRepository`、`CustomerProjectRepository` 与 `LeadCommissionRepository`；列表/详情 DTO 包含已分配测量员；GET /api/leads/[id] 的 publishedSchemes 图片含 stageKey/publishedAt，供小程序只读方案册时间轴使用、派生 `serviceStage`/`nextAction`/`nextActionKind`/`canRebook`（测量员匹配后客户下一步为 `book`，设计师仍可代约）、当前有效预约（重约后未结束的 `confirmed` 档优先于过期或已过结束时间的旧行），以及由 `referrerMembershipId -> referrerEnterpriseMemberships -> referrerProfiles` 解析出的推广人（使用资料展示名/电话，不依赖无租户基础用户行）。后台线索 DTO 还会从既有员工资料返回绑定设计师和测量员手机号，供卡片只读展示。企业负责人/平台管理员 `POST /api/leads` 创建 `manual_entry` 线索，走与推荐网络相同的设计师/测量员池子自动派单（不绑定 `customer_user_id`）。未归档线索详情抽屉的「客户资料」区提供「补充资料」，企业负责人、负责设计师与已派测量员可经 `PUT /api/leads/[id]` 更新称呼、手机、小区、面积和风格，不会变更设计师绑定或提成受益人。`/api/appointments` 与 `/api/appointments/availability` 支持授权的后台 Cookie 设计师/企业负责人创建、读取可用时段和后台改期线索预约；`POST /api/appointments/[id]/cancel` 与 `/complete` 现已接受与小程序一致的后台 Cookie 角色边界，线索详情抽屉提供取消、完成、重新预约及改期/补地址操作，过期行完成规则与 Slice 2 一致。`GET/POST /api/leads/[id]/ai-publications` 与 `DELETE /api/leads/[id]/ai-publications/[generationId]` 仍接受后台 Cookie，供小程序单图发布/撤回兼容。负责设计师和企业负责人在 `/ai-studio/scenarios` 通过 `POST /api/leads/[id]/ai-scheme-publications` 以“合并发布”方式发送命名多图方案：同一 `workflowId` 内的本次勾选会增量更新/插入活跃发布记录，未勾选但已确认的图片不会因重复发送而隐式撤回；整套撤回仍走 `DELETE /api/leads/[id]/ai-scheme-publications/[workflowId]`，单张撤回走 `DELETE /api/leads/[id]/ai-scheme-publications/[workflowId]/generations/[generationId]`；线索详情抽屉展示这些方案套并深链到 AI 工作台。后台预约审计通过 `admin_users.user_id` 映射到 `users.id`；`/leads/[id]` 通过查询参数打开共享线索详情抽屉 | 客户授权或租户/责任人校验；负责设计师仅管理自己线索的已成功 generation，并只能为本人线索创建、改期或取消预约；已派测量员可完成本人预约；企业负责人可管理本企业；撤销签单仅企业管理员且已支付提成会阻止撤销；Implemented/Limited | 客户项目已展示受保护聚合中的真实服务事实、完成正式户型摘要及主动发布方案；后台 ProTable 保留筛选/分页，并将每条线索渲染为响应式 ProCard，分组展示客户、派单、推广人、预约和操作；卡片会在已解析的推广人、绑定设计师和测量员姓名后显示已有手机号；卡片与小程序共用服务端日期/时段选择合同，改期写入审计并通知相关人员；后台员工主键与用户审计主键已分离处理。后台 /leads 列表只在进入页面、筛选/分页和写操作后请求，不后台轮询 |
| 正式户型 | `/floorplans`、`/floorplans/[id]`、`/floorplans/kujiale`；`GET /api/floorplans/[id]/export/dxf` | `FloorPlanRepository` 与只读量房适配器。`/floorplans/[id]` 用小程序 `surveyCanvasRenderer` 的后台镜像（`admin/src/lib/survey-runtime/`）绘制当前楼层，去掉编辑 session，只支持平移/缩放，不写回 graph。已完成的正式 v4 户型在创建/更新后把同一套 canvas 导出为 PNG 快照，存入 `media_assets`（`ownerType: floor_plan_preview`），由 `floor_plans.preview_asset_id` / `preview_render_revision` 指向，不写入 `layoutData`。AI 工作台整屋控制图和 `GET /api/ai/workflows/[id]/floor-plan-preview` 复用该快照；同一预览路由加 `?roomId=` 输出单房间 Mini SVG 裁切图，与批次作为首张参考图上传的控制图一致。`GET /api/floorplans/[id]/preview` 输出整屋快照。正式查看器通过 `@tarikjabiri/dxf@2.8.9`（MIT）下载分图层、毫米单位、AutoCAD 2007+ DXF，图层为国内 CAD 习惯（`墙`、`门`、`窗`、`尺寸标注`、`空间名称`、`指北针`），并带毫米 DIMSTYLE（`标注线` / `标注线-内墙`）、`_ARCHTICK`、黑体和门扇弧虚线 `ACAD_ISO03W100`；墙体按开口切开后交给 `surveyWallSolidPlan` 并集，导出内外皮 `LINE`（含门垛），不再按墙段输出厚度矩形；平开门以 `DOOR` 块 `INSERT` 到洞口面，门扇为绿色打开 90° 的厚矩形加灰色虚线弧和 50mm 门垛；推拉门仍为洞口内双轨，窗为离开墙皮的四条洞口内玻璃线；尺寸复用同一套闭合尺寸计划，写成旋转 `AcDbRotatedDimension`（内圈分段含墙厚刻度用 `标注线-内墙`，外圈总长用 `标注线`，文字为整毫米且洋红 DIMCLRT 193，加大标注间距，按 `dimensionStart`/`dimensionEnd` 落标注线，`DIMTAD` 2 / `DIMGAP` 10，角度 0 或 90，L 形凹口就近标注）；闭合房间四行 MTEXT 在 `空间名称` 图层（ACI 7 白字）；固定横版图框 30640×21660，户型按左侧绘图区约 90% 等比放大或缩小居中，标题栏比例约 1:85，整图套青色双层圆角外框、Y 轴取反对齐画布、项目名称与 DXF 文件名取客户姓名+小区+面积+导出时间、右侧标题栏（中文/英文/内容竖排、无格内竖线）和黄色填充指北针，并带图纸名、计算比例与完成日期。共享墙、斜墙、门窗、尺寸、闭合空间标注及多楼层横向分区均从权威 v4 graph 映射，不复制布局。端点要求 `completed`、正式 v4 数据和至少一个闭合空间。后台与小程序 DXF 端点把户型名、关联线索的企业名（否则用户型租户）、线索负责设计师和户型完成日期写入图框 | 原有租户、负责设计师或已派测量员权限；Implemented | 2D 画布只读，不是小程序编辑器或 BLE 入口。不存储 CAD 文件，不导出客户电话地址；图框含双层圆角外框、通高右侧 MTEXT 标题栏、填充指北针、图纸名「原始户型平面图」、计算比例、完成日期，公司取关联线索企业并置于栏首，设计师取线索负责设计师；酷家乐供应商能力为 Limited |
| 测量与 BLE 设备 | `/measurements`、`/devices` | 测量、设备、绑定、审计 Repository | 平台/企业分配边界；Implemented | 仅支持协议文档定义的测距仪 |
| AI 工作室与生成 | `/ai-studio/scenarios`（AI 工作台）、`/ai-studio/create`（创作台）、供应商/预设/点数页面 | PostgreSQL AI Repository 与供应商适配器。商户工作台 `/ai-studio/scenarios` 与创作台一样新开全屏窗口。左侧选线索和命名对话，右侧用创作台出图条（应用到哪里：默认完整户型或闭合单房间、模型、张数、比例、分辨率、提示词模板（点封面放大预览，点名称/「使用」才应用）、参考图和提示词优化）。开对话要求合格已完成正式 v4 户型。出图条提供「应用到哪里」作用域（默认完整户型 / `whole_floor_plan`，或绑定户型中的闭合单房间），语义对齐小程序 `recipe-project` 的 `targetScope`/`roomId`；说明文案与小程序一致：「只应用到当前选择，不自动为其他房间生成、不额外扣点」。每一轮经 `POST /api/ai/creation/tasks/[id]/batches` 可选提交 `targetScope`/`roomId`（省略或 `whole_floor_plan` 即沿用整屋控制图）。`preparePostgresCreationBatch` 复用 `resolveMiniAiFloorPlanTarget`，写入 generation `input.roomData`，把量房房间摘要拼进供应商提示词，并在批次 `parameterSnapshot` 持久化 `targetScope`/`targetLabel`/`roomId`/`controlKind`，便于轮次回看「整屋方案」或某房间。控制图：整屋仍用已存储的量房 canvas 快照（与 `/floorplans/[id]` 同一套 `surveyCanvasRenderer`）；单房间优先按 `roomId` 裁切 Mini SVG，裁切失败则回退整屋快照（`roomData.controlKind` 为 `room_crop` 或 `survey_snapshot_fallback`）。出图条参考图槽会展示同一张锁定控制图缩略图（选闭合房间时为该房间裁切图），生成时仍把它作为首张参考图上传；额外用户参考图可选。`GET /api/ai/workflows/[id]` 在 `sourceFloorPlan.rooms` 返回闭合房间列表供选择。对话标题旁展示整屋快照供设计师对照（可放大、与效果图左右对照，并链到 `/floorplans/[id]`）；`GET /api/ai/workflows/[id]/floor-plan-preview` 输出整屋快照，加 `?roomId=` 则输出对应房间裁切图，不发给客户。夜间主题沿用创作台深色，日间用后台绿/白色系，选择记在 `localStorage`。`POST /api/leads/[id]/ai-scheme-publications` 以“合并发布”方式把该对话勾选图集同步到客户可见方案：同一 `workflowId` 内的选中轮次会增量更新/插入活跃发布记录，未勾选但已确认的图片保持可见，除非被单张撤回或删轮次/删方案显式下架。设计师可通过 `DELETE /api/ai/workflows/[id]/generations/[generationId]` 删除轮次、通过 `DELETE /api/ai/workflows/[id]` 存档方案（会同步撤回客户可见发布）。小程序设计师单图发布仍走 `/api/leads/[id]/ai-publications`。设计师/企业负责人移动端方案读取与出图走 `/api/miniprogram/ai/studio/*`（bootstrap、leads、workflows、创作任务/批次/重试、assets、提示词模板/优化，以及签名 generation/户型预览 URL）；小程序 `scheme-studio` 已渲染合并轮次与已确认角标、对齐创作台的 Composer（模型/张数/比例/分辨率、参考图、模板、提示词优化、4 秒轮询与重试）、经 `POST /api/leads/[id]/ai-scheme-publications` 的合并发送弹窗，以及重命名/删方案、删轮次与单张撤回。移动端 Limited：无后台画布标注精修（仅「作参考图继续」）、无深色主题切换，九阶段 `proposal_pack`/`lighting` 仍走既有 admin_handoff。九阶段 `run-stage` API 保留兼容，不再作为后台工作台界面。所有请求触发的 PostgreSQL 对账按租户共享一次进程内运行，并复用一秒内的完成结果；数据库轮询租约仍是多实例最终保护，后台/小程序并发刷新不会反复扫描同一租户或重复轮询供应商任务 | 平台及租户 `ai-scenarios` 权限；Implemented/Limited | 供应商可用性和图片存储依赖外部服务。创作台在挂到 workflow 前仍不绑定线索。单房间控制图走 Mini SVG 裁切而非量房 canvas 裁切；裁切失败时仍发送整屋快照，但提示词/`roomData` 保持房间作用域。应用到哪里仅方案对话工作台可用；创作台在挂到 workflow 前不能传 `targetScope`/`roomId`。 |
| 媒体存储 | `/media-storage`；`npm run db:backup`、`npm run db:restore-drill`、`npm run db:cleanup:dry-run` 和 `npm run db:cleanup:execute` | `media_assets`、供应商配置、存储适配器；备份输出 PostgreSQL 自定义 dump 与耗时，恢复演练只使用 `smart_floor_planner_restore_drill` 并在移除该演练库前校验当前 app schema；清理 dry-run 会在只读事务中启用现有的平台读取范围，再校验目标指纹并输出七牛候选清单；执行命令要求精确指纹、已审核清单 SHA-256、显式本地正式库开关及操作者身份，才会以单事务清理数据库并输出审计 | 平台管理员；Implemented/Limited | 非空七牛清单仍须在人工审核后异步删除；执行命令不调用七牛 |
| 小程序支撑 API | 诊断页及共用 API handler；匿名领取生产路由已接入小程序 | 身份/上下文、双码/推荐成员 API； GET /api/miniprogram/workbench 支持 period（自定义另传 from/to），返回周期五卡 dashboard 与 signedCount/signingRate（负责人全店可含签约金额；设计师/测量员个人归因）。`GET /api/miniprogram/bootstrap` 以签名 JWT 实时校验 `contextVersion` 与活动上下文，返回当前角色、有效角色组、企业/成员关系、落点、能力白名单和按角色范围统计的服务端徽标摘要（`status`/`message`/`counts`；查询失败为 `unavailable` 与「暂时无法读取」，不阻断身份 bootstrap、不以 0 占位），无效上下文返回 `identity_context_invalid`。`GET /api/miniprogram/staff-earnings` 返回已签名设计师或测量员在当前企业的本人线索提成（`beneficiaryUserId` + 岗位角色），拒绝客户、推荐人和企业负责人。 `GET /api/miniprogram/enterprise-commissions` 仅授权已签名企业负责人，返回本企业未归档线索提成台账及待支付/已支付/已作废汇总；`POST /api/miniprogram/enterprise-commissions/mark-paid` 对待支付行执行与后台相同的线下打款确认。`/api/miniprogram/codes/resolve` 会在手机号授权前返回入驻码类型和目标企业展示名称，或为平台 `er_` 开户码返回 `{ kind: 'enterprise_registration', displayName, valid }`，或为推广码签发 10 分钟待确认来源，或为员工活动码签发 `kind: staff_activity` 待确认来源（活动落地可含企业名称）。`POST /api/miniprogram/enterprise-registration` 在 Bearer JWT 手机号与 `contactPerson.phone` 一致且 `er_` 码有效时创建待审自助企业。已签名客户若已有未关闭且未归档线索的归属锁，resolve 返回 `existingAttribution` 与已有线索，不再签发待确认来源；授权也不再返回新领取设计师资料。归档或关闭线索会释放该锁，客户可再次领取。`GET /api/miniprogram/staff-activity-code` 与 `/image` 为设计师/测量员签发活动码。`GET /api/miniprogram/enterprise-join-codes` 与 `/[type]/image`（`staff`|`referrer`）供企业负责人出示并管理与后台一致的企业双码（生成/换新/停用走 POST rotate/disable；私有 no-store 图片；查看不换新）。`/api/miniprogram/referrer-memberships/[id]/promotion-code/image` 返回由微信生成的受保护 PNG/JPEG，`/api/miniprogram/referrals/authorize-and-create-lead` 原子关联客户、锁归属、建线索和派单；手机号/微信身份冲突返回 409 `staff_phone_linked_to_other_user` / `wechat_identity_conflict` 与中文提示（`/api/auth/miniprogram` 同码）。活动线索锁定出示人为 `measurerId`；领取时若出示人暂停派单则先不写入测量员，重试派单会在出示人恢复后补派，不从池中另换测量员。）；第 5 阶段提供预约 API；第 6 阶段提供客户项目聚合、仅客户本人读取的已发布方案图片，以及设计师/企业负责人发布或撤回方案 API；第 13 阶段新增仅客户本人的 `GET /api/miniprogram/customer-projects` 索引，以及按签名成员关系读取的 `GET /api/miniprogram/referrer-progress`、`GET /api/miniprogram/referrer-earnings`；第 14 阶段新增面向当前签名设计师、测量员或企业负责人的 `GET /api/miniprogram/workbench`（负责人异常卡含人员缺口，「查看详情」为原生说明弹窗，不提供小程序人员管理页）。已移除未使用的 POST /api/location/reverse（旧版小程序城市定位） | 推广解析与开户/服务码图片边界如上；Implemented/Limited | 客户项目、推荐人聚合与工作台摘要保持只读/所有权边界；微信码生成、授权与通知依赖外部配置 |
| 通知、自动化与诊断 | 通知设置、提醒运行时、诊断 | 通知模板、调度器、运营记录 | 平台/企业角色；Implemented/Limited | 订阅投递可被微信拒绝 |

## 全局小程序码环境

`/mini-program-code-settings` 为后续新生成的企业入驻码（`ej_`）、平台企业开户码（`er_`）、推荐推广码和员工活动码提供统一平台设置。`GET/PATCH /api/platform/mini-program-code-config` 读写 `platform_configs.mini_program_code_config` 中的 `develop`、`trial` 或 `release`。仅 `super_admin` 与 `admin` 可使用该页或 API。设置立即作用于之后生成的码图片，不改写已下载或已分发的历史图片。它取代此前固定开发版环境的行为。

## 正式量房边界

权威合同见 [`surveying-module/formal-surveying.md`](./surveying-module/formal-surveying.md)。`FloorPlan.layoutData` 只含 `version: 4`、`measurementMode: 'surveying'` 与 `surveyGraph`。测量为不可变审计；尺寸与房间摘要为派生读模型。`/floorplans/[id]` 用小程序 `surveyCanvasRenderer` 的后台镜像（`admin/src/lib/survey-runtime/`）绘制，不写回 graph。已完成正式 v4 保存会把该画布 PNG 快照存到 `floor_plans.preview_asset_id`，不写入 `layoutData`。DXF 仍使用与小程序规划器相同局部凸/凹规则的 `admin/src/lib/surveyWallSolidPlan.js`。DXF 生成同样只读：`admin/src/lib/dxf.ts` 适配 MIT 许可写入器，开口切开后导出并集内外皮墙 `LINE`，插入打开 90° 厚门扇、灰色虚线弧和门垛的平开门块，推拉门为双轨、窗为离开墙皮的四线，闭合房间写四行 MTEXT，并用关联客户线索填充通高标题栏与黄色指北针（企业名与负责设计师）；草稿、无效 v4 或无闭合空间的户型在下载前被拒绝。

## 提成边界

量房师-设计师获客合同已下线，仅作为 [`measurer-designer-acquisition.md`](./measurer-designer-acquisition.md) 留档。推荐网络签单提成以本清单与开发计划为准。转化快照对 `referrer_network` 生成三条应付行，对 `staff_activity` 与 `manual_entry` 生成两条（设计师 + 测量员；同一受益人可同时持有两行）。应付行后续可按金额和/或受益人调整并保留审计原因；已付与已作废不可调整。独立后台工作台已按其批准的第 7 阶段设计源实现，并可按线索来源筛选。

## 维护

当路由、API、模型、权限、状态或限制变化时，更新受影响行及其英文镜像。每个模块只保留一条当前描述；不要在本文件记录修改序列或粘贴测试全文。

English mirror: [admin-system-modules.md](./admin-system-modules.md)
