# Mini Program Design Restoration Ledger

The current `packages/business/referrer-progress/referrer-progress` source is `design-references/referrer-customers-v1/referrer-customers-v1.png`. Production restores a capsule-safe 客户 Tab with Xiao K as customer-file clerk, opaque record codes, a solid 撤回 pill, and a 10-minute 恢复 countdown. The referrer TabBar label is 客户. Runtime visual QA remains pending the user's manual `390x844` screenshot.

This is the current route-to-design lookup. Keep exactly one row per runtime
route and one latest approved design source. Replace a row when the source or
production state changes; do not append restoration history here.

For `packages/surveying/editor/surveying-editor`, the approved navigation-measure
flow keeps the absolute azimuth readout (for example `231°`) separate from the
canvas transform. Relative room bearings snap to the four orthogonal axes and
crossing the `20°` trigger threshold animates the canvas between those axes over
`420ms`; the canvas must never settle at the raw diagonal heading. Navigation
sensor callbacks rotate the canvas only; direction arrows remain manual choices.
The current navigation label and live bearing readout are `28rpx`; calibration title/body/
actions are `36/26/28rpx`. This enlarged navigation/calibration typography is
part of the current restored state; runtime
visual QA remains pending the user's manual screenshot.

The current `pages/mine/mine` account panel also includes the native
**检查当前版本** row, using the existing account-row/icon treatment and no new
artwork. Its helper text reflects the global `wx.getUpdateManager` lifecycle;
authenticated `390x844` visual QA for the new row remains pending the user's
manual screenshot.

The shared custom TabBar source is `design-references/tabbar-icons/tabbar-earnings-appointment-v1.png`:
Earnings/Commissions use its paired wallet-and-income states. Enterprise-owner appointments remain an
`enterprise.appointments`-protected contextual route, entered from the workbench rather than a fixed TabBar item.

The shared `components/designer-contact-sheet` source is
`design-references/designer-contact-sheet/designer-contact-sheet-xiao-k-bubble-v8-candidate.png` for
`pages/index/index`, `packages/business/free-design-service/free-design-service`, and
`packages/business/customer-project/customer-project`. Its production mapping is the native WXML/Less
QR-first overlay plus `miniprogram/images/designer-contact/xiao-k-peeking.png` and the three
license-documented professional icons under the same runtime directory. Native text in a warm-white,
green-outlined speech bubble now sits to Xiao K's right with its tail pointing back to the mascot; its
`22rpx` lead-in reads `比小红书更方便贴心的` and its `32rpx` emphasis reads `家装顾问`, without
changing the QR stage or contact actions. The root reads the native menu-button bottom on attach and
every open, then keeps the bubble `16rpx` below that capsule boundary; the runtime screenshot that exposed
the prior overlap remains the QA evidence for this correction. When the host page has a visible custom
TabBar, the overlay docks above `--sfp-custom-tabbar-safe-height` so the outside close control is not
covered by the tab lane; pages without a TabBar keep the original bottom inset. The user-supplied
Service-tab screenshot of the clipped close control is the QA evidence for this correction. The relationship pill reads
only `你的专属家装设计顾问` when a visible title exists; the credential heading combines the resolved title and
designer name, while `50rpx` compass and customer-heart glyphs label the experience and service proof.
The source-calibrated element ledger is a `316rpx × 112rpx` speech bubble, `22rpx` bubble lead-in,
`32rpx` bubble emphasis, `24rpx` identity/proof copy, `32rpx` credential heading,
`96rpx` badge container with a `62rpx` glyph, and `50rpx` proof glyphs. Title-hidden states keep the
designer name in the identity pill and collapse only the title heading. Focused component, typography,
packaging, and caller-route tests pass; the refreshed `390x844` native-capsule runtime capture remains
pending the user's manual screenshot.

The following V3 source is historical context for the enterprise-owner portion of `pages/index/index`:
`design-references/enterprise-owner-activity-code-entry-v3/enterprise-owner-operations-home-v3.png`.
V3 uses equal-width Hero actions, a regular three-column operations path, and two short card-to-card connectors,
and replaces the owner-ambiguous `闭合率` detail with the period-scoped `已发布方案 N 份` value
from `schemeFacts.publishedLeadCount`. Its standalone ImageGen cutouts originally mapped to
`miniprogram/images/operations-dashboard/enterprise-guide.png` (17,943 bytes)
and `staff-load.png`. Unused V3 rasters (`lead-inbox.png`, `staff-onboarding.png`,
`scheme-delivery-rate.png`, `signing-rate.png`) were later removed from the runtime
package; current production uses the density-v2 / operations KPI cutouts listed below.
All remaining packaged cutouts are transparent PNGs,
independent of the composite design, and under the packaged `300KB` limit. The production mapping
and no-slicing rule apply to the existing single `pages/index/index` row below; this paragraph is the
historical source record for its former V2 wording. Authenticated `390x844` native-capsule runtime
visual QA remains pending the user's manual screenshot. The latest user-supplied tall-device capture
exposed a duplicated onboarding plus and a full-width rail below the three stages. The V3 correction
uses one `64rpx` generated onboarding cutout, a `26rpx` plain-green kicker, `24rpx` board padding,
`18rpx 18rpx 24rpx` stage-card padding, `44rpx` stage gaps with two card-to-card connectors,
`18rpx 20rpx` efficiency-card padding, `72rpx` generated icon boxes, and no rail or dots below the cards.
The latest tall-device comparison then exposed that the shared flex row was pushing the owner status pills too
low and shrinking the Hero hierarchy. The owner-only mapping now uses a `114rpx` top reserve, `12rpx` pill
offset, absolutely positioned `190rpx` Xiao K box, `136rpx` equal-width white actions, `64rpx` / `72rpx`
action artwork, and `30rpx` / `24rpx` action copy; the narrow-screen guard preserves `60rpx` / `68rpx`
artwork instead of reverting to the earlier undersized icons.
The next runtime capture showed the white-card onboarding asset losing contrast, the staff-load Xiao K reading too
small, and the stage hierarchy under-scaled. `staff-onboarding-white-card-v2.png` is the new built-in-ImageGen
transparent source; that V3 raster was later removed from the runtime package in favor of `team-onboarding-v2.png`. Staff-load uses `120rpx × 104rpx`, while the stage
cards use a `168rpx` height, `34rpx` ordinal disc, `26rpx` label, and `14rpx` lower inset. The reduced lower inset removes unused space while a two-line detail can still grow intrinsically.

The current approved enterprise-owner workbench source is
`design-references/enterprise-owner-workbench-density-v2/comp-b-with-operations-reminder.png`.
`pages/index/index` is the **工作台**: a compact Hero, then one white **获客与团队** hub containing the 430rpx left acquisition card, the paired 208rpx stacked **邀请入驻** / **查看推广人** actions, the contextual **查看预约安排** row, the thin B+C **经营提醒** strip, and the two blue/purple insight cards. The reminder reads only live `newLeads` and `signedCount` workbench rows (including returned units), never illustrative comp values, and switches to the **经营** Tab. Priority items remain a separate single white grouped tray with an alert icon and independent CTA geometry. Current standalone production art is `activity-code-share-v3.png` (592×464, 20,073 bytes), `team-onboarding-v3.png` (480×384, 21,267 bytes), `referrer-roster-v2.png` (180×201, 10,011 bytes), `lead-inbox-v2.png` (240×239, 10,044 bytes), and `priority-alert-v2.png` (90×88, 3,347 bytes) under `miniprogram/images/operations-dashboard/`; all are transparent PNGs under 300KB and were generated independently, never sliced from the comp. The enterprise-owner TabBar remains **工作台 / 经营 / 客户 / 提成 / 我的**; `客户` remains `pages/leads-management/leads-management`, and Appointments is not a fixed Tab without changing appointment route permission or behavior. This is the sole current **工作台** composition; the separate current **经营**-Tab source is recorded in its own route row below. Focused structure, navigation, typography, asset-signature, and package-budget verification passed; authenticated `390x844` native-capsule QA remains pending the user's manual screenshot.

The current source-calibrated element ledger preserves behavior while fixing the runtime hierarchy: reminder and insights remain inside the same action hub as the approved comp; the action-hub title is `34rpx`; the primary acquisition title/detail/CTA are `36rpx`/`26rpx`/`28rpx`. The landscape acquisition art is inset on both sides in a `220rpx` box (`184rpx` at `<=360px`) below the native copy. Right actions use `32rpx`/`24rpx` copy; invite/referrer art boxes are `150rpx × 140rpx` and `148rpx × 160rpx`. The `80rpx` appointment row displays the live appointment-array count. The `72rpx` reminder row uses a `32rpx` title divider and two evenly distributed live facts. Insight cards are `176rpx`: copy remains in the upper lane, the `40rpx` arrow occupies the lower lane, lead art uses `124rpx × 116rpx`, and staff-load art uses a lowered `120rpx × 96rpx` box. Priority rows keep a `104rpx` content-intrinsic minimum, `38rpx` visible alert art, and a `52rpx` CTA with `10rpx` corners. Runtime visual QA remains pending the user's next manual capture.

The current workflow typography baseline is verified against the design sources
listed below: page/navigation titles are at least `32rpx`, section titles at
least `28rpx`, business values/actions/body copy at least `24rpx`, and
supporting copy at least `22rpx`. Only non-essential artwork badges may remain
at `20rpx`; the audited service-workflow styles are guarded by
`miniprogram/test/miniprogram-typography-floor.test.js`.

For the existing `packages/business/onboarding/onboarding` source, a personal
referrer invitation additionally renders the native `22rpx` provenance line
“由企业员工 {name} 发起邀请” between the invitation badge and enterprise name.
It appears only when the resolved code supplies an active inviter's display
name; enterprise-wide codes retain the approved generic invitation card. This
is a data-state addition within the same approved composition; a refreshed
`390x844` manual native-capsule capture remains pending.

| Runtime route | Latest design source | Current QA state | Restored |
| --- | --- | --- | :---: |
| `pages/enterprise-operations/enterprise-operations` | `design-references/role-workbench-unification-v1/pages-enterprise-operations-role-data-v1.png` | This native-tab route is role-aware. Enterprise owners retain the approved **经营** dashboard, its four KPI Hero including contract amount, three-step enterprise loop, efficiency cards, real amount trend, and `enterprise.operations` scope. Designers and measurers see **数据** with the same period controls and exactly five own-scope facts from `GET /api/miniprogram/workbench?period=`: new leads, completed surveys, scheme delivery rate, signed count, and signing rate; no contract amount, amount trend, or store-wide wording is rendered in the employee branch. The employee branch now directly restores the approved composition: a `290rpx` mint Hero with a `46rpx` title and role-specific business artwork; a source-ordered `3+2` KPI matrix using `270rpx` cards, `78rpx` visible icon boxes, `28rpx` labels, and `60rpx` values; and one `304rpx` four-stage path with `92rpx` icon discs and short card-to-card arrows. Production mappings are `images/operations-dashboard/{staff-data-designer-v1,staff-data-measurer-v1,scheme-delivery-kpi-v1,signing-rate-kpi-v1}.png`; all are independent transparent PNG assets under 300KB, never slices of the composite design. Bootstrap and route guards add `staff.data`; the designer custom TabBar is **工作台 / 数据 / 客户 / 设计 / 收益 / 我的** and the measurer TabBar is **工作台 / 数据 / 客户 / 收益 / 我的**, while the native `tabBar.list` remains at five registered pages. Focused structure, asset, data, capability, TabBar, and owner-regression tests pass; authenticated `390x844` native-capsule screenshots remain pending the user's manual review | Yes |
| `packages/business/profile-edit/profile-edit` | Existing JoveKore account/profile surface governed by `miniprogram/DESIGN.md` and `design-tokens.json`; professional endorsement is an approved functional-state extension, not a replacement composition | The designer/measurer profile keeps the established account shell and labelled title/year fields, enterprise/lock notices, accessible switches, an internal-count disclosure, and a final customer preview. The career-start-year helper now stacks full-width and left-aligned under the label/input row so the 7-character label stays on one line and lines up with the title and switch rows. Enterprise-controlled and locked states disable only the affected controls. Focused static tests pass; the user deferred `390x844` native-capsule runtime QA and will provide a device screenshot | Yes |
| `packages/business/service-needs/service-needs` | `design-references/customer-workbench-tob-needs-v3.png` | Secondary customer service-needs page implemented in native WXML/Less: capsule-safe brand lock, multi-select service records, empty choice, loading/error recovery, and fixed save action. It is reached only from the approved success-state weak link; it does not alter the service-stage rail. Focused packaging, typography, and route-contract tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/lead-claim-pool/lead-claim-pool` | `design-references/lead-claim-racing-v1/designer-lead-claim-pool-v1.jpg` | User-approved `390x844` capsule-safe claim pool restored with masked lead facts, server-calibrated countdown, capacity state, three-second refresh, and native claim/reminder actions. The approved companion state sheet defines loading, empty, disabled, expired, concurrent-loss, and success behavior without replacing the canonical latest source. Focused page, subscription, navigation, and typography tests pass; authenticated native-capsule host capture remains pending | Yes |
| `pages/index/index` | `design-references/role-workbench-unification-v1/pages-index-three-role-workbench-v2.png` | Approved three-role composition is now the canonical professional-workbench source for this runtime route; the existing customer Service state remains unchanged. Enterprise owner, designer, and measurer use the same capsule-safe identity row and a fixed `252rpx` green Hero containing only the role title, one role-specific Xiao K illustration, and three server-derived summaries. All three professional roles use three compact intrinsic-width left-lane summary rows with separate number badges, a `12rpx` title offset, and a `12rpx` row gap. Each row now shrink-wraps its own label plus value instead of sharing a fixed minimum width, while `stat.count` compacts growing values to 千/万/亿 units before they can enter the mascot lane. The former designer raster contained an opaque green rectangle, a right-edge white line, and cropped body parts. Built-in ImageGen recreated the same floor-plan + tablet metaphor as the independent transparent source `design-references/role-workbench-unification-v1/xiao-k-designer-workbench-v2-source.png`; its optimized 480×480, 42,704-byte alpha PNG is packaged at `images/airy-v1/xiao-k-designer-workbench-v2.png`. The designer **客户线索 / 方案交付** helpers are explicitly two native-text lines inside a narrowed copy lane so the phone/palette art cannot overlap them. The same asymmetric acquisition hub follows. Owner copy is **获客与团队** with **分享活动码 / 邀请入驻（员工 · 推荐人）/ 查看推广人（全店推广网络）**; designer and measurer copy is **获客与推广** with **分享活动码 / 邀请入驻（仅推荐人）/ 我的推广人（仅查看本人网络）**. Existing standalone PNGs under `images/operations-dashboard/` are reused without slicing; designer customer/claim/design tasks and measurer BLE/calendar/unavailability/formal-survey tasks continue below the hub. API actions are server-owned, and existing join-code/referrer pages still enforce enterprise versus own scope. Focused workbench, navigation, permission, and asset tests pass; authenticated `390x844` and tall-device native-capsule screenshots remain pending the user's manual review | Yes |
| `packages/guides/enterprise-owner-guide/enterprise-owner-guide` | `design-references/role-onboarding-v1/enterprise-owner-onboarding-v1.png` | Approved three-step native onboarding for `enterprise_admin`: activity-code acquisition, partner onboarding, and operations-priority handling. The three slides are a native non-looping `swiper` (swipe or tap dots to review; 「下一步」 still advances; no autoplay). The route is optional after the first automatic presentation, replayable from Mine, and ends at the existing `staff-activity-code` presenter. Independent ImageGen transparent PNG mappings are `packages/guides/assets/enterprise-owner-v1/{activity-code,team-onboarding,operations-priority}.png` (palette-optimized, each <=300KB); title `32rpx`, slide title `40rpx`, body `24rpx`, assurance `22rpx`, CTA `28rpx`. The custom nav is `border-box` with `navigationRight` inside its width;「跳过」sits centered below the primary CTA (`16rpx` top margin, `24rpx` type). Static packaging/contract checks pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/guides/designer-guide/designer-guide` | `design-references/role-onboarding-v1/designer-advisor-onboarding-v1.png` | Approved three-step native onboarding for `designer`: lead claiming, survey-material handoff, and scheme delivery with earnings record. The three slides are a native non-looping `swiper` (swipe or tap dots to review; 「下一步」 still advances; no autoplay). It presents once on the designer overview after first entry, is replayable from Mine, and ends at the existing `staff-earnings` route. Independent ImageGen transparent PNG mappings are `packages/guides/assets/designer-advisor-v1/{lead-claim,survey-sync,scheme-delivery}.png` (palette-optimized transparent cutouts, each <=300KB; 640×529 / 640×514 / 439×448). The previous fully opaque exports had a baked transparency checkerboard; production keeps a real transparent channel so the mint stage shows through.; title `32rpx`, slide title `40rpx`, body `24rpx`, assurance `22rpx`, CTA `28rpx`. The designer workbench defers the WeChat-profile completion prompt until the guide has been seen. The custom nav is `border-box` with `navigationRight` inside its width;「跳过」sits centered below the primary CTA (`16rpx` top margin, `24rpx` type). Focused guide, packaging, and typography tests pass; authenticated `390x844` native-capsule capture remains pending the user's screenshot of the skip placement below the primary CTA | Yes |
| `packages/guides/measurer-guide/measurer-guide` | `design-references/measurer-onboarding-v1/measurer-onboarding-directions-v1.png` (approved direction B, 现场路径) | Approved three-step native onboarding for `measurer`: connect the laser meter, measure along wall lines, and complete a deliverable survey. The three slides are a native non-looping `swiper` (swipe or tap the step rail to review; 「下一步」 still advances; no autoplay). It presents once on the measurer overview after first entry, is replayable from Mine, and the final CTA `去看今日任务` returns to the existing workbench task list without inventing a new destination. The B-source scenarios map to regenerated high-resolution independent ImageGen transparent PNGs: `packages/guides/assets/measurer-v1/measurement-bench.png` (900×960, 100,280 bytes), `measurement-path.png` (960×640, 64,543 bytes), and `measurement-complete.png` (867×960, 82,506 bytes). They use indexed-palette PNG compression, retain alpha, remain below the 300KB per-artwork limit, and keep the complete mascot/equipment silhouette clear at the existing artwork scale. The full `packages/guides` subpackage is 1,276,739 bytes, below WeChat's 2MB limit. Title `32rpx`, slide title `40rpx`, body `24rpx`, assurance `22rpx`, CTA `28rpx`. The custom nav is `border-box` with `navigationRight` inside its width;「跳过」sits centered below the primary CTA (`16rpx` top margin, `24rpx` type). Focused guide, packaging, and typography tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `pages/mine/mine` | `docs/miniprogram-role-shell-design-v1.md`, `design-references/auth/miniprogram-guest-login-jovekore-v2-full.png` | Signed customer, referrer, and staff states retain the approved profile-card shell, inline account rows, and two-line current-identity helper. The WeChat permission-management and referrer-network rows now use regenerated, transparent, green rounded-outline PNGs at `images/mine-icons/{settings,user-round-plus}.png`, matching the neighbouring account icons without changing their routes or interactions. Signed `platform_admin` uses that same role-shell staff Mine and no longer renders the legacy **我的空间档案** / **我的户型** / **开始量房** archive. The logged-out state keeps the capsule-safe JoveKore｜家客来 lockup, standalone `images/home-ip-v1/login-identity-portal-v2.jpg` doorway scene, native `个人用户 / 员工 / 推荐人` rail, sole `立即登录` CTA, and account-switch trust row. Production keeps the source-calibrated narrow-screen scene/panel/title scale, visible identity-glyph bounds, full-width native CTA, and packaged `images/mine-icons/shield-check.png`; the approved composite remains unsliced and the guest gateway docks above `--sfp-custom-tabbar-safe-height`. Unsigned visitors reuse the customer Service/Mine TabBar and bootstrap restores signed role/capability navigation. The signed account card keeps the native `Role guide` row for approved work identities. Server-provided action rows now use `referrer.network`: designer, measurer, and salesperson show **我的推广人 / 邀请并查看我的推广人**, while the enterprise owner shows **推广网络 / 查看员工分支与全部推广人**; both navigate to the existing `enterprise-referrers` route. This reuses the approved native account-action treatment and adds no artwork or layout system. Focused layout, narrow-screen guard, asset, and action-routing checks pass; revised authenticated `390x844` and user-tall native-capsule captures remain pending manual review | No |
| `packages/business/login/login` | `design-references/all-pages-ip-v1/06-login.png` | Approved Xiao K door-entry composition unchanged except WeChat-review copy: the phone-tab title is「手机号授权登录」and the official WeChat logo is replaced by the existing circular phone-authorization PNG so the pre-`getPhoneNumber` surface no longer shows「微信授权登录」or Tencent branding. The green CTA remains「手机号快捷登录」. The agreement row is a larger hit target that toggles the existing 24rpx circle; WeChat `getPhoneNumber` mounts only after `agreed`, so the native button cannot steal the tap or authorize without consent. Unchecked phone CTA toasts「请先勾选同意协议」. 《用户协议》/《隐私政策》`catchtap` into `legal-webview` and do not toggle the checkbox. 《免责协议》 remains resolved and hosted but is hidden on the login row for now. The two titles may wrap; type stays `24rpx`. Focused contract tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/account-security/account-security` | `design-references/miniprogram-airy-minimalist-v1/30-account-security.jpg` | Existing approved account-security layout, typography, assets, and navigation are unchanged. Password-authenticated staff carrying `requiresPasswordChange` receive a one-time native reminder after login, then enter the signed role landing with a working TabBar; **去修改** may `navigateTo` this route, and Mine still opens it at any time. Mini Program APIs are not locked. A successful change clears the server flag, clears local session, and requires login with the new password. WeChat/phone quick login does not enter this state. Focused login/token contract tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/legal-webview/legal-webview` | No independent design file; native WeChat `web-view` with the default navigation bar | Guest-reachable document viewer: `onLoad` reads `url` + optional `title`, loads only `https:` into `<web-view>`, and sets the nav title. Login currently opens `https://smartfloor.zlyun168.com/user-agreement.html` and `https://smartfloor.zlyun168.com/privacy-policy.html`; `disclaimer.html` remains resolvable via `legal-docs.js` but is hidden on the login row for now. The domain must be in the Mini Program business-domain whitelist. Focused contract tests pass | Yes |
| `pages/leads-management/leads-management` | `docs/miniprogram-role-shell-design-v1.md` | The approved designer/owner customer route remains available; list cards put community on its own row under the phone (two-line clamp) so a long POI name is not squeezed beside the plan thumbnail. Owner branch **查看推广客户** reuses this Tab via `pendingLeadReferrerFilter` + Mini `GET /api/leads?referrerMembershipId=`; a clearable filter chip sits under the search row, and leaving then returning without a new pending filter clears it. The signed measurer state renders own confirmed tasks plus unscheduled work that still needs a formal survey, and completed surveys leave that pending queue. Cold-launch role selection uses the stored signed context so the customer/designer route does not first render the legacy owner shell. Focused navigation tests pass; authenticated `390x844` native-capsule host capture remains pending | No |
| `packages/business/lead-detail/lead-detail` | `design-references/lead-detail-delivery-stack-v1/lead-detail-below-survey-v1.png` (lower stack); formal-survey card uses right-aligned next-action, a full-width address row, and appointment/start-survey CTAs that share one equal-width 84rpx pill row when both are visible; Hero profile-edit and dialable phones follow the packaged icon-action convention (user-approved over the prior status∥补充资料 pair) | Standalone appointment card removed. Formal-survey card: tab on the left, **下一步** copy on the right, address on its own row, and **安排上门量房** / rebook / **查看预约** (relabeled **确认完成量房** when `serviceStage` is `survey_ready`) sharing one equal-width 84rpx pill row with **开始量房** / **继续量房** when both are visible (16rpx gap); a lone CTA stays full-width, matching the conversion and published-scheme CTA language. After survey completion the same card keeps house facts (community, area, stored appointment address, closed room names) plus a protected floor-plan PNG preview with **查看大图** (`fetchProtectedImage` + `wx.previewImage`). Assigned designers, enterprise owners, and assigned measurers keep the approved **开始量房** / **继续量房**, **新增量房**, and delete actions on their authorized leads. The two survey-action rows now share the same two equal columns, `84rpx` control height, `16rpx` gap, and full-pill radius; color hierarchy and behavior stay unchanged. Below it, **房屋现场图** uses the shared tag-first grid. Each staff card shows **分配** / **更换** on the right when `assignmentActions` allows it (24rpx brand green); phone stays a separate `.sfp-icon-action` tap. The picker loads `GET /api/leads/[id]/assignable-staff?role=` and submits `POST /api/leads/[id]/assign-staff` (fill or replace; measurers see no action). Scheme card reuses the same stack chrome: designers and enterprise owners see **进入 AI 设计** on open unarchived leads even before survey completion (empty title **方案设计**, helper **可用现场图开始出图**; after survey or a completed formal plan, **量房完成，可开始出图**); published rounds still open the read-only folio, row meta shows **定稿** when finalized, and **查看全部方案** / folio entry default to the finalized `schemeId` when present. **查看全部方案** and **进入 AI 设计** share one equal-width row (16rpx gap) when both are visible; a lone CTA stays full-width. History/conversion unchanged below. Hero community is a wrapping block in a 360rpx green copy lane whose diagonal edge stops just left of Xiao K, with a light text-shadow so long white type stays off the pale scene; stage rail unchanged. Assigned designer/measurer name and phone sit in a two-card strip between the hero and stage rail (`GET /api/leads/[id]` `assignedTo`/`measurerId` staff summaries; 待分配 when unset; a stored phone opens `wx.makePhoneCall`). Hero **补充资料** is an absolute top-right white pill with packaged `images/mine-icons/edit.png` (not paired beside the read-only status pill); customer phone and staff phone rows use `.sfp-icon-action` + `images/leads-v4/phone.png` and dial via `onCallStaff` / `wx.makePhoneCall`; the phone crop punches the white sheet to alpha so the handset sits on the green hero without an opaque square, `.hero-phone` left-aligns with the copy lane (`justify-content: flex-start`), the handset icon sits after the number, and the row keeps the original `手机：` + number copy. Status pill stays text-only. Focused owner-permission, route-guard, preview/assign, and icon-action contract tests pass; native `390x844` host capture remains pending | Yes |
| `packages/business/lead-form/lead-form` | Existing all-pages-ip-v3 lead-form chrome (`packages/business/assets/leads/lead-form.png`); native WeChat navigation (user-requested default back); community field follows appointment-booking map-pick plus manual input | Default nav bar with the native back control; the community row keeps the existing input-shell and adds a 24rpx **选择地点** `.sfp-icon-action` with packaged `images/leads-v4/map-pin.png` that writes `wx.chooseLocation` POI name into editable `communityName` (160-char cap). Map coordinates are not stored on the lead. Focused tests cover default nav and map fill; authenticated `390x844` native-capsule capture pending | Yes |
| `packages/business/settings/settings` | `design-references/miniprogram-airy-minimalist-v1/29-settings.jpg` (content merged into `pages/mine/mine`) | Compatibility deep-link route only; `onLoad`/`onShow` immediately `switchTab` back to Mine and no longer render a standalone settings shell. Visual and interaction source of truth is the inline Mine Tab account block | Yes |
| `packages/business/identity-switch/identity-switch` | `design-references/identity-switch-card-grid-v2/identity-switch-card-grid-v2.png` | Approved V2 replaces the narrow horizontal rail with a natural-flow two-column identity-card grid, so every server-defined role label (including `家装设计顾问` and `家装现场顾问`) remains native, readable, and never ellipsized. The selected card has the green outline and `当前使用` status; its locally previewed top archive scene has one complete Xiao K only. Independent transparent ImageGen role-tool PNGs map to `packages/business/assets/identity-switch/role-cards/{customer-service,designer-plan,measurer-laser,referrer-contact,enterprise-operations,sales-promotion,platform-console}.png` (RGBA, 48px, 3.8–4.6KB each); no composite design slicing or repeated mascot card artwork is used. One context still hides selection controls; two or more show cards. Tapping previews locally; only a non-current identity opens the existing native confirmation and signed-token switch. Loading, failure, expired-session, API, and permission behavior are unchanged. Focused identity-switch/source checks pass; authenticated `390x844` native-capsule capture remains pending manual review | Yes |
| `pages/ai-design/ai-design` | `design-references/miniprogram-airy-minimalist-v1/37-ai-design-workbench.jpg` | Designer Design tab restores D01: brand header (lockup only; no competing **设计记录** task-log link), green create-scheme hero with credit pill, static non-actionable floor-plan/photo capability descriptions (no arrow or selected state), strict space-category recipe filtering with the existing empty state, and recent design-project cards that open `scheme-studio`; every recipe opens with a compatible input mode. Deep links with `leadId` plus `floorPlanId`/`workflowId` enter scheme-studio. Measurer/enterprise alternate shells unchanged. Focused contract tests pass; authenticated `390x844` native-capsule capture deferred to owner local verification | Yes |
| `packages/ai-workflow/create/ai-design-create` | `design-references/all-pages-ip-v3/14-ai-design-create-v3.png` | Existing restoration | Yes |
| `packages/ai-workflow/recipe-detail/recipe-detail` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Current recipe explanation and input contract are implemented; native capture pending | Yes |
| `packages/ai-workflow/recipe-project/recipe-project` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Picker now follows Admin workbench: customer (`studio/leads`, 可设计/待量房) → scheme conversation (`studio/workflows`, 新建 continues as a new named scheme) → apply-to space (default **完整户型** / `whole_floor_plan`, closed rooms as **单房间** from the scheme-bound floor plan). Scheme cards keep the existing cover slot and show a signed latest confirmed image, otherwise the latest succeeded generation, otherwise the folio placeholder. Existing chrome/cards reused; list unit is no longer formal surveys from `/ai/sources`. Customer picker paginates `studio/leads` with `.sfp-list-footer`. Native capture pending | Yes |
| `packages/ai-workflow/recipe-confirm/recipe-confirm` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Confirm loads the chosen scheme via studio workflow detail and continues that conversation; photo input uses 拍照 / 从微信相册选择 / 从本户现场图选择; new captures pick a room tag first then store on the lead gallery and reuse that assetId. After create/run the lead workflow path redirects into `scheme-studio` (isolated tasks still open `ai-design-result`). Native capture pending | Yes |
| `packages/ai-workflow/result/ai-design-result` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Current generation, failure, delivery, and truthful customer-publication controls are implemented; refreshed native capture pending | Yes |
| `packages/ai-workflow/history/ai-design-history` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Page and history APIs remain; the Design tab header no longer opens this route. Isolated-task results still offer **查看历史**. Task filters and truthful task cards remain; synced workflow items expose **进入方案** into `scheme-studio`. Native capture pending | Yes |
| `packages/ai-workflow/scheme-studio/scheme-studio` | Existing D09 chrome: `design-references/miniprogram-airy-minimalist-v1/45-ai-scheme-studio.jpg`, `45b-ai-scheme-studio-templates.jpg`; approved new-round flow: `design-references/ai-design/mode-flow-v1/01-mode-select.jpg`, `02-whole-floor-plan.jpg`, `03-single-room-photo.jpg` | The existing scheme archive, Xiaoyunque-style composer dock, model/template/settings sheets, publication, and scheme controls remain. **开始新一轮设计** now opens a mutually exclusive **设计整屋 / 设计单间** bottom sheet; selection expands a compact responsibility banner with **更换方式**. Whole-house uses the formal floor-plan control image for structure and optional tagged site photos for camera evidence. Single-room requires a tagged site photo, may optionally carry a selected closed-room identity when a plan exists, omits the floor-plan control image, and can submit directly from the site photo without a room selection; it offers **全空间设计 / 仅软装换搭**; continuation-generation references are baseline images and do not satisfy the site-photo requirement. Generated route-specific transparent PNGs map `02` → `packages/ai-workflow/assets/mode-flow-v1/whole-house-material-board.png` and `03` → `packages/ai-workflow/assets/mode-flow-v1/single-room-camera-board.png`; both are below 300KB. Source-calibrated element ledger at `390x844`: sheet title `42rpx`, mode-card title `34rpx`, helper `24rpx`, compact-banner title/helper `28/22rpx`, actions `26rpx`, artwork containers `250/214rpx`. Focused composer and AI service tests pass; authenticated native-capsule screenshot remains pending the user's manual capture. Limited: no Admin canvas annotation editor; no dark theme | Yes |
| `packages/surveying/editor/surveying-editor` | User-provided 2026-09-01 **Navigation Measurement** icon crop (current Fig. 1), competitor-flow comparison Figs. 2–6, the earlier runtime/layout and rotation-control screenshots, plus the existing BLE direction candidate/selected references; the established formal-survey visual contract remains authoritative outside these controls | At the `390x844` baseline, the `190rpx` top action row shows a `132×88rpx` **导航测量** action in an independent far-left slot. The height reserves the full native control bounds, so its icon and label cannot be clipped at the topbar boundary. Guide / Save / CAD / Finish occupy the remaining width as four equal, no-wrap controls with a `12rpx` sibling spacing, so the full labels and tap targets remain visible on the baseline and narrower phone widths. The `40rpx` visible glyph uses the independently generated and optimized transparent `packages/surveying/assets/icons/navigation-measure.png` (128×128, 7,858 bytes; source `design-references/surveying/navigation-measure-v1/navigation-measure-source.png`), and the native `24rpx` label replaces the former north-compass presentation without rendering `N`. The overlay row begins `12rpx` below the canvas-content top: its magnifier starts at `16rpx` left and is a `132px` square, image-only crop with dashed guides and centre crosshair; snap-status and X/Y captions are no longer painted. The live `L… / ∠…` metric pill is `236×64rpx`, shares the lens top edge, keeps at least `16rpx` clearance from the lens panel, and otherwise centres in the canvas so it remains readable. The paired left canvas-rotation controls use independently generated transparent PNGs, `packages/surveying/assets/icons/angle/{rotate-counterclockwise-v2,rotate-clockwise-v2}.png`, at `40rpx` within their existing buttons and a `24rpx` vertical gap; their separate arrowheads make counterclockwise and clockwise unambiguous. BLE input keeps the blue dashed cardinal cross visible from the first wall. Before selection, three or four candidate controls sit clear of the reticle as compact single-layer `22px`-deep, `26px`-wide pointers with translucent light-green fill and green outline; no nested inner triangle is drawn, while the existing `28px` hit radius remains. During `closing` / `mergeClosing`, the candidate pointers remain rendered beside the `合` close action; after manual or automatic selection, unselected controls are removed from both rendering and hit controls, leaving one `14px`-deep small blue arrow on the locked guide. If the arrow hit radius overlaps an opening or wall, object selection wins; short taps beside the reticle also select the object underneath, while moved gestures retain wall dragging. The operator can therefore select a wall and use **复尺** without leaving input mode. A tap on canvas space that misses arrows, openings, walls, and closed rooms clears the transient bearing and restores the multi-arrow candidate state without moving the cursor or changing walls. The existing Fig. 1 green reticle remains visible throughout pan/pinch gesture frames: its plan coordinate follows the same live viewport transform as walls while its glyph keeps a stable screen size as the zoom reference. Navigation Measurement now opens a centred entry-door calibration dialog: the operator stands inside, faces the phone's top edge toward the entry door, keeps it level/still, and connects the BLE meter. Nine circular samples with no more than `6°` spread capture the real absolute door azimuth; the active control displays the live absolute value such as `231°`, while only the internal direction mapping treats the door axis as the room-relative reference. Automatic cardinal changes rotate the canvas along the shortest arc with a `420ms` ease-out transition; tapping the active control supports relocation or disabling navigation. Calibration and rotation remain editor-session state and do not change routes, APIs, permissions, BLE `ATD` semantics, or v4 graph persistence. Focused Canvas/state/asset tests pass; corrected `390x844` native-capsule visual QA awaits the user's manual screenshot | Yes |
| `packages/business/promotion-service-code/promotion-service-code` | `design-references/enterprise-code-presenters-fullscreen-v1/staff-activity-code-fullscreen-v1.png` (user-directed size match to the activity-code presenter; enterprise name stays omitted) | Referrer promotion-code presenter now shares the activity-code intrinsic mint poster scale: large title/subtitle, QR frame, overlapping scan-guide Xiao K, caption, white benefits, safety note, and share CTA. At `390x844`: nav `36rpx`; title/subtitle `64/30rpx`; QR frame/image `478/450rpx`; Xiao K `260rpx`; caption `30rpx`; benefit icon/title/helper `92/26/22rpx`; trust copy `24rpx`; CTA `96rpx` high with `30rpx` label. Reuses packaged `packages/business/assets/code-presenter-v3/{xiao-k-scan-presenter,home-outline,advisor-outline}.png` plus V2 scan glyph; native `¥0` badge. Anonymous presentation (no enterprise name), claim-route share, membership API, loading/error recovery, and capsule-safe back remain unchanged. The `open-type="share"` CTA says「分享给客户」. Focused contract tests pass; refreshed authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/staff-activity-code/staff-activity-code` | `design-references/enterprise-code-presenters-fullscreen-v1/staff-activity-code-fullscreen-v1.png` | Activity-code presenter is restored as one intrinsic mint poster: large title/subtitle, QR frame, overlapping scan-guide Xiao K, caption, white benefits, safety note, and share CTA. At `390x844`: nav `36rpx`; title/subtitle `64/30rpx`; QR frame/image `478/450rpx`; Xiao K `260rpx`; caption `30rpx`; benefit icon/title/helper `92/26/22rpx`; trust copy `24rpx`; CTA `96rpx` high with `30rpx` label. Independent transparent ImageGen PNGs map to `packages/business/assets/code-presenter-v3/{xiao-k-scan-presenter,home-outline,advisor-outline}.png`; native `¥0` badge and V2 scan glyph remain. All generated files are alpha PNGs below `300KB`. Capsule-safe lockup/back, enterprise name, share target, and profile recovery are unchanged. Focused contract/package checks pass; refreshed authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/enterprise-join-codes/enterprise-join-codes` | `design-references/enterprise-code-presenters-fullscreen-v1/enterprise-join-codes-fullscreen-v1.png` | The presenter keeps the approved split composition: tabs and intrinsic mint QR card, then white-page journey rail, safety note, and share/manage actions. At `390x844`: nav/tab `36/32rpx`; title/subtitle `56/28rpx`; QR frame/image `380/352rpx`; Xiao K `188rpx`; caption `28rpx`; journey icon/title/helper `92/26/22rpx`; trust copy `24rpx`; actions `96rpx` with `30rpx` labels. Existing independent PNGs remain mapped under `packages/business/assets/code-presenter-v3/`; no new art is introduced. Under `referrer.network`, an owner sees **员工入驻码 / 我的推广人入驻码**, while a designer, measurer, or salesperson sees only **我的推广人入驻码**. Personal generation, image, rotation, disable, and share remain in the same controls but are server-forced to the signed staff scope; staff-code operations stay owner-only. The 26rpx roster link now reads **查看推广网络** for the owner and **查看我的推广人** for ordinary staff. QR layout, journey rail, confirmations, no-rotate viewing, capsule-safe back, and onboarding share target retain the approved source. Focused contract/package checks pass; refreshed authenticated `390x844` native-capsule captures for both scopes remain pending the user's manual screenshot | Yes |
| `packages/business/enterprise-staff/enterprise-staff` | Reuses R03 `20-enterprise-appointments.jpg` card chrome plus claim-page capsule-safe back; Xiao K guide uses packaged `images/airy-v1/xiao-k-measurer-3d.png` | Owner-only deep roster for designer/measurer assignment eligibility: role chips, pause/resume via `GET/PATCH /api/miniprogram/enterprise-staff`, empty CTA to join codes; no create-staff or WeChat/QR edit. Workbench staffing 查看详情 and 人员负荷 navigate here. Status chips flex-center 22rpx labels (`line-height: 1`) so「可派单」/「待补微信资料」sit optically in the pill. Ghost **电话联系** uses `.sfp-icon-action` + `images/leads-v4/phone.png` (solid pause/resume CTA stays text-only). Roster `GET` is paged (`page`/`limit`, default 20) with `.sfp-list-footer` load-more; summaries use COUNT. Focused contract tests pass; authenticated `390x844` native-capsule capture pending | Yes |
| `packages/business/enterprise-referrers/enterprise-referrers` | `design-references/enterprise-referrers-network-redesign-v3/promotion-network-overview.jpg`; independently generated network-coordinator Xiao K maps to `packages/business/assets/enterprise-referrers-v3/xiao-k-network-coordinator.png` (RGBA `420×504`, `123,772` bytes) | Owner **推广网络** is now a summary-only first level: enterprise facts, one compact card per eligible employee (including zero invitees), and non-tappable **历史未归属**. It does not render the employee's referrer list inline; a real employee card opens the dedicated detail route with `staffId`. **全部推广人** retains its paged query/status list; ordinary staff remain server-forced to their own read-only roster. Owner disable remains idempotent, with no re-enable/reassignment and no lead/commission rewrite. Focused contract tests pass; authenticated `390x844` native-capsule captures remain pending manual review | Yes |
| `packages/business/enterprise-referrer-branch/enterprise-referrer-branch` | `design-references/enterprise-referrers-network-redesign-v3/employee-referrers-detail.jpg` | Owner-only employee-detail roster entered from the overview. `view=staff&staffId` validates current-enterprise eligible staff, shows the unfiltered employee summary, and pages that employee's referrers through status/name/phone filters. Every card exposes **查看推广客户** (reuses the staff Customers Tab with `referrerMembershipId`); active cards also retain **电话联系 / 停用后续扫码**. It introduces no re-enable, reassignment, or changed historical attribution. Focused contract tests pass; authenticated `390x844` native-capsule capture remains pending manual review | Yes |
| `packages/business/free-design-service/free-design-service` | `docs/superpowers/specs/2026-08-25-free-design-service-contact-conversion-design.md` | Phone authorization retains its content-intrinsic three-benefit state. Success remains archive-first with native `服务进度 / 户型档案 / 设计方案`, the sole filled `查看服务档案`, real `professionalProfile` proof, and a fixed `148rpx` supporting WeChat action. The user has now approved existing-attribution option C at `design-references/free-design-service-existing-redesign-v2/option-c-continuity-archive-drawer.png`. Production restores it in native WXML/Less with `已为你接续原有服务`, truthful stage/update facts, a three-stop path, three tappable folder tabs, the only filled `继续查看服务档案`, compact real designer proof, and the preservation assurance; the former second full-width contact CTA is removed. `现场顾问`/`上门` labels map to the surveying-arrangement stage. The drawer Xiao K maps to the transparent `720×346`, `25733`-byte indexed-colour `xiao-k-continuity-archive-drawer.png`; all copy, progress, and controls remain native. Existing-state ledger: Hero `48/28rpx`, preservation/service reference `24/22rpx`, stage label/value/update `22/36/22rpx`, stage circle/copy `46/24rpx`, drawer stage `318rpx`, folder labels `24rpx`, CTA `100/32rpx`, designer `32/24/22/24rpx`, proof `22/32rpx`. Focused drawer, transparent-PNG, typography, and package checks pass; revised tall-device and `390x844` native-capsule captures await manual review | Yes |
| `packages/business/onboarding/onboarding` | User-approved `design-references/onboarding/referrer-enterprise-invitation-20260828-v7.png` for the referrer ready state; staff/recovery retain `design-references/miniprogram-airy-minimalist-v1/16-onboarding.jpg` and `17-onboarding-recovery.jpg` | Referrer QR-ready state restores the supplied full-height invitation: a capsule-safe lockup, mint architectural hero with **欢迎加入 / 推广团队**, three truthful benefits, enterprise invitation card, three-step promotion flow, phone-authorization trust row, and **立即加入推广团队** CTA. Dynamic enterprise name remains native text. The runtime-review correction replaces the old gray mine icons with standalone generated transparent PNGs: hero/step QR, progress and commission glyphs, enterprise-promoter person-plus glyph, and high-contrast white CTA shield at `packages/business/assets/onboarding-referrer-v7/{promotion-code,promotion-progress,promotion-commission,promotion-person-plus,promotion-cta-shield}-v7.png` (all under `5KB`); the hero Xiao K and building remain `images/onboarding-referrer-v7/{xiao-k-promoter-hero-v7,enterprise-building}-v7.png` (`620×688`, `274,863` bytes; `840×560`, `75,311` bytes). All artwork is independently generated transparent PNG, not sliced from the composite. At `390x844`, targets are hero title `58/78rpx`, enlarged benefit glyph `34rpx`, business-card title `48rpx`, unbroken first invitation sentence at `23rpx`, step icon/number/title/helper `104/38/27/22rpx` with number and title in one aligned row, trust row `24rpx`, and CTA `96rpx` high with `34rpx` copy. Staff-ready and design-17 recovery behavior, APIs, roles, token resolve/reopen, authorization, and name-confirmation flow are unchanged; **加入后即可获取专属推广码** is display-only rather than a navigation action. Focused contract/package checks pass; updated native-capsule runtime screenshot remains pending manual verification | Yes |
| `packages/business/enterprise-register/enterprise-register` | No independent design file; reuses onboarding brand-lock / airy-minimalist tokens from `miniprogram/DESIGN.md` + `design-tokens.json` (no new IP art). Back chevron matches claim/join-code custom nav (`09`/`11`) | Platform `er_` open-account flow: resolve → fill Web-aligned form → editable contact-phone field → tap **授权手机号并提交** to invoke WeChat phone auth and immediately submit. Manual entry is supported for readability/convenience but remains subject to the API rule that it must match the authorized WeChat phone; inline helper copy explains this and the post-auth auto-submit behavior. Primary CTA keeps the mint `--action-disabled-bg` fill while enterprise name, credit code, or contact name is empty, but stays tappable so it can list `还需填写：…` and highlight those inputs with inline `请填写…` copy (WeChat `getPhoneNumber` is bound only after the three fields are filled; native `#f7f7f7` is not used). Capsule-safe brand lockup includes the claim-page back chevron (`navigateBack` or role landing/Mine) so a QR stack-root cannot trap the user. A later WeChat recents/home/desktop reopen with any signed identity leaves to role landing; a fresh QR scan or chat-card share keeps any signed identity, including workbench, on the form. Ready/success/error/recovery reuse the onboarding identity-link / state-action language for **去登录**; the form link is a native button; `ACCOUNT_CONFLICT` after approval is an already-account exit; phone authorization that is already a workbench identity stays on the already-account state and confirms with `wx.showModal` before leaving. Focused contract tests pass; authenticated `390x844` native-capsule capture pending | Yes |
| `packages/business/onboarding-debug/onboarding-debug` | Development-only extension of the onboarding route's confirmed `miniprogram/DESIGN.md`, `design-tokens.json`, and `app.less` language; no separate production design source | Development build only. The native scanner selects a local Mini Program code, accepts only the onboarding target path, then opens the normal server-backed resolution, phone authorization, and identity-write flow; the route is locked outside development builds | No |
| `packages/business/referrer-workbench/referrer-workbench` | `design-references/miniprogram-airy-minimalist-v1/06b-promoter-workbench-signed-count.jpg` | Preserves the source composition while improving readability: the identity-nav no longer paints decorative scan/bell icons, so `家客来 · 推广端` keeps the capsule-safe lockup width; Logo + `家客来 · 推广端` stay left-aligned on the capsule row, and the signed user name stacks directly under `家客来` inside the same capsule height (40rpx brand mark; long names ellipsize; the previous same-line gray identity pill is gone); the brand mark uses production `/images/home-ip-v1/brand-logo.png` (house + JK lockup shared with customer/staff workbenches) instead of the compositor's green `JK` text squircle; nav title is `32rpx`; business labels are `24–28rpx` and explanatory text is `20–22rpx`. Enterprise pills use natural single-line widths in the source's horizontal selector rather than index-based fixed widths, keeping long names, subsequent memberships, and the add-enterprise action separate. Add enterprise invokes the native scanner and accepts only the existing onboarding path with a code parameter before entering server-validated onboarding. Hero stats now include read-only `已签约` count from progress `stage.key=converted` beside 今日扫码/累计客户/待发放 count (no pending-earnings ¥); no full ops dashboard on this role. The 280rpx「出示推广服务码」CTA wraps icon+label in an inner view with `nowrap` so ColorOS/OnePlus cannot wrap the last character. Identity switch and logout are not on this landing (they remain on Mine); 「退出该企业」stays. Referrer-workbench and role-navigation regression tests pass; milestone status tags flex-center 22rpx labels. Authenticated native `390x844` host capture remains pending because the current DevTools window has no verified automator endpoint | Yes |
| `packages/guides/referrer-guide/referrer-guide` | `design-references/role-onboarding-v1/referrer-onboarding-v1.png` | Three native, capsule-safe steps explain service code, masked progress, and own earnings. The three slides are a native non-looping `swiper` (swipe or tap dots to review; 「下一步」 still advances; no autoplay). The approved composite is not sliced: ImageGen-produced transparent artwork maps to `packages/guides/assets/referrer-v1/service-code.png` (720×531, 86,655 bytes), `progress.png` (720×359, 116,232 bytes), and `earnings.png` (720×461, 123,044 bytes); visible titles, descriptions, assurance copy, dots, skip, and CTA remain native WXML. The page and artwork live in the dedicated `packages/guides` subpackage, while the main package keeps only the small versioned role-guide registry and Mine trigger. Referrers open v1 automatically once per local signed account/role/version after entering the workbench, may skip, and may replay it from Mine; the final CTA opens the selected membership's existing service-code route. The deep link requires `referrer.promotion`. Customer has no forced display and no unapproved guide placeholder. The `390x844` element ledger is content-intrinsic: capsule-safe nav uses a `32rpx` title, the artwork stage is `690rpx`, the step headline/body are `40rpx`/`24rpx`, the assurance copy and shield box are `22rpx`/`26rpx`, dots follow after `24rpx`, and the `96rpx` CTA uses `28rpx` type with bottom-safe padding. The custom nav is `border-box` with `navigationRight` inside its width;「跳过」sits centered below the primary CTA (`16rpx` top margin, `24rpx` type). Focused navigation, storage-version, package-size, alpha-channel, typography, and three-step behavior checks pass; native-capsule runtime QA remains pending the user's manual screenshot | Yes |
| `packages/business/referrer-progress/referrer-progress` | `design-references/referrer-customers-v1/referrer-customers-v1.png`; packaged `packages/business/assets/referrer-customers-v1/xiao-k-customer-dossier.png` (531×720, 18,329-byte indexed transparent PNG) | Capsule-safe 客户 Tab for the signed membership: mint Hero `我的推荐客户` with Xiao K as customer-file clerk, opaque record-code cards, solid green 撤回, and muted 恢复 inside the 10-minute window. Loading/retry/empty states expose no phone, precise address, wall graph, internal appointment reason, or design file. The referrer TabBar and workbench shortcut now read 客户. Source-calibrated ledger: nav `34rpx`, Hero title/helper `36/24rpx`, Xiao K box `176×212rpx`, record code `32rpx`, stage/action `24/26rpx`, 撤回 pill `64rpx` high. Customer list paginates with `onReachBottom` and `.sfp-list-footer`. Focused route/privacy/asset tests pass; authenticated `390x844` native-capsule capture remains pending the user's screenshot | Yes |
| `packages/business/referrer-earnings/referrer-earnings` | User-authorized Phase-13 extension of `miniprogram/DESIGN.md`, `design-tokens.json`, `app.less`, and the existing green workbench language | Only the current member's payable/paid **count** summary and each masked customer label plus payment status (no amounts). Payment-status chips flex-center labels. Rows paginate with `onReachBottom` and `.sfp-list-footer`; counts stay COUNT-based. WeChat DevTools `390x844` verified the summary, empty-state CTA, and capsule-safe header | Yes |
| `packages/business/staff-earnings/staff-earnings` | Same Phase-13 earnings language as `referrer-earnings` (`miniprogram/DESIGN.md`, `design-tokens.json`, `app.less`); designer/measurer copy only | Own current-enterprise payable/paid **count** summary and each masked customer label plus payment status for the signed designer or measurer (no amounts). Payment-status chips flex-center labels. Rows paginate with `onReachBottom` and `.sfp-list-footer`. Focused TabBar/capability tests pass; authenticated `390x844` native-capsule capture remains pending | No |
| `packages/business/enterprise-commissions/enterprise-commissions` | User-directed payout-ledger redesign: `design-references/enterprise-commissions-payout-ledger-v1/enterprise-commissions-payout-ledger-v1.png`; the approved row/action hierarchy remains the design source for the amount-adjust extension | The owner payout ledger remains a customer-contained payment batch. Every payable referrer, designer, and measurer row now places a mint secondary **调整金额** action beside the green **确认线下付款** action; it opens a viewport-pinned native amount dialog, accepts a non-negative two-decimal value, preserves input/errors on failure, and reloads totals after success without marking the row paid. `PATCH /api/miniprogram/enterprise-commissions/[id]` is owner-only, tenant-scoped, reuses the shared payable adjustment audit, and never changes the beneficiary; paid and voided rows stay read-only. Missing/stale IDs and duplicate in-flight taps show feedback. A payable `¥0.00` row still has the separate quick-ledger payment path: `POST /api/miniprogram/enterprise-commissions/record-zero-payment` records `小程序线下付款补录` and marks it paid in one transaction, while normal Mini Program `markPaid` rejects zero amounts and batches containing one omit batch confirm. Both centered amount dialogs use a border-box shell and shrinkable flex action row so native buttons stay inside the card on narrow screens. Top summary, filters, server pagination, the payout-checker Xiao K asset (`100×102`, `14,456`-byte RGBA PNG), and the approved customer-batch composition remain. Element ledger at `390x844`: title `36rpx`, Hero title/helper/primary amount `38/22/48rpx`, mascot box `228×232rpx`, section title `30rpx`, customer/amount `28/32rpx`, role/actions `24rpx`, helper/status `22rpx`, both row actions `68rpx` high, and amount/quick-ledger dialog title/input/actions `32/32/26rpx`. Focused model/page/API-contract and dialog-containment checks pass; authenticated `390x844` native-capsule runtime capture of the two-action row and open amount dialog remains pending the user's manual screenshot | Yes |
| `packages/business/customer-projects/customer-projects` | Compatibility deep-link shell only (product list retired; stage-companion home owns ranking/switcher per `docs/superpowers/specs/2026-08-21-customer-service-home-stage-companion-design.md`) | Not a product project list. On load the route ranks current-JWT customer-owned unarchived projects and redirects to the highest-urgency service archive (`customer-project`) or falls back to the Service tab; no TabBar list UI. The project folio remains the real deep route without the TabBar. Legacy inbound links may still hit this path | Yes |
| `packages/business/customer-project/customer-project` | `design-references/customer-project-archive-redesign-v4/customer-project-archive-balanced-color-v4.png` | V4 is implemented as native WXML/Less: compact single-role Xiao K archive hero, four numbered service stages, a warm-apricot appointment panel using the non-confrontational customer label `预约量房`, with the status badge on the title row so helper copy `选择方便的时间，家装现场顾问会提前与你确认` wraps on its own full-width line instead of being ellipsized beside the badge, mint designer and mist-blue measurer cards, and one continuous three-row archive book with green/amber/blue status semantics. User-approved Hero copy is now fixed as `您的家装顾问` / `现场顾问与设计方案全记录` instead of the lead-name `heroTitle`. Assigned staff cards render the resolved customer `professionalProfile` (`title` when visible, plus `experienceLabel` / `serviceLabel`) from stored assignment-slot copy without rewriting 设计师/测量员; the designer card role is `家装设计顾问` without a 专属 prefix. The existing designer contact, staff dialing, appointment booking/detail/reschedule, protected floor-plan preview, tagged site-photo capture (native footer hides while the room-tag sheet is open), read-only scheme folio, and scheme-poster share flows remain wired to their current data and permission contracts. The first user-supplied tall-device runtime capture exposed legacy substitute icons and loose internal spacing; the route now uses one license-documented rounded-line family for calendar, person, ruler, document, image, delivery-file, phone, and WeChat semantics. Source SVGs live in `docs/icon-sources/customer-project-v4/` and packaged PNGs in `miniprogram/packages/business/assets/customer-project-v4/`. The source-calibrated element ledger is nav `36rpx`, Hero title/helper `44/26rpx`, Xiao K container `118×132rpx`, stage circle/label `38/24rpx`, appointment title/helper/CTA `34/24/28rpx` with an `84rpx` icon container and `60rpx` visible glyph, service heading/role/proof `32/28/24rpx`, person icon container/glyph `72/50–52rpx`, person-card minimum/padding `296/20rpx`, dossier title/helper/status `30/24/24rpx`, dossier-row minimum `110rpx`, dossier icon container/glyph `72/52rpx`, and footer label/contact icon `28/34rpx`. Focused source/template tests pass; authenticated `390x844` native-capsule and revised tall-device optical checks remain pending the user's manual runtime screenshots | Yes |
| `packages/business/customer-ai-schemes/customer-ai-schemes` | `design-references/miniprogram-airy-minimalist-v1/44-customer-ai-scheme-timeline.jpg` (D08) | Read-only customer AI scheme folio restored from D08: capsule-safe custom nav, customer-summary hero with house-block Xiao K (`project-delivery-xiao-k.png`, not the robot stand-in), scheme-round chips (finalized round prefixed **定稿**), current-round hero preview and delivery timeline, footer back/(customer) save-share; without a `schemeId` deep link the default selection is the finalized album, otherwise the last round; customer share opens `scheme-share-poster` (current-round cover + scheme title + 家客来 brand, no QR), album save then image share via `wx.showShareImageMenu`, with capsule page-forward hidden—no mini-program card share; non-owner poster entrances reuse this D08 folio via `GET /api/miniprogram/published-scheme-folios/[leadId]` without the save/share CTA; staff mode still omits the share CTA; no generation or edit actions. Entries come from lead detail (`mode=staff`), Service-home `免费效果图`, and the customer project archive (`mode=customer`), reusing `GET /api/leads/[id].publishedSchemes` and the customer-project aggregate. Focused contract tests pass; authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/appointment-detail/appointment-detail` | `docs/superpowers/specs/2026-08-21-appointment-detail-inline-reschedule-design.md`; `design-references/miniprogram-airy-minimalist-v1/03-appointment-reschedule.jpg`; secondary staff row follows `07-measurer-calendar.jpg` card-button language; sticky bar matches customer-project `footer-bar` full-bleed shell | Detail hosts inline availability picker (5-day window + slots) when `canReschedule`; archived/missing/converted/closed leads stay terminal and do not fetch availability slots. Sticky cancel|confirm sits in a full-bleed frosted page-tone parent (safe-area aware) with ~0.9∶1.3 flex + 20rpx gap and shared 26rpx centered labels; disabled confirm keeps `--action-disabled-bg` against the `#f8faf9` bar; page `padding-bottom: 200rpx` clears the bar; address∥navigate secondary row is equal-width pills with 16rpx gap and packaged edit/map-pin icons (no button emoji); staff **拍现场图** is the same card-button language and opens tag-first capture into the lead gallery; the room-tag sheet uses `root-portal` at z-index 2000 and hides the sticky native cancel|confirm bar while open so living-room/bedroom chips are not clipped; **开始量房** uses `images/leads-v4/ruler-green.png`. Focused tests pass; authenticated `390x844` native-capsule capture remains pending the user's screenshot of the open tag sheet | Yes |
| `packages/business/appointment-reschedule/appointment-reschedule` | Compat redirect shell for `03` / detail inline reschedule | No standalone UI; `onLoad` `redirectTo` `appointment-detail` with mode mapping (`internal` → staff detail; absent/`customer` → `mode=customer`). Callers prefer detail directly. Focused shell tests pass | Yes |
| `packages/business/appointment-booking/appointment-booking` | `design-references/referrer-network-appointment-v1/phase-5-designer-appointment-booking-v1.png` | Keeps the approved booking structure while splitting the prior address row into native map-place selection and manual building/unit/room detail. **选择地点** / **重新选择** uses `.sfp-icon-action` + `images/leads-v4/map-pin.png`. Owning customers and assigned designers submit the selected real slot, service address, and optional GCJ-02 point; focused tests pass. Refreshed `390x844` designer/customer captures remain pending | Yes |
| `packages/business/measurer-calendar/measurer-calendar` | `design-references/miniprogram-airy-minimalist-v1/07-measurer-calendar.jpg` | Restored matching the airy minimalist measurer calendar: capsule-safe top navigation with month switcher, lightweight overview card with schedule metrics and 3D Xiao K partner, 7-day horizontal calendar strip with task indicator dots, structured appointment cards with quick call/navigation/survey actions using packaged phone/map-pin/ruler icons (no button emoji), and quick unavailability shortcut. Appointment status tags and hero stat pills flex-center labels. All tests pass | Yes |
| `packages/business/measurer-unavailability/measurer-unavailability` | `design-references/miniprogram-airy-minimalist-v1/33-measurer-unavailability.jpg` | Restored matching the airy minimalist unavailability editor: smart guide notice card, touch-friendly date picker box, segmented time slot selector (all-day/morning/afternoon/custom), preset high-frequency reason tags, refined input with char counter, prominent green save CTA, and active schedule cards with status dot and delete action. All tests pass | Yes |
| `packages/business/enterprise-appointments/enterprise-appointments` | `design-references/miniprogram-airy-minimalist-v1/20-enterprise-appointments.jpg` | Enterprise-owner appointment schedule is a TabBar-free secondary page from workbench **查看预约安排** (`navigateTo`, capsule-safe back). Restored from R03: “预约调度中心” header with real period count, chips 本周/本月/本年 plus custom range (same operations period controls). Selected custom dates render after the 自定义 chip (`8/1 ~ 8/31`, cross-year keeps years) so they are not clipped by the WeChat capsule; the nav subtitle stays `自定义 N 单预约`. A day strip for the selected Shanghai window, and `confirmed`/`expired` left-accent cards; open leads with expired or past-end appointments that fall before today still appear on the today list for coordination; archived or missing linked leads hydrate as `closed` so they render the read-only “已关闭” badge instead of a fake 客户量房 card; `converted`/`closed` leads render a read-only “已签约”/“已关闭” badge with no CTA or detail navigation. No invented dispatch actions and no area/layout/measurer-phone fields absent from the workbench item. Card status chips flex-center 22rpx labels (`line-height: 1`). Focused contract tests pass; authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/promotion-records/promotion-records` | Existing enterprise promotion-records chrome with packaged `packages/business/assets/promotion-records/hero-scene.jpg` (not a new airy salesperson home); channel-salesperson TabBar `报备/我的` reuses `tab-home` / `tab-mine` | Channel salesperson bootstrap lands here; page embeds custom TabBar. List tabs paginate (`page`/`limit` plus `search=`) with `.sfp-list-footer`; overdue todos stay on the capped workbench API. Focused identity/TabBar/Mine regression tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/platform/devices/devices` | Moved page of the former main-package `platform-device-workbench`; same capsule-safe identity nav and BLE enroll/list language, no new design file | Platform-admin Devices tab in ordinary subpackage `packages/platform` (`reLaunch`, not native `tabBar.list`). BLE scan/enroll/`GET/POST /api/miniprogram/devices` unchanged except registered-device rows now page; the enterprise picker stays a full list. Focused contract tests pass; authenticated `390x844` native-capsule capture pending the user's screenshot | Yes |
| `packages/platform/enterprise-review/enterprise-review` | Reuses `platform-device-workbench` capsule-safe identity nav plus `enterprise-appointments` list-card language; no new Xiao K or sliced Admin screenshot | Platform-admin review queue: default `pending_approval`, status chips, name/phone/code search, list cards, approve confirm, reject reason sheet, phone call, nav **开户码**, embedded custom TabBar. Queue `GET` paginates with `.sfp-list-footer`; subtitle uses COUNT. Focused contract tests pass; authenticated `390x844` native-capsule capture pending the user's screenshot | Yes |
| `packages/platform/enterprise-review-detail/enterprise-review-detail` | Same operational-list language as the review list, plus claim-page capsule-safe back | Platform-admin enterprise detail and follow-on status actions (`approve` / `reject` / `resubmit_review` / `disable` / `enable`) without AI/automation keys. Focused contract tests pass; authenticated `390x844` native-capsule capture pending the user's screenshot | Yes |
| `packages/platform/registration-code/registration-code` | Join-code presenter information structure plus review operational-list tokens; main-package `scan.png` / `shield-check.png` / brand-logo, no new Xiao K and no `packages/business` assets | Read-only current `er_` open-account QR (view without rotating). Entries: review nav and Mine account row. Focused contract tests pass; authenticated `390x844` native-capsule capture pending the user's screenshot | Yes |

| `packages/guides/customer-guide/customer-guide` | `design-references/customer-onboarding-v1/customer-onboarding-four-slides-v2.png`; Service-home entry `design-references/customer-onboarding-v1/customer-service-home-guide-entry-v1.png` | Optional customer-only four-slide native onboarding. On wide Service-home screens, the Xiao K speech bubble reads `我是小K / 想了解服务？ / 点击我带你看看`; tapping it or the mascot opens this non-looping swiper. At `<=400px`, the multi-line overlay is hidden and the same entry becomes an in-flow single-line `点击我带你看看` button below the Hero helper copy, preserving the unobscured headline and full Xiao K artwork. It never auto-opens on first entry, appears in Mine, or records a seen state. The pages cover three free benefits, expressing needs, the actual demand/booking/design steps, and the existing service archive; skip and the final CTA return to the Service tab. Independent ImageGen alpha PNGs map to `packages/guides/assets/customer-v1/{three-free-benefits,home-archive,service-route,service-archive}.png`, each <=300KB. At `390x844`, nav/title/body/assurance/CTA use `32/40/24/22/28rpx` and the artwork stage is `500rpx`. The `装修服务向导` title is explicitly single-line. The custom nav is `border-box` with `navigationRight` inside its width;「跳过」sits centered below the primary CTA (`16rpx` top margin, `24rpx` type). Manual native-capsule runtime QA is pending the user's screenshot. No capability, API, or permission boundary changes | Yes |

## Role guide update

The approved replay entry now covers `referrer`, `enterprise_admin`,
`designer`, and `measurer`; the enterprise owner, designer, and measurer routes
are recorded above with their separate `packages/guides` pages and asset
mappings. All four guides now use a native non-looping `swiper` so earlier
slides can be reviewed by swipe or tap. Customer now has a separate manual four-slide guide, opened only from the Service-home Xiao K speech bubble; it is never forced or replayed from Mine.

## Recording rules

Current route note — the owner branch keeps its real `contractAmountTrend` canvas with compact 万元 labels and the existing two-series/empty-state contract. The employee branch introduced by the current combined source never mounts that canvas or any amount field; refreshed authenticated `390x844` native-capsule verification is pending the user's screenshot.

- Use the normalized route from `miniprogram/app.json` as the unique key.
- Record the design mapping and one concise current QA result.
- Keep screenshots, metrics, and test logs in local evidence directories, not in
  this canonical ledger.
- Update the English and Chinese ledgers together when a visual restoration changes.

Chinese mirror: [miniprogram-design-restoration-ledger.zh-CN.md](./miniprogram-design-restoration-ledger.zh-CN.md)
