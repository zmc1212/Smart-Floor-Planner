# Mini Program Design Restoration Ledger

This is the current route-to-design lookup. Keep exactly one row per runtime
route and one latest approved design source. Replace a row when the source or
production state changes; do not append restoration history here.

The shared custom TabBar source is `design-references/tabbar-icons/tabbar-earnings-appointment-v1.png`:
Earnings/Commissions use its paired wallet-and-income states, and enterprise-owner Appointments uses
its paired calendar-check states. This shared source applies wherever the capability-allowlisted TabBar renders.

The shared `components/designer-contact-sheet` source is
`design-references/designer-contact-sheet/designer-contact-sheet-professional-card-v7.png` for
`pages/index/index`, `packages/business/free-design-service/free-design-service`, and
`packages/business/customer-project/customer-project`. Its production mapping is the native WXML/Less
QR-first overlay plus `miniprogram/images/designer-contact/xiao-k-peeking.png` and the three
license-documented professional icons under the same runtime directory. The relationship pill now reads
only `你的专属设计师` when a visible title exists; the credential heading combines the resolved title and
designer name, while `50rpx` compass and customer-heart glyphs label the experience and service proof.
The source-calibrated element ledger is `24rpx` identity/proof copy, `32rpx` credential heading,
`96rpx` badge container with a `62rpx` glyph, and `50rpx` proof glyphs. Title-hidden states keep the
designer name in the identity pill and collapse only the title heading. Focused component, typography,
packaging, and caller-route tests pass; the refreshed `390x844` native-capsule runtime capture remains
pending the user's manual screenshot.

The current workflow typography baseline is verified against the design sources
listed below: page/navigation titles are at least `32rpx`, section titles at
least `28rpx`, business values/actions/body copy at least `24rpx`, and
supporting copy at least `22rpx`. Only non-essential artwork badges may remain
at `20rpx`; the audited service-workflow styles are guarded by
`miniprogram/test/miniprogram-typography-floor.test.js`.

| Runtime route | Latest design source | Current QA state | Restored |
| --- | --- | --- | :---: |
| `packages/business/profile-edit/profile-edit` | Existing JoveKore account/profile surface governed by `miniprogram/DESIGN.md` and `design-tokens.json`; professional endorsement is an approved functional-state extension, not a replacement composition | The designer/measurer profile keeps the established account shell and labelled title/year fields, enterprise/lock notices, accessible switches, an internal-count disclosure, and a final customer preview. The career-start-year helper now stacks full-width and left-aligned under the label/input row so the 7-character label stays on one line and lines up with the title and switch rows. Enterprise-controlled and locked states disable only the affected controls. Focused static tests pass; the user deferred `390x844` native-capsule runtime QA and will provide a device screenshot | Yes |
| `packages/business/service-needs/service-needs` | `design-references/customer-workbench-tob-needs-v3.png` | Secondary customer service-needs page implemented in native WXML/Less: capsule-safe brand lock, multi-select service records, empty choice, loading/error recovery, and fixed save action. It is reached only from the approved success-state weak link; it does not alter the service-stage rail. Focused packaging, typography, and route-contract tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/lead-claim-pool/lead-claim-pool` | `design-references/lead-claim-racing-v1/designer-lead-claim-pool-v1.jpg` | User-approved `390x844` capsule-safe claim pool restored with masked lead facts, server-calibrated countdown, capacity state, three-second refresh, and native claim/reminder actions. The approved companion state sheet defines loading, empty, disabled, expired, concurrent-loss, and success behavior without replacing the canonical latest source. Focused page, subscription, navigation, and typography tests pass; authenticated native-capsule host capture remains pending | Yes |
| `pages/index/index` | `docs/superpowers/specs/2026-08-21-customer-service-home-stage-companion-design.md`; `design-references/customer-service-home-dual-free-v1/option-b-dual-benefit-ticket.png` (customer dual-benefit ticket; live dual thumbs only when real floor-plan/scheme media exist), `04b-designer-workbench-dashboard.jpg`, `05b-measurer-workbench-dashboard.jpg`, `18b-enterprise-ops-dashboard-period.jpg` (`18c` custom period sheet) | Customer Service home restores the approved B dual-benefit ticket: one capsule-safe green field leads with「两项服务，全程免费」and the two explicit benefits「免费量房 / 免费设计」; complete Xiao K is the single spatial-service guide; the overlapping white service ticket renders only real floor-plan/scheme thumbnails, one customer-readable `appointmentSummary`, a connected four-step rail, and the server-derived primary action (`book` / `reschedule` / `rebook` / wait / archive). The staggered measurement/design boards reuse packaged laser-meter/easel assets and route through the existing booking, designer-contact, or archive paths; the assurance strip pairs「量房、设计不收费」with a stage-derived truthful status. Empty/early states keep the same structure without fake media, and the multi-project switcher remains `N = length − 1`. An unsigned root-home visit still switches to the JoveKore｜家客来 Mine gateway. Designer/measurer booking on the same lead remains available from staff surfaces. Role workbenches keep the airy-minimalist shell; task-card outline chips (**电话联系** / **导航** / **立即量房** / **继续量房** / **新增量房** / **确认完成量房** / **改期** / **预约上门** / **查看预约**) reuse shared `.sfp-icon-action` with packaged phone, map-pin, ruler, and calendar PNGs; the isolated `role-workbench` also sizes those CTA `<image>` nodes to the shared `28rpx` so WeChat's native 320×240 image default cannot overflow a survey/phone/nav chip; the measurer overview hero is **链接测距仪** with live BLE status and the existing `ble-connector` sheet (formal survey remains task-card entry only); the designer overview no longer shows the static「常用配方」strip below the delivery list (quick-nav「风格配方」to the Design tab remains); enterprise-owner / designer / measurer Operations restore the period-filtered five-card ops dashboard (signed KPI read-only; owner may show签约金额) from `GET /api/miniprogram/workbench?period=`, with hero pills (`待派单`/`待量房`/`待交付`; third pill is unpublished designing-group leads, signed KPI stays on the period dashboard) and exception cards unchanged in meaning. The `18c` custom period sheet docks above `--sfp-custom-tabbar-safe-height` so **取消**/**确定** stay in the visible Tab page (TabBar pages clip `bottom: 0` sheets). Staff/enterprise identity-nav is capsule-safe via runtime `navigationTop`/`navigationRight` but omits scan and bell so lockups such as `家客来 · 设计师端` can sit beside the current enterprise-name tag (the tag is hidden when no enterprise name is present; it no longer repeats 设计师/测量员 or staff name); activity-code and secondary destinations stay on the hero CTA, section links, and TabBar badge. Customer Service home keeps the same capsule-safe lockup and the scan control (claim a service/invite code while already in-app) and omits the bell, which only duplicated the Mine tab. Authenticated `390x844` native-capsule visual QA for the customer stage-companion home and enhanced dashboard states remains pending. The canonical shared v7 designer-contact overlay is restored for this caller; its user-supplied runtime screenshot remains pending | Yes |
| `pages/mine/mine` | `docs/miniprogram-role-shell-design-v1.md`, `design-references/auth/miniprogram-guest-login-jovekore-v1.png` | The signed customer, referrer, and staff states retain the approved profile-card shell and inline account rows. The「当前身份」row keeps that account-card slot and reuses the workbench two-line switch-identity helper「在客户、员工和推荐人身份之间切换」instead of a live identityLabel line. The logged-out state is the capsule-safe JoveKore｜家客来 gateway: it uses the exact packaged `images/home-ip-v1/brand-logo.png`, a single real login CTA, and no business navigation. With no valid signed identity the shared custom TabBar is suppressed rather than falling back to legacy staff tabs; after bootstrap, the existing role/capability navigation returns. 「编辑资料」is an inline「账号」row (packaged `images/mine-icons/edit.png` plus two-line helper) shared by signed staff and customer/referrer shells; the header no longer hosts a compact chip. Focused custom-TabBar regression passes; `390x844` native-capsule host capture remains pending | No |
| `packages/business/login/login` | `design-references/all-pages-ip-v1/06-login.png` | Approved Xiao K door-entry composition unchanged. The agreement row is a larger hit target that toggles the existing 24rpx circle; WeChat `getPhoneNumber` mounts only after `agreed`, so the native button cannot steal the tap or authorize without consent. Unchecked phone CTA toasts「请先勾选同意协议」. 《用户协议》/《隐私政策》/《免责协议》`catchtap` into `legal-webview` and do not toggle the checkbox. The three titles may wrap; type stays `24rpx`. Focused contract tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/legal-webview/legal-webview` | No independent design file; native WeChat `web-view` with the default navigation bar | Guest-reachable document viewer: `onLoad` reads `url` + optional `title`, loads only `https:` into `<web-view>`, and sets the nav title. Login legal links open `https://smartfloor.zlyun168.com/user-agreement.html`, `https://smartfloor.zlyun168.com/privacy-policy.html`, and `https://smartfloor.zlyun168.com/disclaimer.html`. The domain must be in the Mini Program business-domain whitelist. Focused contract tests pass | Yes |
| `pages/leads-management/leads-management` | `docs/miniprogram-role-shell-design-v1.md` | The approved designer/owner customer route remains available; list cards put community on its own row under the phone (two-line clamp) so a long POI name is not squeezed beside the plan thumbnail. The signed measurer state renders own confirmed tasks plus unscheduled work that still needs a formal survey, and completed surveys leave that pending queue. Cold-launch role selection uses the stored signed context so the customer/designer route does not first render the legacy owner shell. Focused navigation tests pass; authenticated `390x844` native-capsule host capture remains pending | No |
| `packages/business/lead-detail/lead-detail` | `design-references/lead-detail-delivery-stack-v1/lead-detail-below-survey-v1.png` (lower stack); formal-survey card uses right-aligned next-action, a full-width address row, and a full-width appointment CTA; Hero profile-edit and dialable phones follow the packaged icon-action convention (user-approved over the prior status∥补充资料 pair) | Standalone appointment card removed. Formal-survey card: tab on the left, **下一步** copy on the right, address on its own row, and **安排上门量房** / rebook / **查看预约** (relabeled **确认完成量房** when `serviceStage` is `survey_ready`) matching the full-width 84rpx conversion CTA. After survey completion the same card keeps house facts (community, area, stored appointment address, closed room names) plus a protected read-only floor-plan PNG preview with **查看大图** (`fetchProtectedImage` + `wx.previewImage`; no `surveying-editor`). Below it, **房屋现场图** uses the shared tag-first grid. Each staff card shows **分配** / **更换** on the right when `assignmentActions` allows it (24rpx brand green); phone stays a separate `.sfp-icon-action` tap. The picker loads `GET /api/leads/[id]/assignable-staff?role=` and submits `POST /api/leads/[id]/assign-staff` (fill or replace; measurers see no action). Scheme card reuses the same stack chrome: designers see **进入 AI 设计** on open unarchived leads even before survey completion (empty title **方案设计**, helper **可用现场图开始出图**; after survey or a completed formal plan, **量房完成，可开始出图**); published rounds still open the read-only folio, row meta shows **定稿** when finalized, and **查看全部方案** / folio entry default to the finalized `schemeId` when present. **查看全部方案** and **进入 AI 设计** share one equal-width row (16rpx gap) when both are visible; a lone CTA stays full-width. History/conversion unchanged below. Hero community is a wrapping block in the 300rpx green copy lane with a light text-shadow so white type does not run into the right-side illustration; stage rail unchanged. Assigned designer/measurer name and phone sit in a two-card strip between the hero and stage rail (`GET /api/leads/[id]` `assignedTo`/`measurerId` staff summaries; 待分配 when unset; a stored phone opens `wx.makePhoneCall`). Hero **补充资料** is an absolute top-right white pill with packaged `images/mine-icons/edit.png` (not paired beside the read-only status pill); customer phone and staff phone rows use `.sfp-icon-action` + `images/leads-v4/phone.png` and dial via `onCallStaff` / `wx.makePhoneCall`; the phone crop punches the white sheet to alpha so the handset sits on the green hero without an opaque square, `.hero-phone` left-aligns with the copy lane (`justify-content: flex-start`), the handset icon sits after the number, and the row keeps the original `手机：` + number copy. Status pill stays text-only. Focused preview/assign/icon-action contract tests pass; native `390x844` host capture remains pending | Yes |
| `packages/business/lead-form/lead-form` | Existing all-pages-ip-v3 lead-form chrome (`packages/business/assets/leads/lead-form.png`); native WeChat navigation (user-requested default back); community field follows appointment-booking map-pick plus manual input | Default nav bar with the native back control; the community row keeps the existing input-shell and adds a 24rpx **选择地点** `.sfp-icon-action` with packaged `images/leads-v4/map-pin.png` that writes `wx.chooseLocation` POI name into editable `communityName` (160-char cap). Map coordinates are not stored on the lead. Focused tests cover default nav and map fill; authenticated `390x844` native-capsule capture pending | Yes |
| `packages/business/settings/settings` | `design-references/miniprogram-airy-minimalist-v1/29-settings.jpg` (content merged into `pages/mine/mine`) | Compatibility deep-link route only; `onLoad`/`onShow` immediately `switchTab` back to Mine and no longer render a standalone settings shell. Visual and interaction source of truth is the inline Mine Tab account block | Yes |
| `packages/business/identity-switch/identity-switch` | `design-references/identity-switch-role-tiles-v1/identity-switch-role-tiles-v1.png` | The user-directed role-tile layout centers only the selected Xiao K; adjacent role previews and the redundant top「选择你的角色」label are removed. The central preview sits above the horizontally scrollable larger vertical role tiles and real confirmation CTA. The packaged non-designer Xiao K PNGs retain the original character pixels but clear their lowermost light ground residue to transparency, eliminating the white splash below the feet on the green stage; each rail icon is `118rpx` in a `148rpx × 178rpx` tile. The disabled current-identity CTA stays distinct with an opaque mint fill, green border, and dark-green text. No composite source slicing and no generated `role-gallery-stage` runtime image remain. One context hides selection controls; two or more retain the scrollable rail. Tapping previews locally; only a non-current identity opens the existing native confirmation and signed-token switch. Loading, failure, expired-session and single-context states remain real. Route/API/permission boundaries unchanged. Focused identity-switch test and source review pass; authenticated `390x844` host capture pending | Yes |
| `pages/ai-design/ai-design` | `design-references/miniprogram-airy-minimalist-v1/37-ai-design-workbench.jpg` | Designer Design tab restores D01: brand header (lockup only; no competing **设计记录** task-log link), green create-scheme hero with credit pill, static non-actionable floor-plan/photo capability descriptions (no arrow or selected state), strict space-category recipe filtering with the existing empty state, and recent design-project cards that open `scheme-studio`; every recipe opens with a compatible input mode. Deep links with `leadId` plus `floorPlanId`/`workflowId` enter scheme-studio. Measurer/enterprise alternate shells unchanged. Focused contract tests pass; authenticated `390x844` native-capsule capture deferred to owner local verification | Yes |
| `packages/ai-workflow/create/ai-design-create` | `design-references/all-pages-ip-v3/14-ai-design-create-v3.png` | Existing restoration | Yes |
| `packages/ai-workflow/recipe-detail/recipe-detail` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Current recipe explanation and input contract are implemented; native capture pending | Yes |
| `packages/ai-workflow/recipe-project/recipe-project` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Picker now follows Admin workbench: customer (`studio/leads`, 可设计/待量房) → scheme conversation (`studio/workflows`, 新建 continues as a new named scheme) → apply-to space (default **完整户型** / `whole_floor_plan`, closed rooms as **单房间** from the scheme-bound floor plan). Scheme cards keep the existing cover slot and show a signed latest confirmed image, otherwise the latest succeeded generation, otherwise the folio placeholder. Existing chrome/cards reused; list unit is no longer formal surveys from `/ai/sources`. Native capture pending | Yes |
| `packages/ai-workflow/recipe-confirm/recipe-confirm` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Confirm loads the chosen scheme via studio workflow detail and continues that conversation; photo input uses 拍照 / 从微信相册选择 / 从本户现场图选择; new captures pick a room tag first then store on the lead gallery and reuse that assetId. After create/run the lead workflow path redirects into `scheme-studio` (isolated tasks still open `ai-design-result`). Native capture pending | Yes |
| `packages/ai-workflow/result/ai-design-result` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Current generation, failure, delivery, and truthful customer-publication controls are implemented; refreshed native capture pending | Yes |
| `packages/ai-workflow/history/ai-design-history` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Page and history APIs remain; the Design tab header no longer opens this route. Isolated-task results still offer **查看历史**. Task filters and truthful task cards remain; synced workflow items expose **进入方案** into `scheme-studio`. Native capture pending | Yes |
| `packages/ai-workflow/scheme-studio/scheme-studio` | `design-references/miniprogram-airy-minimalist-v1/45-ai-scheme-studio.jpg`, `45b-ai-scheme-studio-templates.jpg`; composer follows the user-approved Xiaoyunque bottom-toolbar interaction in 家客来 tokens (scheme chrome from D09 retained) | Mobile-AI composer: collapsed floating dock by default (theme-green `#00c365` credit bar with white copy + one-line prompt + generate FAB kept inside the dock); focus expands the locked **控制图** row, then the prompt, then a non-scrolling bottom toolbar (**户型** / **模型** / **模板** / **设置** + generate FAB); tapping a tool blurs the prompt so the native keyboard hides before the bottom sheet; a selection or mask dismiss restores prompt focus; apply-to uses the **户型** tool plus the「出图设置」row (default **完整户型**, closed rooms as **单房间**, helper copy 只应用到当前选择…) and posts `targetScope`/`roomId` on continuation batches; the composer reference row locks a **控制图** thumbnail of the bound floor-plan preview (room crop when a closed room is selected) matching the first reference the batch uploads; extra user references come from tag-first camera/album capture into the lead gallery or an existing 本户现场图 picker (selected assetId used directly); nav action is **切换方案**; send/update keeps an editable scheme name and writes it to both the workflow and the published album; send/rename/finalize confirm actions wrap native `button` in flex cells so the pair stays inside the sheet with `constant()`/`env()` bottom safe-area padding; **设为定稿** beside the published banner reuses the confirm-dialog pattern (finalized banners read **客户可见定稿**; layout unchanged); project context card and empty-round card keep a 24rpx gutter matching the round list; page scroll padding grows with dock state and a bottom gradient scrim separates dock from white scheme cards. Template sheet keeps category/search and enlarge-before-apply. Authenticated `390x844` capture deferred. Limited: no Admin canvas annotation editor; no dark theme | Yes |
| `packages/surveying/editor/surveying-editor` | `docs/superpowers/specs/2026-08-21-surveying-cursor-reticle-design.md`; Fig.1 green reticle crop (user-approved); prior chrome from `design-references/surveying/cursor-guide-state-reference-20260812.jpg` | Canvas default cursor and dock「重置光标」/「光标拖动到墙体」icons share a hairline Fig.1 green reticle (`drawCursorGlyph` + `icons/cursor-reticle.png`, dock icon 108rpx ≈ canvas 56px span). Both labels sit below the bottom dock container. Dock wall-drop aims that reticle 12×20 CSS px upper-left of the finger (clamped to the canvas); snap, the corner magnifier, and release use the aim point. Dock drag coalesces snap/lens paint onto one canvas animation frame. Canvas wall drags keep a sticky grab delta and a south-east-biased hit and do not apply that dock offset. The drag magnifier overlays a small green `+` at its centre and does not magnify the Fig.1 reticle (formal blit uses `omitCursor`). The “合” control stays Canvas-drawn with pan/pinch; CAD chip contracts unchanged. The top-bar back chevron keeps the same glyph and is an `88rpx` cover-view painted after the centered title overlay so the title cannot steal the tap. Focused renderer/chrome/cursor-aim tests cover the glyph, lens crosshair, dock asset, back hit target, and finger-offset mapping; fresh `390x844` native-capsule capture remains pending | Yes |
| `packages/business/promotion-service-code/promotion-service-code` | `design-references/service-code-redesign/free-design-service-qr-focus-v3.jpg` | Reopened by explicit user approval and restored as one QR-first service credential. Runtime feedback now aligns the protected code frame to the approved comp at `520rpx` (about 69% of the `390px` baseline width) and `508rpx` on `<=360px` narrow screens; the QR group no longer vertically centers inside leftover flex space, so the title-to-code rhythm matches the comp. Xiao K stays below the unobstructed code as a supporting guide, and the three truthful service promises fill the same mint hero. The capsule-safe brand lockup/back contract, anonymous presentation, packaged scan glyphs, loading/error recovery, and existing claim-route share target remain unchanged. The `open-type="share"` CTA says「分享给客户」. Focused contract tests pass; refreshed authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/staff-activity-code/staff-activity-code` | `design-references/service-code-redesign/free-design-service-qr-focus-v3.jpg` | Reuses the approved QR-first credential for designer/measurer activity codes, including the `520rpx` baseline / `508rpx` narrow-screen protected code frame, top-aligned QR rhythm, supporting scan-guide Xiao K, expanded three-promise strip, capsule-safe brand lockup/back behavior, optional enterprise name, and truthful「分享给客户」share CTA. Error/profile recovery keeps the wide clipped QR stage plus native-button width reset; `designer_profile_incomplete` still routes to `profile-edit`. Focused contract tests pass; refreshed authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/enterprise-join-codes/enterprise-join-codes` | `design-references/miniprogram-airy-minimalist-v1/09-promotion-service-code.jpg` | Enterprise-owner dual-code presenter reuses the 09 show template with tabs for Admin staff/referrer join codes; generate/rotate/disable with native confirm (confirmText <=4 chars); view does not rotate. Capsule-safe brand lockup includes the claim-page back chevron (`navigateBack` or `navigateToRoleLanding`). Active codes expose the 09「一键分享」CTA into onboarding. The 请扫码 plaque uses packaged `images/mine-icons/scan.png`. Error-state retry uses the same native-button width reset as the other 09 presenters. Focused contract tests pass; authenticated `390x844` native-capsule capture pending | Yes |
| `packages/business/enterprise-staff/enterprise-staff` | Reuses R03 `20-enterprise-appointments.jpg` card chrome plus claim-page capsule-safe back; Xiao K guide uses packaged `images/airy-v1/xiao-k-measurer-3d.png` | Owner-only deep roster for designer/measurer assignment eligibility: role chips, pause/resume via `GET/PATCH /api/miniprogram/enterprise-staff`, empty CTA to join codes; no create-staff or WeChat/QR edit. Workbench staffing 查看详情 and 人员负荷 navigate here. Status chips flex-center 22rpx labels (`line-height: 1`) so「可派单」/「待补微信资料」sit optically in the pill. Ghost **电话联系** uses `.sfp-icon-action` + `images/leads-v4/phone.png` (solid pause/resume CTA stays text-only). Focused contract tests pass; authenticated `390x844` native-capsule capture pending | Yes |
| `packages/business/free-design-service/free-design-service` | `docs/superpowers/specs/2026-08-25-free-design-service-contact-conversion-design.md`; phone authorization: `design-references/free-design-service-phone-auth-three-benefits-v1/free-design-service-phone-auth-three-benefits-v1.png`; contact dialog: `design-references/designer-contact-sheet/designer-contact-sheet-professional-card-v7.png` | Phone authorization is restored as one capsule-safe three-benefit reading group: `装修问题找微信家装顾问，免费问清楚`, the three approved free-benefit rows, phone privacy note, authorization CTA, and skip action. It removes the acquisition stepper and all visit, surveying, appointment, address, and designer-matching prompts. The standalone `xiao-k-three-benefits.png` is a transparent `560x473` PNG under `300KB`; structure and copy remain native. The first user `1080x2400` screenshot exposed a flex-grown hole; content-intrinsic sizing fixed it. The second screenshot confirmed that flow but showed typography and visible icon bounds one hierarchy step below the source. Production now uses the route-specific `48/36/28/32rpx` title/label/helper/CTA scale plus `124rpx` icon circles and `78–88rpx` glyphs, guarded independently from global minimum floors. The user's subsequent manual tall-device review judged the calibrated restoration essentially matched and accepted the typography/icon scale. Pending, success, existing attribution, APIs, permissions, and contact-dialog behavior are unchanged | Yes |
| `packages/business/onboarding/onboarding` | `design-references/miniprogram-airy-minimalist-v1/16-onboarding.jpg`, `17-onboarding-recovery.jpg`; packaged `packages/business/assets/referral-service-v1/xiao-k-onboarding-welcome-complete.png` | Employee/referrer scan landing restored to design 16: capsule-safe brand lockup with the claim-page back chevron (`navigateBack` or role landing/Mine), green welcome hero with a complete, uncropped door Xiao K, icon role/referrer card, enterprise pill, authorization CTA, and identity-switch entry. Design-16 typography gives the hero, card title, body information, and CTA distinct `44/34/26/32rpx` roles; the hint, CTA, and identity link remain in normal reading flow rather than leaving a viewport-sized gap, and the ready state scrolls on compact screens. Ready/recovery CTAs wrap icon+label in an inner view and reset native `button` width/padding/`nowrap` so ColorOS/OnePlus cannot wrap the last character. Recoverable invalid/disabled/conflict errors open design-17 recovery with door Xiao K crop, green recovery-option icons, scan-new-invite and switch-identity actions with icon CTAs. A later WeChat recents/home reopen with a signed identity leaves to role landing; a fresh QR scan or chat share stays on the form. Source-artwork inspection and focused contract tests pass; WeChat DevTools `390x844` route and full-host capture remain pending | Yes |
| `packages/business/enterprise-register/enterprise-register` | No independent design file; reuses onboarding brand-lock / airy-minimalist tokens from `miniprogram/DESIGN.md` + `design-tokens.json` (no new IP art). Back chevron matches claim/join-code custom nav (`09`/`11`) | Platform `er_` open-account flow: resolve → fill Web-aligned form → one-tap WeChat phone auth that immediately submits → pending-approval success (copy states initial password `123456`). Primary CTA stays disabled until enterprise name, credit code, and contact name are filled; the disabled pill uses `--action-disabled-bg` mint fill so it stays distinct from the `#f8faf9` page (WeChat native `#f7f7f7` is not used); label is **提交开户申请** (no separate authorize-then-submit step). Capsule-safe brand lockup includes the claim-page back chevron (`navigateBack` or role landing/Mine) so a QR stack-root cannot trap the user. A later WeChat recents/home/desktop reopen with any signed identity leaves to role landing; a fresh QR scan or chat-card share still keeps a customer on the form. Ready/success/error/recovery reuse the onboarding identity-link / state-action language for **去登录**; the form link is a native button; `ACCOUNT_CONFLICT` after approval is an already-account exit; phone authorization that is already a workbench identity leaves the form; signed workbench identities leave to role landing with password-login fallback. Focused contract tests pass; authenticated `390x844` native-capsule capture pending | Yes |
| `packages/business/onboarding-debug/onboarding-debug` | Development-only extension of the onboarding route's confirmed `miniprogram/DESIGN.md`, `design-tokens.json`, and `app.less` language; no separate production design source | Development build only. The native scanner selects a local Mini Program code, accepts only the onboarding target path, then opens the normal server-backed resolution, phone authorization, and identity-write flow; the route is locked outside development builds | No |
| `packages/business/referrer-workbench/referrer-workbench` | `design-references/miniprogram-airy-minimalist-v1/06b-promoter-workbench-signed-count.jpg` | Preserves the source composition while improving readability: the identity-nav no longer paints decorative scan/bell icons, so `家客来 · 推广端` keeps the capsule-safe lockup width; business labels are `24–28rpx` and explanatory text is `20–22rpx`. Enterprise pills use natural single-line widths in the source's horizontal selector rather than index-based fixed widths, keeping long names, subsequent memberships, and the add-enterprise action separate. Add enterprise invokes the native scanner and accepts only the existing onboarding path with a code parameter before entering server-validated onboarding. Hero stats now include read-only `已签约` count from progress `stage.key=converted` beside 今日扫码/累计客户/待发放 count (no pending-earnings ¥); no full ops dashboard on this role. The 280rpx「出示推广服务码」CTA wraps icon+label in an inner view with `nowrap` so ColorOS/OnePlus cannot wrap the last character. Identity switch and logout are not on this landing (they remain on Mine); 「退出该企业」stays. Referrer-workbench and role-navigation regression tests pass; milestone status tags flex-center 22rpx labels. Authenticated native `390x844` host capture remains pending because the current DevTools window has no verified automator endpoint | Yes |
| `packages/business/referrer-progress/referrer-progress` | User-authorized Phase-13 extension of `miniprogram/DESIGN.md`, `design-tokens.json`, `app.less`, and the existing green workbench language | Current-membership enterprise name, masked customer label, service stage, and update time, with loading/retry/empty states that expose no phone, precise address, wall graph, internal appointment reason, or design file. WeChat DevTools `390x844` verified the loading state and capsule-safe header; empty/error states retain the same layout contract | Yes |
| `packages/business/referrer-earnings/referrer-earnings` | User-authorized Phase-13 extension of `miniprogram/DESIGN.md`, `design-tokens.json`, `app.less`, and the existing green workbench language | Only the current member's payable/paid **count** summary and each masked customer label plus payment status (no amounts). Payment-status chips flex-center labels. WeChat DevTools `390x844` verified the summary, empty-state CTA, and capsule-safe header | Yes |
| `packages/business/staff-earnings/staff-earnings` | Same Phase-13 earnings language as `referrer-earnings` (`miniprogram/DESIGN.md`, `design-tokens.json`, `app.less`); designer/measurer copy only | Own current-enterprise payable/paid **count** summary and each masked customer label plus payment status for the signed designer or measurer (no amounts). Payment-status chips flex-center labels. Focused TabBar/capability tests pass; authenticated `390x844` native-capsule capture remains pending | No |
| `packages/business/enterprise-commissions/enterprise-commissions` | Same Phase-13 earnings language as `referrer-earnings` / Admin `/lead-commissions` three-card totals; owner payout copy, not personal earnings | Tenant payable/paid/voided totals, lead-grouped role rows, and payable-only mark-paid with native confirm. Amount/beneficiary adjust stays Admin-only. Payment-status chips flex-center labels. Focused TabBar/capability tests pass; authenticated `390x844` native-capsule capture remains pending | No |
| `packages/business/customer-projects/customer-projects` | Compatibility deep-link shell only (product list retired; stage-companion home owns ranking/switcher per `docs/superpowers/specs/2026-08-21-customer-service-home-stage-companion-design.md`) | Not a product project list. On load the route ranks current-JWT customer-owned unarchived projects and redirects to the highest-urgency service archive (`customer-project`) or falls back to the Service tab; no TabBar list UI. The project folio remains the real deep route without the TabBar. Legacy inbound links may still hit this path | Yes |
| `packages/business/customer-project/customer-project` | `design-references/customer-project-archive-redesign-v4/customer-project-archive-balanced-color-v4.png` | V4 is implemented as native WXML/Less: compact single-role Xiao K archive hero, four numbered service stages, a warm-apricot appointment panel using the non-confrontational customer label `预约量房`, mint designer and mist-blue measurer cards, and one continuous three-row archive book with green/amber/blue status semantics. Assigned staff show the user-approved fixed endorsements `金牌设计师 / 7年设计经验 / 已免费服务客户100+` and `资深测量师 / 7年量房经验 / 已免费服务客户100+`; the existing designer contact, staff dialing, appointment booking/detail/reschedule, protected floor-plan preview, tagged site-photo capture, read-only scheme folio, and scheme-poster share flows remain wired to their current data and permission contracts. The first user-supplied tall-device runtime capture exposed legacy substitute icons and loose internal spacing; the route now uses one license-documented rounded-line family for calendar, person, ruler, document, image, delivery-file, phone, and WeChat semantics. Source SVGs live in `docs/icon-sources/customer-project-v4/` and packaged PNGs in `miniprogram/packages/business/assets/customer-project-v4/`. The source-calibrated element ledger is nav `36rpx`, Hero title/helper `44/26rpx`, Xiao K container `118×132rpx`, stage circle/label `38/24rpx`, appointment title/helper/CTA `34/24/28rpx` with an `84rpx` icon container and `60rpx` visible glyph, service heading/role/proof `32/28/24rpx`, person icon container/glyph `72/50–52rpx`, person-card minimum/padding `296/20rpx`, dossier title/helper/status `30/24/24rpx`, dossier-row minimum `110rpx`, dossier icon container/glyph `72/52rpx`, and footer label/contact icon `28/34rpx`. Focused source/template tests pass; authenticated `390x844` native-capsule and revised tall-device optical checks remain pending the user's manual runtime screenshots | Yes |
| `packages/business/customer-ai-schemes/customer-ai-schemes` | `design-references/miniprogram-airy-minimalist-v1/44-customer-ai-scheme-timeline.jpg` (D08) | Read-only customer AI scheme folio restored from D08: capsule-safe custom nav, customer-summary hero with house-block Xiao K (`project-delivery-xiao-k.png`, not the robot stand-in), scheme-round chips (finalized round prefixed **定稿**), current-round hero preview and delivery timeline, footer back/(customer) save-share; without a `schemeId` deep link the default selection is the finalized album, otherwise the last round; customer share opens `scheme-share-poster` (current-round cover + scheme title + 家客来 brand, no QR), album save then image share via `wx.showShareImageMenu`, with capsule page-forward hidden—no mini-program card share; non-owner poster entrances reuse this D08 folio via `GET /api/miniprogram/published-scheme-folios/[leadId]` without the save/share CTA; staff mode still omits the share CTA; no generation or edit actions. Entries come from lead detail (`mode=staff`) and the customer project archive (`mode=customer`), reusing `GET /api/leads/[id].publishedSchemes` and the customer-project aggregate. Focused contract tests pass; authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/appointment-detail/appointment-detail` | `docs/superpowers/specs/2026-08-21-appointment-detail-inline-reschedule-design.md`; `design-references/miniprogram-airy-minimalist-v1/03-appointment-reschedule.jpg`; secondary staff row follows `07-measurer-calendar.jpg` card-button language; sticky bar matches customer-project `footer-bar` full-bleed shell | Detail hosts inline availability picker (5-day window + slots) when `canReschedule`; sticky cancel|confirm sits in a full-bleed frosted page-tone parent (safe-area aware) with ~0.9∶1.3 flex + 20rpx gap and shared 26rpx centered labels; disabled confirm keeps `--action-disabled-bg` against the `#f8faf9` bar; page `padding-bottom: 200rpx` clears the bar; address∥navigate secondary row is equal-width pills with 16rpx gap and packaged edit/map-pin icons (no button emoji); staff **拍现场图** is the same card-button language and opens tag-first capture into the lead gallery; **开始量房** uses `images/leads-v4/ruler-green.png`. Focused tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/appointment-reschedule/appointment-reschedule` | Compat redirect shell for `03` / detail inline reschedule | No standalone UI; `onLoad` `redirectTo` `appointment-detail` with mode mapping (`internal` → staff detail; absent/`customer` → `mode=customer`). Callers prefer detail directly. Focused shell tests pass | Yes |
| `packages/business/appointment-booking/appointment-booking` | `design-references/referrer-network-appointment-v1/phase-5-designer-appointment-booking-v1.png` | Keeps the approved booking structure while splitting the prior address row into native map-place selection and manual building/unit/room detail. **选择地点** / **重新选择** uses `.sfp-icon-action` + `images/leads-v4/map-pin.png`. Owning customers and assigned designers submit the selected real slot, service address, and optional GCJ-02 point; focused tests pass. Refreshed `390x844` designer/customer captures remain pending | Yes |
| `packages/business/measurer-calendar/measurer-calendar` | `design-references/miniprogram-airy-minimalist-v1/07-measurer-calendar.jpg` | Restored matching the airy minimalist measurer calendar: capsule-safe top navigation with month switcher, lightweight overview card with schedule metrics and 3D Xiao K partner, 7-day horizontal calendar strip with task indicator dots, structured appointment cards with quick call/navigation/survey actions using packaged phone/map-pin/ruler icons (no button emoji), and quick unavailability shortcut. Appointment status tags and hero stat pills flex-center labels. All tests pass | Yes |
| `packages/business/measurer-unavailability/measurer-unavailability` | `design-references/miniprogram-airy-minimalist-v1/33-measurer-unavailability.jpg` | Restored matching the airy minimalist unavailability editor: smart guide notice card, touch-friendly date picker box, segmented time slot selector (all-day/morning/afternoon/custom), preset high-frequency reason tags, refined input with char counter, prominent green save CTA, and active schedule cards with status dot and delete action. All tests pass | Yes |
| `packages/business/enterprise-appointments/enterprise-appointments` | `design-references/miniprogram-airy-minimalist-v1/20-enterprise-appointments.jpg` | Enterprise-owner Appointments tab restored from R03: capsule-safe “预约调度中心” header with real week count, rolling 7-day strip, and `confirmed`/`expired` left-accent cards; open leads with expired appointments keep “需协调改期” + view CTA into appointment detail, while `converted`/`closed` leads render a read-only “已签约”/“已关闭” badge with no CTA or detail navigation. No invented dispatch actions and no area/layout/measurer-phone fields absent from the workbench item. Card status chips flex-center 22rpx labels (`line-height: 1`). Focused contract tests pass; authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/promotion-records/promotion-records` | Existing enterprise promotion-records chrome with packaged `packages/business/assets/promotion-records/hero-scene.jpg` (not a new airy salesperson home); channel-salesperson TabBar `报备/我的` reuses `tab-home` / `tab-mine` | Channel salesperson bootstrap lands here; page embeds custom TabBar. Focused identity/TabBar/Mine regression tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |

The approved success-state refinement for `packages/business/free-design-service/free-design-service` is recorded in `design-references/customer-workbench-tob-service-needs-entry-v3-final.png`: keep the existing service-established composition, remove `稍后再看`, and place `有其他服务需求？补充一下 ›` as the only secondary action below the primary archive CTA. The phone-authorisation and pending states remain governed by the route source above.

## Recording rules

- Use the normalized route from `miniprogram/app.json` as the unique key.
- Record the design mapping and one concise current QA result.
- Keep screenshots, metrics, and test logs in local evidence directories, not in
  this canonical ledger.
- Update the English and Chinese ledgers together when a visual restoration changes.

Chinese mirror: [miniprogram-design-restoration-ledger.zh-CN.md](./miniprogram-design-restoration-ledger.zh-CN.md)
