# Mini Program Design Restoration Ledger

This file is the canonical cross-session lookup for Mini Program visual
restorations. Read it before changing visible Mini Program UI.

## Recording rules

- Use the normalized runtime route from `miniprogram/app.json` as the unique
  key and keep exactly one current row per route.
- `Latest design source` contains exactly one current approved design file. A
  newer approved source replaces the older entry instead of creating history.
- Record the design-source mapping and route-level visual-QA status. HTML
  prototypes and similarity scores are optional historical evidence only while
  the HTML-first gate is suspended.
- Set `Mini Program restored` to `Yes` only after the production WXML/WXSS/JS
  or packaged assets have actually been updated.
- Update this ledger and its Chinese mirror when a visual restoration changes.

## Current ledger

Surveying editor QA correction (2026-08-14): the current production renderer is
`degree-aware-branch-far-face-v13`. The wall-local union correction remains in
place; selected-wall fill uses square caps clipped to the actual adjacent-wall
solid footprint, and a newly drawn T branch now starts at the source wall's far
face instead of painting its light body through the source wall.
The user-approved `d4920bfd0ddb84a810c2109b344016d1.jpg` and
`f275da8992b3901e9165f4d13d28c81e.jpg` states are the local selection reference.
The later `3004eb002d311652998850a69781cf2d.jpg` is the branch-origin reference.
The preceding T/corner disappearance was upstream
of Canvas painting: absolute `0.01px` vertex snapping changed rounding phase
after a pan and rebuilt the same walls into different union rings. Union
canonicalization now runs relative to each wall group's first vertex and then
restores Canvas coordinates. Formal and gesture rings are therefore invariant
under pure translation without changing the approved UI or paint order. A
failing-first regression covers seven T/cross/delete scenes at three sub-pixel
and large translations; the focused solid/renderer/topology/visual/viewport/
  editor run passes `92/92`, and the regenerated seven-scene atlas passes manual
  fill/outline inspection. The two-wall fractional-offset regression also keeps
  the corner as one six-point L ring, including a `0.004px` projected endpoint
  discrepancy. The full Mini Program suite passes `332/335`; the three
existing acquisition, local API-port, and offline-debug-default failures are
unrelated. Native iPhone 13 Pro `390x844` confirmation remains
pending because the existing DevTools window has no compatible Automator
endpoint. This correction supersedes the earlier renderer-revision conclusion
inside the surveying route row below; its design source and restoration status
remain unchanged.

| Runtime route | Latest design source | Visual-QA status | Mini Program restored | Updated |
| --- | --- | --- | :---: | --- |
| `pages/index/index` | `design-references/all-pages-ip-v1/01-home-v2.png` | Existing restoration; route-level evidence to be refreshed on the next visual change | Yes | 2026-08-06 |
| `packages/business/lead-detail/lead-detail` | `design-references/all-pages-ip-v3/08-lead-detail-v3.png` | Retains the restored customer profile, four-stage rail, acquisition collaboration, and formal-surveying structure. A server-permission-driven conversion area now sits below the rail with safe-area bottom confirmation sheets for enterprise-admin/assigned-designer conversion and manager-only reasoned reversion. Native readable controls expose signing date, amount, note, and the explicit boundary that no order, charge, or acquisition commission is created; no decorative asset was added. Focused conversion, detail-layout, and V3-restoration static tests pass. The existing WeChat DevTools window is confirmed to have this repository's `miniprogram` open, but it exposes no `autoPort`/compatible Automator endpoint. No duplicate window was opened; a fresh compile, exact top-route check, and native-capsule `390x844` capture remain pending. | Yes | 2026-08-14 |
| `packages/business/settings/settings` | `design-references/account/settings-v1.png` | The approved single-row notification layout, spacing, icon, and hierarchy are unchanged. Its existing trailing status now represents all four V2 templates as fully allowed, partially allowed, rejected, disabled, unset, or temporarily unavailable; static settings and aggregate-authorization tests pass. A fresh compile, exact top-route check, and native-capsule `390x844` capture in the existing WeChat DevTools window remain pending. | Yes | 2026-08-12 |
| `pages/ai-design/ai-design` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Replaced the old four-stage project workbench with the user-approved spatial-recipe folio: native floor-plan/photo entry, search, space categories, featured recipes, and a same-top full-image two-column waterfall. Published prompt-template records drive recipe data, while prompts, models, and internal workflow IDs are never returned to the client. On 2026-08-14, real-device overlap evidence moved Design History and credits from absolute positioning into a two-column `88rpx` toolbar in normal flow below the capsule, with the Hero following it. Static route, asset, structure, and functional regressions pass; recompilation in the existing DevTools window and the native-capsule `390x844` capture remain pending. | Yes | 2026-08-14 |
| `packages/ai-workflow/create/ai-design-create` | `design-references/all-pages-ip-v3/14-ai-design-create-v3.png` | Existing restoration; HTML evidence is optional historical evidence | Yes | 2026-08-11 |
| `packages/ai-workflow/recipe-detail/recipe-detail` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Uses the approved warm-white, deep-green, full-image folio language for recipe explanation, input requirements, whole-plan/room support, and real credits without technical controls. Static regressions pass; native `390x844` route capture is pending. | Yes | 2026-08-14 |
| `packages/ai-workflow/recipe-project/recipe-project` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Project and scope selection share one continuous route without a four-step rail. Cards expose only real formal-survey eligibility, customer/community identity, and closed rooms; incomplete projects can only continue surveying. Static regressions pass; native `390x844` route capture is pending. | Yes | 2026-08-14 |
| `packages/ai-workflow/recipe-confirm/recipe-confirm` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | The single-screen confirmation also hosts photo upload/existing-customer-result choice and the workflow-choice sheet only when automatic disambiguation is impossible; internal IDs remain hidden. Static regressions pass; native `390x844` route capture is pending. | Yes | 2026-08-14 |
| `packages/ai-workflow/result/ai-design-result` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Generation, failure, and delivery states now use the folio visual language. Recipe tasks show only real queue/provider states rather than inferred percentages; delivery keeps comparison, save, share, recipe continuation, and history while adding customer project, space, recipe, and actual credits. Static regressions pass; native `390x844` route capture is pending. | Yes | 2026-08-14 |
| `packages/ai-workflow/history/ai-design-history` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | My Designs uses the same deep-green folio header and native task cards. All/processing/completed/failed filters remain, and each item exposes customer project, space, recipe, status, time, and real credits. Static regressions pass; native `390x844` route capture is pending. | Yes | 2026-08-14 |
| `packages/surveying/editor/surveying-editor` | `design-references/surveying/cursor-guide-state-reference-20260812.jpg` | The latest six-state reference board defines guide semantics: no blue dashed guide appears in the initial state or before the first wall commit; while another wall is dragged, the active marker and preview follow the pointer but the blue crosshair stays at the previous committed point and advances only after commit. Free dragging produces no orange guide. Wall, vertex, axis-alignment, and closure states render only their constrained axis/path as an orange dashed guide, and transient guides clear on release, cancellation, reset, undo/redo, or state change. The approved three-state bottom cursor dock and separate helper copy remain unchanged; panning, pinch zooming, and opening movement still do not show the lens. The upper-left lens is now a single Canvas panel, without a second native background container. When dragging the current cursor inside the Canvas, the formal Canvas remains the sole cursor/guide renderer and the transient layer supplies only that lens; it survives each formal redraw, suppresses the competing upper-left live-measurement bubble, and uses the final formal preview/display point—not the raw finger coordinate—for its centred target and X/Y label. A released outside-edge or outside-vertex snap keeps the stationary cursor on that visible target while graph topology remains centerline-based. Shared-wall deletion and inner-/outer-face adjacent-room rendering retain their previously approved behavior. Layout, cursor marker, walls, dimensions, tools, navigation, APIs, roles, the version-4 graph, and measurement audits are unchanged; focused guide and existing formal-rendering regressions pass. The existing DevTools window is confirmed to have this repository's `miniprogram` open; its log records the changed renderer/editor files, an app-service restart, and `webview page ready`. The window was not started with `autoPort`, so the top page stack and native iPhone 13 Pro `390x844` state captures remain pending; no duplicate window was opened. The 2026-08-13 geometry V1/V2 runtime experiment was subsequently removed because synchronous topology normalization blocked page startup on real devices. The 2026-08-14 behavior source is the user's defect set `6475f5780dc029bfa918dba09954fc4c.jpg`, `99e89446853768a52b11fc60baa8dc59.jpg`, `9e37904e8637f3c7280319a6a911d9b9.jpg`, `e0e4092fae8ff103e29260a78a0aff35.jpg`, `6984d8a41017d3e97394f727a0e52c2c.jpg`, `281f295ee0d54aa908237afea5d3f484.jpg`, and `2ef3a3cd6e3e23ed79ae86aa0f81c5a8.jpg`, plus target states `d4920bfd0ddb84a810c2109b344016d1.jpg` and `f275da8992b3901e9165f4d13d28c81e.jpg`. T-junction source segments retain independent red measurement spans separated by the full branch thickness, but those measurement insets no longer crop the topology-node-based physical wall solid; diagonal near-coincident join vertices are canonicalized, and branch deletion recomputes surviving split-wall insets. Native Canvas wall fill now paints one union-source polygon per call and outlines only classified boundary segments; open walls paint first and completed walls paint over the overlap, while formal and pan frames project the same geometry. Formal scenes now use monotonic revisions, stale page-update callbacks are dropped, redraws arriving during gesture ownership are deferred to the final handoff, and late rect/main/drag-canvas initialization callbacks are generation-guarded. This prevents unchanged intersections from randomly disappearing until a repaint and keeps the completed source wall visually continuous instead of light-notched by an open branch. No WXML/WXSS or control layout changed. The 780-case scenario matrix passes all `12/12` groups, the prior focused suites pass `124/124`, structural/draw-order assertions pass `68/68`, and the expanded renderer/topology/visual/viewport/editor run passes `103/103`; the full Mini Program run is `327/330` with the same three unrelated failures. Seven representative scenes are rendered to `tmp/survey-topology-visual-regression.png`; no full background pixel or internal black cap/seam occurs in the marked junctions, and the runtime-equivalent atlas passes manual inspection. Device confirmation of `native-polygon-frame-owner-v3` ruled out cache and stale callbacks as the remaining cause; fill still consumed independent open/closed unions while the outline consumed the complete union, leaving cross-group T/corner patches outline-only. Formal and gesture frames underpaint the complete union, repaint closed walls, and stroke every classified boundary segment independently. Continued device evidence showed that a complete join patch path could still be skipped; the solid plan now exposes `joinPolygons`, and both frames deterministically repaint them as DPR-aligned device-pixel `fillRect()` scanlines with closed-wall intersections restored last. That scanline experiment is superseded by the Git comparison against stable parent `ad28143`: per-polygon, per-segment, and scanline painting itself caused mixed open/closed junctions to compete for colour ownership, with pan changing the sampled pixels. Current renderer `git-union-ring-restore-v6` restores one classified union-ring fill per non-empty wall colour group and one complete closed union-ring outline, while retaining topology-node solids and frame-generation guards; scanline and independent-patch painting are removed. The focused renderer/solid/visual/viewport run passes `68/68`, and the full Mini Program suite passes `326/329` with the same three unrelated existing failures. The runtime-equivalent seven-scene atlas was regenerated with the restored ring order and manually checked for continuous wall bodies and outlines. The later `wall-local-selection-junction-clip-v11` correction keeps those solids unchanged and clips square-ended selected-wall fill against the actual adjacent-wall footprint; H5 browser interaction verifies ordinary closed corners, T-junction source walls, and both sides of a room partition. The current `wall-local-branch-far-face-v12` revision also starts a newly drawn inset branch body at the source wall's far face; H5 verifies the source wall remains uninterrupted while the prior neutral selection behavior is retained. Native iPhone 13 Pro `390x844` interaction QA remains pending because the existing window has no compatible Automator endpoint. | Yes | 2026-08-14 |
