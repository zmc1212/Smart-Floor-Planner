# Mini Program Design Restoration Ledger

State extension for the existing `packages/business/referrer-progress/referrer-progress` row: production now includes opaque record codes, referrer withdrawal confirmation/countdown, and the existing approved green workbench language is preserved. `role-workbench` and `customer-project` consume the matching muted withdrawal/termination states; runtime visual QA remains pending the user's manual `390x844` screenshot.

This is the current route-to-design lookup. Keep exactly one row per runtime
route and one latest approved design source. Replace a row when the source or
production state changes; do not append restoration history here.

The shared custom TabBar source is `design-references/tabbar-icons/tabbar-earnings-appointment-v1.png`:
Earnings/Commissions use its paired wallet-and-income states, and enterprise-owner Appointments uses
its paired calendar-check states. This shared source applies wherever the capability-allowlisted TabBar renders.

The shared `components/designer-contact-sheet` source is
`design-references/designer-contact-sheet/designer-contact-sheet-xiao-k-bubble-v8-candidate.png` for
`pages/index/index`, `packages/business/free-design-service/free-design-service`, and
`packages/business/customer-project/customer-project`. Its production mapping is the native WXML/Less
QR-first overlay plus `miniprogram/images/designer-contact/xiao-k-peeking.png` and the three
license-documented professional icons under the same runtime directory. Native text in a warm-white,
green-outlined speech bubble now sits to Xiao K's right with its tail pointing back to the mascot; its
`22rpx` lead-in reads `比小红书更方便贴心的` and its `32rpx` emphasis reads `家装顾问`, without
changing the QR stage or contact actions. The root reads the native menu-button bottom on attach and
every open, then keeps the bubble `16rpx` below that capsule boundary; the runtime screenshot that exposed
the prior overlap remains the QA evidence for this correction. When the host page has a visible custom
TabBar, the overlay docks above `--sfp-custom-tabbar-safe-height` so the outside close control is not
covered by the tab lane; pages without a TabBar keep the original bottom inset. The user-supplied
Service-tab screenshot of the clipped close control is the QA evidence for this correction. The relationship pill reads
only `你的专属家装设计顾问` when a visible title exists; the credential heading combines the resolved title and
designer name, while `50rpx` compass and customer-heart glyphs label the experience and service proof.
The source-calibrated element ledger is a `316rpx × 112rpx` speech bubble, `22rpx` bubble lead-in,
`32rpx` bubble emphasis, `24rpx` identity/proof copy, `32rpx` credential heading,
`96rpx` badge container with a `62rpx` glyph, and `50rpx` proof glyphs. Title-hidden states keep the
designer name in the identity pill and collapse only the title heading. Focused component, typography,
packaging, and caller-route tests pass; the refreshed `390x844` native-capsule runtime capture remains
pending the user's manual screenshot.

The current approved source for the enterprise-owner portion of `pages/index/index` is
`design-references/enterprise-owner-activity-code-entry-v3/enterprise-owner-operations-home-v3.png`.
V3 uses equal-width Hero actions, a regular three-column operations path, and two short card-to-card connectors,
and replaces the owner-ambiguous `闭合率` detail with the period-scoped `已发布方案 N 份` value
from `schemeFacts.publishedLeadCount`. Its standalone ImageGen cutouts map to
`miniprogram/images/operations-dashboard/enterprise-guide.png` (17,943 bytes),
`lead-inbox.png` (38,328 bytes), `staff-load.png` (33,567 bytes), `staff-onboarding.png`
(7,996 bytes), `scheme-delivery-rate.png` (13,966 bytes), and `signing-rate.png`
(11,725 bytes); all are transparent PNGs,
independent of the composite design, and under the packaged `300KB` limit. The production mapping
and no-slicing rule apply to the existing single `pages/index/index` row below; this paragraph is the
current source override for its legacy V2 wording. Authenticated `390x844` native-capsule runtime
visual QA remains pending the user's manual screenshot. The latest user-supplied tall-device capture
exposed a duplicated onboarding plus and a full-width rail below the three stages. The V3 correction
uses one `64rpx` generated onboarding cutout, a `26rpx` plain-green kicker, `24rpx` board padding,
`18rpx 18rpx 24rpx` stage-card padding, `44rpx` stage gaps with two card-to-card connectors,
`18rpx 20rpx` efficiency-card padding, `72rpx` generated icon boxes, and no rail or dots below the cards.
The latest tall-device comparison then exposed that the shared flex row was pushing the owner status pills too
low and shrinking the Hero hierarchy. The owner-only mapping now uses a `114rpx` top reserve, `12rpx` pill
offset, absolutely positioned `190rpx` Xiao K box, `136rpx` equal-width white actions, `64rpx` / `72rpx`
action artwork, and `30rpx` / `24rpx` action copy; the narrow-screen guard preserves `60rpx` / `68rpx`
artwork instead of reverting to the earlier undersized icons.
The next runtime capture showed the white-card onboarding asset losing contrast, the staff-load Xiao K reading too
small, and the stage hierarchy under-scaled. `staff-onboarding-white-card-v2.png` is the new built-in-ImageGen
transparent source mapped to the existing production path; staff-load uses `120rpx × 104rpx`, while the stage
cards use a `168rpx` height, `34rpx` ordinal disc, `26rpx` label, and `14rpx` lower inset. The reduced lower inset removes unused space while a two-line detail can still grow intrinsically.

The current workflow typography baseline is verified against the design sources
listed below: page/navigation titles are at least `32rpx`, section titles at
least `28rpx`, business values/actions/body copy at least `24rpx`, and
supporting copy at least `22rpx`. Only non-essential artwork badges may remain
at `20rpx`; the audited service-workflow styles are guarded by
`miniprogram/test/miniprogram-typography-floor.test.js`.

| Runtime route | Latest design source | Current QA state | Restored |
| --- | --- | --- | :---: |
| `packages/business/profile-edit/profile-edit` | Existing JoveKore account/profile surface governed by `miniprogram/DESIGN.md` and `design-tokens.json`; professional endorsement is an approved functional-state extension, not a replacement composition | The designer/measurer profile keeps the established account shell and labelled title/year fields, enterprise/lock notices, accessible switches, an internal-count disclosure, and a final customer preview. The career-start-year helper now stacks full-width and left-aligned under the label/input row so the 7-character label stays on one line and lines up with the title and switch rows. Enterprise-controlled and locked states disable only the affected controls. Focused static tests pass; the user deferred `390x844` native-capsule runtime QA and will provide a device screenshot | Yes |
| `packages/business/service-needs/service-needs` | `design-references/customer-workbench-tob-needs-v3.png` | Secondary customer service-needs page implemented in native WXML/Less: capsule-safe brand lock, multi-select service records, empty choice, loading/error recovery, and fixed save action. It is reached only from the approved success-state weak link; it does not alter the service-stage rail. Focused packaging, typography, and route-contract tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/lead-claim-pool/lead-claim-pool` | `design-references/lead-claim-racing-v1/designer-lead-claim-pool-v1.jpg` | User-approved `390x844` capsule-safe claim pool restored with masked lead facts, server-calibrated countdown, capacity state, three-second refresh, and native claim/reminder actions. The approved companion state sheet defines loading, empty, disabled, expired, concurrent-loss, and success behavior without replacing the canonical latest source. Focused page, subscription, navigation, and typography tests pass; authenticated native-capsule host capture remains pending | Yes |
| `pages/index/index` | `docs/superpowers/specs/2026-08-25-customer-service-home-three-free-design.md`; `design-references/customer-service-home-three-free-v1/customer-service-home-three-free-v1.png` (customer three-free-benefit source), `04b-designer-workbench-dashboard.jpg`, `05b-measurer-workbench-dashboard.jpg`, `design-references/enterprise-owner-activity-code-entry-v3/enterprise-owner-operations-home-v3.png` (enterprise-owner acquisition-entry source; custom period sheet remains `design-references/miniprogram-airy-minimalist-v1/18c-enterprise-ops-dashboard-filter-sheet.jpg`) | Customer Service home restores the approved three-free-benefit design: one capsule-safe green field leads with `三项免费权益` and `三个免费，装修更省心`; complete Xiao K holds the effect-image, design-advisor, and on-site-advisor cards once. The overlapping white service ticket keeps real floor-plan/scheme thumbnails, one customer-readable `appointmentSummary`, the connected four-step rail, and server-derived primary action. Its primary and archive actions share one row when both exist. Three compact native benefit cards read `免费效果图 / 出到客户满意为止`, `免费家装设计顾问 / 解答你的装修问题`, and `免费家装现场顾问 / 解答现场问题`; they route to the current lead's delivered-scheme folio `customer-ai-schemes` (unpublished empty state kept), shared designer contact, and booking/reschedule/archive flows. The assurance strip pairs `三项服务不收费` with a stage-derived truthful status. The whole-page mockup is not sliced: production uses the approved standalone Xiao K plus the route-specific ImageGen-produced `effect-room.jpg`, `design-advisor-3d.png`, and `onsite-advisor-3d.png` assets recorded in the design spec; generic line icons are no longer used for these designed illustrations. Empty/early states keep the same structure without fake media, and the multi-project switcher remains `N = length − 1`. An unsigned root-home visit stays on this route and renders the approved empty/early three-free-benefit companion so the homepage can be browsed before login; phone authorization remains user-initiated on Mine/`login`. The legacy marketing home shell is still not a second logged-out page. Unsigned sessions reuse the customer Service/Mine TabBar. Designer/measurer booking on the same lead remains available from staff surfaces. Role workbenches keep the airy-minimalist shell; task-card outline chips (**电话联系** / **导航** / **立即量房** / **继续量房** / **新增量房** / **确认完成量房** / **改期** / **预约上门** / **查看预约**) reuse shared `.sfp-icon-action` with packaged phone, map-pin, ruler, and calendar PNGs; the isolated `role-workbench` also sizes those CTA `<image>` nodes to the shared `28rpx` so WeChat's native 320×240 image default cannot overflow a survey/phone/nav chip; the measurer overview hero is **链接测距仪** with live BLE status and the existing `ble-connector` sheet (formal survey remains task-card entry only); the designer overview no longer shows the static「常用配方」strip below the delivery list (quick-nav「风格配方」to the Design tab remains); enterprise-owner / designer / measurer Operations restore the period-filtered five-card ops dashboard (signed KPI read-only; owner may show签约金额) from `GET /api/miniprogram/workbench?period=`, with hero pills (`待派单`/`待量房`/`待交付`; third pill is unpublished designing-group leads, signed KPI stays on the period dashboard) and exception cards unchanged in meaning. The `18c` custom period sheet docks above `--sfp-custom-tabbar-safe-height` so **取消**/**确定** stay in the visible Tab page (TabBar pages clip `bottom: 0` sheets). Staff/enterprise identity-nav keeps Logo + `家客来 · 家装设计顾问端` / `家装现场顾问端` / `经营端` left-aligned on the capsule row, with the current enterprise name directly under `家客来`; the two-line stack is packed into the native capsule height so the green Hero is not pushed down (the name is hidden when absent; long names ellipsize on one line); the signed staff name appears above the green Hero title (hidden when absent); scan and bell stay omitted; activity-code and secondary destinations stay on the hero CTA, section links, and TabBar badge; enterprise Ops now follows the approved acquisition-first Hero pair: the white primary 分享活动码 tile with helper 发给客户 · 扫码留资 opens the existing staff-activity presenter, while the white secondary 邀请入驻 tile with helper 员工 · 推荐人 opens the enterprise dual join codes. The Mini Program normalizes the old single join-code payload during staggered deployment so it is never mislabeled as customer acquisition. Customer Service home keeps the same capsule-safe lockup and the scan control (claim a service/invite code while already in-app) and omits the bell, which only duplicated the Mine tab. The user-supplied tall-device screenshot exposed generic benefit-card artwork and a vertically stacked no-media ticket; both have been corrected with route-specific artwork and a copy/rail two-column ticket. A later runtime screenshot showed the three benefit-card arrows sitting off-center in their green circles; production now places the licensed Lucide `chevron-right` PNG (`images/customer-service-three-free/chevron-right.png`) inside the `58rpx` green control instead of a CSS border chevron. The user-supplied `1080x2400` runtime screenshot on 2026-08-26 confirms the enterprise Hero container and the `邀请入驻` styling in the production build. The user-supplied `1080x2400` runtime screenshot on 2026-08-27 confirms the full V2 owner composition in the production build: dual Hero actions, three-stage asymmetric operations path, two efficiency cards, and the priority tray. After that capture, production added three native route nodes for the lead/survey/signing semantics, lowered and softened the background floor-plan linework, and replaced the empty-state dot with the packaged `images/leads-v4/map-pin.png`; focused structural tests remain green. The latest user-supplied tall-device capture confirms that the V3 business cutouts render, and exposes a duplicated onboarding plus, underspaced stages, and an incorrect full-width rail below the cards. Production now uses the ImageGen-produced `staff-onboarding.png` (7,996 bytes), `scheme-delivery-rate.png`, and `signing-rate.png` transparent cutouts, `18rpx 18rpx 24rpx` stage-card padding, `18rpx 20rpx` efficiency-card padding, `44rpx` stage gaps with two card-to-card connectors, and the truthful `方案同步中` fallback while a legacy backend still returns `闭合率`. The owner Hero now decouples Xiao K from flex height: a `190rpx` absolute mascot and `12rpx` pill offset align the status row at the mascot base; both white actions use a `136rpx` minimum height with enlarged artwork and copy. The final two-tile activity-code/invite-code composition remains dependent on the production Workbench returning the new owner payload (`activityCode` plus `joinCode`), and authenticated `390x844` native-capsule visual QA remains pending until the production backend is released and rechecked. The canonical shared v8 designer-contact overlay is restored for this caller and now docks above the custom TabBar so the close control stays tappable; its user-supplied runtime screenshot remains pending | No |
| `packages/guides/enterprise-owner-guide/enterprise-owner-guide` | `design-references/role-onboarding-v1/enterprise-owner-onboarding-v1.png` | Approved three-step native onboarding for `enterprise_admin`: activity-code acquisition, partner onboarding, and operations-priority handling. The three slides are a native non-looping `swiper` (swipe or tap dots to review; 「下一步」 still advances; no autoplay). The route is optional after the first automatic presentation, replayable from Mine, and ends at the existing `staff-activity-code` presenter. Independent ImageGen transparent PNG mappings are `packages/guides/assets/enterprise-owner-v1/{activity-code,team-onboarding,operations-priority}.png` (palette-optimized, each <=300KB); title `32rpx`, slide title `40rpx`, body `24rpx`, assurance `22rpx`, CTA `28rpx`. The custom nav is `border-box` with `navigationRight` inside its width so「跳过」clears the native WeChat capsule. Static packaging/contract checks pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/guides/designer-guide/designer-guide` | `design-references/role-onboarding-v1/designer-advisor-onboarding-v1.png` | Approved three-step native onboarding for `designer`: lead claiming, survey-material handoff, and scheme delivery with earnings record. The three slides are a native non-looping `swiper` (swipe or tap dots to review; 「下一步」 still advances; no autoplay). It presents once on the designer overview after first entry, is replayable from Mine, and ends at the existing `staff-earnings` route. Independent ImageGen transparent PNG mappings are `packages/guides/assets/designer-advisor-v1/{lead-claim,survey-sync,scheme-delivery}.png` (palette-optimized transparent cutouts, each <=300KB; 640×529 / 640×514 / 439×448). The previous fully opaque exports had a baked transparency checkerboard; production keeps a real transparent channel so the mint stage shows through.; title `32rpx`, slide title `40rpx`, body `24rpx`, assurance `22rpx`, CTA `28rpx`. The designer workbench defers the WeChat-profile completion prompt until the guide has been seen. The custom nav is `border-box` with `navigationRight` inside its width so「跳过」clears the native WeChat capsule. Focused guide, packaging, and typography tests pass; authenticated `390x844` native-capsule capture remains pending the user's screenshot of the skip/capsule lane | Yes |
| `packages/guides/measurer-guide/measurer-guide` | `design-references/measurer-onboarding-v1/measurer-onboarding-directions-v1.png` (approved direction B, 现场路径) | Approved three-step native onboarding for `measurer`: connect the laser meter, measure along wall lines, and complete a deliverable survey. The three slides are a native non-looping `swiper` (swipe or tap the step rail to review; 「下一步」 still advances; no autoplay). It presents once on the measurer overview after first entry, is replayable from Mine, and the final CTA `去看今日任务` returns to the existing workbench task list without inventing a new destination. The B-source scenarios map to regenerated high-resolution independent ImageGen transparent PNGs: `packages/guides/assets/measurer-v1/measurement-bench.png` (900×960, 100,280 bytes), `measurement-path.png` (960×640, 64,543 bytes), and `measurement-complete.png` (867×960, 82,506 bytes). They use indexed-palette PNG compression, retain alpha, remain below the 300KB per-artwork limit, and keep the complete mascot/equipment silhouette clear at the existing artwork scale. The full `packages/guides` subpackage is 1,276,739 bytes, below WeChat's 2MB limit. Title `32rpx`, slide title `40rpx`, body `24rpx`, assurance `22rpx`, CTA `28rpx`. The custom nav is `border-box` with `navigationRight` inside its width so「跳过」clears the native WeChat capsule. Focused guide, packaging, and typography tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `pages/mine/mine` | `docs/miniprogram-role-shell-design-v1.md`, `design-references/auth/miniprogram-guest-login-jovekore-v2-full.png` | Signed customer, referrer, and staff states retain the approved profile-card shell, inline account rows, and two-line current-identity helper. The logged-out state keeps the capsule-safe JoveKore｜家客来 lockup, standalone `images/home-ip-v1/login-identity-portal-v2.jpg` doorway scene, native `个人用户 / 员工 / 推荐人` rail, sole `立即登录` CTA, and account-switch trust row. The user's `1080x2400` runtime screenshot exposed cumulative narrow-screen compression, an undersized CTA, small visible identity-glyph bounds, and a CSS pill substituting for the trust shield. Production now keeps the `700rpx` scene, `32rpx` panel margins, and `40rpx` heading at `<=360px`; the transparent-padded Lucide identity PNGs use `70rpx` boxes for about `58rpx` visible bounds; the CTA explicitly fills the panel content width and clears native max-width/horizontal margins; and the trust row reuses `images/mine-icons/shield-check.png`. The approved composite remains unsliced; logo, copy, rail, button, and trust line stay native. Unsigned visitors reuse the customer Service/Mine TabBar so Service home stays browseable and login stays the Mine CTA; bootstrap still restores signed role/capability navigation. The guest gateway docks above `--sfp-custom-tabbar-safe-height` instead of filling `100vh`. The signed account card now exposes a native `Role guide` row when any valid account identity has an approved guide (referrer, enterprise owner, designer, measurer), including while the current identity is customer; a customer-only account still has no automatic display or unapproved placeholder. Tapping opens the current role's guide when one exists, otherwise replays a held work identity. Route/API/permission boundaries are otherwise unchanged. Focused layout, narrow-screen guard, and asset checks pass; revised `390x844` and user-tall native-capsule captures remain pending manual review | No |
| `packages/business/login/login` | `design-references/all-pages-ip-v1/06-login.png` | Approved Xiao K door-entry composition unchanged except WeChat-review copy: the phone-tab title is「手机号授权登录」and the official WeChat logo is replaced by the existing circular phone-authorization PNG so the pre-`getPhoneNumber` surface no longer shows「微信授权登录」or Tencent branding. The green CTA remains「手机号快捷登录」. The agreement row is a larger hit target that toggles the existing 24rpx circle; WeChat `getPhoneNumber` mounts only after `agreed`, so the native button cannot steal the tap or authorize without consent. Unchecked phone CTA toasts「请先勾选同意协议」. 《用户协议》/《隐私政策》`catchtap` into `legal-webview` and do not toggle the checkbox. 《免责协议》 remains resolved and hosted but is hidden on the login row for now. The two titles may wrap; type stays `24rpx`. Focused contract tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/account-security/account-security` | `design-references/miniprogram-airy-minimalist-v1/30-account-security.jpg` | Existing approved account-security layout, typography, assets, and navigation are unchanged. Password-authenticated staff carrying `requiresPasswordChange` receive a one-time native reminder after login, then enter the signed role landing with a working TabBar; **去修改** may `navigateTo` this route, and Mine still opens it at any time. Mini Program APIs are not locked. A successful change clears the server flag, clears local session, and requires login with the new password. WeChat/phone quick login does not enter this state. Focused login/token contract tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/legal-webview/legal-webview` | No independent design file; native WeChat `web-view` with the default navigation bar | Guest-reachable document viewer: `onLoad` reads `url` + optional `title`, loads only `https:` into `<web-view>`, and sets the nav title. Login currently opens `https://smartfloor.zlyun168.com/user-agreement.html` and `https://smartfloor.zlyun168.com/privacy-policy.html`; `disclaimer.html` remains resolvable via `legal-docs.js` but is hidden on the login row for now. The domain must be in the Mini Program business-domain whitelist. Focused contract tests pass | Yes |
| `pages/leads-management/leads-management` | `docs/miniprogram-role-shell-design-v1.md` | The approved designer/owner customer route remains available; list cards put community on its own row under the phone (two-line clamp) so a long POI name is not squeezed beside the plan thumbnail. The signed measurer state renders own confirmed tasks plus unscheduled work that still needs a formal survey, and completed surveys leave that pending queue. Cold-launch role selection uses the stored signed context so the customer/designer route does not first render the legacy owner shell. Focused navigation tests pass; authenticated `390x844` native-capsule host capture remains pending | No |
| `packages/business/lead-detail/lead-detail` | `design-references/lead-detail-delivery-stack-v1/lead-detail-below-survey-v1.png` (lower stack); formal-survey card uses right-aligned next-action, a full-width address row, and appointment/start-survey CTAs that share one equal-width 84rpx pill row when both are visible; Hero profile-edit and dialable phones follow the packaged icon-action convention (user-approved over the prior status∥补充资料 pair) | Standalone appointment card removed. Formal-survey card: tab on the left, **下一步** copy on the right, address on its own row, and **安排上门量房** / rebook / **查看预约** (relabeled **确认完成量房** when `serviceStage` is `survey_ready`) sharing one equal-width 84rpx pill row with **开始量房** / **继续量房** when both are visible (16rpx gap); a lone CTA stays full-width, matching the conversion and published-scheme CTA language. After survey completion the same card keeps house facts (community, area, stored appointment address, closed room names) plus a protected read-only floor-plan PNG preview with **查看大图** (`fetchProtectedImage` + `wx.previewImage`; no `surveying-editor`). Below it, **房屋现场图** uses the shared tag-first grid. Each staff card shows **分配** / **更换** on the right when `assignmentActions` allows it (24rpx brand green); phone stays a separate `.sfp-icon-action` tap. The picker loads `GET /api/leads/[id]/assignable-staff?role=` and submits `POST /api/leads/[id]/assign-staff` (fill or replace; measurers see no action). Scheme card reuses the same stack chrome: designers see **进入 AI 设计** on open unarchived leads even before survey completion (empty title **方案设计**, helper **可用现场图开始出图**; after survey or a completed formal plan, **量房完成，可开始出图**); published rounds still open the read-only folio, row meta shows **定稿** when finalized, and **查看全部方案** / folio entry default to the finalized `schemeId` when present. **查看全部方案** and **进入 AI 设计** share one equal-width row (16rpx gap) when both are visible; a lone CTA stays full-width. History/conversion unchanged below. Hero community is a wrapping block in the 300rpx green copy lane with a light text-shadow so white type does not run into the right-side illustration; stage rail unchanged. Assigned designer/measurer name and phone sit in a two-card strip between the hero and stage rail (`GET /api/leads/[id]` `assignedTo`/`measurerId` staff summaries; 待分配 when unset; a stored phone opens `wx.makePhoneCall`). Hero **补充资料** is an absolute top-right white pill with packaged `images/mine-icons/edit.png` (not paired beside the read-only status pill); customer phone and staff phone rows use `.sfp-icon-action` + `images/leads-v4/phone.png` and dial via `onCallStaff` / `wx.makePhoneCall`; the phone crop punches the white sheet to alpha so the handset sits on the green hero without an opaque square, `.hero-phone` left-aligns with the copy lane (`justify-content: flex-start`), the handset icon sits after the number, and the row keeps the original `手机：` + number copy. Status pill stays text-only. Focused preview/assign/icon-action contract tests pass; native `390x844` host capture remains pending | Yes |
| `packages/business/lead-form/lead-form` | Existing all-pages-ip-v3 lead-form chrome (`packages/business/assets/leads/lead-form.png`); native WeChat navigation (user-requested default back); community field follows appointment-booking map-pick plus manual input | Default nav bar with the native back control; the community row keeps the existing input-shell and adds a 24rpx **选择地点** `.sfp-icon-action` with packaged `images/leads-v4/map-pin.png` that writes `wx.chooseLocation` POI name into editable `communityName` (160-char cap). Map coordinates are not stored on the lead. Focused tests cover default nav and map fill; authenticated `390x844` native-capsule capture pending | Yes |
| `packages/business/settings/settings` | `design-references/miniprogram-airy-minimalist-v1/29-settings.jpg` (content merged into `pages/mine/mine`) | Compatibility deep-link route only; `onLoad`/`onShow` immediately `switchTab` back to Mine and no longer render a standalone settings shell. Visual and interaction source of truth is the inline Mine Tab account block | Yes |
| `packages/business/identity-switch/identity-switch` | `design-references/identity-switch-role-tiles-v1/identity-switch-role-tiles-v1.png` | The user-directed role-tile layout centers only the selected Xiao K; adjacent role previews and the redundant top「选择你的角色」label are removed. The central preview sits above the horizontally scrollable larger vertical role tiles and real confirmation CTA. The packaged non-designer Xiao K PNGs retain the original character pixels but clear their lowermost light ground residue to transparency, eliminating the white splash below the feet on the green stage; each rail icon is `118rpx` in a `148rpx × 178rpx` tile. The disabled current-identity CTA stays distinct with an opaque mint fill, green border, and dark-green text. No composite source slicing and no generated `role-gallery-stage` runtime image remain. One context hides selection controls; two or more retain the scrollable rail. Tapping previews locally; only a non-current identity opens the existing native confirmation and signed-token switch. Loading, failure, expired-session and single-context states remain real. Route/API/permission boundaries unchanged. Focused identity-switch test and source review pass; authenticated `390x844` host capture pending | Yes |
| `pages/ai-design/ai-design` | `design-references/miniprogram-airy-minimalist-v1/37-ai-design-workbench.jpg` | Designer Design tab restores D01: brand header (lockup only; no competing **设计记录** task-log link), green create-scheme hero with credit pill, static non-actionable floor-plan/photo capability descriptions (no arrow or selected state), strict space-category recipe filtering with the existing empty state, and recent design-project cards that open `scheme-studio`; every recipe opens with a compatible input mode. Deep links with `leadId` plus `floorPlanId`/`workflowId` enter scheme-studio. Measurer/enterprise alternate shells unchanged. Focused contract tests pass; authenticated `390x844` native-capsule capture deferred to owner local verification | Yes |
| `packages/ai-workflow/create/ai-design-create` | `design-references/all-pages-ip-v3/14-ai-design-create-v3.png` | Existing restoration | Yes |
| `packages/ai-workflow/recipe-detail/recipe-detail` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Current recipe explanation and input contract are implemented; native capture pending | Yes |
| `packages/ai-workflow/recipe-project/recipe-project` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Picker now follows Admin workbench: customer (`studio/leads`, 可设计/待量房) → scheme conversation (`studio/workflows`, 新建 continues as a new named scheme) → apply-to space (default **完整户型** / `whole_floor_plan`, closed rooms as **单房间** from the scheme-bound floor plan). Scheme cards keep the existing cover slot and show a signed latest confirmed image, otherwise the latest succeeded generation, otherwise the folio placeholder. Existing chrome/cards reused; list unit is no longer formal surveys from `/ai/sources`. Native capture pending | Yes |
| `packages/ai-workflow/recipe-confirm/recipe-confirm` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Confirm loads the chosen scheme via studio workflow detail and continues that conversation; photo input uses 拍照 / 从微信相册选择 / 从本户现场图选择; new captures pick a room tag first then store on the lead gallery and reuse that assetId. After create/run the lead workflow path redirects into `scheme-studio` (isolated tasks still open `ai-design-result`). Native capture pending | Yes |
| `packages/ai-workflow/result/ai-design-result` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Current generation, failure, delivery, and truthful customer-publication controls are implemented; refreshed native capture pending | Yes |
| `packages/ai-workflow/history/ai-design-history` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Page and history APIs remain; the Design tab header no longer opens this route. Isolated-task results still offer **查看历史**. Task filters and truthful task cards remain; synced workflow items expose **进入方案** into `scheme-studio`. Native capture pending | Yes |
| `packages/ai-workflow/scheme-studio/scheme-studio` | `design-references/miniprogram-airy-minimalist-v1/45-ai-scheme-studio.jpg`, `45b-ai-scheme-studio-templates.jpg`; composer follows the user-approved Xiaoyunque bottom-toolbar interaction in 家客来 tokens (scheme chrome from D09 retained) | Mobile-AI composer: collapsed floating dock by default (theme-green `#00c365` credit bar with white copy + one-line prompt + generate FAB kept inside the dock); focus expands the locked **控制图** row, then the prompt, then a non-scrolling bottom toolbar (**户型** / **模型** / **模板** / **设置** + generate FAB); tapping a tool blurs the prompt so the native keyboard hides before the bottom sheet; a selection or mask dismiss restores prompt focus; apply-to uses the **户型** tool plus the「出图设置」row (default **完整户型**, closed rooms as **单房间**, helper copy 只应用到当前选择…) and posts `targetScope`/`roomId` on continuation batches; the composer reference row locks a **控制图** thumbnail of the bound floor-plan preview (room crop when a closed room is selected) matching the first reference the batch uploads; extra user references come from tag-first camera/album capture into the lead gallery or an existing 本户现场图 picker (selected assetId used directly); nav action is **切换方案**; send/update keeps an editable scheme name and writes it to both the workflow and the published album; send/rename/finalize confirm actions wrap native `button` in flex cells so the pair stays inside the sheet with `constant()`/`env()` bottom safe-area padding; **设为定稿** beside the published banner reuses the confirm-dialog pattern (finalized banners read **客户可见定稿**; layout unchanged); project context card and empty-round card keep a 24rpx gutter matching the round list; page scroll padding grows with dock state and a bottom gradient scrim separates dock from white scheme cards. Template sheet keeps category/search and enlarge-before-apply. Authenticated `390x844` capture deferred. Limited: no Admin canvas annotation editor; no dark theme | Yes |
| `packages/surveying/editor/surveying-editor` | `docs/superpowers/specs/2026-08-21-surveying-cursor-reticle-design.md`; Fig.1 green reticle crop (user-approved); prior chrome from `design-references/surveying/cursor-guide-state-reference-20260812.jpg` | Canvas default cursor and dock「重置光标」/「光标拖动到墙体」icons share a hairline Fig.1 green reticle (`drawCursorGlyph` + `icons/cursor-reticle.png`, dock icon 108rpx ≈ canvas 56px span). Both labels sit below the bottom dock container. Dock wall-drop aims that reticle 24×40 CSS px upper-left of the finger (clamped to the canvas); snap, the corner magnifier, and release use the aim point. Dock drag throttles touchmove to 24 ms, skips snap on free-follow frames, and dirty-clears the reticle on free placement. Canvas wall drags keep a sticky grab delta and a south-east-biased hit and do not apply that dock offset. The drag magnifier overlays a small green `+` at its centre and does not magnify the Fig.1 reticle (formal blit uses `omitCursor`). The “合” control stays Canvas-drawn with pan/pinch; CAD chip contracts unchanged. The top-bar back chevron keeps the same glyph and is an `88rpx` cover-view painted after the centered title overlay so the title cannot steal the tap. Focused renderer/chrome/cursor-aim tests cover the glyph, lens crosshair, dock asset, back hit target, and finger-offset mapping; fresh `390x844` native-capsule capture remains pending | Yes |
| `packages/business/promotion-service-code/promotion-service-code` | `design-references/service-code-redesign/free-design-service-qr-focus-v3.jpg` | Reopened by explicit user approval and restored as one QR-first service credential. Runtime feedback now aligns the protected code frame to the approved comp at `520rpx` (about 69% of the `390px` baseline width) and `508rpx` on `<=360px` narrow screens; the QR group no longer vertically centers inside leftover flex space, so the title-to-code rhythm matches the comp. Xiao K stays below the unobstructed code as a supporting guide, and the three truthful service promises fill the same mint hero. The capsule-safe brand lockup/back contract, anonymous presentation, packaged scan glyphs, loading/error recovery, and existing claim-route share target remain unchanged. The `open-type="share"` CTA says「分享给客户」. Focused contract tests pass; refreshed authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/staff-activity-code/staff-activity-code` | `design-references/enterprise-code-presenters-fullscreen-v1/staff-activity-code-fullscreen-v1.png` | Activity-code presenter is restored as one intrinsic mint poster: large title/subtitle, QR frame, overlapping scan-guide Xiao K, caption, white benefits, safety note, and share CTA. At `390x844`: nav `36rpx`; title/subtitle `64/30rpx`; QR frame/image `478/450rpx`; Xiao K `260rpx`; caption `30rpx`; benefit icon/title/helper `92/26/22rpx`; trust copy `24rpx`; CTA `96rpx` high with `30rpx` label. Independent transparent ImageGen PNGs map to `packages/business/assets/code-presenter-v3/{xiao-k-scan-presenter,home-outline,advisor-outline}.png`; native `¥0` badge and V2 scan glyph remain. All generated files are alpha PNGs below `300KB`. Capsule-safe lockup/back, enterprise name, share target, and profile recovery are unchanged. Focused contract/package checks pass; refreshed authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/enterprise-join-codes/enterprise-join-codes` | `design-references/enterprise-code-presenters-fullscreen-v1/enterprise-join-codes-fullscreen-v1.png` | Enterprise dual-code presenter matches the split composition: tabs and an intrinsic mint QR card, then a white-page journey rail, safety note, and share/manage actions. At `390x844`: nav/tab `36/32rpx`; title/subtitle `56/28rpx`; QR frame/image `380/352rpx`; Xiao K `188rpx`; caption `28rpx`; journey icon/title/helper `92/26/22rpx`; trust copy `24rpx`; actions `96rpx` high with `30rpx` labels. Independent transparent ImageGen PNGs map to `packages/business/assets/code-presenter-v3/{xiao-k-scan-presenter,scan-journey,role-journey,start-journey}.png`; V2 scan glyph remains for plaque/share. Confirmations, no-rotate viewing, capsule-safe back, onboarding share target, API and role boundaries remain unchanged. Focused contract/package checks pass; refreshed authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/enterprise-staff/enterprise-staff` | Reuses R03 `20-enterprise-appointments.jpg` card chrome plus claim-page capsule-safe back; Xiao K guide uses packaged `images/airy-v1/xiao-k-measurer-3d.png` | Owner-only deep roster for designer/measurer assignment eligibility: role chips, pause/resume via `GET/PATCH /api/miniprogram/enterprise-staff`, empty CTA to join codes; no create-staff or WeChat/QR edit. Workbench staffing 查看详情 and 人员负荷 navigate here. Status chips flex-center 22rpx labels (`line-height: 1`) so「可派单」/「待补微信资料」sit optically in the pill. Ghost **电话联系** uses `.sfp-icon-action` + `images/leads-v4/phone.png` (solid pause/resume CTA stays text-only). Focused contract tests pass; authenticated `390x844` native-capsule capture pending | Yes |
| `packages/business/free-design-service/free-design-service` | `docs/superpowers/specs/2026-08-25-free-design-service-contact-conversion-design.md` | Phone authorization retains its content-intrinsic three-benefit state. Success remains archive-first with native `服务进度 / 户型档案 / 设计方案`, the sole filled `查看服务档案`, real `professionalProfile` proof, and a fixed `148rpx` supporting WeChat action. The user has now approved existing-attribution option C at `design-references/free-design-service-existing-redesign-v2/option-c-continuity-archive-drawer.png`. Production restores it in native WXML/Less with `已为你接续原有服务`, truthful stage/update facts, a three-stop path, three tappable folder tabs, the only filled `继续查看服务档案`, compact real designer proof, and the preservation assurance; the former second full-width contact CTA is removed. `现场顾问`/`上门` labels map to the surveying-arrangement stage. The drawer Xiao K maps to the transparent `720×346`, `25733`-byte indexed-colour `xiao-k-continuity-archive-drawer.png`; all copy, progress, and controls remain native. Existing-state ledger: Hero `48/28rpx`, preservation/service reference `24/22rpx`, stage label/value/update `22/36/22rpx`, stage circle/copy `46/24rpx`, drawer stage `318rpx`, folder labels `24rpx`, CTA `100/32rpx`, designer `32/24/22/24rpx`, proof `22/32rpx`. Focused drawer, transparent-PNG, typography, and package checks pass; revised tall-device and `390x844` native-capsule captures await manual review | Yes |
| `packages/business/onboarding/onboarding` | `design-references/miniprogram-airy-minimalist-v1/16-onboarding.jpg`, `17-onboarding-recovery.jpg`; packaged `packages/business/assets/referral-service-v1/xiao-k-onboarding-welcome-complete.png` | Employee/referrer scan landing restored to design 16: capsule-safe brand lockup with the claim-page back chevron (`navigateBack` or role landing/Mine), green welcome hero with a complete, uncropped door Xiao K, icon role/referrer card, enterprise pill, authorization CTA, and identity-switch entry. Design-16 typography gives the hero, card title, body information, and CTA distinct `44/34/26/32rpx` roles; the hint, CTA, and identity link remain in normal reading flow rather than leaving a viewport-sized gap, and the ready state scrolls on compact screens. Ready/recovery CTAs wrap icon+label in an inner view and reset native `button` width/padding/`nowrap` so ColorOS/OnePlus cannot wrap the last character. Recoverable invalid/disabled/conflict/limit errors open design-17 recovery with door Xiao K crop, green recovery-option icons, scan-new-invite and switch-identity actions with icon CTAs. The recovery subtitle binds server `errorMessage` (`membership_limit_reached` / `referrer_protection_limit` and other recovery codes) and keeps the designed fallback copy when empty; layout, assets, and CTAs are unchanged. A later WeChat recents/home reopen with a signed identity leaves to role landing; a fresh QR scan or chat share stays on the form. Source-artwork inspection and focused contract tests pass; WeChat DevTools `390x844` route and full-host capture remain pending | Yes |
| `packages/business/enterprise-register/enterprise-register` | No independent design file; reuses onboarding brand-lock / airy-minimalist tokens from `miniprogram/DESIGN.md` + `design-tokens.json` (no new IP art). Back chevron matches claim/join-code custom nav (`09`/`11`) | Platform `er_` open-account flow: resolve → fill Web-aligned form → editable contact-phone field → tap **授权手机号并提交** to invoke WeChat phone auth and immediately submit. Manual entry is supported for readability/convenience but remains subject to the API rule that it must match the authorized WeChat phone; inline helper copy explains this and the post-auth auto-submit behavior. Primary CTA keeps the mint `--action-disabled-bg` fill while enterprise name, credit code, or contact name is empty, but stays tappable so it can list `还需填写：…` and highlight those inputs with inline `请填写…` copy (WeChat `getPhoneNumber` is bound only after the three fields are filled; native `#f7f7f7` is not used). Capsule-safe brand lockup includes the claim-page back chevron (`navigateBack` or role landing/Mine) so a QR stack-root cannot trap the user. A later WeChat recents/home/desktop reopen with any signed identity leaves to role landing; a fresh QR scan or chat-card share keeps any signed identity, including workbench, on the form. Ready/success/error/recovery reuse the onboarding identity-link / state-action language for **去登录**; the form link is a native button; `ACCOUNT_CONFLICT` after approval is an already-account exit; phone authorization that is already a workbench identity leaves the form. Focused contract tests pass; authenticated `390x844` native-capsule capture pending | Yes |
| `packages/business/onboarding-debug/onboarding-debug` | Development-only extension of the onboarding route's confirmed `miniprogram/DESIGN.md`, `design-tokens.json`, and `app.less` language; no separate production design source | Development build only. The native scanner selects a local Mini Program code, accepts only the onboarding target path, then opens the normal server-backed resolution, phone authorization, and identity-write flow; the route is locked outside development builds | No |
| `packages/business/referrer-workbench/referrer-workbench` | `design-references/miniprogram-airy-minimalist-v1/06b-promoter-workbench-signed-count.jpg` | Preserves the source composition while improving readability: the identity-nav no longer paints decorative scan/bell icons, so `家客来 · 推广端` keeps the capsule-safe lockup width; the 52rpx brand mark uses production `/images/home-ip-v1/brand-logo.png` (house + JK lockup shared with customer/staff workbenches) instead of the compositor's green `JK` text squircle; nav title is `32rpx`; business labels are `24–28rpx` and explanatory text is `20–22rpx`. Enterprise pills use natural single-line widths in the source's horizontal selector rather than index-based fixed widths, keeping long names, subsequent memberships, and the add-enterprise action separate. Add enterprise invokes the native scanner and accepts only the existing onboarding path with a code parameter before entering server-validated onboarding. Hero stats now include read-only `已签约` count from progress `stage.key=converted` beside 今日扫码/累计客户/待发放 count (no pending-earnings ¥); no full ops dashboard on this role. The 280rpx「出示推广服务码」CTA wraps icon+label in an inner view with `nowrap` so ColorOS/OnePlus cannot wrap the last character. Identity switch and logout are not on this landing (they remain on Mine); 「退出该企业」stays. Referrer-workbench and role-navigation regression tests pass; milestone status tags flex-center 22rpx labels. Authenticated native `390x844` host capture remains pending because the current DevTools window has no verified automator endpoint | Yes |
| `packages/guides/referrer-guide/referrer-guide` | `design-references/role-onboarding-v1/referrer-onboarding-v1.png` | Three native, capsule-safe steps explain service code, masked progress, and own earnings. The three slides are a native non-looping `swiper` (swipe or tap dots to review; 「下一步」 still advances; no autoplay). The approved composite is not sliced: ImageGen-produced transparent artwork maps to `packages/guides/assets/referrer-v1/service-code.png` (720×531, 86,655 bytes), `progress.png` (720×359, 116,232 bytes), and `earnings.png` (720×461, 123,044 bytes); visible titles, descriptions, assurance copy, dots, skip, and CTA remain native WXML. The page and artwork live in the dedicated `packages/guides` subpackage, while the main package keeps only the small versioned role-guide registry and Mine trigger. Referrers open v1 automatically once per local signed account/role/version after entering the workbench, may skip, and may replay it from Mine; the final CTA opens the selected membership's existing service-code route. The deep link requires `referrer.promotion`. Customer has no forced display and no unapproved guide placeholder. The `390x844` element ledger is content-intrinsic: capsule-safe nav uses a `32rpx` title, the artwork stage is `690rpx`, the step headline/body are `40rpx`/`24rpx`, the assurance copy and shield box are `22rpx`/`26rpx`, dots follow after `24rpx`, and the `96rpx` CTA uses `28rpx` type with bottom-safe padding. The custom nav is `border-box` with `navigationRight` inside its width so「跳过」clears the native WeChat capsule. Focused navigation, storage-version, package-size, alpha-channel, typography, and three-step behavior checks pass; native-capsule runtime QA remains pending the user's manual screenshot | Yes |
| `packages/business/referrer-progress/referrer-progress` | User-authorized Phase-13 extension of `miniprogram/DESIGN.md`, `design-tokens.json`, `app.less`, and the existing green workbench language | Current-membership enterprise name, masked customer label, service stage, and update time, with loading/retry/empty states that expose no phone, precise address, wall graph, internal appointment reason, or design file. WeChat DevTools `390x844` verified the loading state and capsule-safe header; empty/error states retain the same layout contract | Yes |
| `packages/business/referrer-earnings/referrer-earnings` | User-authorized Phase-13 extension of `miniprogram/DESIGN.md`, `design-tokens.json`, `app.less`, and the existing green workbench language | Only the current member's payable/paid **count** summary and each masked customer label plus payment status (no amounts). Payment-status chips flex-center labels. WeChat DevTools `390x844` verified the summary, empty-state CTA, and capsule-safe header | Yes |
| `packages/business/staff-earnings/staff-earnings` | Same Phase-13 earnings language as `referrer-earnings` (`miniprogram/DESIGN.md`, `design-tokens.json`, `app.less`); designer/measurer copy only | Own current-enterprise payable/paid **count** summary and each masked customer label plus payment status for the signed designer or measurer (no amounts). Payment-status chips flex-center labels. Focused TabBar/capability tests pass; authenticated `390x844` native-capsule capture remains pending | No |
| `packages/business/enterprise-commissions/enterprise-commissions` | Same Phase-13 earnings language as `referrer-earnings` / Admin `/lead-commissions` three-card totals; owner payout copy, not personal earnings | Tenant payable/paid/voided totals, lead-grouped role rows, and payable-only mark-paid with native confirm. Amount/beneficiary adjust stays Admin-only. Payment-status chips flex-center labels. Focused TabBar/capability tests pass; authenticated `390x844` native-capsule capture remains pending | No |
| `packages/business/customer-projects/customer-projects` | Compatibility deep-link shell only (product list retired; stage-companion home owns ranking/switcher per `docs/superpowers/specs/2026-08-21-customer-service-home-stage-companion-design.md`) | Not a product project list. On load the route ranks current-JWT customer-owned unarchived projects and redirects to the highest-urgency service archive (`customer-project`) or falls back to the Service tab; no TabBar list UI. The project folio remains the real deep route without the TabBar. Legacy inbound links may still hit this path | Yes |
| `packages/business/customer-project/customer-project` | `design-references/customer-project-archive-redesign-v4/customer-project-archive-balanced-color-v4.png` | V4 is implemented as native WXML/Less: compact single-role Xiao K archive hero, four numbered service stages, a warm-apricot appointment panel using the non-confrontational customer label `预约量房`, mint designer and mist-blue measurer cards, and one continuous three-row archive book with green/amber/blue status semantics. User-approved Hero copy is now fixed as `您的家装顾问` / `现场顾问与设计方案全记录` instead of the lead-name `heroTitle`. Assigned staff cards render the resolved customer `professionalProfile` (`title` when visible, plus `experienceLabel` / `serviceLabel`) from stored assignment-slot copy without rewriting 设计师/测量员; the designer card role is `家装设计顾问` without a 专属 prefix. The existing designer contact, staff dialing, appointment booking/detail/reschedule, protected floor-plan preview, tagged site-photo capture (native footer hides while the room-tag sheet is open), read-only scheme folio, and scheme-poster share flows remain wired to their current data and permission contracts. The first user-supplied tall-device runtime capture exposed legacy substitute icons and loose internal spacing; the route now uses one license-documented rounded-line family for calendar, person, ruler, document, image, delivery-file, phone, and WeChat semantics. Source SVGs live in `docs/icon-sources/customer-project-v4/` and packaged PNGs in `miniprogram/packages/business/assets/customer-project-v4/`. The source-calibrated element ledger is nav `36rpx`, Hero title/helper `44/26rpx`, Xiao K container `118×132rpx`, stage circle/label `38/24rpx`, appointment title/helper/CTA `34/24/28rpx` with an `84rpx` icon container and `60rpx` visible glyph, service heading/role/proof `32/28/24rpx`, person icon container/glyph `72/50–52rpx`, person-card minimum/padding `296/20rpx`, dossier title/helper/status `30/24/24rpx`, dossier-row minimum `110rpx`, dossier icon container/glyph `72/52rpx`, and footer label/contact icon `28/34rpx`. Focused source/template tests pass; authenticated `390x844` native-capsule and revised tall-device optical checks remain pending the user's manual runtime screenshots | Yes |
| `packages/business/customer-ai-schemes/customer-ai-schemes` | `design-references/miniprogram-airy-minimalist-v1/44-customer-ai-scheme-timeline.jpg` (D08) | Read-only customer AI scheme folio restored from D08: capsule-safe custom nav, customer-summary hero with house-block Xiao K (`project-delivery-xiao-k.png`, not the robot stand-in), scheme-round chips (finalized round prefixed **定稿**), current-round hero preview and delivery timeline, footer back/(customer) save-share; without a `schemeId` deep link the default selection is the finalized album, otherwise the last round; customer share opens `scheme-share-poster` (current-round cover + scheme title + 家客来 brand, no QR), album save then image share via `wx.showShareImageMenu`, with capsule page-forward hidden—no mini-program card share; non-owner poster entrances reuse this D08 folio via `GET /api/miniprogram/published-scheme-folios/[leadId]` without the save/share CTA; staff mode still omits the share CTA; no generation or edit actions. Entries come from lead detail (`mode=staff`), Service-home `免费效果图`, and the customer project archive (`mode=customer`), reusing `GET /api/leads/[id].publishedSchemes` and the customer-project aggregate. Focused contract tests pass; authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/appointment-detail/appointment-detail` | `docs/superpowers/specs/2026-08-21-appointment-detail-inline-reschedule-design.md`; `design-references/miniprogram-airy-minimalist-v1/03-appointment-reschedule.jpg`; secondary staff row follows `07-measurer-calendar.jpg` card-button language; sticky bar matches customer-project `footer-bar` full-bleed shell | Detail hosts inline availability picker (5-day window + slots) when `canReschedule`; archived/missing/converted/closed leads stay terminal and do not fetch availability slots. Sticky cancel|confirm sits in a full-bleed frosted page-tone parent (safe-area aware) with ~0.9∶1.3 flex + 20rpx gap and shared 26rpx centered labels; disabled confirm keeps `--action-disabled-bg` against the `#f8faf9` bar; page `padding-bottom: 200rpx` clears the bar; address∥navigate secondary row is equal-width pills with 16rpx gap and packaged edit/map-pin icons (no button emoji); staff **拍现场图** is the same card-button language and opens tag-first capture into the lead gallery; the room-tag sheet uses `root-portal` at z-index 2000 and hides the sticky native cancel|confirm bar while open so living-room/bedroom chips are not clipped; **开始量房** uses `images/leads-v4/ruler-green.png`. Focused tests pass; authenticated `390x844` native-capsule capture remains pending the user's screenshot of the open tag sheet | Yes |
| `packages/business/appointment-reschedule/appointment-reschedule` | Compat redirect shell for `03` / detail inline reschedule | No standalone UI; `onLoad` `redirectTo` `appointment-detail` with mode mapping (`internal` → staff detail; absent/`customer` → `mode=customer`). Callers prefer detail directly. Focused shell tests pass | Yes |
| `packages/business/appointment-booking/appointment-booking` | `design-references/referrer-network-appointment-v1/phase-5-designer-appointment-booking-v1.png` | Keeps the approved booking structure while splitting the prior address row into native map-place selection and manual building/unit/room detail. **选择地点** / **重新选择** uses `.sfp-icon-action` + `images/leads-v4/map-pin.png`. Owning customers and assigned designers submit the selected real slot, service address, and optional GCJ-02 point; focused tests pass. Refreshed `390x844` designer/customer captures remain pending | Yes |
| `packages/business/measurer-calendar/measurer-calendar` | `design-references/miniprogram-airy-minimalist-v1/07-measurer-calendar.jpg` | Restored matching the airy minimalist measurer calendar: capsule-safe top navigation with month switcher, lightweight overview card with schedule metrics and 3D Xiao K partner, 7-day horizontal calendar strip with task indicator dots, structured appointment cards with quick call/navigation/survey actions using packaged phone/map-pin/ruler icons (no button emoji), and quick unavailability shortcut. Appointment status tags and hero stat pills flex-center labels. All tests pass | Yes |
| `packages/business/measurer-unavailability/measurer-unavailability` | `design-references/miniprogram-airy-minimalist-v1/33-measurer-unavailability.jpg` | Restored matching the airy minimalist unavailability editor: smart guide notice card, touch-friendly date picker box, segmented time slot selector (all-day/morning/afternoon/custom), preset high-frequency reason tags, refined input with char counter, prominent green save CTA, and active schedule cards with status dot and delete action. All tests pass | Yes |
| `packages/business/enterprise-appointments/enterprise-appointments` | `design-references/miniprogram-airy-minimalist-v1/20-enterprise-appointments.jpg` | Enterprise-owner Appointments tab restored from R03: capsule-safe “预约调度中心” header with real week count, rolling 7-day strip, and `confirmed`/`expired` left-accent cards; open leads with expired or past-end appointments that fall before today still appear on the today list for coordination; archived or missing linked leads hydrate as `closed` so they render the read-only “已关闭” badge instead of a fake 客户量房 card; `converted`/`closed` leads render a read-only “已签约”/“已关闭” badge with no CTA or detail navigation. No invented dispatch actions and no area/layout/measurer-phone fields absent from the workbench item. Card status chips flex-center 22rpx labels (`line-height: 1`). Focused contract tests pass; authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/promotion-records/promotion-records` | Existing enterprise promotion-records chrome with packaged `packages/business/assets/promotion-records/hero-scene.jpg` (not a new airy salesperson home); channel-salesperson TabBar `报备/我的` reuses `tab-home` / `tab-mine` | Channel salesperson bootstrap lands here; page embeds custom TabBar. Focused identity/TabBar/Mine regression tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |

| `packages/guides/customer-guide/customer-guide` | `design-references/customer-onboarding-v1/customer-onboarding-four-slides-v2.png`; Service-home entry `design-references/customer-onboarding-v1/customer-service-home-guide-entry-v1.png` | Optional customer-only four-slide native onboarding. On wide Service-home screens, the Xiao K speech bubble reads `我是小K / 想了解服务？ / 点击我带你看看`; tapping it or the mascot opens this non-looping swiper. At `<=400px`, the multi-line overlay is hidden and the same entry becomes an in-flow single-line `点击我带你看看` button below the Hero helper copy, preserving the unobscured headline and full Xiao K artwork. It never auto-opens on first entry, appears in Mine, or records a seen state. The pages cover three free benefits, expressing needs, the actual demand/booking/design steps, and the existing service archive; skip and the final CTA return to the Service tab. Independent ImageGen alpha PNGs map to `packages/guides/assets/customer-v1/{three-free-benefits,home-archive,service-route,service-archive}.png`, each <=300KB. At `390x844`, nav/title/body/assurance/CTA use `32/40/24/22/28rpx` and the artwork stage is `500rpx`. The `装修服务向导` title is explicitly single-line, while the skip action remains non-shrinking; the title can therefore use the full capsule-left navigation lane without an artificial wrap. The custom nav is `border-box` with `navigationRight` inside its width so「跳过」clears the native WeChat capsule. Manual native-capsule runtime QA is pending the user's screenshot. No capability, API, or permission boundary changes | Yes |

## Role guide update

The approved replay entry now covers `referrer`, `enterprise_admin`,
`designer`, and `measurer`; the enterprise owner, designer, and measurer routes
are recorded above with their separate `packages/guides` pages and asset
mappings. All four guides now use a native non-looping `swiper` so earlier
slides can be reviewed by swipe or tap. Customer now has a separate manual four-slide guide, opened only from the Service-home Xiao K speech bubble; it is never forced or replayed from Mine.

## Recording rules

- Use the normalized route from `miniprogram/app.json` as the unique key.
- Record the design mapping and one concise current QA result.
- Keep screenshots, metrics, and test logs in local evidence directories, not in
  this canonical ledger.
- Update the English and Chinese ledgers together when a visual restoration changes.

Chinese mirror: [miniprogram-design-restoration-ledger.zh-CN.md](./miniprogram-design-restoration-ledger.zh-CN.md)
