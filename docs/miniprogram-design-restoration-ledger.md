# Mini Program Design Restoration Ledger

This is the current route-to-design lookup. Keep exactly one row per runtime
route and one latest approved design source. Replace a row when the source or
production state changes; do not append restoration history here.

The shared custom TabBar source is `design-references/tabbar-icons/tabbar-earnings-appointment-v1.png`:
Earnings/Commissions use its paired wallet-and-income states, and enterprise-owner Appointments uses
its paired calendar-check states. This shared source applies wherever the capability-allowlisted TabBar renders.

| Runtime route | Latest design source | Current QA state | Restored |
| --- | --- | --- | :---: |
| `pages/index/index` | `docs/superpowers/specs/2026-08-21-customer-service-home-stage-companion-design.md`; `design-references/miniprogram-airy-minimalist-v1/01-customer-workbench.jpg` (dual thumbs only when real floor-plan/scheme media exist), `04b-designer-workbench-dashboard.jpg`, `05b-measurer-workbench-dashboard.jpg`, `18b-enterprise-ops-dashboard-period.jpg` (`18c` custom period sheet) | Customer Service home is the stage-companion shell: house-block Xiao K (`project-delivery-xiao-k.png`) fills early stages without inventing media or using the robot stand-in; inset title shows current progress-step status (e.g. confirmed → 已预约上门量房) rather than 下一步+CTA; primary CTAs open booking/reschedule/rebook or go straight to the service archive (`customer-project`); dual CTAs share pill radius with retained vertical spacing; multi-project switcher shows `N = length − 1` remaining services; the「专属设计师」shortcut reuses the same archive contact sheet (QR-first / clipboard WeChat-ID fallback); `GET /api/miniprogram/customer-projects` ranks the featured card and feeds the switcher. An unsigned root-home visit immediately `switchTab`s to the JoveKore｜家客来 guest gateway on `pages/mine/mine`, so this route has no second visitor shell. Designer/measurer booking on the same lead remains available from staff surfaces. Role workbenches keep the airy-minimalist shell; the designer overview no longer shows the static「常用配方」strip below the delivery list (quick-nav「风格配方」to the Design tab remains); enterprise-owner / designer / measurer Operations restore the period-filtered five-card ops dashboard (signed KPI read-only; owner may show签约金额) from `GET /api/miniprogram/workbench?period=`, with hero pills and exception cards unchanged in meaning. Staff/enterprise identity-nav scan and bell sit left of the native capsule via runtime `navigationTop`/`navigationRight` (same recipe as customer home). Authenticated `390x844` native-capsule visual QA for the customer stage-companion home and enhanced dashboard states remains pending | Yes |
| `pages/mine/mine` | `docs/miniprogram-role-shell-design-v1.md`, `design-references/auth/miniprogram-guest-login-jovekore-v1.png` | The signed customer, referrer, and staff states retain the approved profile-card shell and inline account rows. The logged-out state is the capsule-safe JoveKore｜家客来 gateway: it uses the exact packaged `images/home-ip-v1/brand-logo.png`, a single real login CTA, and no business navigation. With no valid signed identity the shared custom TabBar is suppressed rather than falling back to legacy staff tabs; after bootstrap, the existing role/capability navigation returns. Focused custom-TabBar regression passes; `390x844` native-capsule host capture remains pending | No |
| `pages/leads-management/leads-management` | `docs/miniprogram-role-shell-design-v1.md` | The approved designer/owner customer route remains available; the signed measurer state renders own confirmed tasks plus unscheduled work that still needs a formal survey, and completed surveys leave that pending queue. Cold-launch role selection uses the stored signed context so the customer/designer route does not first render the legacy owner shell. Focused navigation tests pass; authenticated `390x844` native-capsule host capture remains pending | No |
| `packages/business/lead-detail/lead-detail` | `design-references/lead-detail-delivery-stack-v1/lead-detail-below-survey-v1.png` (lower stack); formal-survey card uses right-aligned next-action, a full-width address row, and a full-width appointment CTA | Standalone appointment card removed. Formal-survey card: tab on the left, **下一步** copy on the right, address on its own row, and **安排上门量房** / rebook / **查看预约** matching the full-width 84rpx conversion CTA. Scheme/history/conversion stack unchanged below. Hero and stage rail unchanged. Native `390x844` host capture remains pending | Yes |
| `packages/business/settings/settings` | `design-references/miniprogram-airy-minimalist-v1/29-settings.jpg` (content merged into `pages/mine/mine`) | Compatibility deep-link route only; `onLoad`/`onShow` immediately `switchTab` back to Mine and no longer render a standalone settings shell. Visual and interaction source of truth is the inline Mine Tab account block | Yes |
| `packages/business/identity-switch/identity-switch` | `design-references/account/settings-v1.png` | Approved account-system extension implemented with the existing permission-guardian Xiao K asset, real identity contexts, current state, confirm, loading, failure, and single-context states; exact route and localized expired-session state verified after a fresh compile at `390x844`, authenticated list capture pending | Yes |
| `pages/ai-design/ai-design` | `design-references/miniprogram-airy-minimalist-v1/37-ai-design-workbench.jpg` | Designer Design tab restores D01: brand header (lockup only; no competing **设计记录** task-log link), green create-scheme hero with credit pill, static non-actionable floor-plan/photo capability descriptions (no arrow or selected state), strict space-category recipe filtering with the existing empty state, and recent design-project cards that open `scheme-studio`; every recipe opens with a compatible input mode. Deep links with `leadId` plus `floorPlanId`/`workflowId` enter scheme-studio. Measurer/enterprise alternate shells unchanged. Focused contract tests pass; authenticated `390x844` native-capsule capture deferred to owner local verification | Yes |
| `packages/ai-workflow/create/ai-design-create` | `design-references/all-pages-ip-v3/14-ai-design-create-v3.png` | Existing restoration | Yes |
| `packages/ai-workflow/recipe-detail/recipe-detail` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Current recipe explanation and input contract are implemented; native capture pending | Yes |
| `packages/ai-workflow/recipe-project/recipe-project` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Apply-to space picker remains the creation-entry scope control: default **完整户型** (`whole_floor_plan`) plus closed survey rooms as **单房间**; helper copy states the recipe applies only to the current selection without extra rooms or credits. Formal-survey eligibility states unchanged. Native capture pending | Yes |
| `packages/ai-workflow/recipe-confirm/recipe-confirm` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Confirmation and source-choice states remain; after create/run the lead workflow path redirects into `scheme-studio` (isolated tasks still open `ai-design-result`). Native capture pending | Yes |
| `packages/ai-workflow/result/ai-design-result` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Current generation, failure, delivery, and truthful customer-publication controls are implemented; refreshed native capture pending | Yes |
| `packages/ai-workflow/history/ai-design-history` | `design-references/ai-design/ai-recipe-discovery-home-v2/ai-recipe-discovery-home-v2.png` | Page and history APIs remain; the Design tab header no longer opens this route. Isolated-task results still offer **查看历史**. Task filters and truthful task cards remain; synced workflow items expose **进入方案** into `scheme-studio`. Native capture pending | Yes |
| `packages/ai-workflow/scheme-studio/scheme-studio` | `design-references/miniprogram-airy-minimalist-v1/45-ai-scheme-studio.jpg`, `45b-ai-scheme-studio-templates.jpg`, `45c-ai-scheme-studio-composer-dock-v1.png`, `45d-ai-scheme-studio-composer-settings-v1.png` (D09) | Mobile-AI composer: collapsed floating dock by default (theme-green `#00c365` credit bar with white copy + one-line prompt + generate); focus expands tools with animation; project context card and empty-round card keep a 24rpx gutter matching the round list; page scroll padding grows with dock state and a bottom gradient scrim separates dock from white scheme cards. Template sheet keeps category/search and enlarge-before-apply. Authenticated `390x844` capture deferred. Limited: no Admin canvas annotation editor; no dark theme | Yes |
| `packages/surveying/editor/surveying-editor` | `design-references/surveying/cursor-guide-state-reference-20260812.jpg`; user-approved extension of this existing top bar | The “合” control is drawn on Canvas and follows pan/pinch. During cursor/wall drag it is painted on the rAF overlay instead of the formal canvas, and the native cover-view hit target is unmounted so fillText does not stall every touchmove. The 120px cursor lens stacks snap and coordinate labels. The right-rail clear action still matches the other tools with the two-character 清空 label. The persistent CAD chip reuses the approved compact top-bar control language and sits after Save, disabled until the loaded cloud plan is completed; it preserves the capsule-safe right-side lane. Focused tests cover the chrome state; fresh `390x844` native-capsule capture remains pending | Yes |
| `packages/business/promotion-service-code/promotion-service-code` | `design-references/miniprogram-airy-minimalist-v1/09-promotion-service-code.jpg` | Restored to the airy service-code template: capsule-safe brand lockup with the official logo, mint hero card, protected WeChat code, native 请扫码 plaque, three free-service promises, and a share CTA. Anonymous presentation is unchanged. Authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/staff-activity-code/staff-activity-code` | `design-references/miniprogram-airy-minimalist-v1/09-promotion-service-code.jpg` | Reuses the 09 service-code template for designer/measurer activity codes, including a protected WeChat image, capsule-safe brand lockup, and optional enterprise name on the hero card. Authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/enterprise-join-codes/enterprise-join-codes` | `design-references/miniprogram-airy-minimalist-v1/09-promotion-service-code.jpg` | Enterprise-owner dual-code presenter reuses the 09 show template with tabs for Admin staff/referrer join codes; generate/rotate/disable with native confirm; view does not rotate. Focused contract tests pass; authenticated `390x844` native-capsule capture pending | Yes |
| `packages/business/free-design-service/free-design-service` | `design-references/miniprogram-airy-minimalist-v1/11-free-design-phone-auth.jpg`, `12-free-design-success.jpg`, `13-free-design-assignment-pending.jpg`, `14-free-design-existing-attribution.jpg` (`10-free-design-confirm.jpg` kept as historical reference only; production scan no longer shows the confirm state) | After a successful code resolve the page lands on design-11 phone authorization; authorization then opens assigned-designer success, assignment-pending, or existing in-flight service. Design-11/12 restore tighter stepper, hero glow, green success title, and card spacing; design-14 restores the existing-attribution hero (dedicated Xiao K crop), clipboard service card, dashed stage timeline, dual CTAs, and muted retention banner. Native `390x844` host capture of the restored states remains pending | Yes |
| `packages/business/onboarding/onboarding` | `design-references/miniprogram-airy-minimalist-v1/16-onboarding.jpg`, `17-onboarding-recovery.jpg` | Employee/referrer scan landing restored to design 16: capsule-safe brand lockup, green welcome hero with door Xiao K crop, icon role/referrer card, enterprise pill, authorization CTA, and identity-switch entry. Recoverable invalid/disabled/conflict errors open design-17 recovery with door Xiao K crop, green recovery-option icons, scan-new-invite and switch-identity actions with icon CTAs. Contract tests pass; WeChat DevTools `390x844` route and full-host capture remain pending | Yes |
| `packages/business/enterprise-register/enterprise-register` | No independent design file; reuses onboarding brand-lock / airy-minimalist tokens from `miniprogram/DESIGN.md` + `design-tokens.json` (no new IP art) | Platform `er_` open-account flow: resolve → phone auth → Web-aligned form → pending-approval success (copy states initial password `123456`). Ready/success/error/recovery reuse the onboarding identity-link / state-action language for **去登录** so a later scan or WeChat resume cannot trap the user; `ACCOUNT_CONFLICT` after approval is an already-account exit; signed workbench identities leave to role landing. Focused contract tests pass; authenticated `390x844` native-capsule capture pending | Yes |
| `packages/business/onboarding-debug/onboarding-debug` | Development-only extension of the onboarding route's confirmed `miniprogram/DESIGN.md`, `design-tokens.json`, and `app.less` language; no separate production design source | Development build only. The native scanner selects a local Mini Program code, accepts only the onboarding target path, then opens the normal server-backed resolution, phone authorization, and identity-write flow; the route is locked outside development builds | No |
| `packages/business/referrer-workbench/referrer-workbench` | `design-references/miniprogram-airy-minimalist-v1/06b-promoter-workbench-signed-count.jpg` | Preserves the source composition while improving readability: business labels are `24–28rpx` and explanatory text is `20–22rpx`. Enterprise pills use natural single-line widths in the source's horizontal selector rather than index-based fixed widths, keeping long names, subsequent memberships, and the add-enterprise action separate. Add enterprise invokes the native scanner and accepts only the existing onboarding path with a code parameter before entering server-validated onboarding. Hero stats now include read-only `已签约` count from progress `stage.key=converted` beside 今日扫码/累计客户/待结收益; no full ops dashboard on this role. Referrer-workbench and role-navigation regression tests pass; authenticated native `390x844` host capture remains pending because the current DevTools window has no verified automator endpoint | Yes |
| `packages/business/referrer-progress/referrer-progress` | User-authorized Phase-13 extension of `miniprogram/DESIGN.md`, `design-tokens.json`, `app.less`, and the existing green workbench language | Current-membership enterprise name, masked customer label, service stage, and update time, with loading/retry/empty states that expose no phone, precise address, wall graph, internal appointment reason, or design file. WeChat DevTools `390x844` verified the loading state and capsule-safe header; empty/error states retain the same layout contract | Yes |
| `packages/business/referrer-earnings/referrer-earnings` | User-authorized Phase-13 extension of `miniprogram/DESIGN.md`, `design-tokens.json`, `app.less`, and the existing green workbench language | Only the current member's payable/paid summary and each amount/payment state, including loading/retry/empty states. WeChat DevTools `390x844` verified the summary, empty-state CTA, and capsule-safe header | Yes |
| `packages/business/staff-earnings/staff-earnings` | Same Phase-13 earnings language as `referrer-earnings` (`miniprogram/DESIGN.md`, `design-tokens.json`, `app.less`); designer/measurer copy only | Own current-enterprise payable/paid summary and each amount/payment state for the signed designer or measurer. Focused TabBar/capability tests pass; authenticated `390x844` native-capsule capture remains pending | No |
| `packages/business/enterprise-commissions/enterprise-commissions` | Same Phase-13 earnings language as `referrer-earnings` / Admin `/lead-commissions` three-card totals; owner payout copy, not personal earnings | Tenant payable/paid/voided totals, lead-grouped role rows, and payable-only mark-paid with native confirm. Amount/beneficiary adjust stays Admin-only. Focused TabBar/capability tests pass; authenticated `390x844` native-capsule capture remains pending | No |
| `packages/business/customer-projects/customer-projects` | Compatibility deep-link shell only (product list retired; stage-companion home owns ranking/switcher per `docs/superpowers/specs/2026-08-21-customer-service-home-stage-companion-design.md`) | Not a product project list. On load the route ranks current-JWT customer-owned unarchived projects and redirects to the highest-urgency service archive (`customer-project`) or falls back to the Service tab; no TabBar list UI. The project folio remains the real deep route without the TabBar. Legacy inbound links may still hit this path | Yes |
| `packages/business/customer-project/customer-project` | `design-references/miniprogram-airy-minimalist-v1/02-customer-project-archive.jpg` | The restored archive folio now matches design 02: nav title/subtitle, four-step green hero timeline with the house-block Xiao K asset `project-delivery-xiao-k.png` (not the robot stand-in), compact personnel cards, floor-plan archive with protected HD preview, featured delivery scheme with style tag and published-grid multi-image preview (multiple confirmed designs, each labeled `已发布`), and a fixed footer for WeChat designer contact plus save/share.「微信联系设计师」opens a shared contact sheet when `wechatQrUrl` is present (long-press QR to add friend, with copy WeChat ID kept); WeChat-ID-only falls back to clipboard plus a search-add hint—personal WeChat cannot be auto-added. Booking-card **改期** / **查看预约** share pill radius, 16rpx stack spacing, and flex-centered labels. The delivery header uses the API-provided scheme title (`已发布{title}方案`); `详情` and `查看全部方案` open the read-only `customer-ai-schemes` folio, and the contact label is flex-centered without changing the approved composition. Booking/rebooking remains visible only while appointment actions are still relevant. API adds project identity fields, `featuredScheme`, and `GET /miniprogram/customer-projects/[leadId]/formal-floor-plan/preview`. Focused template/style tests pass; authenticated `390x844` host capture remains pending | Yes |
| `packages/business/customer-ai-schemes/customer-ai-schemes` | `design-references/miniprogram-airy-minimalist-v1/44-customer-ai-scheme-timeline.jpg` (D08) | Read-only customer AI scheme folio restored from D08: capsule-safe custom nav, customer-summary hero with house-block Xiao K (`project-delivery-xiao-k.png`, not the robot stand-in), scheme-round chips, current-round hero preview and delivery timeline, footer back/(customer) save-share; no generation or edit actions. Entries come from lead detail (`mode=staff`) and the customer project archive (`mode=customer`), reusing `GET /api/leads/[id].publishedSchemes` and the customer-project aggregate. Focused contract tests pass; authenticated `390x844` native-capsule host capture remains pending | Yes |
| `packages/business/appointment-detail/appointment-detail` | `docs/superpowers/specs/2026-08-21-appointment-detail-inline-reschedule-design.md`; `design-references/miniprogram-airy-minimalist-v1/03-appointment-reschedule.jpg`; secondary staff row follows `07-measurer-calendar.jpg` card-button language | Detail hosts inline availability picker (5-day window + slots) when `canReschedule`; sticky cancel\|confirm (confirm full-width if no cancel); staff start-survey/complete/address/navigate/sync stay in a secondary scroll region so reschedule owns the sticky primary. Focused tests pass; authenticated `390x844` native-capsule capture remains pending | Yes |
| `packages/business/appointment-reschedule/appointment-reschedule` | Compat redirect shell for `03` / detail inline reschedule | No standalone UI; `onLoad` `redirectTo` `appointment-detail` with mode mapping (`internal` → staff detail; absent/`customer` → `mode=customer`). Callers prefer detail directly. Focused shell tests pass | Yes |
| `packages/business/appointment-booking/appointment-booking` | `design-references/referrer-network-appointment-v1/phase-5-designer-appointment-booking-v1.png` | Keeps the approved booking structure while splitting the prior address row into native map-place selection and manual building/unit/room detail. Owning customers and assigned designers submit the selected real slot, service address, and optional GCJ-02 point; focused tests pass. Refreshed `390x844` designer/customer captures remain pending | Yes |
| `packages/business/measurer-calendar/measurer-calendar` | `design-references/miniprogram-airy-minimalist-v1/07-measurer-calendar.jpg` | Restored matching the airy minimalist measurer calendar: capsule-safe top navigation with month switcher, lightweight overview card with schedule metrics and 3D Xiao K partner, 7-day horizontal calendar strip with task indicator dots, structured appointment cards with quick call/navigation/survey actions, and quick unavailability shortcut. All tests pass | Yes |
| `packages/business/measurer-unavailability/measurer-unavailability` | `design-references/miniprogram-airy-minimalist-v1/33-measurer-unavailability.jpg` | Restored matching the airy minimalist unavailability editor: smart guide notice card, touch-friendly date picker box, segmented time slot selector (all-day/morning/afternoon/custom), preset high-frequency reason tags, refined input with char counter, prominent green save CTA, and active schedule cards with status dot and delete action. All tests pass | Yes |
| `packages/business/enterprise-appointments/enterprise-appointments` | `design-references/miniprogram-airy-minimalist-v1/20-enterprise-appointments.jpg` | Enterprise-owner Appointments tab restored from R03: capsule-safe “预约调度中心” header with real week count, rolling 7-day strip, and `confirmed`/`expired` left-accent cards; open leads with expired appointments keep “需协调改期” + view CTA into appointment detail, while `converted`/`closed` leads render a read-only “已签约”/“已关闭” badge with no CTA or detail navigation. No invented dispatch actions and no area/layout/measurer-phone fields absent from the workbench item. Focused contract tests pass; authenticated `390x844` native-capsule host capture remains pending | Yes |

## Recording rules

- Use the normalized route from `miniprogram/app.json` as the unique key.
- Record the design mapping and one concise current QA result.
- Keep screenshots, metrics, and test logs in local evidence directories, not in
  this canonical ledger.
- Update the English and Chinese ledgers together when a visual restoration changes.

Chinese mirror: [miniprogram-design-restoration-ledger.zh-CN.md](./miniprogram-design-restoration-ledger.zh-CN.md)
