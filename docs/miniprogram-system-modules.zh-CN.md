# 小程序当前功能清单

### PostgreSQL-only AI API（2026-08-05）

小程序 AI 媒体、任务、工作流和能力接口只接受 PostgreSQL bigint 标识。客户端不再依赖 MongoDB 或 ObjectId 兼容层；提示词库和七牛云媒体配置通过 PostgreSQL repository 与存储适配器提供。

> 2026-08-03 PostgreSQL 迁移更新：`app.js` 加载企业品牌时调用的 `/api/branding/[id]` 已改由 PostgreSQL 企业资料提供，客户端请求路径和品牌字段契约不变。

本文档记录 `miniprogram/` 原生微信小程序的当前实现。事实基线是当前
`app.json`、页面处理器、共享工具和实际调用的后台 API。

### 正式量房 CAD 内核重构（2026-08-14）

- 状态：兼容门面、原子编辑事务、`quick/full` 校验、完成提交时的 Face shadow
  和光标吸附迟滞均为 `Implemented`。`miniprogram/utils/surveyWallGraph.js` 仍是唯一
  CommonJS 公共入口，保留此前全部导出、参数、返回值和抛错合同；唯一新增公共接口是
  `validateSurveyDraft(draft, { mode: 'quick' | 'full' })`。
- 内核按 `core`、`geometry`、`domain`、`topology`、`operations`、`snap`、
  `read-model`、`invariants` 分层到 `miniprogram/utils/survey/`。已经验证的编辑状态机
  暂由 `legacy-kernel.js` 作为兼容宿主，纯依赖与操作继续通过上述边界迁移；业务消费者
  不得直接引用该文件。
- 复杂墙体和门窗操作现在只针对一份内存草稿克隆执行。变更或校验失败会直接抛错，调用方
  输入草稿保持字节级不变；编辑器仍只对完整结果执行一次 `applyDraft(...,
  { recordHistory: true })`。不持久化的 `TopologyIndex` 提供节点、墙、空间和门窗反向索引，
  并支持结构变更后的显式失效。
- `quick` 是事务使用的线性校验；`full` 额外检查重复墙、未节点化交点、自交空间、异常
  共享墙及临时 Half-Edge Face。`full`/Face 只在 `completed` 云端保存前、测试和显式
  诊断中执行，页面加载、`touchmove` 和 Canvas 渲染均不调用。Face 与 dangle 只作诊断，
  不重建或持久化 `spaces[]`。
- 光标放置保留原有顶点、墙面和延长轴优先级，并加入按 viewport 换算的 `16px` 获取、
  `26px` 释放迟滞。锁定候选只存在于页面临时状态，坐标仍为整数毫米，松手或取消即清空，
  不写入 version-4 JSON 或测量审计。
- 兼容边界不变：路由、WXML/WXSS/素材、Canvas 实体并集、API、角色、BLE 协议、测量审计、
  数据库结构及 `{ version: 4, measurementMode: 'surveying', surveyGraph }` 顶层信封均未变；
  因此设计还原台账状态不变。
- 验证：既有量房基线保持 `74/74`，新增 CAD 契约/校验/事务/Face/吸附/夹具/运行时边界测试
  `20/20`，H5 `29/29` 且构建通过；小程序全量为 `356/359`，仅保留实施前 3 项非量房失败。
  2,000 次 200 墙提交微基准中，事务门面为兼容内核基线的 `1.166x`（`+16.6%`，低于
  `20%` 门槛）；500 墙 quick/index 校验低于测试规定的 `250ms` 上限。由于现有微信开发者
  工具窗口未暴露兼容自动化端点，真机首帧与正式路由流程截图仍待人工复核。

### 小程序订阅通知模板交接

第一批四个“房屋装修”公共模板、精确关键词和运行时映射记录在 [`docs/miniprogram-subscription-notification-template-baseline.zh-CN.md`](miniprogram-subscription-notification-template-baseline.zh-CN.md)。小程序现通过已认证接口取得完整 V2 列表，一次聚合授权四个不同标题模板并显示部分授权数量；通用待办、客户指派和新增客户发送为 `Implemented`。`上门量房提醒`只完成配置与授权，真实预约触发仍为 `Limited`。

小程序路由与最后设计稿、HTML 相似度及生产界面还原状态的唯一查询入口是
`docs/miniprogram-design-restoration-ledger.zh-CN.md`，其英文镜像为
`docs/miniprogram-design-restoration-ledger.md`。视觉还原必须在同一次修改中同步更新这对台账。

> 2026-08-04 PostgreSQL migration update: Mini Program AI upload, formal-plan source, task lifecycle, and workflow-list routes now use tenant-RLS PostgreSQL bigint assets and generations; historical ObjectId asset URLs remain read-only through the compatibility delivery branch.

## 状态与运行环境

- `Implemented`（已实现）：页面及其真实数据/操作链路可用。
- `Limited`（有限支持）：依赖登录、企业角色、蓝牙硬件、供应商或特定正式墙图形态。
- `Placeholder`（占位/未开放）：只有 mock/本地行为、规划 toast 或没有真实服务端操作。
- 运行环境：原生微信小程序；`utils/api.js` 通过 JWT Bearer 请求；`threejs-miniprogram` 用于 3D 预览；可选蓝牙激光测距仪。
- 源码包保护：`project.config.json` 会从预览/上传包中排除测试夹具、本地开发日志和设计工具元数据；运行所需的页面、素材、工具及 `miniprogram_npm` 依赖仍会打包，以保持在微信 4 MB 源码限制内。
- 主 Tab：首页 `index`、客户线索 `leads-management`、设计 `ai-design`、我的 `mine`，另有自定义中置量房操作。只有这四个 `app.json.tabBar` 路由挂载共享自定义 TabBar；登录、详情、工作流、灵感库、推荐、正式量房及其他二三级页面不得渲染本地或重复 TabBar。共享 `128rpx` 内容区确保中置“量房”文字完全位于底部安全区之上；先保留 `constant(safe-area-inset-bottom)` 兼容旧版 iOS WebView，再使用现代 `env()` 声明。
- 共享 TabBar V3 还原（2026-08-07）：经确认的量房入口使用手持激光测距仪的完整透明小 K，
  不再添加圆形背景、圆形描边或圆形投影。企业员工使用五项版，角色尺寸为 `128rpx` 并向上凸起
  `30rpx`；未开放 AI 设计的地推人员使用四项等分真实入口，小 K 缩为 `112rpx` 并向上凸起
  `24rpx`，不保留空槽或显示禁用“设计”。独立原生“量房”标签保持既有 `96rpx` 安全基线。
  页面路由、最近户型查询、正式量房入口、
  API、认证和权限边界均不改变。TabBar、素材体积、访问可见性和离线调试针对性测试通过，小程序
  全量测试通过 `181/181`；用户现有微信开发者工具已在 `390x844` 基准核验地推人员四项版首页，
  证据位于
  `design-references/qa/v3-tabbar-promoter-four-item-home-390x844.png`；此前企业五项版首页、线索和
  我的的证据继续保留在
  `design-references/qa/v3-tabbar-no-circle-390x844-contact-sheet.png`。

## 分包与体积预算

- 主包只保留首页、客户线索、设计、我的四个 Tab 及其共享运行时。客户线索 Tab 及首页复用的 `components/lead-list` 必须留在主包，因为 TabBar 路由不能放入分包；`packages/surveying` 承载正式量房编辑器、专属渲染/引导资源与本地 `threejs-miniprogram` 适配器，`packages/ai-workflow` 承载 AI 创建、结果、历史、批准的页面角色素材与兼容跳转，`packages/business` 承载登录、低频业务流程及其页面专属素材（含线索表单/详情、灵感库和推荐）。
- 三个分包均为普通分包，只可依赖主包，彼此不得引用；不配置启动预下载。生产资源保持本地，主包预算为 `<= 1.5 MB`、各分包 `< 2 MB`，以微信开发者工具代码依赖分析为最终验收。
## 身份与共享上下文

- `/packages/business/login/login`：通过 `/api/auth/miniprogram` 支持微信手机号快捷登录和账号密码登录，将 JWT/用户信息恢复到本地。
- 会话切换：每个一级 Tab 页面在显示时都会要求共享自定义 TabBar 重新计算当前选中项和企业专属入口可见性；“我的”页在工作台响应补全当前用户上下文后还会再次刷新。因此从独立地推账号切换到企业设计师账号后，无需重启小程序即可显示“设计”Tab；退出登录也会立即重新应用同一可见性规则。“设计”不可用时，TabBar 直接渲染“首页 / 线索 / 量房 / 我的”四个等分真实入口并采用紧凑量房形态，不保留空槽，也不暴露禁用的 AI 入口。该调整仅作用于客户端表现层，不改变认证、路由、API 或 `enterpriseId` 权限边界。
- 登录页视觉：已按 iPhone 13 Pro `390x844` 基准实现
  `design-references/all-pages-ip-v1/06-login.png`。页面使用从参考图派生的本地小 K/F3 入户场景，
  保留已确认的品牌与欢迎锁定画面，并叠加双模式登录面板、本地授权图标、紧凑能力摘要和同屏返回入口；登录行为、
  加载/失败反馈、通知授权与页面返回语义保持不变。
- 访问仅限员工：账号密码登录必须匹配启用中的后台 `AdminUser`；微信 code/手机号登录必须通过已绑定 OpenID 或后台手机号匹配启用中的 `AdminUser`。未匹配的外部用户会收到 `403`，无法建立小程序已登录业务会话。账号、绑定身份、企业和权限解析已切换 PostgreSQL，并在刷新和请求上下文解析时复核启用状态。
- `app.js`：恢复会话，解析二维码 `enterpriseId`/`staffId` 推荐参数，同步员工专业上下文、企业品牌，并对已记忆设备尝试静默 BLE 重连。
- `utils/api.js`：携带 Bearer token，通过显式 `ACTIVE_API_ENVIRONMENT`（`local` 或 `production`）选择唯一 API 地址，401 时清理会话并提示重新登录。请求不会跨环境回退，也不读取持久化 `apiBaseUrl`。
- 小程序 AI 素材交付：`/api/miniprogram/ai/assets/[id]/image` 对新的十进制 PostgreSQL 素材 ID 在租户 RLS 范围内读取；旧 MongoDB ObjectId URL 仅保留给历史 AI 记录只读访问。
- 状态：PostgreSQL 登录与上下文恢复为 `Implemented`；具体路径仍依赖有效微信授权、账号、API 地址和企业/供应商配置。Phase 3 期间为 `Limited`：仍由 MongoDB 支撑的 AI 生成/媒体与订单/提成工作流域要等计划内 PostgreSQL 切换后才兼容 bigint 身份；线索、正式户型、测量和设备绑定现已使用 PostgreSQL。
- 设备授权：`/api/devices/verify-binding` 在平台范围查询已发现设备，但仅授权状态为 `assigned` 且与当前员工或企业匹配的设备。在 `/devices` 指派持有人会将闲置设备自动改为 `assigned`；`maintenance`、`lost` 始终不能被授权。

## 页面清单

### 首页与量房入口

- 页面：`pages/index/index`。
- API：`/api/miniprogram/home`、`/api/floorplans`、户型 POST/PUT、`/api/leads/[id]`、`/api/location/reverse`、`/api/users/me`。
- 已实现：首页统计、定位/城市、按角色过滤的最近云端户型（本地草稿存在时仍显示，且可直接进入正式量房编辑器）、留资弹窗、BLE 连接状态、记忆设备自动连接、新建/继续正式量房、房间进入、企业员工常驻 AI 设计快捷入口，以及首页直达客户线索和 BLE 配对的快捷入口。
- 有限支持/仅调试：底部中置“测距”入口会先请求最近一条 `/api/floorplans` 记录，再打开正式量房编辑器。将 `miniprogram/utils/debugConfig.js` 中的 `ENABLE_OFFLINE_SURVEY_ENTRY_DEBUG` 改为 `true` 后，接口请求失败会以 `startNewSurvey: true` 直接打开本地新建量房会话，可在后端不可达时调试编辑器样式和交互；开关默认是 `false`，保存仍要求正常的鉴权接口。
- 数据边界：线索、正式户型、测量、已分配设备的统计和最近正式户型均来自 PostgreSQL RLS Repository。AI 生成域尚未迁移，因此 `aiGeneratedCases` 明确返回 `0`。最近户型项还会返回其真实关联线索可用的 `customerName` 和 `communityName`；首页的提醒、进行中量房卡和项目进度卡统一消费正式户型只读 `display`：小区为主标题，“客户 · 第 N 次量房”为次级身份，持久化户型名称仅作兼容回退，状态、闭合空间数和时间仍为元数据。按角色过滤的 AI 来源接口也返回同一读模型。
- 权限边界：首页点击最近户型时，详情接口与最近户型列表使用相同的员工归属范围；企业管理员仍按企业范围访问，不属于企业的地推账号按自身 `staffId` 访问，普通员工不会因此获得其他员工户型的访问权限。
- 有限支持：写入/通知特征值的配对由硬件决定。BLE 调试日志会记录枚举到的通道属性、每一次命令写入及每一条带完整 Service/Characteristic UUID 的原始通知；接收缓冲区按通知通道隔离。厂商私有二进制回包仅保留为原始调试信息，确认协议映射前不赋予业务字段含义。
- 视觉基准：以 `design-references/all-pages-ip-v1/01-home-v2.png` 的 iPhone 13 Pro `390x844` 画面为首页基准。交付界面使用 F1/F3 空间服务向导首屏、上浮的正式量房卡、三张快捷服务卡和项目进度卡；本地派生场景素材为 `images/home-ip-v1/hero-scene-wechat-safe-overscan.png`，并为微信原生胶囊预留安全区。实时城市控件位于品牌锁定组右侧，标题使用“让小K陪你 / 把空间一步步 / 变成家”三行构图；Hero 固定为单一 `516rpx` 锚点，不再保留右上角“今日提醒”对话气泡或依赖微信胶囊位置的提醒定位。存在最近户型时，仅在中间正式量房卡内、位于小 K 完成/协助文案之前显示带铃铛图标的绿色圆角“今日提醒”标签。城市、统计、设备状态、最近户型、空状态和全部导航仍由真实数据及角色边界驱动。
- 占位：帮助中心仍显示“即将上线”消息。

### 线索与客户记录

- 页面：`packages/business/lead-form/lead-form`、`pages/leads-management/leads-management`、`packages/business/lead-detail/lead-detail`、`packages/business/acquisition-center/acquisition-center`；设计师联系方式复用 `components/designer-contact-sheet`。
- API：`/api/leads`、`/api/leads/[id]`、`POST /api/leads/[id]/convert`、`POST /api/leads/[id]/revert-conversion`、`/api/acquisition-tasks`、`/api/floorplans/[id]` GET/DELETE。
- 已实现：客户称呼/手机号/小区/面积/风格采集、最近客户、列表/详情、正式户型关联、线索详情中的正式量房状态/闭合空间数/更新时间、继续量房、新建独立量房、删除正式户型时清理本地续测指针、对已加载记录进行客户端搜索，以及通过阶段条或原生操作菜单进行规范状态筛选。删除仅允许关联线索处于 `new` 或 `measuring`；线索进入 `designing`、`converted` 或 `closed` 后，`/api/floorplans/[id]` 返回 `409 FLOOR_PLAN_REQUIRED_FOR_LEAD_STAGE`，避免方案设计阶段丢失正式户型依据并回退为开始量房。客户主流程统一为“新线索→量房中→方案设计→已签约”，“已关闭”为终止筛选；历史 `acquired` 仅兼容显示为“新线索”。获客确认是独立协作事实，不再作为客户业务阶段。2026-08-14 起，客户详情在现有阶段轨道下提供单条签约操作：存在企业上下文的企业管理员可处理本企业线索，设计师仅可处理分配给自己的线索；底部确认层按中国时区当天限制签约日期，可选金额和备注，并明确不生成订单、扣款或结算获客提成。成功后员工视图在同区展示签约日期、标记人、时间、金额和备注；企业管理员可填写原因撤销，设计师、测量员和普通用户不能撤销。普通微信客户详情只保留签约状态和日期，不返回内部金额、备注、操作人或精确操作时间。权限和动作可用性均由详情 API 返回，客户端不自行推断。
- 持久化：线索和正式户型列表/详情/新建/更新/删除均使用 PostgreSQL RLS 事务和十进制字符串 ID；线索-户型关联、主户型及删除清理为原子操作。
- 归档生命周期（2026-08-10）：小程序线索、获客和 AI 客户查询默认只返回在用线索。归档线索从日常列表、获客任务和 AI 项目选择器隐藏；直接更新、绑定户型、获客确认以及新增/重试 AI 任务统一返回 `409 LEAD_ARCHIVED`。手机号录入命中归档档案时返回 `409 ARCHIVED_LEAD_EXISTS`，需要有权限的后台人员先恢复。既有户型、AI 成果、提成及财务结算历史继续在各自资产/历史模块可读，并携带“客户已归档”标识。
- 视觉基准：以 `design-references/all-pages-ip-v1/02-leads-management.png` 和 `390x844` 为基准。交付页面保留小 K 客户管家场景、绿色档案摘要、搜索/筛选/新增操作顺序、四个客户主流程状态索引、叠放式客户档案卡和右侧状态彩色户型缩略图；胶囊安全区下方新增轻量“我的设计师”入口，微信号与签名二维码只在共享底部名片中展示。缩略图仍根据每条线索关联的正式墙图或真实外部预览地址渲染，实时数据、分页、错误/空状态、导航和共享五项自定义 TabBar 仍为准。打包场景素材为透明 PNG `images/leads-ip-v1/client-concierge-scene.png`，其中不包含客户统计或交互控件；共享量房入口使用透明的完整小 K 素材 `images/mine-icons/tab-measure-k.png`，角色手持激光测距仪并直接向上凸起，不再使用圆形背景、圆形描边或圆形投影底座，“量房”仍为独立的原生实时文字标签。两张素材均由生成式源文件导出，不再直接裁切低分辨率整页参考图。今日新增、跟进中和已成交均为按当前租户/员工范围聚合的服务端统计，不再从当前分页记录推算。
- 布局：列表工作区与 `list-scroller` 保持连续的弹性高度链，滚动区域填满页面头部、搜索和阶段控件之外的剩余视口空间。
- 有限支持：需要有效小程序会话；手机号和小区既有客户端校验也有服务端校验。搜索范围为分页当前已经加载的记录；选择状态会触发服务端筛选并重新加载。

### 企业报备与员工任务

- 页面：`packages/business/promotion-records/promotion-records`、`packages/business/promotion-record-detail/promotion-record-detail`。
- API：`/api/promotion-records`、`/promotion-records/[id]`、`/promotion-records/pool`、员工角色列表、工作台 summary/todos 和更新接口。
- 已实现：新建企业报备、`my`/`measure`/`design`/`admin`/`overdue`/`pool` 角色视图、搜索筛选、公海认领/审批申请、冲突归属、跟进内容和截止时间、测量员/设计师/地推分配、业务阶段操作。创建态支持原生行业和地区选择、微信定位、必填校验、防重复提交，并在成功后继续跳转到新建报备详情。
- 视觉基准：报备列表以 `design-references/all-pages-ip-v1/09-promotion-records.png`
  和 `390x844` 为基准，使用派生的本地
  `packages/business/assets/promotion-records/hero-scene.jpg` 场景表达小 K 的企业档案整理角色，
  直接呈现 `my`、`measure`、`design`、`overdue`、`pool` 五个视图，并按企业、
  联系人、电话或地址搜索当前已加载视图；卡片继续渲染真实工作流状态、跟进内容、
  时间、认领操作、加载态和空状态。该页面是三级工作流页面，明确不渲染独立 TabBar
  或重复的顶级导航。
- 创建态视觉基准：`mode=create` 以
  `design-references/all-pages-ip-v1/11-promotion-record-create.png` 和
  `390x844` 为基准，使用派生的本地
  `packages/business/assets/promotion-create/hero-scene.jpg` 场景表达小 K 的企业资料接待员角色，
  将企业资料和联系人拆分为两组合作工单，并统一使用一套有授权记录的本地 Lucide
  图标；标签和控件全部由真实 WXML 渲染，不使用图片文字。真实必填项仍为企业名称、
  联系人和联系电话，城市、行业、地址、备注、定位、提交反馈、API 参数及成功跳转
  仍以当前业务实现为准。
- 视觉基准：报备详情以 `design-references/all-pages-ip-v1/10-promotion-record-detail.png`
  和 `390x844` 为基准，采用紧凑的小 K 报备盖章场景、服务端状态驱动的四段进度、
  跟进表单、活动时间线和管理员任务分配面板。详情页会遮罩联系人手机号；底层记录、
  阶段、时间、员工选项、角色检查和变更操作仍使用真实数据。
- PostgreSQL 边界：报备新建、列表/详情变更、公海/冲突操作、工作台待办、通知轮询和提醒状态均使用带租户 RLS 的 typed PostgreSQL Repository。既有响应 DTO、员工角色检查以及 `leadId`/报备跳转保持不变。订单与提成仍由 MongoDB 支撑，作为独立的 `Limited` 读取/结算域。
- 有限支持：可见操作由员工角色和服务端工作流状态决定。

### 提成记录

- 页面：`packages/business/commission-records/commission-records`。
- API：`/api/commission-records`。
- 已实现：高保真收益中心汇总、交互式状态筛选，以及待结算、已结算、已作废
  提成记录的真实订单、金额、日期和状态展示；包含加载、空数据、失败重试和
  结算说明状态。
- 有限支持：记录由企业订单工作流生成和结算，小程序只读，不是结算权威端。

### 灵感库

- 页面：`packages/business/inspiration/inspiration`。
- API：`/api/inspirations?page=...&style=...&roomType=...`。
- 已实现：分页、下拉刷新、风格/空间筛选、图片预览、分享海报外壳和免费设计留资入口。受管封面和效果图会下发为小程序签名媒体地址，因此本地和七牛存储对页面透明。
- 导航：页面保留为次级入口，不再占用主 Tab。
- 有限支持：内容数量取决于后台已发布的灵感数据。

### AI 设计与企业点数

- 页面：`pages/ai-design/ai-design`、`packages/ai-workflow/recipe-detail/recipe-detail`、`packages/ai-workflow/recipe-project/recipe-project`、`packages/ai-workflow/recipe-confirm/recipe-confirm`、`packages/ai-workflow/result/ai-design-result`、`packages/ai-workflow/history/ai-design-history`；`packages/ai-workflow/create/ai-design-create` 保留旧入口兼容，`packages/ai-workflow/legacy/ai-gen` 仅兼容跳转。
- 导航：`pages/ai-design/ai-design` 已作为 `设计` 主 Tab，使用 `navigationStyle: custom` 实测微信胶囊和安全区，不再渲染居中的微信默认标题栏。标题保留在胶囊左侧安全通道，“设计记录”和真实点数改为胶囊下方正常文档流中的双列 `88rpx` 工具条，Hero 必须排在工具条之后；记录和点数不再使用相对页面/Hero 的绝对定位，因此不同刘海、胶囊和宿主缩放下都不会被主视觉遮挡。页面继续使用共享固定自定义 TabBar 的高度、滚动与下拉刷新契约。首页、我的和旧兼容入口会先通过 `utils/aiDesignNavigation.js` 传递户型、房间、线索、范围和方案上下文，再调用 `switchTab`，以符合微信 Tab 页不能通过 `navigateTo` 查询参数进入的限制。没有 `enterpriseId` 的员工会隐藏设计 Tab 和全部 AI 入口；共享跳转与页面守卫会把旧路径或直达路径返回首页，且不会发起 AI API 请求。
- API：小程序 AI 能力、装修配方列表/详情/同源签名预览、按角色过滤的正式户型/房间来源、由正式墙图派生的标准化墙体/房间导航只读模型、当前完整户型 3D 导航预览状态、当前上下文可见活动方案、媒体上传/签名读取、任务创建/执行/状态/重试、历史读取/删除；历史接口的 `heroFloorPlanId` 窄查询只返回当前员工、该正式户型、成功的完整户型 `floor_plan_render` 成果（最多 5 条），不复用分页的“最近方案”数据；`utils/aiDesignService.js` 统一携带 Bearer JWT。
- 装修配方工作流（2026-08-14）：当前 `设计` Tab 以后台已发布提示词模板为唯一配方数据源。服务端把模板名称、分类、预览图和推断的适用空间/输入要求转换为面向用户的配方 DTO，只在任务创建的服务端内部读取模板提示词；列表、详情、历史和任务 DTO 均不返回提示词、模型、技术模式或内部方案 ID。主路径为“配方发现/搜索 → 配方详情 → 客户正式量房 → 完整户型或闭合房间 → 照片/已有成果（仅照片型需要）→ 单屏确认 → 生成/结果”，不再显示四阶段轨道。相同客户只有一个活动方案时自动续接，多个无法消歧时返回 `WORKFLOW_CONFLICT` 并显示用户可理解的已有方案/新建方案选择。没有客户项目时可返回客户线索；待完善量房只进入唯一正式量房页面。配方下架、点数不足、供应商不可用、上传失败、生成失败和预览失效均保留可执行恢复动作。配方预览始终经 Admin 同源签名路由输出：优先读取已导入存储对象；开发环境没有挂载对应本地存储目录时由服务端回源模板素材的 `sourceUrl`，外部存储重定向同样由服务端转发；所有来源统一缩放并压缩为 JPEG，因此小程序不直连存储域名也不打包 WebP。当前活动 `roomi` 版本的 960 个模板均关联预览素材。
- 项目索引（2026-08-10）：按角色过滤的来源响应同时作为 AI 工作台项目只读模型；数据库继续保存原始 `FloorPlan`、活动 `AiWorkflow` 与小程序 `AiGeneration` 状态，服务端只派生 `generating | continue | retry | stale | ready | needs_survey`。界面据此分为“进行中”（当前员工与该正式户型存在精确活动方案）、“可开始”（通过共享正式户型资格校验且没有活动方案）和“待完善量房”（资格未通过），并返回“量房未完成 / 正式墙图无效 / 还没有闭合空间 / 缺少可用墙体”等稳定原因。来源响应不再过滤掉所有无闭合房间的 version-4 量房记录，而是保留给“继续量房”；兼容的扁平房间数组仍只包含可用房间。方案查询新增 `floorPlanId` 精确过滤；项目索引聚合当前阶段和当前员工可访问的最新小程序任务，不改变租户、角色或员工归属边界。直接进入页面时按服务端顺序优先自动选择首个合格的“进行中”项目，其次为“可开始”项目；若没有合格项目则直接打开三分组项目选择器。页面到达时不再跨项目查询方案，也不会自动弹出旧“选择客户方案”弹层；只有用户发起无法自动消歧的设计动作时，多个方案才要求显式选择。
- 目标上下文：选择户型后的方案查询统一使用 `floorPlanId + targetScope + roomId`（仅 `whole_floor_plan` 省略 `roomId`），并返回状态为 `missing | processing | ready | stale | admin_handoff` 的 `targetContext`。单房间成果必须同时匹配同一正式户型、`single_room` 和相同 `roomId`；全屋成果及缺少范围字段的旧任务不会自动填入房间。若全局已采用成果与目标精确匹配则优先使用，否则使用最新的精确成功成果；早于 `FloorPlan.updatedAt` 的成果仅保留在历史中，不自动续接。PostgreSQL 方案响应继续履行该契约：`workflowId` 在服务端精确过滤，每条匹配方案从租户范围内的生成记录补齐 `selectedTask`、`latestTask` 和精确 `targetContext`。创建页按方案 ID 而不是列表顺序恢复来源，并直接显示基准图已变化等真实错误，不再统一误报为能力配置加载失败。
- 归档线索处理：小程序 AI 来源索引会排除关联线索存在 `archivedAt` 的正式户型。带着已归档线索进入 AI 设计时，页面直接切换到已还原的 `ai-design-customer-workbench-empty-v2` 空状态，不再请求该线索的工作流；恢复线索后同一正式户型可重新选择。工作流、点数和权限边界不变。
- 已实现：企业共享 AI 点数与业务动作价格展示；主 Tab 以客户项目状态工作台为唯一首页，同时保留项目内“参考图复刻、空间换风格、户型生成、软装深化”四项真实任务。选择正式户型后，页面从 version-4 量房墙图派生可导航墙体、房间和完整户型/单房间目标，展示“空间基准、风格方案、软装完善、提案深化”四阶段方案旅程，并只突出一个上下文相关的“下一步”。当前有效且带有当前 `cutaway-v1` 导航渲染契约标记的完整户型成功结果会直接复用为 3D 导航封面；生成中的结果展示实时进度；缺失或早于户型最新更新时间的结果回退为确定性的正式 2D 墙图，并提供会明确消耗点数的生成/重新生成操作。页面不会为了预览自动创建付费任务。旧版无项目室内场景游览及其四个工具锚点已从该路由移除；没有可选择项目时只显示项目型空状态和恢复操作，独立快速 AI 能力不再作为客户项目工作台的隐式默认页。选择后的 `leadId`、`floorPlanId`、`targetScope` 和单房间可选 `roomId` 由四种任务共同继承，只有户型生成强制要求先关联。完整户型一次生成一张带家具、抬高等距视角且移除顶面的 3D 剖切概念图，单房间一次生成一张室内视角概念图；不批量逐房间生成。首页紧凑方案卡会自动带入唯一活动方案，多个方案只在用户执行无法自动消歧的动作时要求选择，并保留“新建备选方案”；创建页、结果页和历史记录显示客户/方案归属、设计范围及“已同步方案/独立任务”。参考图复刻的输出比例跟随参考图；关联正式户型时，服务端将所选完整户型或隔离后的单房间控制图作为第一张墙体/门窗结构输入，将参考图作为第二张镜头、画幅、构图与风格输入，不再要求额外上传空间图；未关联户型时继续使用“参考图第一、空间图第二”。整体换风格和软装深化则跟随空间图比例并使用后台风格预设；完整户型保持方图，单房间默认横图。页面支持相册/拍摄上传，按文件签名字节校验微信 multipart 图片，上传失败时保留稳定的本地预览并支持原位重试；同时支持异步供应商任务状态、任务失败重试/点数释放、按输出比例展示且以参考构图为复刻对比源的可拖动分割线、预览、分享，以及等待相册写入完成并在权限被拒时引导进入微信设置的真实图片保存，另有历史复用和删除。详情读取会对生成中的任务强制查询上游状态；首页“最近方案”会把 `created`、`pending`、`processing` 三种未终态任务置于其他状态之前，以绿色数字显示真实阶段进度且不伪造最低百分比，各状态组内仍保持时间倒序，每 5 秒刷新直至可见任务成功、失败或取消；成果详情同步显示数字与进度条，历史接口每次返回前最多同步 4 条可见生成中任务。首次加载、可恢复的刷新失败、成果缺图、供应商不可用和客户方案加载失败均有明确状态；客户方案查询失败时会阻止继续生成，避免成果静默写入错误方案。
- 工作流协同：显式 `workflowId` 会直接续接；未显式选择时，同一客户/户型只有一个活动方案则自动复用，存在多个方案则拒绝静默合并并要求客户端选择，也可明确新建备选方案。任务映射为 `base_render`、`perspective_upgrade` 或 `soft_furnishing` 阶段；小程序任务的成果图片持久化后，才会与后台场景任务使用同一套工作流同步：基准/软装阶段首个成功成果自动采用并推进，后续同阶段成果只作为候选；`pending`、失败或无成果图片的任务不能推进。小程序防重按方案、阶段、正式户型、目标范围和房间共同判断，因此厨房与客厅可并行，但同一厨房不会重复冻结点数；其他员工的同目标活动任务只返回忙碌状态，不暴露其任务跳转。自动续接传递 `sourceResultTaskId`，服务端在冻结点数前重新校验同方案、同目标和户型时效，将内部成果复制或把外部供应商 URL 下载固化为新的 `ai_generation_input`，并写入 `parentGenerationId`；它与手动空间上传互斥。历史“再设计”继续复用原始输入，缺少精确目标的旧任务只允许显式查看/再设计。换风格/软装可在小程序继续，提案/灯光提示到后台深化。无客户上下文时仍可作为独立快速任务运行。
- 视觉：成功态使用本地渲染的 Lucide 操作图标和极细分隔线，成果/对比容器跟随服务端输出比例，参考复刻以参考图作为对比源，并以 iPhone 13 Pro `390x844` 为基准调整布局。已选择户型的首页以 `design-references/all-pages-ip-v3/04-ai-design-home-v3.png` 为基础还原目标，项目切换版在所选项目没有合格成果时使用 `/images/ai-design-project-hero-v4.jpg`：该约 124KB 的本地场景素材恢复绿色“正式图纸 → 室内方案 → 小 K”构图，但不包含客户、状态、进度、按钮或其他业务文字；这些信息继续由原生组件和真实数据渲染。它可被当前员工为相同 `floorPlanId` 成功生成的完整户型 `floor_plan_render` 成果轮播替换。横向完整户型/房间范围栏只切换真实目标，不伪造量房合同没有提供的空间坐标；默认场景已经包含一次小 K，真实成果轮播才叠加纯角色“空间转换向导”。首屏在项目舞台下只保留一张上浮白色项目状态卡，直接映射后端派生状态并提供唯一真实下一步；四项已实现任务下沉到视觉层级更低的辅助工具区。客户/项目副标题仍打开项目选择器，清除来源移至该选择器内；多个活动方案只在用户执行无法自动消歧的动作时要求显式选择，实时最近成果仍保留在首屏工作台下方。自定义导航继续为实测微信胶囊预留安全区域。旧版未关联户型的空间游览与工具锚点已从该路由移除；无项目状态仍使用同一项目工作台信息架构。自动到达工作台和三分组项目选择器已在用户现有微信开发者工具窗口按 `390x844` 基准复核，最新证据为 `design-references/ai-design/ai-design-customer-project-switcher-v3/qa-restored-ai-design-entry-v4.png` 与同目录 `qa-restored-project-picker-v4.png`。打开抽屉时，绿色项目台会显示真实进度和四阶段轨道；抽屉按参考稿收敛为约半屏高度，项目同时存在正式户型预览与可访问成果时组合双图，远程缩略图加载失败则回退到正式导航/本地图标。项目抽屉打开时会显式隐藏共享自定义 TabBar，关闭或离开页面时恢复。有意保留的差异包括不误导的横向范围栏、可见点数元数据、胶囊安全通道、共享自定义 TabBar，以及既有统一任务图标而不是静态样例户型图。创建页现按 `design-references/all-pages-ip-v3/14-ai-design-create-v3.png` 还原：原生步骤轨道、小 K 材料板场景、正式户型/方案上下文、真实图片上传状态、服务端风格预设、继承且只读的设计范围、企业真实点数和生成主操作组成一条紧凑流程。由于该页没有房间选择器，参考图中的范围卡不会被伪装成可编辑控件；未上传时也不会用示例客厅图冒充用户图片。能力配置加载失败新增页内重试状态。设计稿衍生素材只包含场景图和风格缩略图，业务文字、控件、选中态、点数和上传内容继续原生渲染；路由、API、权限、点数扣费、任务校验、方案归属和正式墙图合同均不变。成果页现按 `design-references/all-pages-ip-v3/15-ai-design-result-v3.png` 还原：原生“设计成果”标题栏、一次小 K 成果交付提示和状态气泡引出按比例显示的前后对比区域；五个可见操作只对应既有的预览、保存到相册、微信分享、继续优化和查看历史。客户方案、范围、生成模式和点数仍由服务端任务数据决定，底部主/次操作继续复用既有的续接与历史处理函数。本次同样仅改变视觉表现，不改变路由、API、权限、点数扣费、任务/方案选择或正式墙图合同。
- 项目切换视觉与交互（2026-08-10）：`design-references/ai-design/ai-design-customer-project-switcher-v3/ai-design-project-state-matrix-v4.png` 是状态参考。胶囊安全区下方保留原生“AI设计工作台 / 让量房数据长成空间方案”和真实点数，选中态的紧凑圆角绿色空间项目台原生显示服务端状态、当前客户、正式量房身份、闭合空间数及常驻“切换项目”；生成图与默认图共享同一舞台。底部项目抽屉覆盖自定义 TabBar并保留底部安全区，支持客户/小区搜索和三个派生分组；卡片使用真实成果图或正式墙图导航读模型，无墙图缩略几何时显示统一本地图标，不复制 CRM 线索卡。`generating / continue / retry / stale` 分别进入真实任务进度、继续任务、失败处理或重建基准；`ready` 默认选择完整户型，随后仍可用横向范围栏切换闭合房间；`needs_survey` 只显示资格原因并通过 `utils/surveyNavigation.js` 跳转唯一正式量房入口。多活动方案仍要求显式选择，切换项目不会取消后台任务。本轮修改不改变点数价格、租户权限或 version-4 墙图持久化合同；`390x844` 现有微信开发者工具核验截图已保留在同一设计参考目录。
- 顶部轮播范围（2026-08-07）：`/images/ai-design-hero-v3.png` 是首页和已选户型的默认首图；未明确选择正式户型、所选户型没有当前成功的全屋户型生成成果时，都不得替换该图。用户明确选择某一正式户型后，顶部最多轮播 5 张由当前员工生成、`mode: floor_plan_render`、`status: succeeded`、`targetScope: whole_floor_plan` 且 `floorPlanId` 与当前选择完全一致的成果；服务端在当前员工边界内以 `heroFloorPlanId` 精确查找，避免该客户成果因员工其他线索的较新任务而落到“最近方案”分页之外。单房间生成、换风格/软装图、过期成果和其他客户线索的成果一律不能进入。每张轮播图点击后只打开它自己的成果详情。这使量房人员在多条客户线索之间切换时不会串图；除该窄查询外，不改变权限、点数扣费、方案选择或正式墙图合同。
- 成果页对比条件修正（2026-08-07）：只有存在真实上传/来源图片时才显示可拖动的前后对比；纯依据正式户型数据生成的 `floor_plan_render` 结果改为单图展示，不再渲染“原始户型/全屋效果”中间拖拽按钮。成果页路由、任务数据、权限与点数扣费契约不变。
- 成果页视觉对齐修正（2026-08-07）：按 `15-ai-design-result-v3.png` 收紧页面左右留白，增加接近方形的成果舞台高度，将来源/结果标签锚定到图片底部，重新校准操作栏图标与文字间距，并裁掉小 K 素材右侧多余装饰画。操作、数据、路由、权限与点数扣费均不变；原生微信胶囊/真机截图仍是待补充的视觉验收证据。
- 成果图交付修正（2026-08-12）：任务 DTO 现在为全部受保护的输入图、控制图和结果 `MediaAsset` 返回带时效、租户签名的同源 URL。平台有意保留 GRS `http(s)` 结果而不转存时，小程序会收到带签名的任务成果图路由；服务端校验租户/任务归属并转发经过格式验证的 JPG/PNG 字节，不再要求微信 `<image>`、预览、相册保存或分享链路直接访问未配置的供应商域名。包括任务 `1071` 在内的既有成功任务重新读取详情后即可恢复，无需重新生成或再次扣点；`MINIPROGRAM_API_PUBLIC_ORIGIN` 为示例值或无效值时会回退到真实/转发请求 Host，避免本机 `3005` 把有效签名图地址改写为 `api.example.com`。唯一批准视觉依据仍为 `design-references/all-pages-ip-v3/15-ai-design-result-v3.png`；WXML/WXSS 几何、操作、路由、角色/租户边界、方案选择、点数扣费和 version-4 正式墙图合同均不改变。定向序列化/签名回归覆盖受保护素材、供应商直链和公开 Origin 回退；任务 `1071` 原无签名素材请求已复现 `401`，同一张 `1,496,827` 字节 PNG 经签名交付返回 `200 image/png`。由于现有微信开发者工具窗口没有暴露兼容自动化端点，新的原生 `390x844` 截图仍待补充。
- 成果页还原度复核（2026-08-11）：继续以 `design-references/all-pages-ip-v3/15-ai-design-result-v3.png` 为唯一视觉基准，按 `390x844` 锚点压缩顶部交付提示，恢复右侧完整“小 K 成果交付者”、紧凑五项操作栏、四行等高方案摘要和正常流式底部主/次操作。成果舞台限制在 `720–760rpx` 的近方形交付窗口；接近方图使用 `aspectFill`，明显横/竖画幅使用 `aspectFit` 保留完整构图，不再让任意服务端比例把首屏结构拉散。分享图标改为与其余操作一致的本地绿色 Lucide 线框资源；候选/定稿状态不再占用成果摘要层级，方案归属、当前空间、生成模式与真实点数仍全部来自任务数据。路由、API、角色/租户权限、点数扣费、任务/方案选择和 version-4 正式墙图合同均不变；当前微信开发者工具窗口的兼容自动化端点仍待确认，因此新的带原生胶囊 `390x844` 截图是发布前检查项。
- AI 工作流子页面还原门槛（2026-08-11）：`design-references/html-prototypes/ai-workflow/` 下的独立 HTML 原型，将创建页、成果页和历史页分别映射到 `design-references/all-pages-ip-v3/14-ai-design-create-v3.png`、`15-ai-design-result-v3.png` 与 `16-ai-design-history-v3.png`。在 iPhone 13 Pro `390x844` 基准下，最终综合相似度依次为 `98.59%`、`98.70%`、`99.30%`，三页均通过综合相似度 `96%` 且资产来源/边界/裁切维度 `96%` 双门槛。12 个设计源区域先集中排入一张本地对比画板，再按 manifest 坐标导出切片；最终浏览器截图、同尺度叠图、逐元素台账、测量数据、评分权重与排除项均保存在原型目录。评分未遮罩任何应用内容，也没有把整页设计图作为实现。生产映射为 `/packages/ai-workflow/assets/page-ip-v3/ai-create.jpg`、`ai-create-style-{modern,cream,chinese}.jpg`、`ai-result.jpg` 与 `ai-history.jpg`，全部来自批准稿的精确切片；引用迁移后删除旧 `ai-create.png`、`ai-result.png`、`ai-history.png` 三张近似素材。生产页现进一步对齐创建页的通栏材料板场景和紧凑上下文卡、成果页的交付标题/四行摘要/首屏操作区，以及历史页带原生时间、目标、状态、进度和跳转提示的紧凑任务卡；历史元数据继续从现有任务响应派生，缺少缩略图时显示真实原生占位。旧 `packages/ai-workflow/legacy/ai-gen` 仍只负责兼容跳转。路由、API、角色/租户权限、企业点数、方案/任务选择、媒体行为和 version-4 正式墙图合同均不变；AI 工作流定向测试通过 `13/13`。本轮复用了当前微信开发者工具项目窗口，但其监听端点均未暴露兼容自动化协议，无法按规则触发新编译、断言页面栈并获取带原生胶囊的截图；该项 `390x844` 原生证据仍作为发布检查，且没有打开重复的开发者工具窗口。
- 顶部默认图对齐修正（2026-08-07）：选中户型后的默认 `/images/ai-design-hero-v3.png` 与生成成果轮播，都从自定义页头下方上移到顶部背景层；舞台在流式布局中等量补偿高度，因此背景可与未选户型状态一样到顶，而阶段轨道、任务入口和下一步仍保持原首屏锚点，不会被挤下或提前上移。该调整仅影响视觉布局，不改变路由、API、权限、点数、方案选择或正式墙图合同。
- 顶部项目副标题对比度修正（2026-08-07）：选中户型后，客户/小区副标题及其户型选择箭头改为白色，并增加克制的深色阴影，确保在明亮的 `/images/ai-design-hero-v3.png` 默认图和真实成果轮播上均可读；未选户型状态的灰色说明保持不变。该调整仅影响表现层，不改变路由、API、权限、点数、方案选择或正式墙图合同。
- 顶部默认态核验（2026-08-07）：用户现有微信开发者工具已在 `390x844` 基准加载“已选正式户型、没有可用全屋生成成果”的回退态，空间图仍可见，证据为 `output/ai-design-hero-default-qa-390x844.png`。自动化截图不包含微信原生胶囊，因此原生顶栏或真机截图仍待补充。
- 创建页状态与核验：上下文卡现在以当前设计模式为主标题，各模式按真实输入合同说明必需素材；继承的设计范围改为只读摘要，不再呈现成可切换的单选卡。生成可用态统一由供应商模式、必需图片、服务端风格选择和企业余额推导，底部通栏操作在 `390x844` 构图中保持可见，并直接显示最先需要补齐的条件；点数不足时会明确引导联系企业管理员补充。上传或提交过程中也会锁定风格选择。只有经过核对的本地衍生缩略图会映射到已知服务端风格键，未知风格使用原生说明占位，不再借用无关样片。用户现有微信开发者工具已在 `390x844` 基准同时核验带原生状态栏/胶囊的独立任务点数不足态、空间换风格点数不足态和正式户型全屋范围态，证据分别为 `output/ai-design-create-v3-qa-390x844.png`、`output/ai-design-create-v3-style-transform-qa-390x844.png` 与 `output/ai-design-create-v3-formal-plan-qa-390x844.png`；小程序全量测试通过 `185/185`，`git diff --check` 通过。
- 正式户型：入口传递 `floorPlanId`、显式 `targetScope: whole_floor_plan | single_room`，仅单房间携带 `roomId`。后台通过正式墙图读适配层派生尺寸、层高、开口摘要和导航只读模型，不修改 `FloorPlan.layoutData`；完整户型生成额外派生 1024px 墙体/门窗控制图并作为独立 `MediaAsset` 通过图片编辑生成抬高等距视角、移除顶面的 3D 剖切概念图，单房间户型生成通过量房摘要调用图片生成。成功的导航封面仍属于 `AiGeneration`/`MediaAsset`，绝不写回正式户型；来源接口只附加派生导航模型和经过权限校验的签名预览数据。关联正式户型的参考复刻同样派生控制图；选择单房间时只绘制该闭合房间及其门窗，并将控制图放在参考图之前提交。项目索引返回当前企业角色可访问的 version-4 正式量房记录；通过资格校验的记录附带闭合房间，未通过的记录只用于解释原因和继续量房，绝不能创建 AI 任务（设计师按分配线索/归属户型、量房员按归属户型、企业负责人按企业范围）。仅凭户型数据生成的是概念效果，不是施工级或像素级还原。
- 有限支持：仅有 `enterpriseId` 的企业员工可用，依赖平台统一供应商路由和企业 AI 点数；独立渠道地推不属于企业，因此不展示 AI 入口，也不会请求 AI API。服务端可在不改变小程序 API 的情况下路由到 GRS、Pollinations 或其他已配置兼容供应商。小程序场景始终使用平台配置的逻辑模型默认值，不向用户开放自由创作台的模型选择器或模型/分辨率价格控件。关联户型的复刻会同时发送量房控制图与参考图，冲突时以结构控制为准；二维墙图不包含实测相机位姿，供应商仍不保证对参考图进行像素级 1:1 复制。3D 导航封面的家具和未测量材质可能变化，过期后不会继续作为当前封面，也不能反向作为量房数据源。纯户型生成也无法推断未测量的材质和精确镜头。没有微信支付、局部遮罩替换或普通业主账户。媒体 URL 始终使用现有小程序鉴权资产接口：带签名的小程序资源无论使用本地还是七牛私有 Bucket，均由配置的 API 源站直接输出图片字节，不再跳转到第二个微信下载域名；非小程序签名的后台读取仍可重定向到短期七牛 URL。后台的七牛存储前缀只影响后续新上传的服务端对象 key，小程序页面和资产 URL 无感。默认情况下 GRS `http(s)` 结果图直接返回上游 URL，正式环境需将其主机加入微信小程序下载合法域名；当平台启用七牛转存后，只有后续结果改走七牛签名读取路径。七牛上传失败不会回退本地，历史资产继续按自身稳定配置读取。生产本地存储必须配置共享 `AI_ASSET_STORAGE_DIR`，小程序签名媒体地址还需要 HTTPS `MINIPROGRAM_API_PUBLIC_ORIGIN`。

### 我的与工作台

- 页面：`pages/mine/mine`，以及
  `packages/business/profile-edit/profile-edit`、
  `packages/business/settings/settings`、
  `packages/business/account-security/account-security`。
- API：`/api/miniprogram/mine`、`/api/miniprogram/profile`、
  `/api/miniprogram/profile/avatar`、带签名的头像读取路由、
  `/api/miniprogram/account/password`、`/api/floorplans`，并跳转线索、报备、提成、量房和 AI 设计首页。
- 已实现：资料/角色、工作台摘要、待办、户型列表、通知/账号入口、持久化昵称/头像编辑、微信订阅与系统权限设置、需验证当前密码的员工改密、退出登录、新建量房、企业员工 AI 设计首页入口和户型卡片上下文 AI 入口；设计师与测量员还会获得真实待办角标的“获客协作”工作台入口。
- 视觉：`我的` 页面以 `design-references/mine/miniprogram-mine-v6.png` 和 iPhone 13 Pro
  `390x844` 为基准。左侧收窄的半透明资料卡保留右侧家居场景，白色圆角统计托盘、前三项指标插画、
  固定四列节奏的角色工作台、两条紧凑待办和 AI 设计横幅使用从
  `design-references/mine/miniprogram-mine-v6.png` 与
  `design-references/mine/miniprogram-mine-v6-icon.png` 派生的生产切图；中置量房操作使用共享的生成式透明
  完整小 K 素材，角色手持激光测距仪并直接向上凸起，不再使用圆形背景、圆形描边或圆形投影底座；
  “量房”保持为独立实时文字标签，扩展后的共享内容区确保该原生“量房”
  标签不会被 iOS Home Indicator 安全区遮挡。资料、操作、指标和角色边界继续以
  服务端数据为准，既有跳转保持不变，不新增后端能力；角色返回超过三项指标时仍可横向查看，少于四项
  真实工作台操作时保留四列尺寸但不伪造业务入口；在宽度不超过 `360px` 的真机视口上仍维持四列，仅收紧
  卡片间距与横向内边距。待办区域展示接口当前最多三条记录中的前两条，并保留“全部待办”入口。在
  `390x844` 基准下，“我的”页面主标签与操作文字不小于 `24rpx`，辅助元数据与
  说明文字不小于 `20rpx`。
- 账号页面（2026-08-12）：原先共用同一方法的三个账号入口已经拆为三个真实页面。头像会通过当前配置的本地/七牛媒体存储归一化为 `512x512` WebP，并在 `users.avatar` 保存带版本的受管引用；签名读取通过 API 源站直接输出存储对象，不会暴露存储细节或要求小程序跟随七牛重定向，历史外链头像仍可读取。员工修改显示名时同时更新 `admin_users.displayName` 与关联用户资料。设置页只提供真实可执行的微信订阅和权限操作；每次请求前通过已认证的 `GET /api/miniprogram/notification-template` 获取有序四模板 V2 列表，一次调用授权四项。客户端只缓存完整 V2 配置，不再内置模板 ID；离线时使用最后一次成功缓存，没有缓存则明确显示配置不可用。设置页保持获批单行构图，状态区分“已允许”“已允许 N/4”“已拒绝”“已关闭”“未设置”和读取/配置失败。修改密码合同不变。
- 账号页面视觉还原（2026-08-10）：`profile-edit`、`settings` 与 `account-security` 以 `design-references/account/` 的 iPhone 13 Pro `390x844` 参考图为基准，统一使用绿色斜切服务场景头图和原生白色信息卡；`packages/business/assets/account-v1/` 内不含文字的小 K 场景派生图只承担装饰。资料、权限状态、账号事实、校验、加载/失败/重试、密码修改、退出登录、路由、API 与角色边界仍完全由既有真实合约驱动。
- 数据与失败态：普通用户户型的空间数从正式 version-4 墙图各楼层的闭合空间派生；
  工作台和户型请求分别维护加载、失败与重试状态，网络失败不会再显示为普通用户工作台
  或“暂无户型”空态。
- PostgreSQL 边界：资料、实时线索/正式户型/测量摘要、户型列表、报备记录和工作台待办均使用 typed RLS Repository。AI 生成域迁移前首页 `aiGeneratedCases` 仍返回 `0`；订单和提成仍由 MongoDB 支撑，不会把 PostgreSQL bigint 身份传入旧 MongoDB 查询。
- 有限支持：工作台内容和任务操作随专业角色变化。手机号同时参与员工身份匹配和普通用户户型归属，因此本次不开放换绑；普通微信用户没有修改密码操作。

### 推荐方案分享页

- 页面：`packages/business/recommendations/index`。
- 有限支持：已注册页面可展示本地风格/进度、选择一个风格、显示微信原生分享控件，并定义
  `onShareAppMessage` 分享内容。
- 占位：推荐方案仍为硬编码 mock；页面明确不提供没有真实后端路径的 PDF 下载、海报保存
  ActionSheet、本地交互日志占位或其他伪操作。

## 正式量房

- 浏览器算法验证台（2026-08-14）：`surveying-h5/` 是仅限本地的 H5 验证面，构建时直接打包当前生产 `surveying-editor.js`、`surveyWallGraph.js` 和 `surveyCanvasRenderer.js`，不维护第二套拓扑算法。浏览器桥只适配 `Page/setData`、存储、Canvas 查询、Pointer 输入、反馈、Fetch 和可控的模拟 BLE 读数；支持真实墙图拖画、平移/缩放、闭合、尺寸、撤销/重做和 version-4 JSON 导入/导出。场景目录现按单空间轮廓、连续测量、共墙多空间、交点与分支、墙体与构件分为 5 类 23 个代表场景，覆盖矩形/L/U/错台/梯形/切角/飘窗位、正交或斜向开放墙链、等高/上下/错层共墙双房、单房分割、三房/四房、直墙/斜墙 T 型与十字交点、门窗和不同墙厚。用户截图 `5faec65980dc0f15a9f19e98d98a5dfe.jpg` 对应 `staggered-adjacent`，定向回归为 8 段墙、2 个闭合空间及 `2761×3223 mm`、`3082×4120 mm` 两组尺寸；全部场景通过生产墙图 `full` 校验。场景自动适配按固定测距栏的实际覆盖高度预留底部安全区，闭合墙与尺寸链不再被控件遮挡；T 型三房按左房、右上房、右下房的现场顺序沿共墙逐间闭合，并用三向共享交点回归防止测量内缩形成墙面三角缺口。它不注册小程序路由，默认不写业务 API 或数据库，也不能证明真实 BLE、原生 `cover-view`、微信页面栈、设备运动传感器或真机 Canvas 兼容性；曲墙、圆弧、独立楼栋和楼层高差仍不属于 version-4 二维墙图能力。这些边界及原生能力仍须用现有微信开发者工具窗口和真机证据验证。生产页面入口、API、角色、测量审计及 `{ version: 4, measurementMode: 'surveying', surveyGraph }` 合同均不变。
- BLE 复尺失败原子性与尺寸规划回退（2026-08-13）：选中墙体后由底部控制坞触发的 BLE 复尺，会在写回墙长或重建闭合尺寸失败时恢复测距前的墙图、撤销/重做历史和显示状态，避免错误中间态在 `onUnload` 时自动写入本地草稿。`createClosedDimensionPlan()` 现在同时校验派生外轮廓和物理外墙；复尺使实体外墙变为斜墙时，整套只读尺寸回退 `createExteriorDimensionPlan()`，不再读取空的正交 frame。小程序尺寸算法源与后台镜像由 `admin/scripts/sync-survey-dimension-plan.mjs` 同步校验；页面路由、API、角色、version-4 墙图和测量审计合同不变。

- 独立 APK 还原研究（2026-08-10）：`research/legacy-zhouse-2d/` 已建立在小程序包之外，提供方法 RVA/证据状态台账、独立毫米制几何基础、平台无关绘制命令和隔离测试。当前首批方法只达到 `located`，尚未声明为 APK 算法还原完成；生产 `miniprogram/` 和 `admin/` 均不得导入该模块。它不注册第二个量房页面，不连接 BLE/API/数据库，也不能写入 `FloorPlan.layoutData`。正式页面、路由、角色、version-4 墙图和测量审计均不变。
- 独立 2D 还原小程序（2026-08-13）：`zhouse-2d-miniprogram/` 是单独的原生小程序工程，唯一页面为本地 `pages/editor/editor`。V0.1 已删除此前通用几何原型核心，改用 APK 形态的 `HousePoint2D`、`HouseWall2D P0-P3`、有符号墙厚、`wallSeries`、`roomSeries` 以及房间内/外/计算点状态。单个轴对齐矩形的四个已采样贯穿内墙方向，现已逐字段匹配标准化 APK `newWall`、输出空值、返回瞬间状态和已有稳定状态，等级为 `state-matched`；四墙矩形已匹配 APK 返回序列 `NewWalls (23) -> OnWalls (2) -> OnWalls (2) -> ClosedRoom0 (5)` 和恢复方法路径，但因没有采集完整闭合后 APK 状态，仅标为 `runtime-trace-matched`。Canvas 直接绘制 `P0 -> P1 -> P3 -> P2` 墙体四边形和 APK 房间字段。页面只开放证据覆盖的正交构造、矩形分割、墙体只读查看、历史、APK schema 本地 JSON、平移、缩放和全图适配；斜墙、曲墙、门窗、复杂拓扑、删墙/移墙、BLE、正式 version-4 导出、API、租户、角色和数据库写入均 fail-closed 或不存在。工程内 `docs/DEVELOPMENT.md` 和 `docs/method-porting-ledger.json` 分别记录持续路线图及可机器校验的方法/RVA/证据/fixture 状态。独立工程已配置非生产 AppID，运行时不依赖 `miniprogram/`、`research/legacy-zhouse-2d/` 或 `admin/`，不会注册正式量房入口，也不能写入 `FloorPlan.layoutData`。隔离、台账、APK 差分、历史、渲染器、fail-closed 和页面契约测试已通过：`cd zhouse-2d-miniprogram && npm run verify`。新工程的微信开发者工具/真机视觉证据仍待在单独导入的项目窗口补充；不得替换或复制现有生产小程序窗口。
- BLE 连接体验：编辑器中的每个实时 BLE 测距入口在设备未连接时，都会提示用户是否在当前编辑器中搜索已授权测距仪；不改变 API、角色边界、墙图合同或审计队列行为。
- 底部测距流程：当前已确认墙体、当前选中墙体、光标吸附到墙体、出现闭合候选，或当前墙段正等待长度确认时，控制坞直接请求 BLE 读数，并以毫米写回对应墙体；没有墙体目标时，保留既有的长度数字输入流程。两种路径均写入正式测量审计，不改变 API、角色边界、墙图合同或审计队列行为。
- 页面：`packages/surveying/editor/surveying-editor`；所有入口由 `utils/surveyNavigation.js` 传递 `leadId` 和/或 `floorPlanId`；入口有线索上下文时同时传递小区名，直接按户型进入时由 `GET /api/floorplans/[id]` 返回关联线索摘要。
- HTML-first 例外（2026-08-12 批准）：正式量房路由在获得明确开发授权后，Canvas 原生几何、测量、吸附、标注和状态渲染可直接实施，不要求 HTML 对照原型或相似度评分。设计源映射、定向测试、本中英文模块清单、还原台账以及现有微信开发者工具窗口/必要真机 Canvas 验收仍为必需；其他小程序路由不变。
- 放大镜渲染韧性（2026-08-12）：底部光标拖动期间，已批准的左上角放大镜以单一 Canvas 面板绘制，镜心绿色目标和底部元数据同层绘制，不再叠加原生 `cover-view` 容器；首个拖动帧会持续刷新直到该状态生效，避免快速移动或 `setData` 延迟导致放大镜漏显。其 Canvas 场景、几何、吸附标签、路由、API、角色、v4 墙图和测量审计均不改变。
- 画布光标拖动的放大镜覆盖（2026-08-12）：在 Canvas 内抓住当前光标并拖出墙体预览时，同一左上角放大镜也必须显示；此路径中绿色光标和辅助线仍只由正式 Canvas 绘制，轻量层只画放大镜，避免叠加第二个“底部拖放”方框十字光标。放大镜在正式 Canvas 重绘期间保持，不会被逐帧清空；其显示期间独占左上角区域，普通实时测量气泡不得叠在后方。镜心和 X/Y 标签必须使用方向吸附后正式预览的最终显示点，不能使用手指原始落点。画布平移、双指缩放和门窗拖动不冒充光标拖动，因而不显示放大镜。该调整不改变几何、路由、API、角色、v4 墙图或测量审计。
- 数据合同：`FloorPlan.layoutData` 只能是 `{ version: 4, measurementMode: 'surveying', surveyGraph }`，墙图单位为毫米。
- 画布绘制细化（2026-08-06，2026-08-12 更新辅助线与标注分层）：正式墙体轮廓、红色活动测量边、光标十字和闭合/对齐辅助线统一使用更细的工程制图线宽。蓝色 `[8, 6]` 十字只表示最后一个已确认点：初始和首墙未确认时不显示，继续拖墙时留在上一确认点，提交后才移动；自由拖放的轻量 Canvas 不再画跟手蓝线。墙面、顶点、轴向对齐等实际吸附状态使用橙色 `[8, 6]` 虚线，闭合路径使用橙色 `[12, 10]` 虚线，两者只覆盖真实受约束的轴或路径，并在松手、取消、重置、撤销、重做或状态切换时清除。小 K 引导连接线保持独立的绿色 `[5, 4]` 节奏与 `1.75px` 线宽。当前未闭合墙链的实时尺寸使用中性灰底牌和 `14px` 蓝色数值，尺寸线从活动测量面向外留出 `32px`；闭合空间的 `opening-segment`、`room-clear`、`building-overall` 属于永久工程标注，使用细灰线、`12px` 深色数值和克制白底，尺寸线从闭合外轮廓向外留出 `60px`。延伸线从尺寸线向靠墙侧固定绘制 `18px`，空间不足时在墙面前保留 `12px` 小间隙；两端使用总长 `4px`、约 `60°` 且中心与延伸线交点重合的短斜线，不使用箭头。红色活动测量边在辅助线和光标之后重绘，交叉处始终保持为最高层的墙体指示。最新状态依据为 `design-references/surveying/cursor-guide-state-reference-20260812.jpg`；页面路由、API、角色权限、v4 墙图合同、BLE 审计和几何交互均不改变。
- 实时/永久标注与外测量面续墙修正（2026-08-12）：实时尺寸和闭合空间尺寸不再共用蓝色实时标签样式。新相邻墙链从闭合房间可见外顶点起画时，会话命中的 `outer` 测量面贯穿全部活动墙和预览墙；红色测量路径、唯一光标/十字线及实时尺寸延伸起点都使用渲染后的外墙面，墙图锚点仍保留在源墙拓扑节点。实时标签继续沿该外墙面的外法向远离墙体排列，因此从左上外顶点先向左、再向下续测时，横向数值位于红线上方、竖向数值位于红线左侧。手输/BLE 毫米读数、测量内缩、路由、角色、API、version-4 持久化和测量审计均不改变。批准的交互依据为 `design-references/surveying/runtime-live-dimension-reference-20260812.jpg`；量房渲染、光标、闭合、尺寸规划和编辑器源码契约定向测试 `113/113` 通过，覆盖两面 L 形墙链及实时/永久样式分层。当前已打开的微信开发者工具窗口确认为本项目，但没有以 `autoPort` 启动，其监听端口仅提供内部调试协议，不能安全执行 Mini Program Automator 的重新编译、页面栈确认或截图；本轮未启动重复窗口，原生 Canvas 仍需在现有窗口启用自动化后或真机补验。
- 闭合房间尺寸端点与续墙避让修正（2026-08-12）：永久闭合标注的尺寸线与墙面保留 `60px` 尺寸带间距；延伸线从尺寸线向靠墙侧固定绘制 `18px`，空间不足时在墙面前留出 `12px` 小间隙。两端约 `60°`、总长 `4px` 的平行短斜线长度和倾角一致，且以同一尺寸线交点为中心，不再使用箭头。实时蓝色尺寸仍保留箭头。活动墙或预览墙从闭合房间外侧延伸时，尺寸规划把可见墙体实体纳入同方向外侧支撑，永久标注随之移到新墙之外。v4 墙图、尺寸值、路由、壳层、API、角色、持久化和测量审计不变；单元/Canvas 回归已覆盖，现有 DevTools 无自动化端点，原生 `390x844` 仍待补验。
- 顶部量测卡对比度修正（2026-08-07）：紧凑白色量测卡现在显式使用深蓝显示实时长度，角度分隔线改为低对比深蓝，可点击角度使用更深的橙色；仍然只有在存在真实长度且光标处于已放置状态时显示，因此不会引入空白卡片。此调整只影响表现层，不改变页面路由、API、权限或 version-4 正式量房墙图契约。
- CAD 平开门几何修正（2026-08-07）：开启状态的门扇，以及连接门洞两端短门套矩形的关闭位窄条，现在分别使用完整的细长描边矩形，不再以单根构造线代替。门套之间的窄矩形位于开启弧相反一侧的墙面，因此横墙、竖墙以及内开/外开旋转后都保持与目标 CAD 图一致的相对关系。90 度开启弧、墙体切口、点击命中、毫米字段、持久化、页面路由、API 和角色边界均不改变。Canvas 渲染回归测试已同时断言两个四边闭合路径及横竖墙方向；用户当前微信开发者工具已在 `390x844` 基准加载目标路由，但自动化截图未包含原生 Canvas 层，因此仍需真机画面复核。
- 门窗状态控件对齐修正（2026-08-07）：门的内开/外开使用等宽轨道和显式水平居中的文字行；窗口态彻底省略没有实际操作的“窗位 / 固定”整组。“继续测墙”与检查器之间保留 `28rpx` 间距，使用与检查器等宽的绿色描边次操作，并增加有授权记录的本地绿色“墙段向前延伸”图标；窗口态以 `design-references/surveying-editor-v5/surveying-window-inspector-continue-wall-v1.png` 为视觉基准。该按钮的最终表面、间距及图文几何直接写在简单的 `opening-resume-*` 原生类上，不再依赖祖先/子级复合选择器覆盖旧绿色基础样式，避免真机 `cover-view` 回退成纯绿色空按钮。此调整只影响表现层，不改变门窗行为、页面路由、API、角色、持久化或 version-4 墙图契约。
- 门窗尺寸编辑器 V1（2026-08-07）：按已批准的 `design-references/surveying-editor-v6/surveying-opening-editor-v1-dimensions-only.png` 收敛全屏编辑器，只显示门/窗真实尺寸、当前值、数字键盘和尺寸上下文预览。门态提供门宽、门高、墙厚、距左、距右，窗态额外提供窗台高；“同步更改墙体厚度”只在选中墙厚时出现。面板内重复的 BLE “测距”入口、入户门附加入口以及“翻转 / 模型”标签和模型库均不再渲染，底部常驻控制坞成为唯一测距入口，并将读数写入当前选中的门窗参数。已有开向/模型字段和 Three.js 尺寸预览仍保留在正式墙图及内部实现中，但 V1 不提供翻转或模型选择入口。构件编辑器开启时隐藏父级量房标题栏、右侧工具轨和门窗检查器，避免原生覆盖层与尺寸编辑流程重叠。该调整不改变页面路由、API、角色、毫米字段、version-4 墙图、持久化或测量审计边界。
- 门窗右侧统一检查器（2026-08-06）：选中门窗时，编辑器按 iPhone 13 Pro `390x844` 基准还原 `design-references/surveying-editor-v4/surveying-opening-inspector-delete-only-v1.png`。宽度、高度、门态的内开/外开二选一、绿色“编辑”主操作和唯一的红色“删除门窗”次操作统一收进不透明的浅灰绿检查器，危险操作前使用一条短发丝线分隔；窗口态不增加无功能状态行。选中门窗时不再显示拆分、添加和布置；新增门窗仍从既有墙体上下文进入，规划中工具在其他位置维持原有限制。未闭合墙链仍在检查器下方提供既有“继续测墙”动作。该调整只细化表现与选中门窗的控件表面，不改变页面路由、API、角色边界、v4 墙图、测量审计、门窗字段、构件编辑器或持久化行为。
- 视觉基准：以 `design-references/all-pages-ip-v1/03-surveying-editor-idle.png`、`18-surveying-editor-active.png`、`19-surveying-state-board.png`、`design-references/all-pages-ip-v1/ChatGPT Image 2026年8月5日 15_44_17.png` 和 `design-references/surveying-editor-v2/sub2api-20260804-095739-1.png` 的 iPhone 13 Pro `390x844` 画面为准。交付编辑器采用参考稿的全宽工作区、四项右侧工具栏，以及聚合撤销/重做、光标放置和 BLE 测距的单一底部控制坞。自定义顶部始终为右上微信胶囊预留安全区：关联线索小区名和真实保存状态位于胶囊左侧，引导/保存/完成位于胶囊下方独立操作行；缺失时才回退为“未填写小区”。每台本地客户端默认开启不会遮罩画布的状态跟随式引导；顶部固定的“引导”操作在启用时使用绿色本地助手图标并可持久化开关，重新开启时从当前真实墙图、光标、BLE、选中对象和面板状态继续，不重播固定步骤。单一白底浅绿描边的小 K 对话框依次覆盖首墙方向、待确认长度、独立墙链首墙内外测量边、续墙、可闭合、空间闭合后的继续量房、下一空间光标吸附和墙体/门窗编辑；各操作状态按目标位置自动选择本地透明左指、右指或下指小 K，墙线、测量边、闭合路径、方向和吸附状态全部由真实 Canvas/控件动态呈现。关闭引导只隐藏教学气泡与高亮，不影响“可闭合”、测量边、吸附、BLE、错误或完成提交反馈，也不写入墙图、草稿或测量审计。当前已确认墙体、当前选中墙体、光标吸附到墙体、出现闭合候选，或当前墙段正在等待长度确认时，控制坞的测距会直接以实时 BLE 读数写回对应墙体；没有墙体目标时才打开既有长度输入。连接样式由 BLE 回调的实时状态驱动，不改变角色边界、墙图合同、审计队列或导出范围。
- 引导条件使用不生成原生节点的 `block`，只有可见气泡及其关闭按钮参与触摸命中，显示引导时画布、右侧工具栏和底部控制坞仍可操作。
- 引导视觉：每个有真实操作目标的量房状态均使用“小K测量搭档”表现层。白底浅绿边框对话框包含绿色“小K提示”标签、本地绿色星芒图标（`images/mine-icons/tab-ai-active.png`）、关闭按钮和可读的多行行动提示；带本地透明的左指、右指、下指小K素材（`packages/surveying/assets/surveying-guide-k-left-v3.png`、`-right-v3.png`、`-down-v3.png`），统一从单张生成画板 `design-references/surveying-editor-v3/sub2api-20260805-075309-1.png` 裁切而来。卡片与完整小 K 姿态作为一个组合共同选位，禁止互相覆盖；障碍集合包含安全区、工具栏、控制坞、顶部测量卡、对象工具栏、目标控件、全部墙体/预览墙、门窗、尺寸文字与尺寸线、房间信息。尺寸和房间文字属于不可穿越障碍，墙体、门窗、红色测量边和尺寸线按权重避让。绿色虚线先对多条贝塞尔候选采样，全部受阻时使用平滑网格路径绕过标注；仍无安全路径时，本帧省略连接线而不穿越标注。上一有效布局具有稳定性偏好，避免小幅平移后跳位。底部控制坞目标按真实 `575rpx × 108rpx` 尺寸和 `64rpx` 底部偏移计算按钮中心；Canvas 虚线在原生控制坞下方完成交接，由真实 `cover-view` 按钮内部的原生描边标记显示更高层高亮。标记仍属于按钮触摸树，不存在会独立截获拖拽或点击的全局覆盖层。纯绿色背景的说明气泡不再绘制，白底小 K 对话框成为唯一教学说明；测量边切换和“可闭合”操作本身仍可用。关闭气泡会关闭本机持久化引导偏好，但不会影响闭合、测量边、吸附、BLE、错误或提交反馈。面板、数字键盘、构件编辑器和测角面板开启时仍由面板自身说明接管。该表现层不写入墙图、草稿或测量审计。
- 引导卡的所有像素定位统一按 `390px` 参考宽度缩放，并在卡片与真实空间目标之间为小K姿态和连接线预留距离；不得虚构编号阶段或分页教程。卡片带有清晰、指向真实目标的浅绿描边对话框箭头；小K必须朝向目标，绿色弧形虚线箭头从其朝向目标的手出发并终止于真实目标。工作区网格以低对比 `250mm` 小步进和克制的 `1250mm` 引导线绘制，保留墙图的毫米单位，但不会让空画布比参考图更沉重。标题、真实草稿/保存状态和绿色状态点拆为独立顶部元素，以保持垂直居中且绝不重叠；`引导`、`保存`、`完成` 使用显式 `20rpx` 相邻间距，不依赖原生 `cover-view` 对 flex `gap` 的支持。右侧工具轨使用本地绿色激活态 PNG，避免不同真机对 `cover-image` CSS 滤色支持不一致。
- 已实现编辑行为：启动恢复、本地/云端草稿、直墙和斜墙预览/确认、BLE/手输墙长、复尺、共享墙闭合（包括重置光标后从既有边界形成的闭合和补边候选）、带紧凑绿色圆形“合”操作及几何锚定小 K 引导的提示性闭合候选，以及从待确认斜线预览直接闭合（“合”会先按当前预览长度落墙再闭合）。直线模式的独立阶梯墙链会优先沿末墙方向补到与起点同轴的拐点，再以第二条正交边回到起点；两条推导补边均须通过既有墙体相交与重叠校验，不再用斜线跨越房间。同一未闭合墙链上连续向前、同向共线且绘制模式、墙厚和测量侧兼容的拖动会延长末墙，不再持久化人为分段；真实转向、绘制模式/墙厚/测量侧变化、独立墙链或闭合边界仍生成独立墙。共享边仅在待切节点实际位于该墙段时切分，保留合并空间外壳所需的全部既有边界；共享墙为新相邻房间切段时，会按每个既有闭合空间自身有向 `wallIds` 链所需方向插入分段，即使原房间采用反向边界顺序，也继续保持闭合，不会在相邻房间闭合后丢失填充、标签、尺寸或面积；门窗、开口尺寸/开向、在已有顶点、墙体内边、墙体外边或画布空白处放置墙链光标，以及由小 K 对话框说明且随目标避障定位的独立墙链第一面确认墙内/外测量边。吸附到未闭合墙体或顶点的续画墙链继承既有边界且不开放切换；从已闭合房间的内/外边角续画时，首条墙会按与拖动方向共线的原房间边界外法线自动选择能让新墙实体连续对齐的一侧，内/外吸附仍分别保留真实起算点，并在预览及首墙确认后显示“当前测量位置”切换控件。内边顶点优先于邻近内边线，但更近的外边不会被内边顶点覆盖；命中墙体外边时仍保留外边测量侧语义，但持久化的共享边界节点会投影回源墙中心线，使直角对齐、闭合候选和闭合房间墙链使用同一坐标；闭合房间的墙体外壳及外侧转角由闭合边界推导，不受所选测量边影响；撤销/重画、完成提交和测量审计队列/补写。
- 共用墙删除合并（2026-08-12）：删除恰好由两个有效闭合空间共同引用的唯一共用墙时，墙图删除该墙及门窗，并把两侧剩余墙段按节点连续性重建为一个新闭合空间；画布只显示合并后的单一填充、房间标签、永久尺寸和净面积。从共墙外墙面起测时，上、下外围墙会在共墙端保存墙厚内缩；合并成功后只清除新外环中指向已删共墙端点的这些失效内缩并刷新对应墙长，使墙体布尔实体恢复连续，不删除或合并用户未选中的外围墙、墙 ID 或门窗；清除起点内缩时还会补偿门窗偏移，保持其绝对墙上位置不动。若剩余边界不能组成唯一闭环，或删除的是外墙/单空间边界，则相关空间仍转为未闭合。确认弹窗沿用现有原生危险操作样式，仅说明这两种结果。页面路由、API、角色边界、version-4 顶层合同和测量审计不变；定向 Canvas 回归覆盖共享墙合并、外墙删除反例以及 `1723 + 200 + 2249 = 4172mm` 的外墙面起测缺口场景。
- 光标松手吸附一致性（2026-08-12）：底部光标拖动期间会保留最后一帧已显示的吸附候选。显示为墙体外边或外边顶点吸附时，`touchend` 直接提交同一目标，不会因释放事件回报的原始触点靠近内边而重新判成内边；未显示吸附时仍按释放坐标自由放置。该修复不改变页面路由、API、角色边界、version-4 墙图或测量审计。
- 外边吸附光标落点（2026-08-12）：光标吸附到墙体外边或外边顶点并松手后，静态光标继续绘制在同一可见外边目标上，不再回到内边。关联的墙图拓扑节点仍保留在源墙中心线，以维持共享墙闭合和持久化；只有光标在新墙链开始前使用保存的外边投影。该修复不改变页面路由、API、角色边界、version-4 墙图或测量审计。
- 底部光标状态明确化（2026-08-12）：画布上已经放置光标时，包括恢复已闭合空间后的光标，底部控件保持“重置光标”。点击后才进入 `wallSnapPending`：控件本体成为准星拖拽起点，独立辅助文案显示“光标拖动到墙体”，不再把长文字塞进按钮。拖动中起点淡化为幽灵标记，既有 Canvas 准星继续随手指进入画布，明确表达从按钮位置拖出光标；再次放置新的墙体起点后恢复“重置光标”。该表现层状态调整不改变页面路由、API、角色边界、version-4 墙图或测量审计。
- 两面直墙即可提示闭合（2026-08-07，2026-08-12 更新状态色）：独立直线墙链确认相互垂直的第二面墙后，立即按末墙方向和首墙正交轴推导两条缺失补边，Canvas 绘制橙色闭合虚线并显示紧凑“可闭合”操作，不再要求用户先拖出第三面墙。补边仍只是提示，只有明确点击确认才写入正式墙图；斜线墙链和共享边闭合继续使用原有门槛。该调整不改变入口、API、角色权限、version-4 墙图合同或测量审计。
- 闭合目标直接吸附闭合（2026-08-11）：直线模式下，只要最终预览端点已经与当前有效闭合目标准确重合，松手就按当前预览落墙并立即闭合；这项可见吸附状态优先于可能滞后一帧的真机触摸坐标，原始拖拽点进入目标容差仍作为后备判据。该规则同时覆盖独立墙链回到首顶点，以及相邻房间从第三面新墙开始命中共享墙闭合点；只有正交投影产生了候选、但最终预览端点并未真正落到目标上时，才保留提示性“合”操作，斜线继续保留角度/长度确认流程。用户补充的 `a91d3f532111f4270a1ba1a13469f806.jpg` 是相邻房间预览已重合但未自动闭合的缺陷状态依据。入口、API、角色权限、version-4 持久化和 BLE/手动测量审计均不变；墙图/编辑器交互定向测试通过 `48/48`。
- 同一未闭合墙链可沿末墙反向回拖，以缩短当前末墙并保留原墙体和端点 ID；已闭合空间、共享末端、分支节点或含门窗的墙体不开放该便捷修正。该操作不改变路由、API、角色权限、v4 墙图合同或测量审计行为。
- 矩形补全吸附会持续作用于该第三面末墙的缩短和延长：即使先拖出半墙，后续前推或反向回拖时，末端仍会吸附到首墙的正交轴线。
- 删除未闭合的当前第三面末墙时，会保留剩余墙链的起点及矩形吸附参考；删除闭合空间墙体或其他非末墙时仍按既有重置规则处理。
- 重置光标吸附到既有边界后，重新起画墙链的第一面直墙在推导闭合边接近直角时，会恢复矩形补边吸附，使预览端点、直角辅助线和闭合候选落在同一坐标。
- 闭合房间边角测量侧修正（2026-08-08）：从闭合房间角点续测时，先选择与首段共线的既有闭合边界，再按该边界实体的外法线自动选择新墙所在侧；左下内边角和对应外边角向下拖动均自动判断为右侧，内/外吸附仍分别保留真实起算点。首段预览和首墙确认后均显示现有“当前测量位置”切换按钮，点击会同步翻转红色测量边、预览墙体、正式首墙及后续墙链。未闭合墙体吸附仍保持锁定。该修正不改变入口、API、角色权限、version-4 墙图合同或测量审计。
- 闭合房间续测起算面与闭合修正（2026-08-08）：从已闭合房间角点续测时，第一面新墙按拖动方向找到垂直的既有闭合墙，并用其真实 `thicknessMm` 的外墙面作为测量起点；拓扑节点仍连接共享边界，但手输/BLE 读数、`lengthMm`、红色测量线、预览终点和确认后的墙体均从外墙面起算。墙体可选持久化 `measurementStartInsetMm`/`measurementEndInsetMm`（旧墙缺失时按 0），复尺、延长/缩短、开口偏移和墙体切分均保留实际读数语义。第一面新墙不生成 `merge` 候选、闭合虚线或闭合按钮。当前测量位置控件改为 Canvas 绘制并按墙体法线旋转的双向矢量箭头（竖墙左右、横墙上下、斜墙随法线），切换测量侧不改变起算面或读数。
- 闭合房间第二面墙吸附修正（2026-08-10）：从闭合房间角点续测的第二面直角墙只进行正交吸附，不创建 `merge` 候选、闭合虚线或闭合按钮。光标按当前位置就近吸附到对侧的内墙拓扑角点或斜接后的外墙角点，两种点位均可独立命中；从第三面新墙开始才允许进入闭合判断。持久化节点、墙长、API、角色、version-4 合同和 BLE 审计内容不变。
- 外墙吸附缩放对齐修正（2026-08-10，2026-08-12 更新状态色）：正式 Canvas 的可见墙厚在所有支持的视口比例下均按 `thicknessMm` 同比缩放，使可见外墙面、橙色外边吸附状态线、对齐辅助线和预览终点在双指缩放前后始终落在同一 Canvas 坐标，不再因墙体外壳受固定像素限宽而分离。该调整只改变渲染及编辑器命中/覆盖层几何，不改变墙图坐标、持久化墙厚、入口、API、角色或测量审计。
- 外边顶点松手稳定性修正（2026-08-11）：拖拽放大镜已经命中斜接后的外墙顶点时，松开底部光标后，绿色静止光标与蓝色整屏十字辅助线继续停在该可见外边顶点，不再重绘到内侧拓扑角点。墙图锚点仍使用源墙中心线节点，已记录的外边吸附侧、墙体几何、入口、API、角色、version-4 持久化和测量审计均不变。用户提供的真机前后状态截图 `cc5e4de4589b3280c15d230fa3367692.jpg` 与 `d955597eeb981aea1c1fac6ea8fc4353.jpg` 是本次缺陷/设计状态依据；墙图与编辑器渲染定向测试 `89/89` 通过。由于当前微信开发者工具窗口未暴露兼容的 Mini Program Automator 端点，新的 `390x844` 真机截图仍待补充。
- 闭合房间续测工作线修正（2026-08-12）：命中外边仍保留为有效吸附元数据，并继续决定新墙实体与相邻墙体的对齐侧；但 Canvas 光标、橙/红预览测量线和实时尺寸统一使用操作员实际拖出的中心线工作锚点。由此消除闭合房间续测时黑色墙线旁平行出现的外边光标/测量线；version-4 图坐标、手输/BLE 毫米值、API、角色、持久化与测量审计均不变。
- 外边角显式候选修正（2026-08-11，2026-08-12 更新）：同一闭合房间边角同时存在内外顶点候选时，位于内角一墙厚保护半径内的落点保持内边顶点；只有落点明确进入可见斜接外角末端带，或已离开内角保护区且外角更近时，才吸附外边顶点。这样既不会因手指或放大镜标记轻微压入墙体而把相邻房间起点改成外边，也保留明确选择外角的能力。用户提供的 `codex-clipboard-3c0e6474-c194-48f3-b8cb-65ab5e43b2db.jpg` 与 `d2a3272efb1adc354dd7ee9479850896.jpg` 是显式外角状态依据；WXML/WXSS、入口、API、角色、version-4 持久化和测量审计均不变。现有微信开发者工具窗口未暴露兼容的 Mini Program Automator 端点，因此新的 `390x844` 路由确认交互截图仍待补充；本轮没有打开重复窗口。
- 远距离顶点轴向吸附（2026-08-12）：底部光标放置与直墙预览现在会把有效闭合房间内边顶点或可见外边顶点的水平/垂直延长轴作为对齐候选；只要对应单轴误差不超过 `350mm`，即使另一轴相隔数米也能吸附。二维近距离顶点/墙边命中、矩形补边、共享边闭合和直接闭合仍保持更高优先级。沿远处顶点轴线放置的独立起点会创建自己的拓扑节点，不会错误连接到远处旧顶点；手输和 BLE 确认长度时会重新应用同一对齐，最终持久化端点不会从预览辅助线跳开。用户提供的真机截图 `b3868f487984b1db1c1875b8934e3cc1.jpg` 是本次交互状态依据。WXML/WXSS、入口、API、角色、version-4 持久化和测量审计内容均不改变。光标、Canvas 与闭合定向测试 `91/91` 通过；现有微信开发者工具窗口没有兼容的 Mini Program Automator 监听端口，本轮未另开重复窗口，新的路由确认 `390x844` 交互截图仍待补验。
- 内边顶点闭合目标修正（2026-08-12）：编辑器显式命中房间1内边顶点后，`snapCursorToWall()` 保留该顶点的 `nodeId` 和 `inner` 吸附语义，不再按松手坐标重新判定到相邻外墙面；从房间1续测并闭合房间2时，光标和闭合起点保持用户选中的内边顶点。路由、API、角色、version-4 墙图合同和测量审计不变。
- 画布内边顶点吸附路径修正（2026-08-12）：画布内“重置光标后点墙”现在和底部光标拖放一样，先解析并把同一份顶点/墙体吸附候选传入墙图；房间1内边顶点不再以原始点击坐标重新判定为外墙面，随后闭合相邻房间时保持用户选择的内边起点。路由、API、角色、version-4 墙图合同和测量审计不变。
- 内部墙拆分房间（2026-08-13）：当光标从唯一已闭合房间中某面墙的中段开始，直线预览到达对侧边界时，预览以及手输/BLE 长度确认都会截断在首个边界交点。确认后按需切分两端边界墙，复用实测分隔墙作为共享墙，并把原闭合边界拆成两个闭合空间；光标不能再穿过对侧内部墙继续形成自由墙链。该变更仅作用于本地 `survey-wall-graph` 拓扑：页面路由、API、角色、version-4 持久化结构、毫米读数和测量审计均不变。用户提供的运行截图是本次交互参照；光标放置定向回归测试已通过，现有开发者工具窗口尚无兼容自动化端点，因此原生 `390x844` Canvas 核验待补。
- 内外角触控保护及删墙稳定性修正（2026-08-12，测量语义于 2026-08-14 修订）：闭合房间内角与斜接外角同时落入 `350mm` 触控容差时，内角在相邻闭合墙最大墙厚范围内保持优先，外墙面投影不得越过该保护抢占；明确进入可见外角末端带时仍可选择外边顶点。此前“新墙首尾内缩均为 0”的解释已废止，当前统一遵守下面的交点墙厚排除规则。删除相邻房间墙体前后，未删除墙的拓扑点、有效测量点和外轮廓仍须稳定。WXML/WXSS、路由、API、角色、version-4 墙图合同和测量审计不变。
- 内边相邻房间闭合几何修正（2026-08-14）：直线相邻房间从已闭合房间内边顶点起测并回到另一内边顶点时，拓扑节点继续落在两个墙体的真实交点，但所有测量记录排除交点覆盖的完整墙厚。首墙保存 `measurementStartInsetMm`，末墙保存 `measurementEndInsetMm`，`lengthMm` 始终是手输/BLE 净测距；新空间对复用共享墙写入 `wallFaceOverrides: 'offset'`，使净尺寸和面积使用共享墙另一侧的真实房间面，不能把拓扑跨度中的墙厚重新计入。房间1 `2205 × 2901mm` 与房间2 `2834 × 2901mm` 的建筑外包应包含外墙与中间共享墙厚，为 `5639 × 3301mm`。version-4 顶层结构、路由、API、角色和测量审计不变。
- T 型交点测量边修正（2026-08-14）：从既有墙中段垂直拉墙时，吸附立即把源墙切为两条同源拓扑边，新墙与两段复用同一度数为 3 的节点。分支墙红线从源墙远侧墙面开始；源墙两段分别保存自己的端点内缩，位于分支墙实体一侧的墙段从完整分支墙厚之后开始，另一段终止于拓扑交点，因此选择任一段都不会让红线穿过相交墙体。墙体端帽仍按拓扑节点连接判断，不受测量红线内缩影响。用户当前缺陷截图 `6475f5780dc029bfa918dba09954fc4c.jpg`、`99e89446853768a52b11fc60baa8dc59.jpg`、`9e37904e8637f3c7280319a6a911d9b9.jpg`、`e0e4092fae8ff103e29260a78a0aff35.jpg` 与目标截图 `d4920bfd0ddb84a810c2109b344016d1.jpg`、`f275da8992b3901e9165f4d13d28c81e.jpg` 是本轮行为依据。光标/拓扑与 Canvas 套件 `105/105`、小程序全量 `300/303` 通过；3 条既有失败与本轮无关。现有开发者工具窗口没有兼容自动化端点，原生 `390x844` 交互截图仍待补验。
- T 型实体连续性与量房场景矩阵（2026-08-14）：`measurementStartInsetMm`/`measurementEndInsetMm` 缩短测量红线、尺寸和门窗坐标。同源切分后的被贯穿源墙，其 Canvas 实体、命中区域和实体并集输入继续覆盖原始拓扑节点，因此分支墙可从读数中扣除完整墙厚，而源墙不会在交点出现白色矩形缺口；没有 `topologySourceWallId` 的新拉分支墙及预览则使用有效内缩端点生成自身实体与命中范围，从源墙远侧墙面开始，不再把浅色分支带画进原墙。斜墙实体并集会先归并亚像素级近重合连接点再闭合轮廓；删除 T 型或十字分支时，则按该节点仍然存在的墙重新计算同源切分段端点内缩，不遗留旧扣减。测试先行矩阵共覆盖 780 组直墙/斜墙、开放/闭合、外墙/共墙、内外边吸附、四向旋转、非等厚、门窗迁移、删除重画、十字删除、正式保存恢复、重置续画和固定种子回放场景，`12/12` 个矩阵组全部通过；相关墙图、Canvas、实体并集、光标及最终场景像素专项测试 `124/124` 通过。等厚/非等厚外墙 T、斜墙 T、截图式双房间外墙 T、共墙 T、十字及删除恢复七种代表场景输出到 `tmp/survey-topology-visual-regression.png`；标记交点内没有完整背景像素、内部黑色端帽或接缝，人工目检也通过。小程序全量 `323/326`，3 条既有获客、API 环境和离线调试失败与本轮无关。本修正不改 WXML/WXSS、路由、API、角色、version-4 字段、BLE 审计、房间尺寸或门窗绝对位置。现有开发者工具窗口仍无兼容自动化端点，原生 Canvas 复验待补。
- 墙体选中交点中性化（2026-08-14）：以用户批准的目标状态 `d4920bfd0ddb84a810c2109b344016d1.jpg` 和 `f275da8992b3901e9165f4d13d28c81e.jpg` 为局部行为依据，选中墙体的红色填充使用垂直端帽，并按每个拓扑端点处非共线相邻墙的实际实体占用范围裁切；不再继承实体斜接角，也不直接复用测量内缩。因此普通转角不出现斜切，T 型/十字/房间分割交点只保留真实相交墙厚的原灰色，两侧不会一边侵入、一边额外退让。实体并集轮廓、拓扑节点和 `topologySourceWallId` 保持原契约；被贯穿源墙的 `bodyPolygon` 与命中范围仍覆盖拓扑节点，新拉分支墙则从源墙远侧墙面开始。路由、API、角色、version-4 数据、毫米读数、复尺、门窗和测量审计不变。房间分割缺陷状态 `codex-clipboard-15fa0150-9139-490d-92e1-e424da9531d5.png` 与 `codex-clipboard-1337ae98-24ed-4e5d-b5ce-52fb651562f6.png` 已由双侧对称回归覆盖；本轮 T 型目标 `3004eb002d311652998850a69781cf2d.jpg` 要求分支墙不从原墙内边拉出。选中裁切由 `wall-local-selection-junction-clip-v11` 引入，当前渲染标识 `wall-local-branch-far-face-v12` 进一步修正分支实体起点。Canvas 专项 `61/61`、H5 合同 `3/3` 及 H5 构建通过；小程序全量 `336/339`，同样 3 条获客、API 环境和离线调试失败与本轮无关。
- 真机 Canvas 交点绘制回退分析与修复（2026-08-14）：对比当前提交 `d3d3a1c` 与上一稳定提交 `ad28143` 后确认，回归来自把“闭合/开放墙分别按并集闭环一次填充、完整并集闭环一次描边”改成“逐输入多边形填充、逐边界段描边、再以设备像素扫描线重画连接补片”。后一方案让浅色开放墙补片与深色闭合墙在混合 T 型/边角处争夺颜色所有权，且正式帧和平移帧的像素采样不同，所以会出现随机掏空、缺角或拖动画布后恢复。当前恢复上一版本的并集环绘制契约，同时保留拓扑节点实体与测量内缩分层、场景代次和迟到回调保护；`joinPolygons` 仅保留诊断元数据，不再额外着色。正式帧和手势帧结构测试锁定每个非空颜色组一次闭环填充、完整墙体一次闭环描边、零墙色扫描线；渲染/实体/视觉/视口专项 `68/68` 通过，七场景图集按运行时并集环顺序重生成并人工检查连续。小程序全量 `326/329`，3 条既有获客、API 环境与离线调试失败无关。渲染版本为 `git-union-ring-restore-v6`。WXML/WXSS、控件、路由、API、角色、墙图数据、测量数值、房间面及门窗位置不变；现有开发者工具仍无兼容自动化端点，真机 `390x844` 复验待用户确认。
- 真机 Canvas 帧所有权补强（2026-08-14）：每次正式场景构建分配递增代次，过期 `setData` 回调直接丢弃；平移/缩放占用主 Canvas 时，图片加载等正式重绘只能挂起，并在手势结束后由最新场景一次性交接。画布尺寸查询、主 Canvas 初始化和拖拽 Canvas 初始化使用各自代次，避免迟到回调重设 Canvas 尺寸并清空当前帧；页面卸载后也不再接受回调。扩展后的渲染器、拓扑、视觉、视口和编辑器专项 `99/99` 通过。vConsole 会输出渲染版本 `native-polygon-frame-owner-v3`，可用于确认真机实际加载了本轮代码。
- 闭合边角墙面自动对齐修正（2026-08-11）：从闭合房间外边顶点拉出的首墙现在继承相邻闭合边界实体的外法线。用户提供的 `fb00dd97d15453c27336410cb7313410.jpg` 场景中，从左上外边顶点向左拉墙会自动选择 `right`，新墙外边与右侧既有顶部墙面共线，不再要求操作员手动点击“当前测量位置”；该按钮仍保留为明确覆盖操作。墙图与渲染定向测试 `77/77` 通过；入口、API、角色、version-4 持久化、毫米几何和测量审计均不变。当前微信开发者工具窗口未暴露兼容自动化端点，新的 `390x844` 截图仍待补充。
- 共享墙末端内缩光标修正（2026-08-10，2026-08-12 更新辅助线归属）：蓝色整屏十字辅助线只归属于最后一个已确认显示点；共享墙闭合预览的有效测量终点即使因墙厚产生末端内缩，也不再在红色测量终点重复绘制第二组蓝色十字，避免两组标记随缩放倍率增大而分离。有效墙长、红色测量边、闭合拓扑、入口、API、角色、version-4 合同和测量审计均不变。
- 同轴闭合预览与墙体归一化（2026-08-10，2026-08-12 更新状态色）：返回既有闭合房间边界的橙色闭合虚线从当前测量墙轴线出发，并终止于确认闭合实际使用的外墙面位置，不再横向平移到共享墙外侧斜接点。若推导出的第一段闭合边与当前末墙同向共线，且模式、墙厚和测量侧兼容，确认闭合时直接把当前墙延长到拓扑目标，保留原 wall ID 并写入外墙面末端内缩，不再持久化一条重复的同轴墙；真实转角仍新增独立墙。入口、API、角色、version-4 合同和 BLE 审计内容不变。
- 错台相邻房间闭合修正（2026-08-10）：从已闭合房间续测的新空间比原房间宽或窄一个墙厚时，合并闭合优先返回起算共享墙的另一端，不再按旧房间节点写入顺序误选绕行目标；正交补边按当前末墙真实来向排列，允许末端墙厚连接段，并把共享墙末端内缩记到新空间外边界墙。新空间的闭环只包含本次测量墙链和直接共享边，保留图三所示约一个墙厚的错台，不再把房间1绕入房间2或放大尺寸、面积。入口、API、角色、version-4 合同和 BLE 审计内容不变。
- ALG-001 共享墙墙面与净面积修正（2026-08-10）：闭合空间的填充、净尺寸和面积现在从有向 `wallIds` 链推导本空间的物理内墙面；同一共享墙仍只生成一个墙体实体，相邻空间分别选择靠近自己的墙面，墙厚连接段不会被当作净边界。只读空间尺寸计划同时区分内边界、外边界和逐墙厚度段；房间标签不再使用最长横墙乘最长竖墙替代不规则空间净面积。`2230 × 3182mm` 上下相邻房间回归确认共享墙实体唯一、两侧外墙连续、两房净面积独立，且第二房拓扑包络面积不再冒充净面积。入口、API、角色、BLE 审计与 version-4 持久化结构不变；聚焦算法/渲染测试 `79/79` 通过，真机视觉复核待完成。
- ALG-002 闭合房间尺寸链修正（2026-08-11）：Mini Program Canvas 与后台只读 `FloorPlanViewer` 共同消费 `createClosedDimensionPlan()`。正交户型按门洞定位 `opening-segment`、建筑外侧房间净尺寸 `room-clear`、真实外包总尺寸 `building-overall` 由近到远布置；没有门洞时净尺寸占第一层。净尺寸从每个空间的有向内墙面链合并同轴段，只保留映射到建筑外轮廓的房间边，共享墙和完全内部墙不标注；外包值直接取墙体实体最外层正向环的真实跨度，延伸起点来自对应方向的真实外角，尺寸线不得穿过房间或墙体。单矩形、并排空间、错台空间及 L/U/阶梯形正交边界使用新语义，建筑外边界含斜墙时整套回退旧斜墙规划。算法权威源为 `miniprogram/packages/surveying/utils/`，后台 `predev`/`prebuild` 从正确目录同步镜像。聚焦尺寸/Canvas 回归测试 `48/48` 通过，后台生产构建通过；当前微信开发者工具窗口只暴露内部调试端口，没有兼容 Mini Program Automator 的控制端点，因此无法安全执行重新编译、页面栈确认及单房间/并排房间 `390x844` 原生截图，本轮未打开重复窗口。WXML/WXSS、页面入口、API、角色权限、面积、BLE/手动审计和 version-4 持久化合同均未改变。
- 已实现测角：斜线方向阈值吸附、数字面板输入、操作员确认的手机姿态角度，以及三次 BLE 三角边长读数和余弦定理校验。关闭面板不改墙体几何，也不遗留姿态监听。手机姿态和勾股测量复用同一固定壳层测角浮层：手机模式提供水平圆盘、一位小数实时角度、基准设置和手输回退；勾股模式提供带编号的 A/B/D 毫米读数、测量中/已完成反馈、校验错误、重测和一位小数计算结果。测角浮层打开期间临时隐藏原生画布层的撤销/重画、拖放光标和拖拽放大镜控件，关闭后按原状态恢复。
- 已实现绘制/编辑：覆盖完整墙厚和开口宽度的 CAD 风格门窗符号、未闭合内测边红线、房间填充仅接受首墙正向或反向能够完整闭合的墙链、单侧墙体与连接节点补面先做全局实体合并；Canvas 对闭合墙、开放墙分别一次填充分类后的并集闭环，并对完整实体并集闭环一次描边，避免逐输入多边形、连接补片、设备像素扫描线或独立边界段在 L/T/十字节点产生颜色冲突、随机白洞、内部端帽、斜缝或叠加方框；以及 ALG-002 所述的闭合尺寸链（正交边界为门洞定位、建筑外侧房间净尺寸、真实建筑外包总尺寸；含外边界斜墙时回退既有规划）。尺寸继续使用现有细延伸线、小箭头、蓝色数值和文字断线样式，窗口不生成重复定位链；画布、拖拽层和底部拖放控件维持统一的细线十字/方框光标。拖动光标时左上角放大镜复用正式 Canvas 场景渲染器，局部墙体实体、转角、选中态和门窗符号与主画布一致，不再使用独立线段近似，并明确显示“顶点吸附”“内边吸附”“外边吸附”或“自由放置”。放大镜条件使用不生成原生节点的 `block`，底部光标控件持续持有 `touchmove`，不会被展示层截断；V1 构件尺寸编辑、由底部唯一入口触发的 BLE 参数测距，以及选中门窗的 Three.js 尺寸预览；翻转与模型选择暂不在 V1 界面开放。画布平移和双指缩放使用按动画帧合并的临时轻量渲染层：手势期间保留墙体、房间填充、轮廓和门窗，临时隐藏尺寸、房间名称、辅助线和操作提示；手势结束后只重建一次完整正式场景及小 K 避障布局。手势帧直接投影同一组并集闭环，不更新页面数据，也不重复计算墙体实体并集、尺寸计划或引导避障。
- 手势临时层直接将已构建的结构路径投影到目标视口，避免原生 Canvas 下闭合房间填充与复合墙实体显示不一致；这不更新页面数据，也不重新计算墙体实体并集或尺寸计划。
- 无限画布视口修正（2026-08-10）：平移不再把墙体实体限制在顶部导航、右侧工具轨和底部控制坞之间的可见工作区，操作员可以把已闭合房间完整移到任意方向，在腾出的空白画布上从左、右、上或下继续新建相邻房间。双指缩放从原 `0.05–0.36 px/mm` 扩展为仅用于数值安全的 `0.002–4 px/mm` 超宽范围，仍以双指中心为锚点并可连续重复缩放。该修正只改变 viewport 交互，不改变页面入口、API、角色、version-4 墙图、毫米坐标、持久化或 BLE/手动测量审计。
- 平移/缩放临时帧直接绘制到主画布，不复用光标拖拽覆盖层，避免原生 Canvas 在墙体吸附后将共享墙房间与旧正式帧合成；光标拖放吸附后也会在正式画布重绘完成后再次清空临时层。
- 光标拖放以底部控件的实时矩形将原生 `cover-view` 局部触摸坐标换算为屏幕坐标，在设备未上报或上报为零的 `pageX`/`clientX` 时仍保持连续拖动。
- 若设备遗漏中间 `touchmove`，起止点存在有效位移时仍按一次拖放完成，普通点击不会触发放置。
- `miniprogram/packages/surveying/utils/surveyDimensionPlan.js` 与 `miniprogram/packages/surveying/utils/surveyWallSolidPlan.js` 是两端无渲染依赖的算法源；后台开发和生产构建前同步本地镜像，不再通过扩大 Turbopack 监控根目录来跨仓库加载。
  - 墙体选中工具轨精简（2026-08-10）：选中墙体后不再以“编辑 / 拆分 / 添加 / 布置 / 删除”的上下文面板替换普通右侧工具轨；普通四项工具轨保持可见，画布内既有墙体快捷工具栏仍是墙体专属操作入口。该纯表现层移除不改变页面路由、API、角色权限、v4 墙图、持久化或测量审计。
  - 有限支持：BLE 操作要求兼容且已连接设备；部分保留底部工具会有意显示规划中或暂未开放。
- 边界：小程序当前没有真实报告导出，也不提供全户型 CAD/3D 导出；后台 `FloorPlanViewer` 通过适配层提供全户型 2D/3D 查看和 DXF 下载。不得保存旧布局镜像。
- 运维细节和清理流程：`docs/surveying-module/README.md`、`formal-surveying.md`。

## 共享组件与工具

- BLE：`components/ble-connector`、`components/ble-gate`、`utils/bluetooth.js`。排查设备资产标识期间，已按现有授权连接的设备会记录 BLE 广播数据，并仅发送一次厂商定义的只读 `ATC001#`；通过 CRC 校验的原始 `ID` 帧、96 位 ID 十六进制值和可打印 ASCII 形式仅输出到控制台。该诊断尚未把机器 ID 认定为机身 SN，也不改变授权、API、持久化或角色边界。
- 导航：`utils/surveyNavigation.js` 负责正式编辑器入口和本地续测指针清理。
- 墙图/渲染：`surveyWallGraph.js`、`surveyCanvasRenderer.js`、`surveyLayout.js`；AI 设计使用 `aiDesignService.js` 和 `aiDesignValidation.js`。
- 几何管线桥接（2026-08-13）：`surveyWallGraph.js` 暴露只读的
  `buildGeometryPipelineInput()` 适配器，供 Node-only 的
  `research/survey-geometry-poc` 桥接层读取正式墙图。它不替换小程序现有
  闭合或持久化逻辑，原实现仍是运行时事实来源。接入前备份保存在
  `research/survey-geometry-poc/backups/surveyWallGraph.js.backup-20260813-194929`。
- 平面拓扑 V2（2026-08-13）：`utils/surveyGeometryPipeline.js`
  在编辑器草稿更新后执行无依赖的交点节点化和半边面提取。墙体内部交点会正式
  写成共享节点和原子墙段，有界面重建为标准闭合 `spaces[]`；门窗迁移到包含其
  原墙绝对位置的切分墙段。开放活动墙链单独记录为 `openEnds`，不算闭合边界的
  `DANGLE`。正式草稿恢复、显式闭合及已提交墙体发生内部相交时都会归一化；没有
  缺失的 T 型/十字节点始终物化；若切分后面数仍匹配，则只局部替换既有空间墙段，
  保留墙链方向和 `wallFaceOverrides`。
  页面路由、API、角色、毫米单位、测量审计和 version-4 顶层合同不变。
  从内边或外边续测时都保留墙体交点处的共享拓扑节点，并通过
  `measurementStartInsetMm` 从 `lengthMm` 中排除完整的交点墙厚，使净测距与墙厚
  始终作为独立尺寸表达。
  闭合墙与既有墙共线时，墙中段光标只用于选择目标墙，不物化新拓扑节点；系统沿
  拖动方向吸附到最先遇到的既有拓扑端点。非共线相交仍在实际交点切分节点。
  从既有墙中段起测新墙时，光标吸附确认就立即物化交点：源墙原子替换为
  两条同源墙段，所有引用它的空间同步替换 `wallIds`；新墙提交后复用该共享节点，
  形成度数为 3 的 T 型连接。该操作不等待房间闭合，开放分支不误建新空间。填充渲染可省略
  同源共线显示点，但持久化墙图保留两段墙和 `topologySourceWallId`。
  分支墙红线从源墙远侧墙面开始；两段源墙分别保存端点内缩，使分支墙实体一侧的红线从完整墙厚之后开始，另一侧止于拓扑交点。实体端帽只由拓扑连接决定。
  2026-08-14 光标/拓扑与 Canvas 直接回归 `105/105` 通过，小程序全量测试 `300/303` 通过；
  3 条既有失败分别涉及获客通知路径、本地 API `3005/3006` 不一致和离线调试默认值，与拓扑无关。
  本轮未改 WXML/WXSS 或可见控件。现有开发者工具窗口没有兼容自动化
  端点，无法按规则补做原生 `390x844` Canvas 核验；本轮未打开重复窗口。
- 开放墙链重置续画一致性（2026-08-14）：操作员重置光标后重新吸附到开放墙链
  悬空端点的内边或外边顶点时，下一面墙按源墙的有向测量侧继续；吸附源墙终点
  保持 `left`/`right`，吸附源墙起点则翻转测量侧以表示同一行进方向。这样转角结果
  与不重置光标的连续拖墙一致，不再把既有源墙的测量终点额外向内推进一个墙厚。
  闭合边界重新起测和墙中 T 型连接规则不变。光标定向测试 `54/54` 通过；小程序
  全量测试 `301/304` 通过，3 条既有失败仍分别为获客通知路径、本地 API 端口及
  离线调试默认值，与本修复无关。本轮不改变 WXML/WXSS、路由、API、角色、
  version-4 字段或测量审计。
- 纯前端几何 V1（2026-08-13）：共线、连续且墙厚一致的拓扑墙段在 Canvas
  墙体合并前共享同一派生物理墙面，切分点不再显示成墙端。新墙链从既有
  闭合墙借一个点起测并回到同一点时可独立闭合，不再虚构公共边。路由、
  权限、version-4 持久化、门窗和测量审计均不变。
- 几何运行时撤回（2026-08-13）：实验管线模块、入口归一化、草稿审计、
  提交交点归一化和物理墙归并均已删除，因为同步全图计算会阻塞真机正式
  量房页面启动。`surveyWallGraph.js` 已恢复到接入前备份。前述 V1/V2 条目
  仅作为历史实验记录保留，不代表当前运行时能力。
- UI：导航栏、自定义 Tab、线索列表/弹窗、分享海报、房间库和量房指南针。

## 测绘引导 Canvas 实现说明（2026-08-05）

底部光标和测距目标采用专用末端布局：按真实控制坞几何上移对话框，优先把小 K 放在卡片下方，并用直线虚线直接终止在按钮正上方；按钮内部的绿色原生描边和顶部菱形继续负责高层高亮，按钮触摸树不变。空间闭合后的等待拖放和光标拖动状态会同步清空、隐藏旧的顶部原生测量浮层，禁止在左上角留下无文字的白色矩形。下一墙链光标放置后，当前测量段只从 `activeSpaceStartWallIndex` 及其后的新墙中解析；尚未拉出新墙时不得复用上一闭合房间的末墙，也不得额外绘制第二组蓝色十字辅助线或恢复旧量测浮层。

正式量房引导是已实现的本地表现层。白底对话框、浅绿箭头尾巴、水平居中的“小K提示”标签、小K图片、目标光圈、绿色虚线贝塞尔弧线和箭头均由主 `survey-canvas` 绘制，不再使用 WXML `cover-view` 覆盖节点，因此不会截获画布拖动、平移或双指缩放手势。顶部持久化“引导”开关仍是唯一控制入口；该视觉层不改变 API、角色边界、v4 墙图、草稿或测量审计合同。

引导正文必须以当前 Canvas 字体和卡片实际内宽通过 `CanvasRenderingContext2D.measureText()` 换行，禁止仅按字数截行导致中文越出气泡。旧的 WXML 测量气泡不保留 `wx:else` 回退分支；没有顶部测量值时不得出现空白白色浮卡。

对话框尾巴使用连续 Canvas 轮廓：填充部分压入卡片边缘，仅描绘两条外露斜边，并按卡片同比例缩放，禁止拼接三角形。小K左右指向在每一种引导状态都按真实目标的水平几何关系推导，包括测量边状态；卡片高度需在最后一行测量正文后保留明确的底部内边距。

卡片、小 K 与连接线统一进入一次避障求解。卡片和完整角色必须保持间距且位于安全工作区；尺寸文字、房间信息和固定控件是不可覆盖、不可穿线的硬障碍，墙体、门窗、尺寸线与红色测量边按权重避让。连接线先对多条贝塞尔曲线采样，无法绕开硬障碍时改用平滑网格路径，仍无安全路径时省略本帧连接线；小幅平移优先复用上一有效布局以避免跳位。指向底部光标或测距按钮时，目标使用控制坞真实几何，按钮内部的原生描边标记显示高层高亮，事件继续冒泡给原按钮。旧的纯绿色背景说明气泡已删除，白底小 K 对话框是唯一教学说明；测量边切换与“可闭合”操作仍保留。

- 闭合房间角点续测修正（2026-08-07）：从已闭合房间的角点重新起画直墙时，即使两个角点都已有两条边连接，也允许沿既有共享边路径寻找相邻房间的闭合目标。第一或第二条新测墙会保留矩形补全参考，手输/BLE 确认长度后会重新应用正交吸附，避免确认读数重算端点后丢失吸附。该修正恢复真实 `merge` 闭合提示，不改变页面路由、API、角色边界、毫米/v4 墙图合同或测量审计行为。
- 闭合墙删除后的续测恢复（2026-08-07）：从已闭合相邻房间删除墙体时会清除旧的光标/墙体吸附记录，避免“重置光标”恢复到已删除墙或上一房间节点。重新吸附剩余端点后，缺失墙会再次显示正交辅助和非空共享边闭合候选。

## 测量员—设计师获客流程（已实现）

专项业务与数据契约见 [`docs/measurer-designer-acquisition.zh-CN.md`](measurer-designer-acquisition.zh-CN.md)，英文镜像为 `docs/measurer-designer-acquisition.md`。

- `packages/business/acquisition-center/acquisition-center` 是设计师确认客户已添加微信的唯一入口。它通过 `/api/acquisition-tasks` 提供按角色裁剪的待确认/已完成任务、汇总、分页、失败重试、幂等确认和通知 `leadId` 精确定位；测量员响应只在页面级返回一次当前绑定 `designerProfile`，汇总后显示唯一“我的设计师 / 查看微信”入口，客户任务卡不重复显示设计师联系方式。确认只写 `acquiredAt/acquiredBy` 并生成获客提成，不修改客户主流程状态。任务列表支持原生 `scroll-view` 手动下拉刷新；页面可见时每 30 秒刷新当前状态，页面隐藏或卸载时停止定时器，且不会与已有请求并发。
- `pages/leads-management/leads-management` 与 `components/lead-list` 使用四阶段客户主流程，不再提供“已获客”筛选。测量员在胶囊安全区下方通过“我的设计师”打开共享 `designer-contact-sheet`；列表卡不再重复铺设微信号和二维码。受保护二维码会经已获授权的小程序请求通道按字节读取，再以小程序本地临时文件渲染，避免独立远程图片下载域名/缓存通道失败。重试使用有时限的刷新状态、重新获取的 `designerProfile` 和仅客户端使用的缓存标识。
- `packages/business/lead-detail/lead-detail` 同时接受普通 `id` 和通知深链 `leadId`，显示四阶段状态时间线、下一步提示和普通信息组中的获客事实，不再承载获客确认主按钮。阶段轨道下方的独立签约进度区完全使用详情 API 返回的 `conversionActions`：企业负责人或负责设计师可打开原生底部确认层标记签约，已签约时展示日期、标记人、时间、金额和备注，仅企业负责人可填写原因撤销。正式量房卡只显示真实墙图状态、闭合空间数和更新时间，缺少正式户型时明确提示“从墙图开始建立客户户型”；绑定设计师仍通过共享底部名片查看。
- `packages/business/commission-records/commission-records` 通过 `/api/commission-records` 读取独立获客提成；测量员看到待结算/已发放汇总，salesperson 原有订单提成保持兼容。
- “我的”页面从 `/api/miniprogram/mine` 获取按角色裁剪的“获客协作”入口和真实待办角标；站内通知通过 `/api/miniprogram/notifications/read` 标记已读，并按 `metadata.page` 优先深链到协作中心的对应记录。微信发送失败由站内通知兜底。

### AI 设计工作台还原度修正（2026-08-11）

- 最新视觉基准仍为 `design-references/ai-design/ai-design-customer-project-switcher-v3/ai-design-customer-workbench-home-v2.png` 与 `ai-design-customer-project-switcher-v3.png`，本记录同时取代旧条目中的 `hero-v4`、首屏上浮状态卡和首页房间 Chip 描述。绿色项目舞台改用 `design-references/html-prototypes/ai-design-customer-workbench/` 中已确认的独立素材 `/images/generated-hero-bleed-v2.png`，以 `aspectFill` 撑满完整 Hero 容器；正式图纸、室内方案和小 K 重新按参考稿比例排布，为左上客户信息和底部原生进度留出稳定净空。已有成果轮播不再叠加“完整户型 / 轻触查看设计成果”旧文案，也不再叠加独立的 3D 导览生成进度；真实项目进度、增强后的本地选中阶段发光切图、四阶段轨道和唯一真实下一步成为底部唯一状态层。点数胶囊上移，与“切换项目”保持间距；切换项目双向箭头、设计准备区量房图纸卷轴均改用参考稿衍生的本地切图。其后仍依次为客户/小区搜索、由角色范围内真实项目索引驱动的横向“空间方案”卡，以及只汇总待完善量房项目的“设计准备区”，四项真实设计任务继续位于首屏以下。项目抽屉保持约半屏高度、搜索和三个派生状态分组；大比例缩略图左侧原生渲染正式墙图、右侧显示最新可访问成果，只有正式墙图不可用时才回退到统一本地图标，不用样例室内图冒充。`qa-restored-ai-design-entry-v5.png` 和 `qa-restored-project-picker-v5.png` 仅保留为本轮修正前证据；本轮当时记录的自动化端点限制已被下一条“未选择项目”复核流程取代。路由、API、权限、点数扣费、任务/方案选择、量房入口和 version-4 墙图持久化合同均不变。
- 未选择项目分支以 `design-references/ai-design/ai-design-customer-project-switcher-v3/ai-design-customer-workbench-empty-v2.png` 为唯一视觉基准完成还原。生产素材 `/images/ai-design-empty-v2/stage-art.jpg` 以及 `step-customer.png`、`step-survey.png`、`step-ai.png` 三个路径图标均直接派生自该设计稿，对应可编辑源切图保留在设计稿同目录；页面不再使用近似生成场景或无关通用图标。标题、说明、三段路径、“选择客户量房”主操作、搜索/筛选、“可开始设计”项目区和设计准备汇总仍由原生组件渲染。项目卡只消费当前角色范围内接口返回的真实来源；没有在用正式量房时明确显示真实空数据，不用设计稿中的样例项目补位。该分支不再显示与当前项目无关的“最近方案”占位，因此归档深链不会再呈现成方案加载失败。

  整页视觉验收将 `853x1844` 设计源换算到 iPhone 12/13 Pro `390x844` 基准，并记录实际运行边界，不再只检查“不交叠”。绿色主卡为 `left 8 / top 127 / width 374 / height 314`，标题说明从 `left 22 / top 143` 开始；“选择客户量房”主按钮实测 `left 22.5 / top 390 / width 345 / height 37`，与设计稿换算约 `344x38px` 的目标一致。三个设计稿路径切图均为 `28x28px`；搜索框为 `294x31px`，筛选为 `60x31px`；“可开始设计”标题从 `left 12 / top 497` 开始，真实空来源行和设计准备行宽度均为 `366px`。原生菜单胶囊为 `left 296 / top 51 / right 383 / bottom 83`，点数胶囊为 `top 91..119`，绿色主卡从 `top 127` 开始，两组相邻间距均为 `8px`。设计源同目录保留 `visual-qa-empty-390x844-2026-08-11-final-v2.png`、`visual-qa-empty-390x844-2026-08-11-final-v2.metrics.json` 与 `visual-qa-empty-native-capsule-2026-08-11-final-v2.png` 作为截图和逐元素数据证据。调试复用了用户现有微信开发者工具窗口；自动化连接后先明确重新编译，再切换到 `pages/ai-design/ai-design`，并通过已开放端点读取页面元素。小程序定向测试覆盖素材体积、设计稿文案和归档线索不查询工作流流程。路由、API、权限、点数扣费、项目选择和 version-4 墙图合同不变。
- 后续选中项目还原修正以用户提供的窄屏真机截图、`ai-design-customer-workbench-home-v2.png` 和 `ai-design-customer-project-switcher-v3.png` 联合为证据，但不照抄其中的样例状态或数据：首页稿负责首屏整体构图，项目切换稿负责 Hero 客户身份层和项目抽屉。标题区现在为点数胶囊保留原生布局通道；`<=360px` 视口与 `390x844` 基准一样保留左右内边距和 `568rpx` 绿色项目卡，不再把 Hero 拉成贴边全屏。无成果时改用用户确认的独立 `/images/generated-hero-bleed-v2.png`，移除额外的 `plan-default-scene-shade`，并以 `aspectFill` 和 `width: 100%; height: 100%` 覆盖整个 Hero 容器，让正式图纸、室内材料板和小 K 保持设计源原有亮度；Hero 按项目切换稿保留“当前客户”眉题、真实项目标题以及“正式量房 / 户型 / 闭合空间数”元数据行。首张 `341x728` 运行截图确认普通流把客户身份块推低、图纸主体提前进入元数据区域，因此身份块相对原基线整体上提 `26rpx`；后续编译截图又确认状态胶囊仍贴住眉题、箭头仍进入元数据基线，所以状态胶囊改为 `4rpx` 纵向内边距。当前阶段节点不再透明地只依赖弱光晕，而是同时使用 `96rpx` 批准本地光晕切图、绿色实体背景、`3rpx` 白色高光环、更强外层亮光和加粗标签。进度行显示四阶段旅程中的当前阶段，不再重复服务端派生的项目状态。首页“空间方案”卡只使用真实可访问成果/导航预览或正式墙图导航几何，不再为当前项目重复通用 Hero；当“设计”为当前 Tab 时，中间量房入口文字继承共享未选中颜色，消除双激活误导。`ai-design-home` 与 `ai-design-tab` 定向测试覆盖这些约束，包括项目切换稿所有 Hero 身份文案必须保留、默认场景遮罩不得回归及场景/文案锚点不得再次重叠。现有开发者工具窗口已在 iPhone 12/13 基准渲染当前无项目分支，证据为 `design-references/ai-design/ai-design-customer-project-switcher-v3/qa-fidelity-pass-empty-state-390x844-devtools.png`；但该实时会话没有可选项目，也未提供兼容自动化端点，因此选中项目的 `390x844` 和窄屏 `360x800` 截图仍是发布前待核验项。本轮仅修正表现层；路由、API、角色/租户权限、项目/方案选择、点数、量房入口和 version-4 墙图合同均不变。

#### HTML 工作台布局对齐（2026-08-12）

- 已选项目分支以 `design-references/html-prototypes/ai-design-customer-workbench/index.html`
  的 `390x844` 首屏为直接布局依据：胶囊安全区下的标题和点数胶囊之后，Hero
  固定为 `left 9px / top 114px / right 381px / bottom 410px`（实际
  `372px x 296px`）；其内原生状态、
  客户身份、进度轨道和唯一真实下一步继续占用对应的测量区域。真实搜索行、
  `166px x 197px` 空间方案卡和 `58px` 设计准备区按原型节奏排列。Hero 继续使用
  打包后的 `/images/generated-hero-bleed-v2.png` 并以 `aspectFill` 渲染；真实来源、
  成果、空态、权限、API 和全部跳转均不变。静态素材/布局断言已通过；当前微信开发者
  工具窗口未开放可用自动化端点，带原生胶囊的截图仍保留为发布前检查项。

#### 客户项目抽屉对齐（2026-08-12）

- `pages/ai-design/ai-design` 的客户项目抽屉现以
  `design-references/ai-design/ai-design-customer-project-switcher-v3/ai-design-customer-project-switcher-v3.png`
  为直接还原依据。原生半屏抽屉继续使用按角色范围返回的真实项目索引、搜索和“进行中”/
  “可开始”/“待完善量房”派生分组；项目卡按设计稿组织真实数据：可访问成果存在时留在缩略图中，
  没有成果时，满足量房条件的项目使用通用项目档案引导封面，只有“待完善量房”保留简化墙体几何。
  当前项目显示真实进度、当前项目标签和勾选，其余项目保留真实下一步操作。来源卡恢复设计稿的舒展媒体行密度（`170rpx` 卡片高度、`270rpx × 150rpx` 缩略图和
  独立留白的操作列），不再把项目识别信息和操作压缩为设置列表行。路由、API、租户/角色边界、
  点数、量房入口及 version-4 墙图合同均未改变。静态素材/
  布局断言已通过；现有开发者工具窗口未开放自动化端点，带原生胶囊截图仍待补。

#### 阶段引导场景与项目档案封面（2026-08-12）

- `pages/ai-design/ai-design` 不再把原始正式墙图导航几何当作通用项目封面。“空间方案”和
  客户项目抽屉中，可访问的真实成果始终优先；没有成果时，已满足量房条件的项目使用明确不表示
  AI 成果的项目档案引导封面
  `/images/ai-design-project-folio-cover-v1.png`。只有“待完善量房”分组保留简化的
  实时户型图，因为其任务是识别尚未完成的量房，而不是展示设计结果。全屋成果缺失时，Hero 按
  已批准的 `/images/generated-hero-bleed-v2.png` PNG 恢复原有构图；客户身份、状态、阶段和
  进度仍由原生真实数据表达。真实全屋成果仍优先显示在轮播中。路由、来源 API、租户/角色边界、
  选择行为、点数和 version-4 墙图合同均未改变。项目档案素材由 Sub2API `gpt-image-2` 生成并以
  PNG 打包；可编辑源保留在
  `design-references/ai-design/ai-design-stage-fallbacks-v1/`。

### 小程序资源体积清理（2026-08-12）

- 资源引用审计删除了 33 个运行时代码零引用文件，包括已被 `-v3` 引导素材替代的旧版量房引导图以及未使用的历史页面/工具栏素材；页面路由、运行时引用和业务契约均未改变。
- 对仍需保留且编码后明显更小的大图执行原地压缩：`images/generated-hero-bleed-v2.png`（1,605.3KB -> 1,503.8KB）、`images/home-ip-v1/hero-scene-wechat-safe-overscan.png`（765.4KB -> 290.8KiB；1,022x934 -> 900x823）、`packages/business/assets/login-v1/hero-scene.jpg`（115.1KB -> 81.0KB）、`packages/business/assets/promotion-create/hero-scene.jpg`（28.9KB -> 22.8KB）、`packages/business/assets/commission-records/hero-scene.jpg`（19.3KB -> 14.2KB）和 `images/ai-design-empty-v2/stage-art.jpg`（52.7KB -> 49.9KB）。格式、透明通道、资源路径和视觉构图保持不变；首页 Hero 为满足 300KB 限额而单独缩小了位图尺寸。
- 透明 PNG 在优化后没有明显收益的保持原文件不变。`miniprogram/node_modules` 仅用于开发，且 `project.config.json` 设置 `nodeModules: false`，不计入微信小程序实际打包体积。

## 维护规则

修改小程序页面、组件、工具、API 流程或数据合同前，必须先阅读根目录指令、本清单以及对应的设计或正式量房文档。完成后必须在同一次修改中同步本文件和英文镜像，记录真实入口、API、角色/条件、数据合同、状态和限制。正式量房规则必须与 v4 墙图合同一致；不得把 mock 或规划控件写成在线功能。如果确实没有功能文档影响，必须在交接说明中明确写出。
